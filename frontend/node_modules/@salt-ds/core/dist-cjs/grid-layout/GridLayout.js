'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var jsxRuntime = require('react/jsx-runtime');
var React = require('react');
var clsx = require('clsx');
var makePrefixer = require('../utils/makePrefixer.js');
require('../utils/useFloatingUI/useFloatingUI.js');
require('../utils/useId.js');
var useResponsiveProp = require('../utils/useResponsiveProp.js');
var GridLayout$1 = require('./GridLayout.css.js');
var window = require('@salt-ds/window');
var styles = require('@salt-ds/styles');

const withBaseName = makePrefixer.makePrefixer("saltGridLayout");
const GridLayout = React.forwardRef(
  ({
    as,
    children,
    className,
    columns = 12,
    rows = 1,
    gap,
    columnGap,
    rowGap,
    style,
    ...rest
  }, ref) => {
    const targetWindow = window.useWindow();
    styles.useComponentCssInjection({
      testId: "salt-grid-layout",
      css: GridLayout$1,
      window: targetWindow
    });
    const Component = as || "div";
    const gridColumns = useResponsiveProp.useResponsiveProp(columns, 12);
    const gridRows = useResponsiveProp.useResponsiveProp(rows, 1);
    const gridGap = useResponsiveProp.useResponsiveProp(gap, 3);
    const gridColumnGap = useResponsiveProp.useResponsiveProp(columnGap, 3);
    const gridRowGap = useResponsiveProp.useResponsiveProp(rowGap, 3);
    const gridLayoutStyles = {
      ...style,
      "--gridLayout-columns": gridColumns,
      "--gridLayout-rows": gridRows,
      "--gridLayout-columnGap": gridColumnGap != null ? gridColumnGap : gridGap,
      "--gridLayout-rowGap": gridRowGap != null ? gridRowGap : gridGap
    };
    return /* @__PURE__ */ jsxRuntime.jsx(Component, {
      className: clsx.clsx(withBaseName(), className),
      style: gridLayoutStyles,
      ref,
      ...rest,
      children
    });
  }
);

exports.GridLayout = GridLayout;
//# sourceMappingURL=GridLayout.js.map
