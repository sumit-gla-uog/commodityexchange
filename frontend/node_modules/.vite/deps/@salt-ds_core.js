import { n as __toESM, t as __commonJSMin } from "./rolldown-runtime-FDOR9p9I.js";
import { t as require_react } from "./react.js";
import { t as require_react_dom } from "./react-dom-CVUXoGTx.js";
//#region node_modules/react/cjs/react-jsx-runtime.development.js
/**
* @license React
* react-jsx-runtime.development.js
*
* Copyright (c) Meta Platforms, Inc. and affiliates.
*
* This source code is licensed under the MIT license found in the
* LICENSE file in the root directory of this source tree.
*/
var require_react_jsx_runtime_development = /* @__PURE__ */ __commonJSMin(((exports) => {
	(function() {
		function getComponentNameFromType(type) {
			if (null == type) return null;
			if ("function" === typeof type) return type.$$typeof === REACT_CLIENT_REFERENCE ? null : type.displayName || type.name || null;
			if ("string" === typeof type) return type;
			switch (type) {
				case REACT_FRAGMENT_TYPE: return "Fragment";
				case REACT_PROFILER_TYPE: return "Profiler";
				case REACT_STRICT_MODE_TYPE: return "StrictMode";
				case REACT_SUSPENSE_TYPE: return "Suspense";
				case REACT_SUSPENSE_LIST_TYPE: return "SuspenseList";
				case REACT_ACTIVITY_TYPE: return "Activity";
			}
			if ("object" === typeof type) switch ("number" === typeof type.tag && console.error("Received an unexpected object in getComponentNameFromType(). This is likely a bug in React. Please file an issue."), type.$$typeof) {
				case REACT_PORTAL_TYPE: return "Portal";
				case REACT_CONTEXT_TYPE: return type.displayName || "Context";
				case REACT_CONSUMER_TYPE: return (type._context.displayName || "Context") + ".Consumer";
				case REACT_FORWARD_REF_TYPE:
					var innerType = type.render;
					type = type.displayName;
					type || (type = innerType.displayName || innerType.name || "", type = "" !== type ? "ForwardRef(" + type + ")" : "ForwardRef");
					return type;
				case REACT_MEMO_TYPE: return innerType = type.displayName || null, null !== innerType ? innerType : getComponentNameFromType(type.type) || "Memo";
				case REACT_LAZY_TYPE:
					innerType = type._payload;
					type = type._init;
					try {
						return getComponentNameFromType(type(innerType));
					} catch (x) {}
			}
			return null;
		}
		function testStringCoercion(value) {
			return "" + value;
		}
		function checkKeyStringCoercion(value) {
			try {
				testStringCoercion(value);
				var JSCompiler_inline_result = !1;
			} catch (e) {
				JSCompiler_inline_result = !0;
			}
			if (JSCompiler_inline_result) {
				JSCompiler_inline_result = console;
				var JSCompiler_temp_const = JSCompiler_inline_result.error;
				var JSCompiler_inline_result$jscomp$0 = "function" === typeof Symbol && Symbol.toStringTag && value[Symbol.toStringTag] || value.constructor.name || "Object";
				JSCompiler_temp_const.call(JSCompiler_inline_result, "The provided key is an unsupported type %s. This value must be coerced to a string before using it here.", JSCompiler_inline_result$jscomp$0);
				return testStringCoercion(value);
			}
		}
		function getTaskName(type) {
			if (type === REACT_FRAGMENT_TYPE) return "<>";
			if ("object" === typeof type && null !== type && type.$$typeof === REACT_LAZY_TYPE) return "<...>";
			try {
				var name = getComponentNameFromType(type);
				return name ? "<" + name + ">" : "<...>";
			} catch (x) {
				return "<...>";
			}
		}
		function getOwner() {
			var dispatcher = ReactSharedInternals.A;
			return null === dispatcher ? null : dispatcher.getOwner();
		}
		function UnknownOwner() {
			return Error("react-stack-top-frame");
		}
		function hasValidKey(config) {
			if (hasOwnProperty.call(config, "key")) {
				var getter = Object.getOwnPropertyDescriptor(config, "key").get;
				if (getter && getter.isReactWarning) return !1;
			}
			return void 0 !== config.key;
		}
		function defineKeyPropWarningGetter(props, displayName) {
			function warnAboutAccessingKey() {
				specialPropKeyWarningShown || (specialPropKeyWarningShown = !0, console.error("%s: `key` is not a prop. Trying to access it will result in `undefined` being returned. If you need to access the same value within the child component, you should pass it as a different prop. (https://react.dev/link/special-props)", displayName));
			}
			warnAboutAccessingKey.isReactWarning = !0;
			Object.defineProperty(props, "key", {
				get: warnAboutAccessingKey,
				configurable: !0
			});
		}
		function elementRefGetterWithDeprecationWarning() {
			var componentName = getComponentNameFromType(this.type);
			didWarnAboutElementRef[componentName] || (didWarnAboutElementRef[componentName] = !0, console.error("Accessing element.ref was removed in React 19. ref is now a regular prop. It will be removed from the JSX Element type in a future release."));
			componentName = this.props.ref;
			return void 0 !== componentName ? componentName : null;
		}
		function ReactElement(type, key, props, owner, debugStack, debugTask) {
			var refProp = props.ref;
			type = {
				$$typeof: REACT_ELEMENT_TYPE,
				type,
				key,
				props,
				_owner: owner
			};
			null !== (void 0 !== refProp ? refProp : null) ? Object.defineProperty(type, "ref", {
				enumerable: !1,
				get: elementRefGetterWithDeprecationWarning
			}) : Object.defineProperty(type, "ref", {
				enumerable: !1,
				value: null
			});
			type._store = {};
			Object.defineProperty(type._store, "validated", {
				configurable: !1,
				enumerable: !1,
				writable: !0,
				value: 0
			});
			Object.defineProperty(type, "_debugInfo", {
				configurable: !1,
				enumerable: !1,
				writable: !0,
				value: null
			});
			Object.defineProperty(type, "_debugStack", {
				configurable: !1,
				enumerable: !1,
				writable: !0,
				value: debugStack
			});
			Object.defineProperty(type, "_debugTask", {
				configurable: !1,
				enumerable: !1,
				writable: !0,
				value: debugTask
			});
			Object.freeze && (Object.freeze(type.props), Object.freeze(type));
			return type;
		}
		function jsxDEVImpl(type, config, maybeKey, isStaticChildren, debugStack, debugTask) {
			var children = config.children;
			if (void 0 !== children) if (isStaticChildren) if (isArrayImpl(children)) {
				for (isStaticChildren = 0; isStaticChildren < children.length; isStaticChildren++) validateChildKeys(children[isStaticChildren]);
				Object.freeze && Object.freeze(children);
			} else console.error("React.jsx: Static children should always be an array. You are likely explicitly calling React.jsxs or React.jsxDEV. Use the Babel transform instead.");
			else validateChildKeys(children);
			if (hasOwnProperty.call(config, "key")) {
				children = getComponentNameFromType(type);
				var keys = Object.keys(config).filter(function(k) {
					return "key" !== k;
				});
				isStaticChildren = 0 < keys.length ? "{key: someKey, " + keys.join(": ..., ") + ": ...}" : "{key: someKey}";
				didWarnAboutKeySpread[children + isStaticChildren] || (keys = 0 < keys.length ? "{" + keys.join(": ..., ") + ": ...}" : "{}", console.error("A props object containing a \"key\" prop is being spread into JSX:\n  let props = %s;\n  <%s {...props} />\nReact keys must be passed directly to JSX without using spread:\n  let props = %s;\n  <%s key={someKey} {...props} />", isStaticChildren, children, keys, children), didWarnAboutKeySpread[children + isStaticChildren] = !0);
			}
			children = null;
			void 0 !== maybeKey && (checkKeyStringCoercion(maybeKey), children = "" + maybeKey);
			hasValidKey(config) && (checkKeyStringCoercion(config.key), children = "" + config.key);
			if ("key" in config) {
				maybeKey = {};
				for (var propName in config) "key" !== propName && (maybeKey[propName] = config[propName]);
			} else maybeKey = config;
			children && defineKeyPropWarningGetter(maybeKey, "function" === typeof type ? type.displayName || type.name || "Unknown" : type);
			return ReactElement(type, children, maybeKey, getOwner(), debugStack, debugTask);
		}
		function validateChildKeys(node) {
			isValidElement(node) ? node._store && (node._store.validated = 1) : "object" === typeof node && null !== node && node.$$typeof === REACT_LAZY_TYPE && ("fulfilled" === node._payload.status ? isValidElement(node._payload.value) && node._payload.value._store && (node._payload.value._store.validated = 1) : node._store && (node._store.validated = 1));
		}
		function isValidElement(object) {
			return "object" === typeof object && null !== object && object.$$typeof === REACT_ELEMENT_TYPE;
		}
		var React = require_react(), REACT_ELEMENT_TYPE = Symbol.for("react.transitional.element"), REACT_PORTAL_TYPE = Symbol.for("react.portal"), REACT_FRAGMENT_TYPE = Symbol.for("react.fragment"), REACT_STRICT_MODE_TYPE = Symbol.for("react.strict_mode"), REACT_PROFILER_TYPE = Symbol.for("react.profiler"), REACT_CONSUMER_TYPE = Symbol.for("react.consumer"), REACT_CONTEXT_TYPE = Symbol.for("react.context"), REACT_FORWARD_REF_TYPE = Symbol.for("react.forward_ref"), REACT_SUSPENSE_TYPE = Symbol.for("react.suspense"), REACT_SUSPENSE_LIST_TYPE = Symbol.for("react.suspense_list"), REACT_MEMO_TYPE = Symbol.for("react.memo"), REACT_LAZY_TYPE = Symbol.for("react.lazy"), REACT_ACTIVITY_TYPE = Symbol.for("react.activity"), REACT_CLIENT_REFERENCE = Symbol.for("react.client.reference"), ReactSharedInternals = React.__CLIENT_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE, hasOwnProperty = Object.prototype.hasOwnProperty, isArrayImpl = Array.isArray, createTask = console.createTask ? console.createTask : function() {
			return null;
		};
		React = { react_stack_bottom_frame: function(callStackForError) {
			return callStackForError();
		} };
		var specialPropKeyWarningShown;
		var didWarnAboutElementRef = {};
		var unknownOwnerDebugStack = React.react_stack_bottom_frame.bind(React, UnknownOwner)();
		var unknownOwnerDebugTask = createTask(getTaskName(UnknownOwner));
		var didWarnAboutKeySpread = {};
		exports.Fragment = REACT_FRAGMENT_TYPE;
		exports.jsx = function(type, config, maybeKey) {
			var trackActualOwner = 1e4 > ReactSharedInternals.recentlyCreatedOwnerStacks++;
			return jsxDEVImpl(type, config, maybeKey, !1, trackActualOwner ? Error("react-stack-top-frame") : unknownOwnerDebugStack, trackActualOwner ? createTask(getTaskName(type)) : unknownOwnerDebugTask);
		};
		exports.jsxs = function(type, config, maybeKey) {
			var trackActualOwner = 1e4 > ReactSharedInternals.recentlyCreatedOwnerStacks++;
			return jsxDEVImpl(type, config, maybeKey, !0, trackActualOwner ? Error("react-stack-top-frame") : unknownOwnerDebugStack, trackActualOwner ? createTask(getTaskName(type)) : unknownOwnerDebugTask);
		};
	})();
}));
//#endregion
//#region node_modules/react/jsx-runtime.js
var require_jsx_runtime = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_react_jsx_runtime_development();
}));
//#endregion
//#region node_modules/clsx/dist/clsx.mjs
var import_react = /* @__PURE__ */ __toESM(require_react(), 1);
var import_jsx_runtime = require_jsx_runtime();
function r(e) {
	var t, f, n = "";
	if ("string" == typeof e || "number" == typeof e) n += e;
	else if ("object" == typeof e) if (Array.isArray(e)) {
		var o = e.length;
		for (t = 0; t < o; t++) e[t] && (f = r(e[t])) && (n && (n += " "), n += f);
	} else for (f in e) e[f] && (n && (n += " "), n += f);
	return n;
}
function clsx() {
	for (var e, t, f = 0, n = "", o = arguments.length; f < o; f++) (e = arguments[f]) && (t = r(e)) && (n && (n += " "), n += t);
	return n;
}
//#endregion
//#region node_modules/@salt-ds/core/dist-es/utils/makePrefixer.js
var makePrefixer = (prefix) => (...names) => [prefix, ...names].join("-");
//#endregion
//#region node_modules/@floating-ui/utils/dist/floating-ui.utils.dom.mjs
function hasWindow() {
	return typeof window !== "undefined";
}
function getNodeName(node) {
	if (isNode(node)) return (node.nodeName || "").toLowerCase();
	return "#document";
}
function getWindow$1(node) {
	var _node$ownerDocument;
	return (node == null || (_node$ownerDocument = node.ownerDocument) == null ? void 0 : _node$ownerDocument.defaultView) || window;
}
function getDocumentElement(node) {
	var _ref;
	return (_ref = (isNode(node) ? node.ownerDocument : node.document) || window.document) == null ? void 0 : _ref.documentElement;
}
function isNode(value) {
	if (!hasWindow()) return false;
	return value instanceof Node || value instanceof getWindow$1(value).Node;
}
function isElement$1(value) {
	if (!hasWindow()) return false;
	return value instanceof Element || value instanceof getWindow$1(value).Element;
}
function isHTMLElement(value) {
	if (!hasWindow()) return false;
	return value instanceof HTMLElement || value instanceof getWindow$1(value).HTMLElement;
}
function isShadowRoot(value) {
	if (!hasWindow() || typeof ShadowRoot === "undefined") return false;
	return value instanceof ShadowRoot || value instanceof getWindow$1(value).ShadowRoot;
}
function isOverflowElement(element) {
	const { overflow, overflowX, overflowY, display } = getComputedStyle$1(element);
	return /auto|scroll|overlay|hidden|clip/.test(overflow + overflowY + overflowX) && display !== "inline" && display !== "contents";
}
function isTableElement(element) {
	return /^(table|td|th)$/.test(getNodeName(element));
}
function isTopLayer(element) {
	try {
		if (element.matches(":popover-open")) return true;
	} catch (_e) {}
	try {
		return element.matches(":modal");
	} catch (_e) {
		return false;
	}
}
var willChangeRe = /transform|translate|scale|rotate|perspective|filter/;
var containRe = /paint|layout|strict|content/;
var isNotNone = (value) => !!value && value !== "none";
var isWebKitValue;
function isContainingBlock(elementOrCss) {
	const css = isElement$1(elementOrCss) ? getComputedStyle$1(elementOrCss) : elementOrCss;
	return isNotNone(css.transform) || isNotNone(css.translate) || isNotNone(css.scale) || isNotNone(css.rotate) || isNotNone(css.perspective) || !isWebKit() && (isNotNone(css.backdropFilter) || isNotNone(css.filter)) || willChangeRe.test(css.willChange || "") || containRe.test(css.contain || "");
}
function getContainingBlock(element) {
	let currentNode = getParentNode(element);
	while (isHTMLElement(currentNode) && !isLastTraversableNode(currentNode)) {
		if (isContainingBlock(currentNode)) return currentNode;
		else if (isTopLayer(currentNode)) return null;
		currentNode = getParentNode(currentNode);
	}
	return null;
}
function isWebKit() {
	if (isWebKitValue == null) isWebKitValue = typeof CSS !== "undefined" && CSS.supports && CSS.supports("-webkit-backdrop-filter", "none");
	return isWebKitValue;
}
function isLastTraversableNode(node) {
	return /^(html|body|#document)$/.test(getNodeName(node));
}
function getComputedStyle$1(element) {
	return getWindow$1(element).getComputedStyle(element);
}
function getNodeScroll(element) {
	if (isElement$1(element)) return {
		scrollLeft: element.scrollLeft,
		scrollTop: element.scrollTop
	};
	return {
		scrollLeft: element.scrollX,
		scrollTop: element.scrollY
	};
}
function getParentNode(node) {
	if (getNodeName(node) === "html") return node;
	const result = node.assignedSlot || node.parentNode || isShadowRoot(node) && node.host || getDocumentElement(node);
	return isShadowRoot(result) ? result.host : result;
}
function getNearestOverflowAncestor(node) {
	const parentNode = getParentNode(node);
	if (isLastTraversableNode(parentNode)) return (node.ownerDocument || node).body;
	if (isHTMLElement(parentNode) && isOverflowElement(parentNode)) return parentNode;
	return getNearestOverflowAncestor(parentNode);
}
function getOverflowAncestors(node, list, traverseIframes) {
	var _node$ownerDocument2;
	if (list === void 0) list = [];
	if (traverseIframes === void 0) traverseIframes = true;
	const scrollableAncestor = getNearestOverflowAncestor(node);
	const isBody = scrollableAncestor === ((_node$ownerDocument2 = node.ownerDocument) == null ? void 0 : _node$ownerDocument2.body);
	const win = getWindow$1(scrollableAncestor);
	if (isBody) {
		const frameElement = getFrameElement(win);
		return list.concat(win, win.visualViewport || [], isOverflowElement(scrollableAncestor) ? scrollableAncestor : [], frameElement && traverseIframes ? getOverflowAncestors(frameElement) : []);
	} else return list.concat(scrollableAncestor, getOverflowAncestors(scrollableAncestor, [], traverseIframes));
}
function getFrameElement(win) {
	return win.parent && Object.getPrototypeOf(win.parent) ? win.frameElement : null;
}
//#endregion
//#region node_modules/@floating-ui/react/dist/floating-ui.react.utils.mjs
function activeElement(doc) {
	let activeElement = doc.activeElement;
	while (((_activeElement = activeElement) == null || (_activeElement = _activeElement.shadowRoot) == null ? void 0 : _activeElement.activeElement) != null) {
		var _activeElement;
		activeElement = activeElement.shadowRoot.activeElement;
	}
	return activeElement;
}
function contains(parent, child) {
	if (!parent || !child) return false;
	const rootNode = child.getRootNode == null ? void 0 : child.getRootNode();
	if (parent.contains(child)) return true;
	if (rootNode && isShadowRoot(rootNode)) {
		let next = child;
		while (next) {
			if (parent === next) return true;
			next = next.parentNode || next.host;
		}
	}
	return false;
}
function getPlatform() {
	const uaData = navigator.userAgentData;
	if (uaData != null && uaData.platform) return uaData.platform;
	return navigator.platform;
}
function getUserAgent() {
	const uaData = navigator.userAgentData;
	if (uaData && Array.isArray(uaData.brands)) return uaData.brands.map((_ref) => {
		let { brand, version } = _ref;
		return brand + "/" + version;
	}).join(" ");
	return navigator.userAgent;
}
function isVirtualClick(event) {
	if (event.mozInputSource === 0 && event.isTrusted) return true;
	if (isAndroid() && event.pointerType) return event.type === "click" && event.buttons === 1;
	return event.detail === 0 && !event.pointerType;
}
function isVirtualPointerEvent(event) {
	if (isJSDOM()) return false;
	return !isAndroid() && event.width === 0 && event.height === 0 || isAndroid() && event.width === 1 && event.height === 1 && event.pressure === 0 && event.detail === 0 && event.pointerType === "mouse" || event.width < 1 && event.height < 1 && event.pressure === 0 && event.detail === 0 && event.pointerType === "touch";
}
function isSafari() {
	return /apple/i.test(navigator.vendor);
}
function isAndroid() {
	const re = /android/i;
	return re.test(getPlatform()) || re.test(getUserAgent());
}
function isMac() {
	return getPlatform().toLowerCase().startsWith("mac") && !navigator.maxTouchPoints;
}
function isJSDOM() {
	return getUserAgent().includes("jsdom/");
}
function isMouseLikePointerType(pointerType, strict) {
	const values = ["mouse", "pen"];
	if (!strict) values.push("", void 0);
	return values.includes(pointerType);
}
function isReactEvent(event) {
	return "nativeEvent" in event;
}
function isRootElement(element) {
	return element.matches("html,body");
}
function getDocument$1(node) {
	return (node == null ? void 0 : node.ownerDocument) || document;
}
function isEventTargetWithin(event, node) {
	if (node == null) return false;
	if ("composedPath" in event) return event.composedPath().includes(node);
	const e = event;
	return e.target != null && node.contains(e.target);
}
function getTarget(event) {
	if ("composedPath" in event) return event.composedPath()[0];
	return event.target;
}
function isTypeableElement(element) {
	return isHTMLElement(element) && element.matches("input:not([type='hidden']):not([disabled]),[contenteditable]:not([contenteditable='false']),textarea:not([disabled])");
}
function stopEvent(event) {
	event.preventDefault();
	event.stopPropagation();
}
function isTypeableCombobox(element) {
	if (!element) return false;
	return element.getAttribute("role") === "combobox" && isTypeableElement(element);
}
//#endregion
//#region node_modules/@floating-ui/utils/dist/floating-ui.utils.mjs
var min = Math.min;
var max = Math.max;
var round = Math.round;
var floor = Math.floor;
var createCoords = (v) => ({
	x: v,
	y: v
});
var oppositeSideMap = {
	left: "right",
	right: "left",
	bottom: "top",
	top: "bottom"
};
function clamp(start, value, end) {
	return max(start, min(value, end));
}
function evaluate(value, param) {
	return typeof value === "function" ? value(param) : value;
}
function getSide(placement) {
	return placement.split("-")[0];
}
function getAlignment(placement) {
	return placement.split("-")[1];
}
function getOppositeAxis(axis) {
	return axis === "x" ? "y" : "x";
}
function getAxisLength(axis) {
	return axis === "y" ? "height" : "width";
}
function getSideAxis(placement) {
	const firstChar = placement[0];
	return firstChar === "t" || firstChar === "b" ? "y" : "x";
}
function getAlignmentAxis(placement) {
	return getOppositeAxis(getSideAxis(placement));
}
function getAlignmentSides(placement, rects, rtl) {
	if (rtl === void 0) rtl = false;
	const alignment = getAlignment(placement);
	const alignmentAxis = getAlignmentAxis(placement);
	const length = getAxisLength(alignmentAxis);
	let mainAlignmentSide = alignmentAxis === "x" ? alignment === (rtl ? "end" : "start") ? "right" : "left" : alignment === "start" ? "bottom" : "top";
	if (rects.reference[length] > rects.floating[length]) mainAlignmentSide = getOppositePlacement(mainAlignmentSide);
	return [mainAlignmentSide, getOppositePlacement(mainAlignmentSide)];
}
function getExpandedPlacements(placement) {
	const oppositePlacement = getOppositePlacement(placement);
	return [
		getOppositeAlignmentPlacement(placement),
		oppositePlacement,
		getOppositeAlignmentPlacement(oppositePlacement)
	];
}
function getOppositeAlignmentPlacement(placement) {
	return placement.includes("start") ? placement.replace("start", "end") : placement.replace("end", "start");
}
var lrPlacement = ["left", "right"];
var rlPlacement = ["right", "left"];
var tbPlacement = ["top", "bottom"];
var btPlacement = ["bottom", "top"];
function getSideList(side, isStart, rtl) {
	switch (side) {
		case "top":
		case "bottom":
			if (rtl) return isStart ? rlPlacement : lrPlacement;
			return isStart ? lrPlacement : rlPlacement;
		case "left":
		case "right": return isStart ? tbPlacement : btPlacement;
		default: return [];
	}
}
function getOppositeAxisPlacements(placement, flipAlignment, direction, rtl) {
	const alignment = getAlignment(placement);
	let list = getSideList(getSide(placement), direction === "start", rtl);
	if (alignment) {
		list = list.map((side) => side + "-" + alignment);
		if (flipAlignment) list = list.concat(list.map(getOppositeAlignmentPlacement));
	}
	return list;
}
function getOppositePlacement(placement) {
	const side = getSide(placement);
	return oppositeSideMap[side] + placement.slice(side.length);
}
function expandPaddingObject(padding) {
	var _padding$top, _padding$right, _padding$bottom, _padding$left;
	return {
		top: (_padding$top = padding.top) != null ? _padding$top : 0,
		right: (_padding$right = padding.right) != null ? _padding$right : 0,
		bottom: (_padding$bottom = padding.bottom) != null ? _padding$bottom : 0,
		left: (_padding$left = padding.left) != null ? _padding$left : 0
	};
}
function getPaddingObject(padding) {
	return typeof padding !== "number" ? expandPaddingObject(padding) : {
		top: padding,
		right: padding,
		bottom: padding,
		left: padding
	};
}
function rectToClientRect(rect) {
	const { x, y, width, height } = rect;
	return {
		width,
		height,
		top: y,
		left: x,
		right: x + width,
		bottom: y + height,
		x,
		y
	};
}
/*!
* tabbable 6.5.0
* @license MIT, https://github.com/focus-trap/tabbable/blob/master/LICENSE
*/
var candidateSelector = /* #__PURE__ */ [
	"input:not([inert]):not([inert] *)",
	"select:not([inert]):not([inert] *)",
	"textarea:not([inert]):not([inert] *)",
	"a[href]:not([inert]):not([inert] *)",
	"area[href]:not([inert]):not([inert] *)",
	"button:not([inert]):not([inert] *)",
	"[tabindex]:not(slot):not([inert]):not([inert] *)",
	"audio[controls]:not([inert]):not([inert] *)",
	"video[controls]:not([inert]):not([inert] *)",
	"[contenteditable]:not([contenteditable=\"false\"]):not([inert]):not([inert] *)",
	"details>summary:first-of-type:not([inert]):not([inert] *)",
	"details:not([inert]):not([inert] *)"
].join(",");
var NoElement = typeof Element === "undefined";
var matches = NoElement ? function() {} : Element.prototype.matches || Element.prototype.msMatchesSelector || Element.prototype.webkitMatchesSelector;
var getRootNode = !NoElement && Element.prototype.getRootNode ? function(element) {
	var _element$getRootNode;
	return element === null || element === void 0 ? void 0 : (_element$getRootNode = element.getRootNode) === null || _element$getRootNode === void 0 ? void 0 : _element$getRootNode.call(element);
} : function(element) {
	return element === null || element === void 0 ? void 0 : element.ownerDocument;
};
/**
* Determines if a node is inert or in an inert ancestor.
* @param {Node} [node]
* @param {boolean} [lookUp] If true and `node` is not inert, looks up at ancestors to
*  see if any of them are inert. If false, only `node` itself is considered.
* @returns {boolean} True if inert itself or by way of being in an inert ancestor.
*  False if `node` is falsy.
*/
var _isInert = function isInert(node, lookUp) {
	var _node$getAttribute;
	if (lookUp === void 0) lookUp = true;
	var inertAtt = node === null || node === void 0 ? void 0 : (_node$getAttribute = node.getAttribute) === null || _node$getAttribute === void 0 ? void 0 : _node$getAttribute.call(node, "inert");
	return inertAtt === "" || inertAtt === "true" || lookUp && node && (typeof node.closest === "function" ? node.closest("[inert]") : _isInert(node.parentNode));
};
/**
* Determines if a node's content is editable.
* @param {Element} [node]
* @returns True if it's content-editable; false if it's not or `node` is falsy.
*/
var isContentEditable = function isContentEditable(node) {
	var _node$getAttribute2;
	var attValue = node === null || node === void 0 ? void 0 : (_node$getAttribute2 = node.getAttribute) === null || _node$getAttribute2 === void 0 ? void 0 : _node$getAttribute2.call(node, "contenteditable");
	return attValue === "" || attValue === "true";
};
/**
* @param {Element} el container to check in
* @param {boolean} includeContainer add container to check
* @param {(node: Element) => boolean} filter filter candidates
* @returns {Element[]}
*/
var getCandidates = function getCandidates(el, includeContainer, filter) {
	if (_isInert(el)) return [];
	var candidates = Array.prototype.slice.apply(el.querySelectorAll(candidateSelector));
	if (includeContainer && matches.call(el, candidateSelector)) candidates.unshift(el);
	candidates = candidates.filter(filter);
	return candidates;
};
/**
* @callback GetShadowRoot
* @param {Element} element to check for shadow root
* @returns {ShadowRoot|boolean} ShadowRoot if available or boolean indicating if a shadowRoot is attached but not available.
*/
/**
* @callback ShadowRootFilter
* @param {Element} shadowHostNode the element which contains shadow content
* @returns {boolean} true if a shadow root could potentially contain valid candidates.
*/
/**
* @typedef {Object} CandidateScope
* @property {Element} scopeParent contains inner candidates
* @property {Element[]} candidates list of candidates found in the scope parent
*/
/**
* @typedef {Object} IterativeOptions
* @property {GetShadowRoot|boolean} getShadowRoot true if shadow support is enabled; falsy if not;
*  if a function, implies shadow support is enabled and either returns the shadow root of an element
*  or a boolean stating if it has an undisclosed shadow root
* @property {(node: Element) => boolean} filter filter candidates
* @property {boolean} flatten if true then result will flatten any CandidateScope into the returned list
* @property {ShadowRootFilter} shadowRootFilter filter shadow roots;
*/
/**
* @param {Element[]} elements list of element containers to match candidates from
* @param {boolean} includeContainer add container list to check
* @param {IterativeOptions} options
* @returns {Array.<Element|CandidateScope>}
*/
var _getCandidatesIteratively = function getCandidatesIteratively(elements, includeContainer, options) {
	var candidates = [];
	var elementsToCheck = Array.from(elements);
	while (elementsToCheck.length) {
		var element = elementsToCheck.shift();
		if (_isInert(element, false)) continue;
		if (element.tagName === "SLOT") {
			var assigned = element.assignedElements();
			var nestedCandidates = _getCandidatesIteratively(assigned.length ? assigned : element.children, true, options);
			if (options.flatten) candidates.push.apply(candidates, nestedCandidates);
			else candidates.push({
				scopeParent: element,
				candidates: nestedCandidates
			});
		} else {
			if (matches.call(element, candidateSelector) && options.filter(element) && (includeContainer || !elements.includes(element))) candidates.push(element);
			var shadowRoot = element.shadowRoot || typeof options.getShadowRoot === "function" && options.getShadowRoot(element);
			var validShadowRoot = !_isInert(shadowRoot, false) && (!options.shadowRootFilter || options.shadowRootFilter(element));
			if (shadowRoot && validShadowRoot) {
				var _nestedCandidates = _getCandidatesIteratively(shadowRoot === true ? element.children : shadowRoot.children, true, options);
				if (options.flatten) candidates.push.apply(candidates, _nestedCandidates);
				else candidates.push({
					scopeParent: element,
					candidates: _nestedCandidates
				});
			} else elementsToCheck.unshift.apply(elementsToCheck, element.children);
		}
	}
	return candidates;
};
/**
* @private
* Determines if the node has an explicitly specified `tabindex` attribute.
* @param {HTMLElement} node
* @returns {boolean} True if so; false if not.
*/
var hasTabIndex = function hasTabIndex(node) {
	return !isNaN(parseInt(node.getAttribute("tabindex"), 10));
};
/**
* Determine the tab index of a given node.
* @param {HTMLElement} node
* @returns {number} Tab order (negative, 0, or positive number).
* @throws {Error} If `node` is falsy.
*/
var getTabIndex = function getTabIndex(node) {
	if (!node) throw new Error("No node provided");
	if (node.tabIndex < 0) {
		if ((/^(AUDIO|VIDEO|DETAILS)$/.test(node.tagName) || isContentEditable(node)) && !hasTabIndex(node)) return 0;
	}
	return node.tabIndex;
};
/**
* Determine the tab index of a given node __for sort order purposes__.
* @param {HTMLElement} node
* @param {boolean} [isScope] True for a custom element with shadow root or slot that, by default,
*  has tabIndex -1, but needs to be sorted by document order in order for its content to be
*  inserted into the correct sort position.
* @returns {number} Tab order (negative, 0, or positive number).
*/
var getSortOrderTabIndex = function getSortOrderTabIndex(node, isScope) {
	var tabIndex = getTabIndex(node);
	if (tabIndex < 0 && isScope && !hasTabIndex(node)) return 0;
	return tabIndex;
};
var sortOrderedTabbables = function sortOrderedTabbables(a, b) {
	return a.tabIndex === b.tabIndex ? a.documentOrder - b.documentOrder : a.tabIndex - b.tabIndex;
};
var isInput = function isInput(node) {
	return node.tagName === "INPUT";
};
var isHiddenInput = function isHiddenInput(node) {
	return isInput(node) && node.type === "hidden";
};
var isDetailsWithSummary = function isDetailsWithSummary(node) {
	return node.tagName === "DETAILS" && Array.prototype.slice.apply(node.children).some(function(child) {
		return child.tagName === "SUMMARY";
	});
};
var getCheckedRadio = function getCheckedRadio(nodes, form) {
	for (var i = 0; i < nodes.length; i++) if (nodes[i].checked && nodes[i].form === form) return nodes[i];
};
var isTabbableRadio = function isTabbableRadio(node) {
	if (!node.name) return true;
	var radioScope = node.form || getRootNode(node);
	var queryRadios = function queryRadios(name) {
		return radioScope.querySelectorAll("input[type=\"radio\"][name=\"" + name + "\"]");
	};
	var radioSet;
	if (typeof window !== "undefined" && typeof window.CSS !== "undefined" && typeof window.CSS.escape === "function") radioSet = queryRadios(window.CSS.escape(node.name));
	else try {
		radioSet = queryRadios(node.name);
	} catch (err) {
		console.error("Looks like you have a radio button with a name attribute containing invalid CSS selector characters and need the CSS.escape polyfill: %s", err.message);
		return false;
	}
	var checked = getCheckedRadio(radioSet, node.form);
	return !checked || checked === node;
};
var isRadio = function isRadio(node) {
	return isInput(node) && node.type === "radio";
};
var isNonTabbableRadio = function isNonTabbableRadio(node) {
	return isRadio(node) && !isTabbableRadio(node);
};
var isNodeAttached = function isNodeAttached(node) {
	var _nodeRoot;
	var nodeRoot = node && getRootNode(node);
	var nodeRootHost = (_nodeRoot = nodeRoot) === null || _nodeRoot === void 0 ? void 0 : _nodeRoot.host;
	var attached = false;
	if (nodeRoot && nodeRoot !== node) {
		var _nodeRootHost, _nodeRootHost$ownerDo, _node$ownerDocument;
		attached = !!((_nodeRootHost = nodeRootHost) !== null && _nodeRootHost !== void 0 && (_nodeRootHost$ownerDo = _nodeRootHost.ownerDocument) !== null && _nodeRootHost$ownerDo !== void 0 && _nodeRootHost$ownerDo.contains(nodeRootHost) || node !== null && node !== void 0 && (_node$ownerDocument = node.ownerDocument) !== null && _node$ownerDocument !== void 0 && _node$ownerDocument.contains(node));
		while (!attached && nodeRootHost) {
			var _nodeRoot2, _nodeRootHost2, _nodeRootHost2$ownerD;
			nodeRoot = getRootNode(nodeRootHost);
			nodeRootHost = (_nodeRoot2 = nodeRoot) === null || _nodeRoot2 === void 0 ? void 0 : _nodeRoot2.host;
			attached = !!((_nodeRootHost2 = nodeRootHost) !== null && _nodeRootHost2 !== void 0 && (_nodeRootHost2$ownerD = _nodeRootHost2.ownerDocument) !== null && _nodeRootHost2$ownerD !== void 0 && _nodeRootHost2$ownerD.contains(nodeRootHost));
		}
	}
	return attached;
};
var isZeroArea = function isZeroArea(node) {
	var _node$getBoundingClie = node.getBoundingClientRect(), width = _node$getBoundingClie.width, height = _node$getBoundingClie.height;
	return width === 0 && height === 0;
};
var isHidden = function isHidden(node, _ref) {
	var displayCheck = _ref.displayCheck, getShadowRoot = _ref.getShadowRoot;
	if (displayCheck === "full-native") {
		if ("checkVisibility" in node) return !node.checkVisibility({
			checkOpacity: false,
			opacityProperty: false,
			contentVisibilityAuto: true,
			visibilityProperty: true,
			checkVisibilityCSS: true
		});
	}
	var visibility = getComputedStyle(node).visibility;
	if (visibility === "hidden" || visibility === "collapse") return true;
	var nodeUnderDetails = matches.call(node, "details>summary:first-of-type") ? node.parentElement : node;
	if (matches.call(nodeUnderDetails, "details:not([open]) *")) return true;
	if (!displayCheck || displayCheck === "full" || displayCheck === "full-native" || displayCheck === "legacy-full") {
		if (typeof getShadowRoot === "function") {
			var originalNode = node;
			while (node) {
				var parentElement = node.parentElement;
				var rootNode = getRootNode(node);
				if (parentElement && !parentElement.shadowRoot && getShadowRoot(parentElement) === true) return isZeroArea(node);
				else if (node.assignedSlot) node = node.assignedSlot;
				else if (!parentElement && rootNode !== node.ownerDocument) node = rootNode.host;
				else node = parentElement;
			}
			node = originalNode;
		}
		if (isNodeAttached(node)) return !node.getClientRects().length;
		if (displayCheck !== "legacy-full") return true;
	} else if (displayCheck === "non-zero-area") return isZeroArea(node);
	return false;
};
var isDisabledFromFieldset = function isDisabledFromFieldset(node) {
	if (/^(INPUT|BUTTON|SELECT|TEXTAREA)$/.test(node.tagName)) {
		var parentNode = node.parentElement;
		while (parentNode) {
			if (parentNode.tagName === "FIELDSET" && parentNode.disabled) {
				for (var i = 0; i < parentNode.children.length; i++) {
					var child = parentNode.children.item(i);
					if (child.tagName === "LEGEND") return matches.call(parentNode, "fieldset[disabled] *") ? true : !child.contains(node);
				}
				return true;
			}
			parentNode = parentNode.parentElement;
		}
	}
	return false;
};
var isNodeMatchingSelectorFocusable = function isNodeMatchingSelectorFocusable(options, node) {
	if (node.disabled || isHiddenInput(node) || isHidden(node, options) || isDetailsWithSummary(node) || isDisabledFromFieldset(node)) return false;
	return true;
};
var isNodeMatchingSelectorTabbable = function isNodeMatchingSelectorTabbable(options, node) {
	if (isNonTabbableRadio(node) || getTabIndex(node) < 0 || !isNodeMatchingSelectorFocusable(options, node)) return false;
	return true;
};
var isShadowRootTabbable = function isShadowRootTabbable(shadowHostNode) {
	var tabIndex = parseInt(shadowHostNode.getAttribute("tabindex"), 10);
	if (isNaN(tabIndex) || tabIndex >= 0) return true;
	return false;
};
/**
* @param {Array.<Element|CandidateScope>} candidates
* @returns Element[]
*/
var _sortByOrder = function sortByOrder(candidates) {
	var regularTabbables = [];
	var orderedTabbables = [];
	candidates.forEach(function(item, i) {
		var isScope = !!item.scopeParent;
		var element = isScope ? item.scopeParent : item;
		var candidateTabindex = getSortOrderTabIndex(element, isScope);
		var elements = isScope ? _sortByOrder(item.candidates) : element;
		if (candidateTabindex === 0) isScope ? regularTabbables.push.apply(regularTabbables, elements) : regularTabbables.push(element);
		else orderedTabbables.push({
			documentOrder: i,
			tabIndex: candidateTabindex,
			item,
			isScope,
			content: elements
		});
	});
	return orderedTabbables.sort(sortOrderedTabbables).reduce(function(acc, sortable) {
		sortable.isScope ? acc.push.apply(acc, sortable.content) : acc.push(sortable.content);
		return acc;
	}, []).concat(regularTabbables);
};
var tabbable = function tabbable(container, options) {
	options = options || {};
	var candidates;
	if (options.getShadowRoot) candidates = _getCandidatesIteratively([container], options.includeContainer, {
		filter: isNodeMatchingSelectorTabbable.bind(null, options),
		flatten: false,
		getShadowRoot: options.getShadowRoot,
		shadowRootFilter: isShadowRootTabbable
	});
	else candidates = getCandidates(container, options.includeContainer, isNodeMatchingSelectorTabbable.bind(null, options));
	return _sortByOrder(candidates);
};
var isTabbable = function isTabbable(node, options) {
	options = options || {};
	if (!node) throw new Error("No node provided");
	if (matches.call(node, candidateSelector) === false) return false;
	return isNodeMatchingSelectorTabbable(options, node);
};
//#endregion
//#region node_modules/@floating-ui/core/dist/floating-ui.core.mjs
var import_react_dom = /* @__PURE__ */ __toESM(require_react_dom(), 1);
function computeCoordsFromPlacement(_ref, placement, rtl) {
	let { reference, floating } = _ref;
	const sideAxis = getSideAxis(placement);
	const alignmentAxis = getAlignmentAxis(placement);
	const alignLength = getAxisLength(alignmentAxis);
	const side = getSide(placement);
	const isVertical = sideAxis === "y";
	const commonX = reference.x + reference.width / 2 - floating.width / 2;
	const commonY = reference.y + reference.height / 2 - floating.height / 2;
	const commonAlign = reference[alignLength] / 2 - floating[alignLength] / 2;
	let coords;
	switch (side) {
		case "top":
			coords = {
				x: commonX,
				y: reference.y - floating.height
			};
			break;
		case "bottom":
			coords = {
				x: commonX,
				y: reference.y + reference.height
			};
			break;
		case "right":
			coords = {
				x: reference.x + reference.width,
				y: commonY
			};
			break;
		case "left":
			coords = {
				x: reference.x - floating.width,
				y: commonY
			};
			break;
		default: coords = {
			x: reference.x,
			y: reference.y
		};
	}
	const alignment = getAlignment(placement);
	if (alignment) coords[alignmentAxis] += commonAlign * (alignment === "end" ? 1 : -1) * (rtl && isVertical ? -1 : 1);
	return coords;
}
/**
* Resolves with an object of overflow side offsets that determine how much the
* element is overflowing a given clipping boundary on each side.
* - positive = overflowing the boundary by that number of pixels
* - negative = how many pixels left before it will overflow
* - 0 = lies flush with the boundary
* @see https://floating-ui.com/docs/detectOverflow
*/
async function detectOverflow(state, options) {
	var _await$platform$isEle;
	if (options === void 0) options = {};
	const { x, y, platform, rects, elements, strategy } = state;
	const { boundary = "clippingAncestors", rootBoundary = "viewport", elementContext = "floating", altBoundary = false, padding = 0 } = evaluate(options, state);
	const paddingObject = getPaddingObject(padding);
	const element = elements[altBoundary ? elementContext === "floating" ? "reference" : "floating" : elementContext];
	const clippingClientRect = rectToClientRect(await platform.getClippingRect({
		element: ((_await$platform$isEle = await (platform.isElement == null ? void 0 : platform.isElement(element))) != null ? _await$platform$isEle : true) ? element : element.contextElement || await (platform.getDocumentElement == null ? void 0 : platform.getDocumentElement(elements.floating)),
		boundary,
		rootBoundary,
		strategy
	}));
	const rect = elementContext === "floating" ? {
		x,
		y,
		width: rects.floating.width,
		height: rects.floating.height
	} : rects.reference;
	const offsetParent = await (platform.getOffsetParent == null ? void 0 : platform.getOffsetParent(elements.floating));
	const offsetScale = await (platform.isElement == null ? void 0 : platform.isElement(offsetParent)) && await (platform.getScale == null ? void 0 : platform.getScale(offsetParent)) || {
		x: 1,
		y: 1
	};
	const elementClientRect = rectToClientRect(platform.convertOffsetParentRelativeRectToViewportRelativeRect ? await platform.convertOffsetParentRelativeRectToViewportRelativeRect({
		elements,
		rect,
		offsetParent,
		strategy
	}) : rect);
	return {
		top: (clippingClientRect.top - elementClientRect.top + paddingObject.top) / offsetScale.y,
		bottom: (elementClientRect.bottom - clippingClientRect.bottom + paddingObject.bottom) / offsetScale.y,
		left: (clippingClientRect.left - elementClientRect.left + paddingObject.left) / offsetScale.x,
		right: (elementClientRect.right - clippingClientRect.right + paddingObject.right) / offsetScale.x
	};
}
var MAX_RESET_COUNT = 50;
/**
* Computes the `x` and `y` coordinates that will place the floating element
* next to a given reference element.
*
* This export does not have any `platform` interface logic. You will need to
* write one for the platform you are using Floating UI with.
*/
var computePosition$1 = async (reference, floating, config) => {
	const { placement = "bottom", strategy = "absolute", middleware = [], platform } = config;
	const platformWithDetectOverflow = platform.detectOverflow ? platform : {
		...platform,
		detectOverflow
	};
	const rtl = await (platform.isRTL == null ? void 0 : platform.isRTL(floating));
	let rects = await platform.getElementRects({
		reference,
		floating,
		strategy
	});
	let { x, y } = computeCoordsFromPlacement(rects, placement, rtl);
	let statefulPlacement = placement;
	let resetCount = 0;
	const middlewareData = {};
	for (let i = 0; i < middleware.length; i++) {
		const currentMiddleware = middleware[i];
		if (!currentMiddleware) continue;
		const { name, fn } = currentMiddleware;
		const { x: nextX, y: nextY, data, reset } = await fn({
			x,
			y,
			initialPlacement: placement,
			placement: statefulPlacement,
			strategy,
			middlewareData,
			rects,
			platform: platformWithDetectOverflow,
			elements: {
				reference,
				floating
			}
		});
		x = nextX != null ? nextX : x;
		y = nextY != null ? nextY : y;
		middlewareData[name] = {
			...middlewareData[name],
			...data
		};
		if (reset && resetCount < MAX_RESET_COUNT) {
			resetCount++;
			if (typeof reset === "object") {
				if (reset.placement) statefulPlacement = reset.placement;
				if (reset.rects) rects = reset.rects === true ? await platform.getElementRects({
					reference,
					floating,
					strategy
				}) : reset.rects;
				({x, y} = computeCoordsFromPlacement(rects, statefulPlacement, rtl));
			}
			i = -1;
		}
	}
	return {
		x,
		y,
		placement: statefulPlacement,
		strategy,
		middlewareData
	};
};
/**
* Provides data to position an inner element of the floating element so that it
* appears centered to the reference element.
* @see https://floating-ui.com/docs/arrow
*/
var arrow$3 = (options) => ({
	name: "arrow",
	options,
	async fn(state) {
		const { x, y, placement, rects, platform, elements, middlewareData } = state;
		const { element, padding = 0 } = evaluate(options, state) || {};
		if (element == null) return {};
		const paddingObject = getPaddingObject(padding);
		const coords = {
			x,
			y
		};
		const axis = getAlignmentAxis(placement);
		const length = getAxisLength(axis);
		const arrowDimensions = await platform.getDimensions(element);
		const isYAxis = axis === "y";
		const minProp = isYAxis ? "top" : "left";
		const maxProp = isYAxis ? "bottom" : "right";
		const clientProp = isYAxis ? "clientHeight" : "clientWidth";
		const endDiff = rects.reference[length] + rects.reference[axis] - coords[axis] - rects.floating[length];
		const startDiff = coords[axis] - rects.reference[axis];
		const arrowOffsetParent = await (platform.getOffsetParent == null ? void 0 : platform.getOffsetParent(element));
		let clientSize = arrowOffsetParent ? arrowOffsetParent[clientProp] : 0;
		if (!clientSize || !await (platform.isElement == null ? void 0 : platform.isElement(arrowOffsetParent))) clientSize = elements.floating[clientProp] || rects.floating[length];
		const centerToReference = endDiff / 2 - startDiff / 2;
		const largestPossiblePadding = clientSize / 2 - arrowDimensions[length] / 2 - 1;
		const minPadding = min(paddingObject[minProp], largestPossiblePadding);
		const maxPadding = min(paddingObject[maxProp], largestPossiblePadding);
		const max = clientSize - arrowDimensions[length] - maxPadding;
		const center = clientSize / 2 - arrowDimensions[length] / 2 + centerToReference;
		const offset = clamp(minPadding, center, max);
		const shouldAddOffset = !middlewareData.arrow && getAlignment(placement) != null && center !== offset && rects.reference[length] / 2 - (center < minPadding ? minPadding : maxPadding) - arrowDimensions[length] / 2 < 0;
		const alignmentOffset = shouldAddOffset ? center < minPadding ? center - minPadding : center - max : 0;
		return {
			[axis]: coords[axis] + alignmentOffset,
			data: {
				[axis]: offset,
				centerOffset: center - offset - alignmentOffset,
				...shouldAddOffset && { alignmentOffset }
			},
			reset: shouldAddOffset
		};
	}
});
/**
* Optimizes the visibility of the floating element by flipping the `placement`
* in order to keep it in view when the preferred placement(s) will overflow the
* clipping boundary. Alternative to `autoPlacement`.
* @see https://floating-ui.com/docs/flip
*/
var flip$2 = function(options) {
	if (options === void 0) options = {};
	return {
		name: "flip",
		options,
		async fn(state) {
			var _middlewareData$arrow, _middlewareData$flip;
			const { placement, middlewareData, rects, initialPlacement, platform, elements } = state;
			const { mainAxis: checkMainAxis = true, crossAxis: checkCrossAxis = true, fallbackPlacements: specifiedFallbackPlacements, fallbackStrategy = "bestFit", fallbackAxisSideDirection = "none", flipAlignment = true, ...detectOverflowOptions } = evaluate(options, state);
			if ((_middlewareData$arrow = middlewareData.arrow) != null && _middlewareData$arrow.alignmentOffset) return {};
			const side = getSide(placement);
			const initialSideAxis = getSideAxis(initialPlacement);
			const isBasePlacement = getSide(initialPlacement) === initialPlacement;
			const rtl = await (platform.isRTL == null ? void 0 : platform.isRTL(elements.floating));
			const fallbackPlacements = specifiedFallbackPlacements || (isBasePlacement || !flipAlignment ? [getOppositePlacement(initialPlacement)] : getExpandedPlacements(initialPlacement));
			const hasFallbackAxisSideDirection = fallbackAxisSideDirection !== "none";
			if (!specifiedFallbackPlacements && hasFallbackAxisSideDirection) fallbackPlacements.push(...getOppositeAxisPlacements(initialPlacement, flipAlignment, fallbackAxisSideDirection, rtl));
			const placements = [initialPlacement, ...fallbackPlacements];
			const overflow = await platform.detectOverflow(state, detectOverflowOptions);
			const overflows = [];
			let overflowsData = ((_middlewareData$flip = middlewareData.flip) == null ? void 0 : _middlewareData$flip.overflows) || [];
			if (checkMainAxis) overflows.push(overflow[side]);
			if (checkCrossAxis) {
				const sides = getAlignmentSides(placement, rects, rtl);
				overflows.push(overflow[sides[0]], overflow[sides[1]]);
			}
			overflowsData = [...overflowsData, {
				placement,
				overflows
			}];
			if (!overflows.every((side) => side <= 0)) {
				var _middlewareData$flip2, _overflowsData$filter;
				const nextIndex = (((_middlewareData$flip2 = middlewareData.flip) == null ? void 0 : _middlewareData$flip2.index) || 0) + 1;
				const nextPlacement = placements[nextIndex];
				if (nextPlacement) {
					if (!(checkCrossAxis === "alignment" ? initialSideAxis !== getSideAxis(nextPlacement) : false) || overflowsData.every((d) => getSideAxis(d.placement) === initialSideAxis ? d.overflows[0] > 0 : true)) return {
						data: {
							index: nextIndex,
							overflows: overflowsData
						},
						reset: { placement: nextPlacement }
					};
				}
				let resetPlacement = (_overflowsData$filter = overflowsData.filter((d) => d.overflows[0] <= 0).sort((a, b) => a.overflows[1] - b.overflows[1])[0]) == null ? void 0 : _overflowsData$filter.placement;
				if (!resetPlacement) switch (fallbackStrategy) {
					case "bestFit": {
						var _overflowsData$filter2;
						const placement = (_overflowsData$filter2 = overflowsData.filter((d) => {
							if (hasFallbackAxisSideDirection) {
								const currentSideAxis = getSideAxis(d.placement);
								return currentSideAxis === initialSideAxis || currentSideAxis === "y";
							}
							return true;
						}).map((d) => [d.placement, d.overflows.filter((overflow) => overflow > 0).reduce((acc, overflow) => acc + overflow, 0)]).sort((a, b) => a[1] - b[1])[0]) == null ? void 0 : _overflowsData$filter2[0];
						if (placement) resetPlacement = placement;
						break;
					}
					case "initialPlacement":
						resetPlacement = initialPlacement;
						break;
				}
				if (placement !== resetPlacement) return { reset: { placement: resetPlacement } };
			}
			return {};
		}
	};
};
var originSides = /*#__PURE__*/ new Set(["left", "top"]);
async function convertValueToCoords(state, options) {
	const { placement, platform, elements } = state;
	const rtl = await (platform.isRTL == null ? void 0 : platform.isRTL(elements.floating));
	const side = getSide(placement);
	const alignment = getAlignment(placement);
	const isVertical = getSideAxis(placement) === "y";
	const mainAxisMulti = originSides.has(side) ? -1 : 1;
	const crossAxisMulti = rtl && isVertical ? -1 : 1;
	const rawValue = evaluate(options, state);
	let { mainAxis, crossAxis, alignmentAxis } = typeof rawValue === "number" ? {
		mainAxis: rawValue,
		crossAxis: 0,
		alignmentAxis: null
	} : {
		mainAxis: rawValue.mainAxis || 0,
		crossAxis: rawValue.crossAxis || 0,
		alignmentAxis: rawValue.alignmentAxis
	};
	if (alignment && typeof alignmentAxis === "number") crossAxis = alignment === "end" ? alignmentAxis * -1 : alignmentAxis;
	return isVertical ? {
		x: crossAxis * crossAxisMulti,
		y: mainAxis * mainAxisMulti
	} : {
		x: mainAxis * mainAxisMulti,
		y: crossAxis * crossAxisMulti
	};
}
/**
* Modifies the placement by translating the floating element along the
* specified axes.
* A number (shorthand for `mainAxis` or distance), or an axes configuration
* object may be passed.
* @see https://floating-ui.com/docs/offset
*/
var offset$2 = function(options) {
	if (options === void 0) options = 0;
	return {
		name: "offset",
		options,
		async fn(state) {
			var _middlewareData$offse, _middlewareData$arrow;
			const { x, y, placement, middlewareData } = state;
			const diffCoords = await convertValueToCoords(state, options);
			if (placement === ((_middlewareData$offse = middlewareData.offset) == null ? void 0 : _middlewareData$offse.placement) && (_middlewareData$arrow = middlewareData.arrow) != null && _middlewareData$arrow.alignmentOffset) return {};
			return {
				x: x + diffCoords.x,
				y: y + diffCoords.y,
				data: {
					...diffCoords,
					placement
				}
			};
		}
	};
};
/**
* Optimizes the visibility of the floating element by shifting it in order to
* keep it in view when it will overflow the clipping boundary.
* @see https://floating-ui.com/docs/shift
*/
var shift$2 = function(options) {
	if (options === void 0) options = {};
	return {
		name: "shift",
		options,
		async fn(state) {
			const { x, y, placement, platform } = state;
			const { mainAxis: checkMainAxis = true, crossAxis: checkCrossAxis = false, limiter = { fn: (_ref) => {
				let { x, y } = _ref;
				return {
					x,
					y
				};
			} }, ...detectOverflowOptions } = evaluate(options, state);
			const coords = {
				x,
				y
			};
			const overflow = await platform.detectOverflow(state, detectOverflowOptions);
			const crossAxis = getSideAxis(placement);
			const mainAxis = getOppositeAxis(crossAxis);
			let mainAxisCoord = coords[mainAxis];
			let crossAxisCoord = coords[crossAxis];
			const clampCoord = (axis, coord) => clamp(coord + overflow[axis === "y" ? "top" : "left"], coord, coord - overflow[axis === "y" ? "bottom" : "right"]);
			if (checkMainAxis) mainAxisCoord = clampCoord(mainAxis, mainAxisCoord);
			if (checkCrossAxis) crossAxisCoord = clampCoord(crossAxis, crossAxisCoord);
			const limitedCoords = limiter.fn({
				...state,
				[mainAxis]: mainAxisCoord,
				[crossAxis]: crossAxisCoord
			});
			return {
				...limitedCoords,
				data: {
					x: limitedCoords.x - x,
					y: limitedCoords.y - y,
					enabled: {
						[mainAxis]: checkMainAxis,
						[crossAxis]: checkCrossAxis
					}
				}
			};
		}
	};
};
/**
* Built-in `limiter` that will stop `shift()` at a certain point.
*/
var limitShift$2 = function(options) {
	if (options === void 0) options = {};
	return {
		options,
		fn(state) {
			var _rawOffset$mainAxis, _rawOffset$crossAxis;
			const { x, y, placement, rects, middlewareData } = state;
			const { offset = 0, mainAxis: checkMainAxis = true, crossAxis: checkCrossAxis = true } = evaluate(options, state);
			const coords = {
				x,
				y
			};
			const crossAxis = getSideAxis(placement);
			const mainAxis = getOppositeAxis(crossAxis);
			let mainAxisCoord = coords[mainAxis];
			let crossAxisCoord = coords[crossAxis];
			const rawOffset = evaluate(offset, state);
			const computedOffset = typeof rawOffset === "number" ? {
				mainAxis: rawOffset,
				crossAxis: 0
			} : {
				mainAxis: (_rawOffset$mainAxis = rawOffset.mainAxis) != null ? _rawOffset$mainAxis : 0,
				crossAxis: (_rawOffset$crossAxis = rawOffset.crossAxis) != null ? _rawOffset$crossAxis : 0
			};
			if (checkMainAxis) {
				const len = mainAxis === "y" ? "height" : "width";
				const limitMin = rects.reference[mainAxis] - rects.floating[len] + computedOffset.mainAxis;
				const limitMax = rects.reference[mainAxis] + rects.reference[len] - computedOffset.mainAxis;
				if (mainAxisCoord < limitMin) mainAxisCoord = limitMin;
				else if (mainAxisCoord > limitMax) mainAxisCoord = limitMax;
			}
			if (checkCrossAxis) {
				var _middlewareData$offse, _middlewareData$offse2;
				const len = mainAxis === "y" ? "width" : "height";
				const isOriginSide = originSides.has(getSide(placement));
				const limitMin = rects.reference[crossAxis] - rects.floating[len] + (isOriginSide ? ((_middlewareData$offse = middlewareData.offset) == null ? void 0 : _middlewareData$offse[crossAxis]) || 0 : 0) + (isOriginSide ? 0 : computedOffset.crossAxis);
				const limitMax = rects.reference[crossAxis] + rects.reference[len] + (isOriginSide ? 0 : ((_middlewareData$offse2 = middlewareData.offset) == null ? void 0 : _middlewareData$offse2[crossAxis]) || 0) - (isOriginSide ? computedOffset.crossAxis : 0);
				if (crossAxisCoord < limitMin) crossAxisCoord = limitMin;
				else if (crossAxisCoord > limitMax) crossAxisCoord = limitMax;
			}
			return {
				[mainAxis]: mainAxisCoord,
				[crossAxis]: crossAxisCoord
			};
		}
	};
};
//#endregion
//#region node_modules/@floating-ui/dom/dist/floating-ui.dom.mjs
function getCssDimensions(element) {
	const css = getComputedStyle$1(element);
	let width = parseFloat(css.width) || 0;
	let height = parseFloat(css.height) || 0;
	const hasOffset = isHTMLElement(element);
	const offsetWidth = hasOffset ? element.offsetWidth : width;
	const offsetHeight = hasOffset ? element.offsetHeight : height;
	const shouldFallback = round(width) !== offsetWidth || round(height) !== offsetHeight;
	if (shouldFallback) {
		width = offsetWidth;
		height = offsetHeight;
	}
	return {
		width,
		height,
		$: shouldFallback
	};
}
function unwrapElement(element) {
	return !isElement$1(element) ? element.contextElement : element;
}
function getScale(element) {
	const domElement = unwrapElement(element);
	if (!isHTMLElement(domElement)) return createCoords(1);
	const rect = domElement.getBoundingClientRect();
	const { width, height, $ } = getCssDimensions(domElement);
	let x = ($ ? round(rect.width) : rect.width) / width;
	let y = ($ ? round(rect.height) : rect.height) / height;
	if (!x || !Number.isFinite(x)) x = 1;
	if (!y || !Number.isFinite(y)) y = 1;
	return {
		x,
		y
	};
}
var noOffsets = /*#__PURE__*/ createCoords(0);
function getVisualOffsets(element) {
	const win = getWindow$1(element);
	if (!isWebKit() || !win.visualViewport) return noOffsets;
	return {
		x: win.visualViewport.offsetLeft,
		y: win.visualViewport.offsetTop
	};
}
function shouldAddVisualOffsets(element, isFixed, floatingOffsetParent) {
	if (isFixed === void 0) isFixed = false;
	return !!floatingOffsetParent && isFixed && floatingOffsetParent === getWindow$1(element);
}
function getBoundingClientRect(element, includeScale, isFixedStrategy, offsetParent) {
	if (includeScale === void 0) includeScale = false;
	if (isFixedStrategy === void 0) isFixedStrategy = false;
	const clientRect = element.getBoundingClientRect();
	const domElement = unwrapElement(element);
	let scale = createCoords(1);
	if (includeScale) if (offsetParent) {
		if (isElement$1(offsetParent)) scale = getScale(offsetParent);
	} else scale = getScale(element);
	const visualOffsets = shouldAddVisualOffsets(domElement, isFixedStrategy, offsetParent) ? getVisualOffsets(domElement) : createCoords(0);
	let x = (clientRect.left + visualOffsets.x) / scale.x;
	let y = (clientRect.top + visualOffsets.y) / scale.y;
	let width = clientRect.width / scale.x;
	let height = clientRect.height / scale.y;
	if (domElement && offsetParent) {
		const win = getWindow$1(domElement);
		const offsetWin = isElement$1(offsetParent) ? getWindow$1(offsetParent) : offsetParent;
		let currentWin = win;
		let currentIFrame = getFrameElement(currentWin);
		while (currentIFrame && offsetWin !== currentWin) {
			const iframeScale = getScale(currentIFrame);
			const iframeRect = currentIFrame.getBoundingClientRect();
			const css = getComputedStyle$1(currentIFrame);
			const left = iframeRect.left + (currentIFrame.clientLeft + parseFloat(css.paddingLeft)) * iframeScale.x;
			const top = iframeRect.top + (currentIFrame.clientTop + parseFloat(css.paddingTop)) * iframeScale.y;
			x *= iframeScale.x;
			y *= iframeScale.y;
			width *= iframeScale.x;
			height *= iframeScale.y;
			x += left;
			y += top;
			currentWin = getWindow$1(currentIFrame);
			currentIFrame = getFrameElement(currentWin);
		}
	}
	return rectToClientRect({
		width,
		height,
		x,
		y
	});
}
function getWindowScrollBarX(element, rect) {
	const leftScroll = getNodeScroll(element).scrollLeft;
	if (!rect) return getBoundingClientRect(getDocumentElement(element)).left + leftScroll;
	return rect.left + leftScroll;
}
function getHTMLOffset(documentElement, scroll) {
	const htmlRect = documentElement.getBoundingClientRect();
	return {
		x: htmlRect.left + scroll.scrollLeft - getWindowScrollBarX(documentElement, htmlRect),
		y: htmlRect.top + scroll.scrollTop
	};
}
function convertOffsetParentRelativeRectToViewportRelativeRect(_ref) {
	let { elements, rect, offsetParent, strategy } = _ref;
	const isFixed = strategy === "fixed";
	const documentElement = getDocumentElement(offsetParent);
	const topLayer = elements ? isTopLayer(elements.floating) : false;
	if (offsetParent === documentElement || topLayer && isFixed) return rect;
	let scroll = {
		scrollLeft: 0,
		scrollTop: 0
	};
	let scale = createCoords(1);
	const offsets = createCoords(0);
	const isOffsetParentAnElement = isHTMLElement(offsetParent);
	if (isOffsetParentAnElement || !isFixed) {
		if (getNodeName(offsetParent) !== "body" || isOverflowElement(documentElement)) scroll = getNodeScroll(offsetParent);
		if (isOffsetParentAnElement) {
			const offsetRect = getBoundingClientRect(offsetParent);
			scale = getScale(offsetParent);
			offsets.x = offsetRect.x + offsetParent.clientLeft;
			offsets.y = offsetRect.y + offsetParent.clientTop;
		}
	}
	const htmlOffset = documentElement && !isOffsetParentAnElement && !isFixed ? getHTMLOffset(documentElement, scroll) : createCoords(0);
	return {
		width: rect.width * scale.x,
		height: rect.height * scale.y,
		x: rect.x * scale.x - scroll.scrollLeft * scale.x + offsets.x + htmlOffset.x,
		y: rect.y * scale.y - scroll.scrollTop * scale.y + offsets.y + htmlOffset.y
	};
}
function getClientRects(element) {
	return element.getClientRects ? Array.from(element.getClientRects()) : [];
}
function getDocumentRect(html) {
	const scroll = getNodeScroll(html);
	const body = html.ownerDocument.body;
	const width = max(html.scrollWidth, html.clientWidth, body.scrollWidth, body.clientWidth);
	const height = max(html.scrollHeight, html.clientHeight, body.scrollHeight, body.clientHeight);
	let x = -scroll.scrollLeft + getWindowScrollBarX(html);
	const y = -scroll.scrollTop;
	if (getComputedStyle$1(body).direction === "rtl") x += max(html.clientWidth, body.clientWidth) - width;
	return {
		width,
		height,
		x,
		y
	};
}
var SCROLLBAR_MAX = 25;
function getViewportRect(element, strategy, rootBoundary) {
	if (rootBoundary === void 0) rootBoundary = "viewport";
	const isLayoutViewport = rootBoundary === "layoutViewport";
	const win = getWindow$1(element);
	const html = getDocumentElement(element);
	const visualViewport = win.visualViewport;
	let width = html.clientWidth;
	let height = html.clientHeight;
	let x = 0;
	let y = 0;
	if (visualViewport) {
		const layoutRelativeClientCoords = !isWebKit() || strategy === "fixed";
		if (isLayoutViewport) {
			if (!layoutRelativeClientCoords) {
				x = -visualViewport.offsetLeft;
				y = -visualViewport.offsetTop;
			}
		} else {
			width = visualViewport.width;
			height = visualViewport.height;
			if (layoutRelativeClientCoords) {
				x = visualViewport.offsetLeft;
				y = visualViewport.offsetTop;
			}
		}
	}
	if (getWindowScrollBarX(html) <= 0) {
		const doc = html.ownerDocument;
		const body = doc.body;
		const bodyStyles = getComputedStyle(body);
		const bodyMarginInline = doc.compatMode === "CSS1Compat" ? parseFloat(bodyStyles.marginLeft) + parseFloat(bodyStyles.marginRight) || 0 : 0;
		const reservedWidth = Math.abs(html.clientWidth - body.clientWidth - bodyMarginInline);
		const gutter = getComputedStyle(html).scrollbarGutter === "stable both-edges" ? reservedWidth / 2 : reservedWidth;
		if (gutter <= SCROLLBAR_MAX) width -= gutter;
	}
	return {
		width,
		height,
		x,
		y
	};
}
function getInnerBoundingClientRect(element, strategy) {
	const clientRect = getBoundingClientRect(element, true, strategy === "fixed");
	const top = clientRect.top + element.clientTop;
	const left = clientRect.left + element.clientLeft;
	const scale = getScale(element);
	return {
		width: element.clientWidth * scale.x,
		height: element.clientHeight * scale.y,
		x: left * scale.x,
		y: top * scale.y
	};
}
function getClientRectFromClippingAncestor(element, clippingAncestor, strategy) {
	let rect;
	if (clippingAncestor === "viewport" || clippingAncestor === "layoutViewport") rect = getViewportRect(element, strategy, clippingAncestor);
	else if (clippingAncestor === "document") rect = getDocumentRect(getDocumentElement(element));
	else if (isElement$1(clippingAncestor)) rect = getInnerBoundingClientRect(clippingAncestor, strategy);
	else {
		const visualOffsets = getVisualOffsets(element);
		rect = {
			x: clippingAncestor.x - visualOffsets.x,
			y: clippingAncestor.y - visualOffsets.y,
			width: clippingAncestor.width,
			height: clippingAncestor.height
		};
	}
	return rectToClientRect(rect);
}
function getClippingElementAncestors(element, cache) {
	const cachedResult = cache.get(element);
	if (cachedResult) return cachedResult;
	let result = getOverflowAncestors(element, [], false).filter((el) => isElement$1(el) && getNodeName(el) !== "body");
	let lastKeptComputedStyle = null;
	const elementIsFixed = getComputedStyle$1(element).position === "fixed";
	let currentNode = elementIsFixed ? getParentNode(element) : element;
	while (isElement$1(currentNode) && !isLastTraversableNode(currentNode)) {
		const computedStyle = getComputedStyle$1(currentNode);
		const currentNodeIsContaining = isContainingBlock(currentNode);
		const lastPosition = lastKeptComputedStyle ? lastKeptComputedStyle.position : elementIsFixed ? "fixed" : "";
		if (!currentNodeIsContaining && (lastPosition === "fixed" || lastPosition === "absolute" && computedStyle.position === "static")) result = result.filter((ancestor) => ancestor !== currentNode);
		else lastKeptComputedStyle = computedStyle;
		currentNode = getParentNode(currentNode);
	}
	cache.set(element, result);
	return result;
}
function getClippingRect(_ref) {
	let { element, boundary, rootBoundary, strategy } = _ref;
	const clippingAncestors = [...boundary === "clippingAncestors" ? isTopLayer(element) ? [] : getClippingElementAncestors(element, this._c) : [].concat(boundary), rootBoundary];
	const firstRect = getClientRectFromClippingAncestor(element, clippingAncestors[0], strategy);
	let top = firstRect.top;
	let right = firstRect.right;
	let bottom = firstRect.bottom;
	let left = firstRect.left;
	for (let i = 1; i < clippingAncestors.length; i++) {
		const rect = getClientRectFromClippingAncestor(element, clippingAncestors[i], strategy);
		top = max(rect.top, top);
		right = min(rect.right, right);
		bottom = min(rect.bottom, bottom);
		left = max(rect.left, left);
	}
	return {
		width: right - left,
		height: bottom - top,
		x: left,
		y: top
	};
}
function getDimensions(element) {
	const { width, height } = getCssDimensions(element);
	return {
		width,
		height
	};
}
function getRectRelativeToOffsetParent(element, offsetParent, strategy) {
	const isOffsetParentAnElement = isHTMLElement(offsetParent);
	const documentElement = getDocumentElement(offsetParent);
	const isFixed = strategy === "fixed";
	const rect = getBoundingClientRect(element, true, isFixed, offsetParent);
	let scroll = {
		scrollLeft: 0,
		scrollTop: 0
	};
	const offsets = createCoords(0);
	if (isOffsetParentAnElement || !isFixed) {
		if (getNodeName(offsetParent) !== "body" || isOverflowElement(documentElement)) scroll = getNodeScroll(offsetParent);
		if (isOffsetParentAnElement) {
			const offsetRect = getBoundingClientRect(offsetParent, true, isFixed, offsetParent);
			offsets.x = offsetRect.x + offsetParent.clientLeft;
			offsets.y = offsetRect.y + offsetParent.clientTop;
		}
	}
	if (!isOffsetParentAnElement && documentElement) offsets.x = getWindowScrollBarX(documentElement);
	const htmlOffset = documentElement && !isOffsetParentAnElement && !isFixed ? getHTMLOffset(documentElement, scroll) : createCoords(0);
	return {
		x: rect.left + scroll.scrollLeft - offsets.x - htmlOffset.x,
		y: rect.top + scroll.scrollTop - offsets.y - htmlOffset.y,
		width: rect.width,
		height: rect.height
	};
}
function isStaticPositioned(element) {
	return getComputedStyle$1(element).position === "static";
}
function getTrueOffsetParent(element, polyfill) {
	if (!isHTMLElement(element) || getComputedStyle$1(element).position === "fixed") return null;
	if (polyfill) return polyfill(element);
	let rawOffsetParent = element.offsetParent;
	if (getDocumentElement(element) === rawOffsetParent) rawOffsetParent = rawOffsetParent.ownerDocument.body;
	return rawOffsetParent;
}
function getOffsetParent(element, polyfill) {
	const win = getWindow$1(element);
	if (isTopLayer(element)) return win;
	if (!isHTMLElement(element)) {
		let svgOffsetParent = getParentNode(element);
		while (svgOffsetParent && !isLastTraversableNode(svgOffsetParent)) {
			if (isElement$1(svgOffsetParent) && !isStaticPositioned(svgOffsetParent)) return svgOffsetParent;
			svgOffsetParent = getParentNode(svgOffsetParent);
		}
		return win;
	}
	let offsetParent = getTrueOffsetParent(element, polyfill);
	while (offsetParent && isTableElement(offsetParent) && isStaticPositioned(offsetParent)) offsetParent = getTrueOffsetParent(offsetParent, polyfill);
	if (offsetParent && isLastTraversableNode(offsetParent) && isStaticPositioned(offsetParent) && !isContainingBlock(offsetParent)) return win;
	return offsetParent || getContainingBlock(element) || win;
}
var getElementRects = async function(data) {
	const getOffsetParentFn = this.getOffsetParent || getOffsetParent;
	const getDimensionsFn = this.getDimensions;
	const floatingDimensions = await getDimensionsFn(data.floating);
	return {
		reference: getRectRelativeToOffsetParent(data.reference, await getOffsetParentFn(data.floating), data.strategy),
		floating: {
			x: 0,
			y: 0,
			width: floatingDimensions.width,
			height: floatingDimensions.height
		}
	};
};
function isRTL(element) {
	return getComputedStyle$1(element).direction === "rtl";
}
var platform = {
	convertOffsetParentRelativeRectToViewportRelativeRect,
	getDocumentElement,
	getClippingRect,
	getOffsetParent,
	getElementRects,
	getClientRects,
	getDimensions,
	getScale,
	isElement: isElement$1,
	isRTL
};
function rectsAreEqual(a, b) {
	return a.x === b.x && a.y === b.y && a.width === b.width && a.height === b.height;
}
function observeMove(element, onMove, ancestorResize) {
	let io = null;
	let timeoutId;
	const root = getDocumentElement(element);
	function cleanup() {
		var _io;
		clearTimeout(timeoutId);
		(_io = io) == null || _io.disconnect();
		io = null;
	}
	function refresh(skip, threshold) {
		if (skip === void 0) skip = false;
		if (threshold === void 0) threshold = 1;
		cleanup();
		const elementRectForRootMargin = element.getBoundingClientRect();
		const { left, top, width, height } = elementRectForRootMargin;
		if (!skip) onMove();
		if (!width || !height) return;
		const insetTop = floor(top);
		const insetRight = floor(root.clientWidth - (left + width));
		const insetBottom = floor(root.clientHeight - (top + height));
		const insetLeft = floor(left);
		const options = {
			rootMargin: -insetTop + "px " + -insetRight + "px " + -insetBottom + "px " + -insetLeft + "px",
			threshold: max(0, min(1, threshold)) || 1
		};
		let isFirstUpdate = true;
		function handleObserve(entries) {
			const ratio = entries[0].intersectionRatio;
			if (!rectsAreEqual(elementRectForRootMargin, element.getBoundingClientRect())) return refresh();
			if (ratio !== threshold) {
				if (!isFirstUpdate) return refresh();
				if (!ratio) timeoutId = setTimeout(() => {
					refresh(false, 1e-7);
				}, 1e3);
				else refresh(false, ratio);
			}
			isFirstUpdate = false;
		}
		try {
			io = new IntersectionObserver(handleObserve, {
				...options,
				root: root.ownerDocument
			});
		} catch (_e) {
			io = new IntersectionObserver(handleObserve, options);
		}
		io.observe(element);
	}
	const win = getWindow$1(element);
	const handleResize = () => refresh(ancestorResize);
	win.addEventListener("resize", handleResize);
	refresh(true);
	return () => {
		win.removeEventListener("resize", handleResize);
		cleanup();
	};
}
/**
* Automatically updates the position of the floating element when necessary.
* Should only be called when the floating element is mounted on the DOM or
* visible on the screen.
* @returns cleanup function that should be invoked when the floating element is
* removed from the DOM or hidden from the screen.
* @see https://floating-ui.com/docs/autoUpdate
*/
function autoUpdate(reference, floating, update, options) {
	if (options === void 0) options = {};
	const { ancestorScroll = true, ancestorResize = true, elementResize = typeof ResizeObserver === "function", layoutShift = typeof IntersectionObserver === "function", animationFrame = false } = options;
	const referenceEl = unwrapElement(reference);
	const ancestors = ancestorScroll || ancestorResize ? [...referenceEl ? getOverflowAncestors(referenceEl) : [], ...floating ? getOverflowAncestors(floating) : []] : [];
	ancestors.forEach((ancestor) => {
		ancestorScroll && ancestor.addEventListener("scroll", update);
		ancestorResize && ancestor.addEventListener("resize", update);
	});
	const cleanupIo = referenceEl && layoutShift ? observeMove(referenceEl, update, ancestorResize) : null;
	let reobserveFrame = -1;
	let resizeObserver = null;
	if (elementResize) {
		resizeObserver = new ResizeObserver((_ref) => {
			let [firstEntry] = _ref;
			if (firstEntry && firstEntry.target === referenceEl && resizeObserver && floating) {
				resizeObserver.unobserve(floating);
				cancelAnimationFrame(reobserveFrame);
				reobserveFrame = requestAnimationFrame(() => {
					var _resizeObserver;
					(_resizeObserver = resizeObserver) == null || _resizeObserver.observe(floating);
				});
			}
			update();
		});
		if (referenceEl && !animationFrame) resizeObserver.observe(referenceEl);
		if (floating) resizeObserver.observe(floating);
	}
	let frameId;
	let prevRefRect = animationFrame ? getBoundingClientRect(reference) : null;
	if (animationFrame) frameLoop();
	function frameLoop() {
		const nextRefRect = getBoundingClientRect(reference);
		if (prevRefRect && !rectsAreEqual(prevRefRect, nextRefRect)) update();
		prevRefRect = nextRefRect;
		frameId = requestAnimationFrame(frameLoop);
	}
	update();
	return () => {
		var _resizeObserver2;
		ancestors.forEach((ancestor) => {
			ancestorScroll && ancestor.removeEventListener("scroll", update);
			ancestorResize && ancestor.removeEventListener("resize", update);
		});
		cleanupIo?.();
		(_resizeObserver2 = resizeObserver) == null || _resizeObserver2.disconnect();
		resizeObserver = null;
		if (animationFrame) cancelAnimationFrame(frameId);
	};
}
/**
* Modifies the placement by translating the floating element along the
* specified axes.
* A number (shorthand for `mainAxis` or distance), or an axes configuration
* object may be passed.
* @see https://floating-ui.com/docs/offset
*/
var offset$1 = offset$2;
/**
* Optimizes the visibility of the floating element by shifting it in order to
* keep it in view when it will overflow the clipping boundary.
* @see https://floating-ui.com/docs/shift
*/
var shift$1 = shift$2;
/**
* Optimizes the visibility of the floating element by flipping the `placement`
* in order to keep it in view when the preferred placement(s) will overflow the
* clipping boundary. Alternative to `autoPlacement`.
* @see https://floating-ui.com/docs/flip
*/
var flip$1 = flip$2;
/**
* Provides data to position an inner element of the floating element so that it
* appears centered to the reference element.
* @see https://floating-ui.com/docs/arrow
*/
var arrow$2 = arrow$3;
/**
* Built-in `limiter` that will stop `shift()` at a certain point.
*/
var limitShift$1 = limitShift$2;
/**
* Computes the `x` and `y` coordinates that will place the floating element
* next to a given reference element.
*/
var computePosition = (reference, floating, options) => {
	const cache = /* @__PURE__ */ new Map();
	const mergedOptions = options != null ? options : {};
	const platformWithCache = {
		...platform,
		...mergedOptions.platform,
		_c: cache
	};
	return computePosition$1(reference, floating, {
		...mergedOptions,
		platform: platformWithCache
	});
};
//#endregion
//#region node_modules/@floating-ui/react-dom/dist/floating-ui.react-dom.mjs
var index$1 = typeof document !== "undefined" ? import_react.useLayoutEffect : function noop() {};
function deepEqual(a, b) {
	if (a === b) return true;
	if (typeof a !== typeof b) return false;
	if (typeof a === "function" && a.toString() === b.toString()) return true;
	let length;
	let i;
	let keys;
	if (a && b && typeof a === "object") {
		if (Array.isArray(a)) {
			length = a.length;
			if (length !== b.length) return false;
			for (i = length; i-- !== 0;) if (!deepEqual(a[i], b[i])) return false;
			return true;
		}
		keys = Object.keys(a);
		length = keys.length;
		if (length !== Object.keys(b).length) return false;
		for (i = length; i-- !== 0;) if (!{}.hasOwnProperty.call(b, keys[i])) return false;
		for (i = length; i-- !== 0;) {
			const key = keys[i];
			if (key === "_owner" && a.$$typeof) continue;
			if (!deepEqual(a[key], b[key])) return false;
		}
		return true;
	}
	return a !== a && b !== b;
}
function getDPR(element) {
	if (typeof window === "undefined") return 1;
	return (element.ownerDocument.defaultView || window).devicePixelRatio || 1;
}
function roundByDPR(element, value) {
	const dpr = getDPR(element);
	return Math.round(value * dpr) / dpr;
}
function useLatestRef$1(value) {
	const ref = import_react.useRef(value);
	index$1(() => {
		ref.current = value;
	});
	return ref;
}
/**
* Provides data to position a floating element.
* @see https://floating-ui.com/docs/useFloating
*/
function useFloating$1(options) {
	if (options === void 0) options = {};
	const { placement = "bottom", strategy = "absolute", middleware = [], platform, elements: { reference: externalReference, floating: externalFloating } = {}, transform = true, whileElementsMounted, open } = options;
	const [data, setData] = import_react.useState({
		x: 0,
		y: 0,
		strategy,
		placement,
		middlewareData: {},
		isPositioned: false
	});
	const [latestMiddleware, setLatestMiddleware] = import_react.useState(middleware);
	if (!deepEqual(latestMiddleware, middleware)) setLatestMiddleware(middleware);
	const [_reference, _setReference] = import_react.useState(null);
	const [_floating, _setFloating] = import_react.useState(null);
	const setReference = import_react.useCallback((node) => {
		if (node !== referenceRef.current) {
			referenceRef.current = node;
			_setReference(node);
		}
	}, []);
	const setFloating = import_react.useCallback((node) => {
		if (node !== floatingRef.current) {
			floatingRef.current = node;
			_setFloating(node);
		}
	}, []);
	const referenceEl = externalReference || _reference;
	const floatingEl = externalFloating || _floating;
	const referenceRef = import_react.useRef(null);
	const floatingRef = import_react.useRef(null);
	const dataRef = import_react.useRef(data);
	const hasWhileElementsMounted = whileElementsMounted != null;
	const whileElementsMountedRef = useLatestRef$1(whileElementsMounted);
	const platformRef = useLatestRef$1(platform);
	const openRef = useLatestRef$1(open);
	const update = import_react.useCallback(() => {
		if (!referenceRef.current || !floatingRef.current) return;
		const config = {
			placement,
			strategy,
			middleware: latestMiddleware
		};
		if (platformRef.current) config.platform = platformRef.current;
		computePosition(referenceRef.current, floatingRef.current, config).then((data) => {
			const fullData = {
				...data,
				isPositioned: openRef.current !== false
			};
			if (isMountedRef.current && !deepEqual(dataRef.current, fullData)) {
				dataRef.current = fullData;
				import_react_dom.flushSync(() => {
					setData(fullData);
				});
			}
		});
	}, [
		latestMiddleware,
		placement,
		strategy,
		platformRef,
		openRef
	]);
	index$1(() => {
		if (open === false && dataRef.current.isPositioned) {
			dataRef.current.isPositioned = false;
			setData((data) => ({
				...data,
				isPositioned: false
			}));
		}
	}, [open]);
	const isMountedRef = import_react.useRef(false);
	index$1(() => {
		isMountedRef.current = true;
		return () => {
			isMountedRef.current = false;
		};
	}, []);
	index$1(() => {
		if (referenceEl) referenceRef.current = referenceEl;
		if (floatingEl) floatingRef.current = floatingEl;
		if (referenceEl && floatingEl) {
			if (whileElementsMountedRef.current) return whileElementsMountedRef.current(referenceEl, floatingEl, update);
			update();
		}
	}, [
		referenceEl,
		floatingEl,
		update,
		whileElementsMountedRef,
		hasWhileElementsMounted
	]);
	const refs = import_react.useMemo(() => ({
		reference: referenceRef,
		floating: floatingRef,
		setReference,
		setFloating
	}), [setReference, setFloating]);
	const elements = import_react.useMemo(() => ({
		reference: referenceEl,
		floating: floatingEl
	}), [referenceEl, floatingEl]);
	const floatingStyles = import_react.useMemo(() => {
		const initialStyles = {
			position: strategy,
			left: 0,
			top: 0
		};
		if (!elements.floating) return initialStyles;
		const x = roundByDPR(elements.floating, data.x);
		const y = roundByDPR(elements.floating, data.y);
		if (transform) return {
			...initialStyles,
			transform: "translate(" + x + "px, " + y + "px)",
			...getDPR(elements.floating) >= 1.5 && { willChange: "transform" }
		};
		return {
			position: strategy,
			left: x,
			top: y
		};
	}, [
		strategy,
		transform,
		elements.floating,
		data.x,
		data.y
	]);
	return import_react.useMemo(() => ({
		...data,
		update,
		refs,
		elements,
		floatingStyles
	}), [
		data,
		update,
		refs,
		elements,
		floatingStyles
	]);
}
/**
* Provides data to position an inner element of the floating element so that it
* appears centered to the reference element.
* This wraps the core `arrow` middleware to allow React refs as the element.
* @see https://floating-ui.com/docs/arrow
*/
var arrow$1 = (options) => {
	function isRef(value) {
		return {}.hasOwnProperty.call(value, "current");
	}
	return {
		name: "arrow",
		options,
		fn(state) {
			const { element, padding } = typeof options === "function" ? options(state) : options;
			if (element && isRef(element)) {
				if (element.current != null) return arrow$2({
					element: element.current,
					padding
				}).fn(state);
				return {};
			}
			if (element) return arrow$2({
				element,
				padding
			}).fn(state);
			return {};
		}
	};
};
/**
* Modifies the placement by translating the floating element along the
* specified axes.
* A number (shorthand for `mainAxis` or distance), or an axes configuration
* object may be passed.
* @see https://floating-ui.com/docs/offset
*/
var offset = (options, deps) => {
	const result = offset$1(options);
	return {
		name: result.name,
		fn: result.fn,
		options: [options, deps]
	};
};
/**
* Optimizes the visibility of the floating element by shifting it in order to
* keep it in view when it will overflow the clipping boundary.
* @see https://floating-ui.com/docs/shift
*/
var shift = (options, deps) => {
	const result = shift$1(options);
	return {
		name: result.name,
		fn: result.fn,
		options: [options, deps]
	};
};
/**
* Built-in `limiter` that will stop `shift()` at a certain point.
*/
var limitShift = (options, deps) => {
	return {
		fn: limitShift$1(options).fn,
		options: [options, deps]
	};
};
/**
* Optimizes the visibility of the floating element by flipping the `placement`
* in order to keep it in view when the preferred placement(s) will overflow the
* clipping boundary. Alternative to `autoPlacement`.
* @see https://floating-ui.com/docs/flip
*/
var flip = (options, deps) => {
	const result = flip$1(options);
	return {
		name: result.name,
		fn: result.fn,
		options: [options, deps]
	};
};
/**
* Provides data to position an inner element of the floating element so that it
* appears centered to the reference element.
* This wraps the core `arrow` middleware to allow React refs as the element.
* @see https://floating-ui.com/docs/arrow
*/
var arrow = (options, deps) => {
	const result = arrow$1(options);
	return {
		name: result.name,
		fn: result.fn,
		options: [options, deps]
	};
};
//#endregion
//#region node_modules/@floating-ui/react/dist/floating-ui.react.mjs
var SafeReact = { ...import_react };
var useSafeInsertionEffect = SafeReact.useInsertionEffect || ((fn) => fn());
function useEffectEvent(callback) {
	const ref = import_react.useRef(() => {
		throw new Error("Cannot call an event handler while rendering.");
	});
	useSafeInsertionEffect(() => {
		ref.current = callback;
	});
	return import_react.useCallback(function() {
		for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) args[_key] = arguments[_key];
		return ref.current == null ? void 0 : ref.current(...args);
	}, []);
}
var ARROW_UP = "ArrowUp";
var ARROW_DOWN = "ArrowDown";
var ARROW_LEFT = "ArrowLeft";
var ARROW_RIGHT = "ArrowRight";
var index = typeof document !== "undefined" ? import_react.useLayoutEffect : import_react.useEffect;
var horizontalKeys = [ARROW_LEFT, ARROW_RIGHT];
var verticalKeys = [ARROW_UP, ARROW_DOWN];
[...horizontalKeys, ...verticalKeys];
function _extends() {
	_extends = Object.assign ? Object.assign.bind() : function(target) {
		for (var i = 1; i < arguments.length; i++) {
			var source = arguments[i];
			for (var key in source) if (Object.prototype.hasOwnProperty.call(source, key)) target[key] = source[key];
		}
		return target;
	};
	return _extends.apply(this, arguments);
}
var serverHandoffComplete = false;
var count = 0;
var genId = () => "floating-ui-" + Math.random().toString(36).slice(2, 6) + count++;
function useFloatingId() {
	const [id, setId] = import_react.useState(() => serverHandoffComplete ? genId() : void 0);
	index(() => {
		if (id == null) setId(genId());
	}, []);
	import_react.useEffect(() => {
		serverHandoffComplete = true;
	}, []);
	return id;
}
/**
* Uses React 18's built-in `useId()` when available, or falls back to a
* slightly less performant (requiring a double render) implementation for
* earlier React versions.
* @see https://floating-ui.com/docs/react-utils#useid
*/
var useId$1 = SafeReact.useId || useFloatingId;
var devMessageSet = /*#__PURE__*/ new Set();
function warn() {
	var _devMessageSet;
	for (var _len = arguments.length, messages = new Array(_len), _key = 0; _key < _len; _key++) messages[_key] = arguments[_key];
	const message = "Floating UI: " + messages.join(" ");
	if (!((_devMessageSet = devMessageSet) != null && _devMessageSet.has(message))) {
		var _devMessageSet2;
		(_devMessageSet2 = devMessageSet) == null || _devMessageSet2.add(message);
		console.warn(message);
	}
}
function error() {
	var _devMessageSet3;
	for (var _len2 = arguments.length, messages = new Array(_len2), _key2 = 0; _key2 < _len2; _key2++) messages[_key2] = arguments[_key2];
	const message = "Floating UI: " + messages.join(" ");
	if (!((_devMessageSet3 = devMessageSet) != null && _devMessageSet3.has(message))) {
		var _devMessageSet4;
		(_devMessageSet4 = devMessageSet) == null || _devMessageSet4.add(message);
		console.error(message);
	}
}
/**
* Renders a pointing arrow triangle.
* @see https://floating-ui.com/docs/FloatingArrow
*/
var FloatingArrow = /*#__PURE__*/ import_react.forwardRef(function FloatingArrow(props, ref) {
	const { context: { placement, elements: { floating }, middlewareData: { arrow, shift } }, width = 14, height = 7, tipRadius = 0, strokeWidth = 0, staticOffset, stroke, d, style: { transform, ...restStyle } = {}, ...rest } = props;
	if (!ref) warn("The `ref` prop is required for `FloatingArrow`.");
	const clipPathId = useId$1();
	const [isRTL, setIsRTL] = import_react.useState(false);
	index(() => {
		if (!floating) return;
		if (getComputedStyle$1(floating).direction === "rtl") setIsRTL(true);
	}, [floating]);
	if (!floating) return null;
	const [side, alignment] = placement.split("-");
	const isVerticalSide = side === "top" || side === "bottom";
	let computedStaticOffset = staticOffset;
	if (isVerticalSide && shift != null && shift.x || !isVerticalSide && shift != null && shift.y) computedStaticOffset = null;
	const computedStrokeWidth = strokeWidth * 2;
	const halfStrokeWidth = computedStrokeWidth / 2;
	const svgX = width / 2 * (tipRadius / -8 + 1);
	const svgY = height / 2 * tipRadius / 4;
	const isCustomShape = !!d;
	const yOffsetProp = computedStaticOffset && alignment === "end" ? "bottom" : "top";
	let xOffsetProp = computedStaticOffset && alignment === "end" ? "right" : "left";
	if (computedStaticOffset && isRTL) xOffsetProp = alignment === "end" ? "left" : "right";
	const arrowX = (arrow == null ? void 0 : arrow.x) != null ? computedStaticOffset || arrow.x : "";
	const arrowY = (arrow == null ? void 0 : arrow.y) != null ? computedStaticOffset || arrow.y : "";
	const dValue = d || "M0,0" + (" H" + width) + (" L" + (width - svgX) + "," + (height - svgY)) + (" Q" + width / 2 + "," + height + " " + svgX + "," + (height - svgY)) + " Z";
	const rotation = {
		top: isCustomShape ? "rotate(180deg)" : "",
		left: isCustomShape ? "rotate(90deg)" : "rotate(-90deg)",
		bottom: isCustomShape ? "" : "rotate(180deg)",
		right: isCustomShape ? "rotate(-90deg)" : "rotate(90deg)"
	}[side];
	return /*#__PURE__*/ import_react.createElement("svg", _extends({}, rest, {
		"aria-hidden": true,
		ref,
		width: isCustomShape ? width : width + computedStrokeWidth,
		height: width,
		viewBox: "0 0 " + width + " " + (height > width ? height : width),
		style: {
			position: "absolute",
			pointerEvents: "none",
			[xOffsetProp]: arrowX,
			[yOffsetProp]: arrowY,
			[side]: isVerticalSide || isCustomShape ? "100%" : "calc(100% - " + computedStrokeWidth / 2 + "px)",
			transform: [rotation, transform].filter((t) => !!t).join(" "),
			...restStyle
		}
	}), computedStrokeWidth > 0 && /*#__PURE__*/ import_react.createElement("path", {
		clipPath: "url(#" + clipPathId + ")",
		fill: "none",
		stroke,
		strokeWidth: computedStrokeWidth + (d ? 0 : 1),
		d: dValue
	}), /*#__PURE__*/ import_react.createElement("path", {
		stroke: computedStrokeWidth && !d ? rest.fill : "none",
		d: dValue
	}), /*#__PURE__*/ import_react.createElement("clipPath", { id: clipPathId }, /*#__PURE__*/ import_react.createElement("rect", {
		x: -halfStrokeWidth,
		y: halfStrokeWidth * (isCustomShape ? -1 : 1),
		width: width + computedStrokeWidth,
		height: width
	})));
});
function createPubSub() {
	const map = /* @__PURE__ */ new Map();
	return {
		emit(event, data) {
			var _map$get;
			(_map$get = map.get(event)) == null || _map$get.forEach((handler) => handler(data));
		},
		on(event, listener) {
			map.set(event, [...map.get(event) || [], listener]);
		},
		off(event, listener) {
			var _map$get2;
			map.set(event, ((_map$get2 = map.get(event)) == null ? void 0 : _map$get2.filter((l) => l !== listener)) || []);
		}
	};
}
var FloatingNodeContext = /*#__PURE__*/ import_react.createContext(null);
var FloatingTreeContext = /*#__PURE__*/ import_react.createContext(null);
/**
* Returns the parent node id for nested floating elements, if available.
* Returns `null` for top-level floating elements.
*/
var useFloatingParentNodeId = () => {
	var _React$useContext;
	return ((_React$useContext = import_react.useContext(FloatingNodeContext)) == null ? void 0 : _React$useContext.id) || null;
};
/**
* Returns the nearest floating tree context, if available.
*/
var useFloatingTree = () => import_react.useContext(FloatingTreeContext);
function createAttribute(name) {
	return "data-floating-ui-" + name;
}
function useLatestRef(value) {
	const ref = (0, import_react.useRef)(value);
	index(() => {
		ref.current = value;
	});
	return ref;
}
var safePolygonIdentifier = /*#__PURE__*/ createAttribute("safe-polygon");
function getDelay$1(value, prop, pointerType) {
	if (pointerType && !isMouseLikePointerType(pointerType)) return 0;
	if (typeof value === "number") return value;
	return value == null ? void 0 : value[prop];
}
/**
* Opens the floating element while hovering over the reference element, like
* CSS `:hover`.
* @see https://floating-ui.com/docs/useHover
*/
function useHover(context, props) {
	if (props === void 0) props = {};
	const { open, onOpenChange, dataRef, events, elements } = context;
	const { enabled = true, delay = 0, handleClose = null, mouseOnly = false, restMs = 0, move = true } = props;
	const tree = useFloatingTree();
	const parentId = useFloatingParentNodeId();
	const handleCloseRef = useLatestRef(handleClose);
	const delayRef = useLatestRef(delay);
	const openRef = useLatestRef(open);
	const pointerTypeRef = import_react.useRef();
	const timeoutRef = import_react.useRef(-1);
	const handlerRef = import_react.useRef();
	const restTimeoutRef = import_react.useRef(-1);
	const blockMouseMoveRef = import_react.useRef(true);
	const performedPointerEventsMutationRef = import_react.useRef(false);
	const unbindMouseMoveRef = import_react.useRef(() => {});
	const restTimeoutPendingRef = import_react.useRef(false);
	const isHoverOpen = import_react.useCallback(() => {
		var _dataRef$current$open;
		const type = (_dataRef$current$open = dataRef.current.openEvent) == null ? void 0 : _dataRef$current$open.type;
		return (type == null ? void 0 : type.includes("mouse")) && type !== "mousedown";
	}, [dataRef]);
	import_react.useEffect(() => {
		if (!enabled) return;
		function onOpenChange(_ref) {
			let { open } = _ref;
			if (!open) {
				clearTimeout(timeoutRef.current);
				clearTimeout(restTimeoutRef.current);
				blockMouseMoveRef.current = true;
				restTimeoutPendingRef.current = false;
			}
		}
		events.on("openchange", onOpenChange);
		return () => {
			events.off("openchange", onOpenChange);
		};
	}, [enabled, events]);
	import_react.useEffect(() => {
		if (!enabled) return;
		if (!handleCloseRef.current) return;
		if (!open) return;
		function onLeave(event) {
			if (isHoverOpen()) onOpenChange(false, event, "hover");
		}
		const html = getDocument$1(elements.floating).documentElement;
		html.addEventListener("mouseleave", onLeave);
		return () => {
			html.removeEventListener("mouseleave", onLeave);
		};
	}, [
		elements.floating,
		open,
		onOpenChange,
		enabled,
		handleCloseRef,
		isHoverOpen
	]);
	const closeWithDelay = import_react.useCallback(function(event, runElseBranch, reason) {
		if (runElseBranch === void 0) runElseBranch = true;
		if (reason === void 0) reason = "hover";
		const closeDelay = getDelay$1(delayRef.current, "close", pointerTypeRef.current);
		if (closeDelay && !handlerRef.current) {
			clearTimeout(timeoutRef.current);
			timeoutRef.current = window.setTimeout(() => onOpenChange(false, event, reason), closeDelay);
		} else if (runElseBranch) {
			clearTimeout(timeoutRef.current);
			onOpenChange(false, event, reason);
		}
	}, [delayRef, onOpenChange]);
	const cleanupMouseMoveHandler = useEffectEvent(() => {
		unbindMouseMoveRef.current();
		handlerRef.current = void 0;
	});
	const clearPointerEvents = useEffectEvent(() => {
		if (performedPointerEventsMutationRef.current) {
			const body = getDocument$1(elements.floating).body;
			body.style.pointerEvents = "";
			body.removeAttribute(safePolygonIdentifier);
			performedPointerEventsMutationRef.current = false;
		}
	});
	const isClickLikeOpenEvent = useEffectEvent(() => {
		return dataRef.current.openEvent ? ["click", "mousedown"].includes(dataRef.current.openEvent.type) : false;
	});
	import_react.useEffect(() => {
		if (!enabled) return;
		function onMouseEnter(event) {
			clearTimeout(timeoutRef.current);
			blockMouseMoveRef.current = false;
			if (mouseOnly && !isMouseLikePointerType(pointerTypeRef.current) || restMs > 0 && !getDelay$1(delayRef.current, "open")) return;
			const openDelay = getDelay$1(delayRef.current, "open", pointerTypeRef.current);
			if (openDelay) timeoutRef.current = window.setTimeout(() => {
				if (!openRef.current) onOpenChange(true, event, "hover");
			}, openDelay);
			else if (!open) onOpenChange(true, event, "hover");
		}
		function onMouseLeave(event) {
			if (isClickLikeOpenEvent()) return;
			unbindMouseMoveRef.current();
			const doc = getDocument$1(elements.floating);
			clearTimeout(restTimeoutRef.current);
			restTimeoutPendingRef.current = false;
			if (handleCloseRef.current && dataRef.current.floatingContext) {
				if (!open) clearTimeout(timeoutRef.current);
				handlerRef.current = handleCloseRef.current({
					...dataRef.current.floatingContext,
					tree,
					x: event.clientX,
					y: event.clientY,
					onClose() {
						clearPointerEvents();
						cleanupMouseMoveHandler();
						if (!isClickLikeOpenEvent()) closeWithDelay(event, true, "safe-polygon");
					}
				});
				const handler = handlerRef.current;
				doc.addEventListener("mousemove", handler);
				unbindMouseMoveRef.current = () => {
					doc.removeEventListener("mousemove", handler);
				};
				return;
			}
			if (pointerTypeRef.current === "touch" ? !contains(elements.floating, event.relatedTarget) : true) closeWithDelay(event);
		}
		function onScrollMouseLeave(event) {
			if (isClickLikeOpenEvent()) return;
			if (!dataRef.current.floatingContext) return;
			handleCloseRef.current == null || handleCloseRef.current({
				...dataRef.current.floatingContext,
				tree,
				x: event.clientX,
				y: event.clientY,
				onClose() {
					clearPointerEvents();
					cleanupMouseMoveHandler();
					if (!isClickLikeOpenEvent()) closeWithDelay(event);
				}
			})(event);
		}
		if (isElement$1(elements.domReference)) {
			var _elements$floating;
			const ref = elements.domReference;
			open && ref.addEventListener("mouseleave", onScrollMouseLeave);
			(_elements$floating = elements.floating) == null || _elements$floating.addEventListener("mouseleave", onScrollMouseLeave);
			move && ref.addEventListener("mousemove", onMouseEnter, { once: true });
			ref.addEventListener("mouseenter", onMouseEnter);
			ref.addEventListener("mouseleave", onMouseLeave);
			return () => {
				var _elements$floating2;
				open && ref.removeEventListener("mouseleave", onScrollMouseLeave);
				(_elements$floating2 = elements.floating) == null || _elements$floating2.removeEventListener("mouseleave", onScrollMouseLeave);
				move && ref.removeEventListener("mousemove", onMouseEnter);
				ref.removeEventListener("mouseenter", onMouseEnter);
				ref.removeEventListener("mouseleave", onMouseLeave);
			};
		}
	}, [
		elements,
		enabled,
		context,
		mouseOnly,
		restMs,
		move,
		closeWithDelay,
		cleanupMouseMoveHandler,
		clearPointerEvents,
		onOpenChange,
		open,
		openRef,
		tree,
		delayRef,
		handleCloseRef,
		dataRef,
		isClickLikeOpenEvent
	]);
	index(() => {
		var _handleCloseRef$curre;
		if (!enabled) return;
		if (open && (_handleCloseRef$curre = handleCloseRef.current) != null && _handleCloseRef$curre.__options.blockPointerEvents && isHoverOpen()) {
			performedPointerEventsMutationRef.current = true;
			const floatingEl = elements.floating;
			if (isElement$1(elements.domReference) && floatingEl) {
				var _tree$nodesRef$curren;
				const body = getDocument$1(elements.floating).body;
				body.setAttribute(safePolygonIdentifier, "");
				const ref = elements.domReference;
				const parentFloating = tree == null || (_tree$nodesRef$curren = tree.nodesRef.current.find((node) => node.id === parentId)) == null || (_tree$nodesRef$curren = _tree$nodesRef$curren.context) == null ? void 0 : _tree$nodesRef$curren.elements.floating;
				if (parentFloating) parentFloating.style.pointerEvents = "";
				body.style.pointerEvents = "none";
				ref.style.pointerEvents = "auto";
				floatingEl.style.pointerEvents = "auto";
				return () => {
					body.style.pointerEvents = "";
					ref.style.pointerEvents = "";
					floatingEl.style.pointerEvents = "";
				};
			}
		}
	}, [
		enabled,
		open,
		parentId,
		elements,
		tree,
		handleCloseRef,
		isHoverOpen
	]);
	index(() => {
		if (!open) {
			pointerTypeRef.current = void 0;
			restTimeoutPendingRef.current = false;
			cleanupMouseMoveHandler();
			clearPointerEvents();
		}
	}, [
		open,
		cleanupMouseMoveHandler,
		clearPointerEvents
	]);
	import_react.useEffect(() => {
		return () => {
			cleanupMouseMoveHandler();
			clearTimeout(timeoutRef.current);
			clearTimeout(restTimeoutRef.current);
			clearPointerEvents();
		};
	}, [
		enabled,
		elements.domReference,
		cleanupMouseMoveHandler,
		clearPointerEvents
	]);
	const reference = import_react.useMemo(() => {
		function setPointerRef(event) {
			pointerTypeRef.current = event.pointerType;
		}
		return {
			onPointerDown: setPointerRef,
			onPointerEnter: setPointerRef,
			onMouseMove(event) {
				const { nativeEvent } = event;
				function handleMouseMove() {
					if (!blockMouseMoveRef.current && !openRef.current) onOpenChange(true, nativeEvent, "hover");
				}
				if (mouseOnly && !isMouseLikePointerType(pointerTypeRef.current)) return;
				if (open || restMs === 0) return;
				if (restTimeoutPendingRef.current && event.movementX ** 2 + event.movementY ** 2 < 2) return;
				clearTimeout(restTimeoutRef.current);
				if (pointerTypeRef.current === "touch") handleMouseMove();
				else {
					restTimeoutPendingRef.current = true;
					restTimeoutRef.current = window.setTimeout(handleMouseMove, restMs);
				}
			}
		};
	}, [
		mouseOnly,
		onOpenChange,
		open,
		openRef,
		restMs
	]);
	const floating = import_react.useMemo(() => ({
		onMouseEnter() {
			clearTimeout(timeoutRef.current);
		},
		onMouseLeave(event) {
			if (!isClickLikeOpenEvent()) closeWithDelay(event.nativeEvent, false);
		}
	}), [closeWithDelay, isClickLikeOpenEvent]);
	return import_react.useMemo(() => enabled ? {
		reference,
		floating
	} : {}, [
		enabled,
		reference,
		floating
	]);
}
var rafId = 0;
function enqueueFocus(el, options) {
	if (options === void 0) options = {};
	const { preventScroll = false, cancelPrevious = true, sync = false } = options;
	cancelPrevious && cancelAnimationFrame(rafId);
	const exec = () => el == null ? void 0 : el.focus({ preventScroll });
	if (sync) exec();
	else rafId = requestAnimationFrame(exec);
}
function getAncestors(nodes, id) {
	var _nodes$find;
	let allAncestors = [];
	let currentParentId = (_nodes$find = nodes.find((node) => node.id === id)) == null ? void 0 : _nodes$find.parentId;
	while (currentParentId) {
		const currentNode = nodes.find((node) => node.id === currentParentId);
		currentParentId = currentNode == null ? void 0 : currentNode.parentId;
		if (currentNode) allAncestors = allAncestors.concat(currentNode);
	}
	return allAncestors;
}
function getChildren(nodes, id) {
	let allChildren = nodes.filter((node) => {
		var _node$context;
		return node.parentId === id && ((_node$context = node.context) == null ? void 0 : _node$context.open);
	});
	let currentChildren = allChildren;
	while (currentChildren.length) {
		currentChildren = nodes.filter((node) => {
			var _currentChildren;
			return (_currentChildren = currentChildren) == null ? void 0 : _currentChildren.some((n) => {
				var _node$context2;
				return node.parentId === n.id && ((_node$context2 = node.context) == null ? void 0 : _node$context2.open);
			});
		});
		allChildren = allChildren.concat(currentChildren);
	}
	return allChildren;
}
var counterMap = /*#__PURE__*/ new WeakMap();
var uncontrolledElementsSet = /*#__PURE__*/ new WeakSet();
var markerMap = {};
var lockCount$1 = 0;
var supportsInert = () => typeof HTMLElement !== "undefined" && "inert" in HTMLElement.prototype;
var unwrapHost = (node) => node && (node.host || unwrapHost(node.parentNode));
var correctElements = (parent, targets) => targets.map((target) => {
	if (parent.contains(target)) return target;
	const correctedTarget = unwrapHost(target);
	if (parent.contains(correctedTarget)) return correctedTarget;
	return null;
}).filter((x) => x != null);
function applyAttributeToOthers(uncorrectedAvoidElements, body, ariaHidden, inert) {
	const markerName = "data-floating-ui-inert";
	const controlAttribute = inert ? "inert" : ariaHidden ? "aria-hidden" : null;
	const avoidElements = correctElements(body, uncorrectedAvoidElements);
	const elementsToKeep = /* @__PURE__ */ new Set();
	const elementsToStop = new Set(avoidElements);
	const hiddenElements = [];
	if (!markerMap[markerName]) markerMap[markerName] = /* @__PURE__ */ new WeakMap();
	const markerCounter = markerMap[markerName];
	avoidElements.forEach(keep);
	deep(body);
	elementsToKeep.clear();
	function keep(el) {
		if (!el || elementsToKeep.has(el)) return;
		elementsToKeep.add(el);
		el.parentNode && keep(el.parentNode);
	}
	function deep(parent) {
		if (!parent || elementsToStop.has(parent)) return;
		[].forEach.call(parent.children, (node) => {
			if (getNodeName(node) === "script") return;
			if (elementsToKeep.has(node)) deep(node);
			else {
				const attr = controlAttribute ? node.getAttribute(controlAttribute) : null;
				const alreadyHidden = attr !== null && attr !== "false";
				const counterValue = (counterMap.get(node) || 0) + 1;
				const markerValue = (markerCounter.get(node) || 0) + 1;
				counterMap.set(node, counterValue);
				markerCounter.set(node, markerValue);
				hiddenElements.push(node);
				if (counterValue === 1 && alreadyHidden) uncontrolledElementsSet.add(node);
				if (markerValue === 1) node.setAttribute(markerName, "");
				if (!alreadyHidden && controlAttribute) node.setAttribute(controlAttribute, "true");
			}
		});
	}
	lockCount$1++;
	return () => {
		hiddenElements.forEach((element) => {
			const counterValue = (counterMap.get(element) || 0) - 1;
			const markerValue = (markerCounter.get(element) || 0) - 1;
			counterMap.set(element, counterValue);
			markerCounter.set(element, markerValue);
			if (!counterValue) {
				if (!uncontrolledElementsSet.has(element) && controlAttribute) element.removeAttribute(controlAttribute);
				uncontrolledElementsSet.delete(element);
			}
			if (!markerValue) element.removeAttribute(markerName);
		});
		lockCount$1--;
		if (!lockCount$1) {
			counterMap = /* @__PURE__ */ new WeakMap();
			counterMap = /* @__PURE__ */ new WeakMap();
			uncontrolledElementsSet = /* @__PURE__ */ new WeakSet();
			markerMap = {};
		}
	};
}
function markOthers(avoidElements, ariaHidden, inert) {
	if (ariaHidden === void 0) ariaHidden = false;
	if (inert === void 0) inert = false;
	const body = getDocument$1(avoidElements[0]).body;
	return applyAttributeToOthers(avoidElements.concat(Array.from(body.querySelectorAll("[aria-live]"))), body, ariaHidden, inert);
}
var getTabbableOptions = () => ({
	getShadowRoot: true,
	displayCheck: typeof ResizeObserver === "function" && ResizeObserver.toString().includes("[native code]") ? "full" : "none"
});
function getTabbableIn(container, direction) {
	const allTabbable = tabbable(container, getTabbableOptions());
	if (direction === "prev") allTabbable.reverse();
	const activeIndex = allTabbable.indexOf(activeElement(getDocument$1(container)));
	return allTabbable.slice(activeIndex + 1)[0];
}
function getNextTabbable() {
	return getTabbableIn(document.body, "next");
}
function getPreviousTabbable() {
	return getTabbableIn(document.body, "prev");
}
function isOutsideEvent(event, container) {
	const containerElement = container || event.currentTarget;
	const relatedTarget = event.relatedTarget;
	return !relatedTarget || !contains(containerElement, relatedTarget);
}
function disableFocusInside(container) {
	tabbable(container, getTabbableOptions()).forEach((element) => {
		element.dataset.tabindex = element.getAttribute("tabindex") || "";
		element.setAttribute("tabindex", "-1");
	});
}
function enableFocusInside(container) {
	container.querySelectorAll("[data-tabindex]").forEach((element) => {
		const tabindex = element.dataset.tabindex;
		delete element.dataset.tabindex;
		if (tabindex) element.setAttribute("tabindex", tabindex);
		else element.removeAttribute("tabindex");
	});
}
var HIDDEN_STYLES = {
	border: 0,
	clip: "rect(0 0 0 0)",
	height: "1px",
	margin: "-1px",
	overflow: "hidden",
	padding: 0,
	position: "fixed",
	whiteSpace: "nowrap",
	width: "1px",
	top: 0,
	left: 0
};
var timeoutId;
function setActiveElementOnTab(event) {
	if (event.key === "Tab") {
		event.target;
		clearTimeout(timeoutId);
	}
}
var FocusGuard = /*#__PURE__*/ import_react.forwardRef(function FocusGuard(props, ref) {
	const [role, setRole] = import_react.useState();
	index(() => {
		if (isSafari()) setRole("button");
		document.addEventListener("keydown", setActiveElementOnTab);
		return () => {
			document.removeEventListener("keydown", setActiveElementOnTab);
		};
	}, []);
	const restProps = {
		ref,
		tabIndex: 0,
		role,
		"aria-hidden": role ? void 0 : true,
		[createAttribute("focus-guard")]: "",
		style: HIDDEN_STYLES
	};
	return /*#__PURE__*/ import_react.createElement("span", _extends({}, props, restProps));
});
var PortalContext = /*#__PURE__*/ import_react.createContext(null);
var attr = /*#__PURE__*/ createAttribute("portal");
/**
* @see https://floating-ui.com/docs/FloatingPortal#usefloatingportalnode
*/
function useFloatingPortalNode(props) {
	if (props === void 0) props = {};
	const { id, root } = props;
	const uniqueId = useId$1();
	const portalContext = usePortalContext();
	const [portalNode, setPortalNode] = import_react.useState(null);
	const portalNodeRef = import_react.useRef(null);
	index(() => {
		return () => {
			portalNode?.remove();
			queueMicrotask(() => {
				portalNodeRef.current = null;
			});
		};
	}, [portalNode]);
	index(() => {
		if (!uniqueId) return;
		if (portalNodeRef.current) return;
		const existingIdRoot = id ? document.getElementById(id) : null;
		if (!existingIdRoot) return;
		const subRoot = document.createElement("div");
		subRoot.id = uniqueId;
		subRoot.setAttribute(attr, "");
		existingIdRoot.appendChild(subRoot);
		portalNodeRef.current = subRoot;
		setPortalNode(subRoot);
	}, [id, uniqueId]);
	index(() => {
		if (root === null) return;
		if (!uniqueId) return;
		if (portalNodeRef.current) return;
		let container = root || (portalContext == null ? void 0 : portalContext.portalNode);
		if (container && !isElement$1(container)) container = container.current;
		container = container || document.body;
		let idWrapper = null;
		if (id) {
			idWrapper = document.createElement("div");
			idWrapper.id = id;
			container.appendChild(idWrapper);
		}
		const subRoot = document.createElement("div");
		subRoot.id = uniqueId;
		subRoot.setAttribute(attr, "");
		container = idWrapper || container;
		container.appendChild(subRoot);
		portalNodeRef.current = subRoot;
		setPortalNode(subRoot);
	}, [
		id,
		root,
		uniqueId,
		portalContext
	]);
	return portalNode;
}
/**
* Portals the floating element into a given container element — by default,
* outside of the app root and into the body.
* This is necessary to ensure the floating element can appear outside any
* potential parent containers that cause clipping (such as `overflow: hidden`),
* while retaining its location in the React tree.
* @see https://floating-ui.com/docs/FloatingPortal
*/
function FloatingPortal(props) {
	const { children, id, root, preserveTabOrder = true } = props;
	const portalNode = useFloatingPortalNode({
		id,
		root
	});
	const [focusManagerState, setFocusManagerState] = import_react.useState(null);
	const beforeOutsideRef = import_react.useRef(null);
	const afterOutsideRef = import_react.useRef(null);
	const beforeInsideRef = import_react.useRef(null);
	const afterInsideRef = import_react.useRef(null);
	const modal = focusManagerState == null ? void 0 : focusManagerState.modal;
	const open = focusManagerState == null ? void 0 : focusManagerState.open;
	const shouldRenderGuards = !!focusManagerState && !focusManagerState.modal && focusManagerState.open && preserveTabOrder && !!(root || portalNode);
	import_react.useEffect(() => {
		if (!portalNode || !preserveTabOrder || modal) return;
		function onFocus(event) {
			if (portalNode && isOutsideEvent(event)) (event.type === "focusin" ? enableFocusInside : disableFocusInside)(portalNode);
		}
		portalNode.addEventListener("focusin", onFocus, true);
		portalNode.addEventListener("focusout", onFocus, true);
		return () => {
			portalNode.removeEventListener("focusin", onFocus, true);
			portalNode.removeEventListener("focusout", onFocus, true);
		};
	}, [
		portalNode,
		preserveTabOrder,
		modal
	]);
	import_react.useEffect(() => {
		if (!portalNode) return;
		if (open) return;
		enableFocusInside(portalNode);
	}, [open, portalNode]);
	return /*#__PURE__*/ import_react.createElement(PortalContext.Provider, { value: import_react.useMemo(() => ({
		preserveTabOrder,
		beforeOutsideRef,
		afterOutsideRef,
		beforeInsideRef,
		afterInsideRef,
		portalNode,
		setFocusManagerState
	}), [preserveTabOrder, portalNode]) }, shouldRenderGuards && portalNode && /*#__PURE__*/ import_react.createElement(FocusGuard, {
		"data-type": "outside",
		ref: beforeOutsideRef,
		onFocus: (event) => {
			if (isOutsideEvent(event, portalNode)) {
				var _beforeInsideRef$curr;
				(_beforeInsideRef$curr = beforeInsideRef.current) == null || _beforeInsideRef$curr.focus();
			} else (getPreviousTabbable() || (focusManagerState == null ? void 0 : focusManagerState.refs.domReference.current))?.focus();
		}
	}), shouldRenderGuards && portalNode && /*#__PURE__*/ import_react.createElement("span", {
		"aria-owns": portalNode.id,
		style: HIDDEN_STYLES
	}), portalNode && /*#__PURE__*/ import_react_dom.createPortal(children, portalNode), shouldRenderGuards && portalNode && /*#__PURE__*/ import_react.createElement(FocusGuard, {
		"data-type": "outside",
		ref: afterOutsideRef,
		onFocus: (event) => {
			if (isOutsideEvent(event, portalNode)) {
				var _afterInsideRef$curre;
				(_afterInsideRef$curre = afterInsideRef.current) == null || _afterInsideRef$curre.focus();
			} else {
				(getNextTabbable() || (focusManagerState == null ? void 0 : focusManagerState.refs.domReference.current))?.focus();
				focusManagerState != null && focusManagerState.closeOnFocusOut && focusManagerState?.onOpenChange(false, event.nativeEvent, "focus-out");
			}
		}
	}));
}
var usePortalContext = () => import_react.useContext(PortalContext);
var FOCUSABLE_ATTRIBUTE = "data-floating-ui-focusable";
function getFloatingFocusElement(floatingElement) {
	if (!floatingElement) return null;
	return floatingElement.hasAttribute(FOCUSABLE_ATTRIBUTE) ? floatingElement : floatingElement.querySelector("[data-floating-ui-focusable]") || floatingElement;
}
var LIST_LIMIT = 20;
var previouslyFocusedElements = [];
function addPreviouslyFocusedElement(element) {
	previouslyFocusedElements = previouslyFocusedElements.filter((el) => el.isConnected);
	let tabbableEl = element;
	if (!tabbableEl || getNodeName(tabbableEl) === "body") return;
	if (!isTabbable(tabbableEl, getTabbableOptions())) {
		const tabbableChild = tabbable(tabbableEl, getTabbableOptions())[0];
		if (tabbableChild) tabbableEl = tabbableChild;
	}
	previouslyFocusedElements.push(tabbableEl);
	if (previouslyFocusedElements.length > LIST_LIMIT) previouslyFocusedElements = previouslyFocusedElements.slice(-20);
}
function getPreviouslyFocusedElement() {
	return previouslyFocusedElements.slice().reverse().find((el) => el.isConnected);
}
var VisuallyHiddenDismiss = /*#__PURE__*/ import_react.forwardRef(function VisuallyHiddenDismiss(props, ref) {
	return /*#__PURE__*/ import_react.createElement("button", _extends({}, props, {
		type: "button",
		ref,
		tabIndex: -1,
		style: HIDDEN_STYLES
	}));
});
/**
* Provides focus management for the floating element.
* @see https://floating-ui.com/docs/FloatingFocusManager
*/
function FloatingFocusManager(props) {
	const { context, children, disabled = false, order = ["content"], guards: _guards = true, initialFocus = 0, returnFocus = true, restoreFocus = false, modal = true, visuallyHiddenDismiss = false, closeOnFocusOut = true } = props;
	const { open, refs, nodeId, onOpenChange, events, dataRef, floatingId, elements: { domReference, floating } } = context;
	const ignoreInitialFocus = typeof initialFocus === "number" && initialFocus < 0;
	const isUntrappedTypeableCombobox = isTypeableCombobox(domReference) && ignoreInitialFocus;
	const guards = supportsInert() ? _guards : true;
	const orderRef = useLatestRef(order);
	const initialFocusRef = useLatestRef(initialFocus);
	const returnFocusRef = useLatestRef(returnFocus);
	const tree = useFloatingTree();
	const portalContext = usePortalContext();
	const startDismissButtonRef = import_react.useRef(null);
	const endDismissButtonRef = import_react.useRef(null);
	const preventReturnFocusRef = import_react.useRef(false);
	const isPointerDownRef = import_react.useRef(false);
	const tabbableIndexRef = import_react.useRef(-1);
	const isInsidePortal = portalContext != null;
	const floatingFocusElement = getFloatingFocusElement(floating);
	const getTabbableContent = useEffectEvent(function(container) {
		if (container === void 0) container = floatingFocusElement;
		return container ? tabbable(container, getTabbableOptions()) : [];
	});
	const getTabbableElements = useEffectEvent((container) => {
		const content = getTabbableContent(container);
		return orderRef.current.map((type) => {
			if (domReference && type === "reference") return domReference;
			if (floatingFocusElement && type === "floating") return floatingFocusElement;
			return content;
		}).filter(Boolean).flat();
	});
	import_react.useEffect(() => {
		if (disabled) return;
		if (!modal) return;
		function onKeyDown(event) {
			if (event.key === "Tab") {
				if (contains(floatingFocusElement, activeElement(getDocument$1(floatingFocusElement))) && getTabbableContent().length === 0 && !isUntrappedTypeableCombobox) stopEvent(event);
				const els = getTabbableElements();
				const target = getTarget(event);
				if (orderRef.current[0] === "reference" && target === domReference) {
					stopEvent(event);
					if (event.shiftKey) enqueueFocus(els[els.length - 1]);
					else enqueueFocus(els[1]);
				}
				if (orderRef.current[1] === "floating" && target === floatingFocusElement && event.shiftKey) {
					stopEvent(event);
					enqueueFocus(els[0]);
				}
			}
		}
		const doc = getDocument$1(floatingFocusElement);
		doc.addEventListener("keydown", onKeyDown);
		return () => {
			doc.removeEventListener("keydown", onKeyDown);
		};
	}, [
		disabled,
		domReference,
		floatingFocusElement,
		modal,
		orderRef,
		isUntrappedTypeableCombobox,
		getTabbableContent,
		getTabbableElements
	]);
	import_react.useEffect(() => {
		if (disabled) return;
		if (!floating) return;
		function handleFocusIn(event) {
			const target = getTarget(event);
			const tabbableIndex = getTabbableContent().indexOf(target);
			if (tabbableIndex !== -1) tabbableIndexRef.current = tabbableIndex;
		}
		floating.addEventListener("focusin", handleFocusIn);
		return () => {
			floating.removeEventListener("focusin", handleFocusIn);
		};
	}, [
		disabled,
		floating,
		getTabbableContent
	]);
	import_react.useEffect(() => {
		if (disabled) return;
		if (!closeOnFocusOut) return;
		function handlePointerDown() {
			isPointerDownRef.current = true;
			setTimeout(() => {
				isPointerDownRef.current = false;
			});
		}
		function handleFocusOutside(event) {
			const relatedTarget = event.relatedTarget;
			queueMicrotask(() => {
				const movedToUnrelatedNode = !(contains(domReference, relatedTarget) || contains(floating, relatedTarget) || contains(relatedTarget, floating) || contains(portalContext == null ? void 0 : portalContext.portalNode, relatedTarget) || relatedTarget != null && relatedTarget.hasAttribute(createAttribute("focus-guard")) || tree && (getChildren(tree.nodesRef.current, nodeId).find((node) => {
					var _node$context, _node$context2;
					return contains((_node$context = node.context) == null ? void 0 : _node$context.elements.floating, relatedTarget) || contains((_node$context2 = node.context) == null ? void 0 : _node$context2.elements.domReference, relatedTarget);
				}) || getAncestors(tree.nodesRef.current, nodeId).find((node) => {
					var _node$context3, _node$context4;
					return ((_node$context3 = node.context) == null ? void 0 : _node$context3.elements.floating) === relatedTarget || ((_node$context4 = node.context) == null ? void 0 : _node$context4.elements.domReference) === relatedTarget;
				})));
				if (restoreFocus && movedToUnrelatedNode && activeElement(getDocument$1(floatingFocusElement)) === getDocument$1(floatingFocusElement).body) {
					if (isHTMLElement(floatingFocusElement)) floatingFocusElement.focus();
					const prevTabbableIndex = tabbableIndexRef.current;
					const tabbableContent = getTabbableContent();
					const nodeToFocus = tabbableContent[prevTabbableIndex] || tabbableContent[tabbableContent.length - 1] || floatingFocusElement;
					if (isHTMLElement(nodeToFocus)) nodeToFocus.focus();
				}
				if ((isUntrappedTypeableCombobox ? true : !modal) && relatedTarget && movedToUnrelatedNode && !isPointerDownRef.current && relatedTarget !== getPreviouslyFocusedElement()) {
					preventReturnFocusRef.current = true;
					onOpenChange(false, event, "focus-out");
				}
			});
		}
		if (floating && isHTMLElement(domReference)) {
			domReference.addEventListener("focusout", handleFocusOutside);
			domReference.addEventListener("pointerdown", handlePointerDown);
			floating.addEventListener("focusout", handleFocusOutside);
			return () => {
				domReference.removeEventListener("focusout", handleFocusOutside);
				domReference.removeEventListener("pointerdown", handlePointerDown);
				floating.removeEventListener("focusout", handleFocusOutside);
			};
		}
	}, [
		disabled,
		domReference,
		floating,
		floatingFocusElement,
		modal,
		nodeId,
		tree,
		portalContext,
		onOpenChange,
		closeOnFocusOut,
		restoreFocus,
		getTabbableContent,
		isUntrappedTypeableCombobox
	]);
	import_react.useEffect(() => {
		var _portalContext$portal;
		if (disabled) return;
		const portalNodes = Array.from((portalContext == null || (_portalContext$portal = portalContext.portalNode) == null ? void 0 : _portalContext$portal.querySelectorAll("[" + createAttribute("portal") + "]")) || []);
		if (floating) {
			const insideElements = [
				floating,
				...portalNodes,
				startDismissButtonRef.current,
				endDismissButtonRef.current,
				orderRef.current.includes("reference") || isUntrappedTypeableCombobox ? domReference : null
			].filter((x) => x != null);
			const cleanup = modal || isUntrappedTypeableCombobox ? markOthers(insideElements, guards, !guards) : markOthers(insideElements);
			return () => {
				cleanup();
			};
		}
	}, [
		disabled,
		domReference,
		floating,
		modal,
		orderRef,
		portalContext,
		isUntrappedTypeableCombobox,
		guards
	]);
	index(() => {
		if (disabled || !isHTMLElement(floatingFocusElement)) return;
		const previouslyFocusedElement = activeElement(getDocument$1(floatingFocusElement));
		queueMicrotask(() => {
			const focusableElements = getTabbableElements(floatingFocusElement);
			const initialFocusValue = initialFocusRef.current;
			const elToFocus = (typeof initialFocusValue === "number" ? focusableElements[initialFocusValue] : initialFocusValue.current) || floatingFocusElement;
			const focusAlreadyInsideFloatingEl = contains(floatingFocusElement, previouslyFocusedElement);
			if (!ignoreInitialFocus && !focusAlreadyInsideFloatingEl && open) enqueueFocus(elToFocus, { preventScroll: elToFocus === floatingFocusElement });
		});
	}, [
		disabled,
		open,
		floatingFocusElement,
		ignoreInitialFocus,
		getTabbableElements,
		initialFocusRef
	]);
	index(() => {
		if (disabled || !floatingFocusElement) return;
		let preventReturnFocusScroll = false;
		const doc = getDocument$1(floatingFocusElement);
		const previouslyFocusedElement = activeElement(doc);
		let openEvent = dataRef.current.openEvent;
		addPreviouslyFocusedElement(previouslyFocusedElement);
		function onOpenChange(_ref) {
			let { open, reason, event, nested } = _ref;
			if (open) openEvent = event;
			if (reason === "escape-key" && refs.domReference.current) addPreviouslyFocusedElement(refs.domReference.current);
			if (reason === "hover" && event.type === "mouseleave") preventReturnFocusRef.current = true;
			if (reason !== "outside-press") return;
			if (nested) {
				preventReturnFocusRef.current = false;
				preventReturnFocusScroll = true;
			} else preventReturnFocusRef.current = !(isVirtualClick(event) || isVirtualPointerEvent(event));
		}
		events.on("openchange", onOpenChange);
		const fallbackEl = doc.createElement("span");
		fallbackEl.setAttribute("tabindex", "-1");
		fallbackEl.setAttribute("aria-hidden", "true");
		Object.assign(fallbackEl.style, HIDDEN_STYLES);
		if (isInsidePortal && domReference) domReference.insertAdjacentElement("afterend", fallbackEl);
		function getReturnElement() {
			if (typeof returnFocusRef.current === "boolean") return getPreviouslyFocusedElement() || fallbackEl;
			return returnFocusRef.current.current || fallbackEl;
		}
		return () => {
			events.off("openchange", onOpenChange);
			const activeEl = activeElement(doc);
			const isFocusInsideFloatingTree = contains(floating, activeEl) || tree && getChildren(tree.nodesRef.current, nodeId).some((node) => {
				var _node$context5;
				return contains((_node$context5 = node.context) == null ? void 0 : _node$context5.elements.floating, activeEl);
			});
			if ((isFocusInsideFloatingTree || openEvent && ["click", "mousedown"].includes(openEvent.type)) && refs.domReference.current) addPreviouslyFocusedElement(refs.domReference.current);
			const returnElement = getReturnElement();
			queueMicrotask(() => {
				if (returnFocusRef.current && !preventReturnFocusRef.current && isHTMLElement(returnElement) && (returnElement !== activeEl && activeEl !== doc.body ? isFocusInsideFloatingTree : true)) returnElement.focus({ preventScroll: preventReturnFocusScroll });
				fallbackEl.remove();
			});
		};
	}, [
		disabled,
		floating,
		floatingFocusElement,
		returnFocusRef,
		dataRef,
		refs,
		events,
		tree,
		nodeId,
		isInsidePortal,
		domReference
	]);
	import_react.useEffect(() => {
		queueMicrotask(() => {
			preventReturnFocusRef.current = false;
		});
	}, [disabled]);
	index(() => {
		if (disabled) return;
		if (!portalContext) return;
		portalContext.setFocusManagerState({
			modal,
			closeOnFocusOut,
			open,
			onOpenChange,
			refs
		});
		return () => {
			portalContext.setFocusManagerState(null);
		};
	}, [
		disabled,
		portalContext,
		modal,
		open,
		onOpenChange,
		refs,
		closeOnFocusOut
	]);
	index(() => {
		if (disabled) return;
		if (!floatingFocusElement) return;
		if (typeof MutationObserver !== "function") return;
		if (ignoreInitialFocus) return;
		const handleMutation = () => {
			const tabIndex = floatingFocusElement.getAttribute("tabindex");
			const tabbableContent = getTabbableContent();
			const activeEl = activeElement(getDocument$1(floating));
			const tabbableIndex = tabbableContent.indexOf(activeEl);
			if (tabbableIndex !== -1) tabbableIndexRef.current = tabbableIndex;
			if (orderRef.current.includes("floating") || activeEl !== refs.domReference.current && tabbableContent.length === 0) {
				if (tabIndex !== "0") floatingFocusElement.setAttribute("tabindex", "0");
			} else if (tabIndex !== "-1") floatingFocusElement.setAttribute("tabindex", "-1");
		};
		handleMutation();
		const observer = new MutationObserver(handleMutation);
		observer.observe(floatingFocusElement, {
			childList: true,
			subtree: true,
			attributes: true
		});
		return () => {
			observer.disconnect();
		};
	}, [
		disabled,
		floating,
		floatingFocusElement,
		refs,
		orderRef,
		getTabbableContent,
		ignoreInitialFocus
	]);
	function renderDismissButton(location) {
		if (disabled || !visuallyHiddenDismiss || !modal) return null;
		return /*#__PURE__*/ import_react.createElement(VisuallyHiddenDismiss, {
			ref: location === "start" ? startDismissButtonRef : endDismissButtonRef,
			onClick: (event) => onOpenChange(false, event.nativeEvent)
		}, typeof visuallyHiddenDismiss === "string" ? visuallyHiddenDismiss : "Dismiss");
	}
	const shouldRenderGuards = !disabled && guards && (modal ? !isUntrappedTypeableCombobox : true) && (isInsidePortal || modal);
	return /*#__PURE__*/ import_react.createElement(import_react.Fragment, null, shouldRenderGuards && /*#__PURE__*/ import_react.createElement(FocusGuard, {
		"data-type": "inside",
		ref: portalContext == null ? void 0 : portalContext.beforeInsideRef,
		onFocus: (event) => {
			if (modal) {
				const els = getTabbableElements();
				enqueueFocus(order[0] === "reference" ? els[0] : els[els.length - 1]);
			} else if (portalContext != null && portalContext.preserveTabOrder && portalContext.portalNode) {
				preventReturnFocusRef.current = false;
				if (isOutsideEvent(event, portalContext.portalNode)) (getNextTabbable() || domReference)?.focus();
				else {
					var _portalContext$before;
					(_portalContext$before = portalContext.beforeOutsideRef.current) == null || _portalContext$before.focus();
				}
			}
		}
	}), !isUntrappedTypeableCombobox && renderDismissButton("start"), children, renderDismissButton("end"), shouldRenderGuards && /*#__PURE__*/ import_react.createElement(FocusGuard, {
		"data-type": "inside",
		ref: portalContext == null ? void 0 : portalContext.afterInsideRef,
		onFocus: (event) => {
			if (modal) enqueueFocus(getTabbableElements()[0]);
			else if (portalContext != null && portalContext.preserveTabOrder && portalContext.portalNode) {
				if (closeOnFocusOut) preventReturnFocusRef.current = true;
				if (isOutsideEvent(event, portalContext.portalNode)) (getPreviousTabbable() || domReference)?.focus();
				else {
					var _portalContext$afterO;
					(_portalContext$afterO = portalContext.afterOutsideRef.current) == null || _portalContext$afterO.focus();
				}
			}
		}
	}));
}
var bubbleHandlerKeys = {
	pointerdown: "onPointerDown",
	mousedown: "onMouseDown",
	click: "onClick"
};
var captureHandlerKeys = {
	pointerdown: "onPointerDownCapture",
	mousedown: "onMouseDownCapture",
	click: "onClickCapture"
};
var normalizeProp = (normalizable) => {
	var _normalizable$escapeK, _normalizable$outside;
	return {
		escapeKey: typeof normalizable === "boolean" ? normalizable : (_normalizable$escapeK = normalizable == null ? void 0 : normalizable.escapeKey) != null ? _normalizable$escapeK : false,
		outsidePress: typeof normalizable === "boolean" ? normalizable : (_normalizable$outside = normalizable == null ? void 0 : normalizable.outsidePress) != null ? _normalizable$outside : true
	};
};
/**
* Closes the floating element when a dismissal is requested — by default, when
* the user presses the `escape` key or outside of the floating element.
* @see https://floating-ui.com/docs/useDismiss
*/
function useDismiss(context, props) {
	if (props === void 0) props = {};
	const { open, onOpenChange, elements, dataRef } = context;
	const { enabled = true, escapeKey = true, outsidePress: unstable_outsidePress = true, outsidePressEvent = "pointerdown", referencePress = false, referencePressEvent = "pointerdown", ancestorScroll = false, bubbles, capture } = props;
	const tree = useFloatingTree();
	const outsidePressFn = useEffectEvent(typeof unstable_outsidePress === "function" ? unstable_outsidePress : () => false);
	const outsidePress = typeof unstable_outsidePress === "function" ? outsidePressFn : unstable_outsidePress;
	const insideReactTreeRef = import_react.useRef(false);
	const endedOrStartedInsideRef = import_react.useRef(false);
	const { escapeKey: escapeKeyBubbles, outsidePress: outsidePressBubbles } = normalizeProp(bubbles);
	const { escapeKey: escapeKeyCapture, outsidePress: outsidePressCapture } = normalizeProp(capture);
	const isComposingRef = import_react.useRef(false);
	const closeOnEscapeKeyDown = useEffectEvent((event) => {
		var _dataRef$current$floa;
		if (!open || !enabled || !escapeKey || event.key !== "Escape") return;
		if (isComposingRef.current) return;
		const nodeId = (_dataRef$current$floa = dataRef.current.floatingContext) == null ? void 0 : _dataRef$current$floa.nodeId;
		const children = tree ? getChildren(tree.nodesRef.current, nodeId) : [];
		if (!escapeKeyBubbles) {
			event.stopPropagation();
			if (children.length > 0) {
				let shouldDismiss = true;
				children.forEach((child) => {
					var _child$context;
					if ((_child$context = child.context) != null && _child$context.open && !child.context.dataRef.current.__escapeKeyBubbles) {
						shouldDismiss = false;
						return;
					}
				});
				if (!shouldDismiss) return;
			}
		}
		onOpenChange(false, isReactEvent(event) ? event.nativeEvent : event, "escape-key");
	});
	const closeOnEscapeKeyDownCapture = useEffectEvent((event) => {
		var _getTarget2;
		const callback = () => {
			var _getTarget;
			closeOnEscapeKeyDown(event);
			(_getTarget = getTarget(event)) == null || _getTarget.removeEventListener("keydown", callback);
		};
		(_getTarget2 = getTarget(event)) == null || _getTarget2.addEventListener("keydown", callback);
	});
	const closeOnPressOutside = useEffectEvent((event) => {
		var _dataRef$current$floa2;
		const insideReactTree = insideReactTreeRef.current;
		insideReactTreeRef.current = false;
		const endedOrStartedInside = endedOrStartedInsideRef.current;
		endedOrStartedInsideRef.current = false;
		if (outsidePressEvent === "click" && endedOrStartedInside) return;
		if (insideReactTree) return;
		if (typeof outsidePress === "function" && !outsidePress(event)) return;
		const target = getTarget(event);
		const inertSelector = "[" + createAttribute("inert") + "]";
		const markers = getDocument$1(elements.floating).querySelectorAll(inertSelector);
		let targetRootAncestor = isElement$1(target) ? target : null;
		while (targetRootAncestor && !isLastTraversableNode(targetRootAncestor)) {
			const nextParent = getParentNode(targetRootAncestor);
			if (isLastTraversableNode(nextParent) || !isElement$1(nextParent)) break;
			targetRootAncestor = nextParent;
		}
		if (markers.length && isElement$1(target) && !isRootElement(target) && !contains(target, elements.floating) && Array.from(markers).every((marker) => !contains(targetRootAncestor, marker))) return;
		if (isHTMLElement(target) && floating) {
			const canScrollX = target.clientWidth > 0 && target.scrollWidth > target.clientWidth;
			const canScrollY = target.clientHeight > 0 && target.scrollHeight > target.clientHeight;
			let xCond = canScrollY && event.offsetX > target.clientWidth;
			if (canScrollY) {
				if (getComputedStyle$1(target).direction === "rtl") xCond = event.offsetX <= target.offsetWidth - target.clientWidth;
			}
			if (xCond || canScrollX && event.offsetY > target.clientHeight) return;
		}
		const nodeId = (_dataRef$current$floa2 = dataRef.current.floatingContext) == null ? void 0 : _dataRef$current$floa2.nodeId;
		const targetIsInsideChildren = tree && getChildren(tree.nodesRef.current, nodeId).some((node) => {
			var _node$context;
			return isEventTargetWithin(event, (_node$context = node.context) == null ? void 0 : _node$context.elements.floating);
		});
		if (isEventTargetWithin(event, elements.floating) || isEventTargetWithin(event, elements.domReference) || targetIsInsideChildren) return;
		const children = tree ? getChildren(tree.nodesRef.current, nodeId) : [];
		if (children.length > 0) {
			let shouldDismiss = true;
			children.forEach((child) => {
				var _child$context2;
				if ((_child$context2 = child.context) != null && _child$context2.open && !child.context.dataRef.current.__outsidePressBubbles) {
					shouldDismiss = false;
					return;
				}
			});
			if (!shouldDismiss) return;
		}
		onOpenChange(false, event, "outside-press");
	});
	const closeOnPressOutsideCapture = useEffectEvent((event) => {
		var _getTarget4;
		const callback = () => {
			var _getTarget3;
			closeOnPressOutside(event);
			(_getTarget3 = getTarget(event)) == null || _getTarget3.removeEventListener(outsidePressEvent, callback);
		};
		(_getTarget4 = getTarget(event)) == null || _getTarget4.addEventListener(outsidePressEvent, callback);
	});
	import_react.useEffect(() => {
		if (!open || !enabled) return;
		dataRef.current.__escapeKeyBubbles = escapeKeyBubbles;
		dataRef.current.__outsidePressBubbles = outsidePressBubbles;
		let compositionTimeout = -1;
		function onScroll(event) {
			onOpenChange(false, event, "ancestor-scroll");
		}
		function handleCompositionStart() {
			window.clearTimeout(compositionTimeout);
			isComposingRef.current = true;
		}
		function handleCompositionEnd() {
			compositionTimeout = window.setTimeout(() => {
				isComposingRef.current = false;
			}, isWebKit() ? 5 : 0);
		}
		const doc = getDocument$1(elements.floating);
		if (escapeKey) {
			doc.addEventListener("keydown", escapeKeyCapture ? closeOnEscapeKeyDownCapture : closeOnEscapeKeyDown, escapeKeyCapture);
			doc.addEventListener("compositionstart", handleCompositionStart);
			doc.addEventListener("compositionend", handleCompositionEnd);
		}
		outsidePress && doc.addEventListener(outsidePressEvent, outsidePressCapture ? closeOnPressOutsideCapture : closeOnPressOutside, outsidePressCapture);
		let ancestors = [];
		if (ancestorScroll) {
			if (isElement$1(elements.domReference)) ancestors = getOverflowAncestors(elements.domReference);
			if (isElement$1(elements.floating)) ancestors = ancestors.concat(getOverflowAncestors(elements.floating));
			if (!isElement$1(elements.reference) && elements.reference && elements.reference.contextElement) ancestors = ancestors.concat(getOverflowAncestors(elements.reference.contextElement));
		}
		ancestors = ancestors.filter((ancestor) => {
			var _doc$defaultView;
			return ancestor !== ((_doc$defaultView = doc.defaultView) == null ? void 0 : _doc$defaultView.visualViewport);
		});
		ancestors.forEach((ancestor) => {
			ancestor.addEventListener("scroll", onScroll, { passive: true });
		});
		return () => {
			if (escapeKey) {
				doc.removeEventListener("keydown", escapeKeyCapture ? closeOnEscapeKeyDownCapture : closeOnEscapeKeyDown, escapeKeyCapture);
				doc.removeEventListener("compositionstart", handleCompositionStart);
				doc.removeEventListener("compositionend", handleCompositionEnd);
			}
			outsidePress && doc.removeEventListener(outsidePressEvent, outsidePressCapture ? closeOnPressOutsideCapture : closeOnPressOutside, outsidePressCapture);
			ancestors.forEach((ancestor) => {
				ancestor.removeEventListener("scroll", onScroll);
			});
			window.clearTimeout(compositionTimeout);
		};
	}, [
		dataRef,
		elements,
		escapeKey,
		outsidePress,
		outsidePressEvent,
		open,
		onOpenChange,
		ancestorScroll,
		enabled,
		escapeKeyBubbles,
		outsidePressBubbles,
		closeOnEscapeKeyDown,
		escapeKeyCapture,
		closeOnEscapeKeyDownCapture,
		closeOnPressOutside,
		outsidePressCapture,
		closeOnPressOutsideCapture
	]);
	import_react.useEffect(() => {
		insideReactTreeRef.current = false;
	}, [outsidePress, outsidePressEvent]);
	const reference = import_react.useMemo(() => ({
		onKeyDown: closeOnEscapeKeyDown,
		[bubbleHandlerKeys[referencePressEvent]]: (event) => {
			if (referencePress) onOpenChange(false, event.nativeEvent, "reference-press");
		}
	}), [
		closeOnEscapeKeyDown,
		onOpenChange,
		referencePress,
		referencePressEvent
	]);
	const floating = import_react.useMemo(() => ({
		onKeyDown: closeOnEscapeKeyDown,
		onMouseDown() {
			endedOrStartedInsideRef.current = true;
		},
		onMouseUp() {
			endedOrStartedInsideRef.current = true;
		},
		[captureHandlerKeys[outsidePressEvent]]: () => {
			insideReactTreeRef.current = true;
		}
	}), [closeOnEscapeKeyDown, outsidePressEvent]);
	return import_react.useMemo(() => enabled ? {
		reference,
		floating
	} : {}, [
		enabled,
		reference,
		floating
	]);
}
function useFloatingRootContext(options) {
	const { open = false, onOpenChange: onOpenChangeProp, elements: elementsProp } = options;
	const floatingId = useId$1();
	const dataRef = import_react.useRef({});
	const [events] = import_react.useState(() => createPubSub());
	const nested = useFloatingParentNodeId() != null;
	{
		const optionDomReference = elementsProp.reference;
		if (optionDomReference && !isElement$1(optionDomReference)) error("Cannot pass a virtual element to the `elements.reference` option,", "as it must be a real DOM element. Use `refs.setPositionReference()`", "instead.");
	}
	const [positionReference, setPositionReference] = import_react.useState(elementsProp.reference);
	const onOpenChange = useEffectEvent((open, event, reason) => {
		dataRef.current.openEvent = open ? event : void 0;
		events.emit("openchange", {
			open,
			event,
			reason,
			nested
		});
		onOpenChangeProp?.(open, event, reason);
	});
	const refs = import_react.useMemo(() => ({ setPositionReference }), []);
	const elements = import_react.useMemo(() => ({
		reference: positionReference || elementsProp.reference || null,
		floating: elementsProp.floating || null,
		domReference: elementsProp.reference
	}), [
		positionReference,
		elementsProp.reference,
		elementsProp.floating
	]);
	return import_react.useMemo(() => ({
		dataRef,
		open,
		onOpenChange,
		elements,
		events,
		floatingId,
		refs
	}), [
		open,
		onOpenChange,
		elements,
		events,
		floatingId,
		refs
	]);
}
/**
* Provides data to position a floating element and context to add interactions.
* @see https://floating-ui.com/docs/useFloating
*/
function useFloating(options) {
	if (options === void 0) options = {};
	const { nodeId } = options;
	const internalRootContext = useFloatingRootContext({
		...options,
		elements: {
			reference: null,
			floating: null,
			...options.elements
		}
	});
	const rootContext = options.rootContext || internalRootContext;
	const computedElements = rootContext.elements;
	const [_domReference, setDomReference] = import_react.useState(null);
	const [positionReference, _setPositionReference] = import_react.useState(null);
	const domReference = (computedElements == null ? void 0 : computedElements.domReference) || _domReference;
	const domReferenceRef = import_react.useRef(null);
	const tree = useFloatingTree();
	index(() => {
		if (domReference) domReferenceRef.current = domReference;
	}, [domReference]);
	const position = useFloating$1({
		...options,
		elements: {
			...computedElements,
			...positionReference && { reference: positionReference }
		}
	});
	const setPositionReference = import_react.useCallback((node) => {
		const computedPositionReference = isElement$1(node) ? {
			getBoundingClientRect: () => node.getBoundingClientRect(),
			contextElement: node
		} : node;
		_setPositionReference(computedPositionReference);
		position.refs.setReference(computedPositionReference);
	}, [position.refs]);
	const setReference = import_react.useCallback((node) => {
		if (isElement$1(node) || node === null) {
			domReferenceRef.current = node;
			setDomReference(node);
		}
		if (isElement$1(position.refs.reference.current) || position.refs.reference.current === null || node !== null && !isElement$1(node)) position.refs.setReference(node);
	}, [position.refs]);
	const refs = import_react.useMemo(() => ({
		...position.refs,
		setReference,
		setPositionReference,
		domReference: domReferenceRef
	}), [
		position.refs,
		setReference,
		setPositionReference
	]);
	const elements = import_react.useMemo(() => ({
		...position.elements,
		domReference
	}), [position.elements, domReference]);
	const context = import_react.useMemo(() => ({
		...position,
		...rootContext,
		refs,
		elements,
		nodeId
	}), [
		position,
		refs,
		elements,
		nodeId,
		rootContext
	]);
	index(() => {
		rootContext.dataRef.current.floatingContext = context;
		const node = tree == null ? void 0 : tree.nodesRef.current.find((node) => node.id === nodeId);
		if (node) node.context = context;
	});
	return import_react.useMemo(() => ({
		...position,
		context,
		refs,
		elements
	}), [
		position,
		refs,
		elements,
		context
	]);
}
/**
* Opens the floating element while the reference element has focus, like CSS
* `:focus`.
* @see https://floating-ui.com/docs/useFocus
*/
function useFocus(context, props) {
	if (props === void 0) props = {};
	const { open, onOpenChange, events, dataRef, elements } = context;
	const { enabled = true, visibleOnly = true } = props;
	const blockFocusRef = import_react.useRef(false);
	const timeoutRef = import_react.useRef();
	const keyboardModalityRef = import_react.useRef(true);
	import_react.useEffect(() => {
		if (!enabled) return;
		const win = getWindow$1(elements.domReference);
		function onBlur() {
			if (!open && isHTMLElement(elements.domReference) && elements.domReference === activeElement(getDocument$1(elements.domReference))) blockFocusRef.current = true;
		}
		function onKeyDown() {
			keyboardModalityRef.current = true;
		}
		win.addEventListener("blur", onBlur);
		win.addEventListener("keydown", onKeyDown, true);
		return () => {
			win.removeEventListener("blur", onBlur);
			win.removeEventListener("keydown", onKeyDown, true);
		};
	}, [
		elements.domReference,
		open,
		enabled
	]);
	import_react.useEffect(() => {
		if (!enabled) return;
		function onOpenChange(_ref) {
			let { reason } = _ref;
			if (reason === "reference-press" || reason === "escape-key") blockFocusRef.current = true;
		}
		events.on("openchange", onOpenChange);
		return () => {
			events.off("openchange", onOpenChange);
		};
	}, [events, enabled]);
	import_react.useEffect(() => {
		return () => {
			clearTimeout(timeoutRef.current);
		};
	}, []);
	const reference = import_react.useMemo(() => ({
		onPointerDown(event) {
			if (isVirtualPointerEvent(event.nativeEvent)) return;
			keyboardModalityRef.current = false;
		},
		onMouseLeave() {
			blockFocusRef.current = false;
		},
		onFocus(event) {
			if (blockFocusRef.current) return;
			const target = getTarget(event.nativeEvent);
			if (visibleOnly && isElement$1(target)) try {
				if (isSafari() && isMac()) throw Error();
				if (!target.matches(":focus-visible")) return;
			} catch (e) {
				if (!keyboardModalityRef.current && !isTypeableElement(target)) return;
			}
			onOpenChange(true, event.nativeEvent, "focus");
		},
		onBlur(event) {
			blockFocusRef.current = false;
			const relatedTarget = event.relatedTarget;
			const nativeEvent = event.nativeEvent;
			const movedToFocusGuard = isElement$1(relatedTarget) && relatedTarget.hasAttribute(createAttribute("focus-guard")) && relatedTarget.getAttribute("data-type") === "outside";
			timeoutRef.current = window.setTimeout(() => {
				var _dataRef$current$floa;
				const activeEl = activeElement(elements.domReference ? elements.domReference.ownerDocument : document);
				if (!relatedTarget && activeEl === elements.domReference) return;
				if (contains((_dataRef$current$floa = dataRef.current.floatingContext) == null ? void 0 : _dataRef$current$floa.refs.floating.current, activeEl) || contains(elements.domReference, activeEl) || movedToFocusGuard) return;
				onOpenChange(false, nativeEvent, "focus");
			});
		}
	}), [
		dataRef,
		elements.domReference,
		onOpenChange,
		visibleOnly
	]);
	return import_react.useMemo(() => enabled ? { reference } : {}, [enabled, reference]);
}
var ACTIVE_KEY = "active";
var SELECTED_KEY = "selected";
function mergeProps$1(userProps, propsList, elementKey) {
	const map = /* @__PURE__ */ new Map();
	const isItem = elementKey === "item";
	let domUserProps = userProps;
	if (isItem && userProps) {
		const { [ACTIVE_KEY]: _, [SELECTED_KEY]: __, ...validProps } = userProps;
		domUserProps = validProps;
	}
	return {
		...elementKey === "floating" && {
			tabIndex: -1,
			[FOCUSABLE_ATTRIBUTE]: ""
		},
		...domUserProps,
		...propsList.map((value) => {
			const propsOrGetProps = value ? value[elementKey] : null;
			if (typeof propsOrGetProps === "function") return userProps ? propsOrGetProps(userProps) : null;
			return propsOrGetProps;
		}).concat(userProps).reduce((acc, props) => {
			if (!props) return acc;
			Object.entries(props).forEach((_ref) => {
				let [key, value] = _ref;
				if (isItem && [ACTIVE_KEY, SELECTED_KEY].includes(key)) return;
				if (key.indexOf("on") === 0) {
					if (!map.has(key)) map.set(key, []);
					if (typeof value === "function") {
						var _map$get;
						(_map$get = map.get(key)) == null || _map$get.push(value);
						acc[key] = function() {
							var _map$get2;
							for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) args[_key] = arguments[_key];
							return (_map$get2 = map.get(key)) == null ? void 0 : _map$get2.map((fn) => fn(...args)).find((val) => val !== void 0);
						};
					}
				} else acc[key] = value;
			});
			return acc;
		}, {})
	};
}
/**
* Merges an array of interaction hooks' props into prop getters, allowing
* event handler functions to be composed together without overwriting one
* another.
* @see https://floating-ui.com/docs/useInteractions
*/
function useInteractions(propsList) {
	if (propsList === void 0) propsList = [];
	const referenceDeps = propsList.map((key) => key == null ? void 0 : key.reference);
	const floatingDeps = propsList.map((key) => key == null ? void 0 : key.floating);
	const itemDeps = propsList.map((key) => key == null ? void 0 : key.item);
	const getReferenceProps = import_react.useCallback((userProps) => mergeProps$1(userProps, propsList, "reference"), referenceDeps);
	const getFloatingProps = import_react.useCallback((userProps) => mergeProps$1(userProps, propsList, "floating"), floatingDeps);
	const getItemProps = import_react.useCallback((userProps) => mergeProps$1(userProps, propsList, "item"), itemDeps);
	return import_react.useMemo(() => ({
		getReferenceProps,
		getFloatingProps,
		getItemProps
	}), [
		getReferenceProps,
		getFloatingProps,
		getItemProps
	]);
}
var componentRoleToAriaRoleMap = /*#__PURE__*/ new Map([
	["select", "listbox"],
	["combobox", "listbox"],
	["label", false]
]);
/**
* Adds base screen reader props to the reference and floating elements for a
* given floating element `role`.
* @see https://floating-ui.com/docs/useRole
*/
function useRole(context, props) {
	var _componentRoleToAriaR;
	if (props === void 0) props = {};
	const { open, floatingId } = context;
	const { enabled = true, role = "dialog" } = props;
	const ariaRole = (_componentRoleToAriaR = componentRoleToAriaRoleMap.get(role)) != null ? _componentRoleToAriaR : role;
	const referenceId = useId$1();
	const isNested = useFloatingParentNodeId() != null;
	const reference = import_react.useMemo(() => {
		if (ariaRole === "tooltip" || role === "label") return { ["aria-" + (role === "label" ? "labelledby" : "describedby")]: open ? floatingId : void 0 };
		return {
			"aria-expanded": open ? "true" : "false",
			"aria-haspopup": ariaRole === "alertdialog" ? "dialog" : ariaRole,
			"aria-controls": open ? floatingId : void 0,
			...ariaRole === "listbox" && { role: "combobox" },
			...ariaRole === "menu" && { id: referenceId },
			...ariaRole === "menu" && isNested && { role: "menuitem" },
			...role === "select" && { "aria-autocomplete": "none" },
			...role === "combobox" && { "aria-autocomplete": "list" }
		};
	}, [
		ariaRole,
		floatingId,
		isNested,
		open,
		referenceId,
		role
	]);
	const floating = import_react.useMemo(() => {
		const floatingProps = {
			id: floatingId,
			...ariaRole && { role: ariaRole }
		};
		if (ariaRole === "tooltip" || role === "label") return floatingProps;
		return {
			...floatingProps,
			...ariaRole === "menu" && { "aria-labelledby": referenceId }
		};
	}, [
		ariaRole,
		floatingId,
		referenceId,
		role
	]);
	const item = import_react.useCallback((_ref) => {
		let { active, selected } = _ref;
		const commonProps = {
			role: "option",
			...active && { id: floatingId + "-option" }
		};
		switch (role) {
			case "select": return {
				...commonProps,
				"aria-selected": active && selected
			};
			case "combobox": return {
				...commonProps,
				...active && { "aria-selected": true }
			};
		}
		return {};
	}, [floatingId, role]);
	return import_react.useMemo(() => enabled ? {
		reference,
		floating,
		item
	} : {}, [
		enabled,
		reference,
		floating,
		item
	]);
}
function isPointInPolygon(point, polygon) {
	const [x, y] = point;
	let isInside = false;
	const length = polygon.length;
	for (let i = 0, j = length - 1; i < length; j = i++) {
		const [xi, yi] = polygon[i] || [0, 0];
		const [xj, yj] = polygon[j] || [0, 0];
		if (yi >= y !== yj >= y && x <= (xj - xi) * (y - yi) / (yj - yi) + xi) isInside = !isInside;
	}
	return isInside;
}
function isInside(point, rect) {
	return point[0] >= rect.x && point[0] <= rect.x + rect.width && point[1] >= rect.y && point[1] <= rect.y + rect.height;
}
/**
* Generates a safe polygon area that the user can traverse without closing the
* floating element once leaving the reference element.
* @see https://floating-ui.com/docs/useHover#safepolygon
*/
function safePolygon(options) {
	if (options === void 0) options = {};
	const { buffer = .5, blockPointerEvents = false, requireIntent = true } = options;
	let timeoutId;
	let hasLanded = false;
	let lastX = null;
	let lastY = null;
	let lastCursorTime = performance.now();
	function getCursorSpeed(x, y) {
		const currentTime = performance.now();
		const elapsedTime = currentTime - lastCursorTime;
		if (lastX === null || lastY === null || elapsedTime === 0) {
			lastX = x;
			lastY = y;
			lastCursorTime = currentTime;
			return null;
		}
		const deltaX = x - lastX;
		const deltaY = y - lastY;
		const speed = Math.sqrt(deltaX * deltaX + deltaY * deltaY) / elapsedTime;
		lastX = x;
		lastY = y;
		lastCursorTime = currentTime;
		return speed;
	}
	const fn = (_ref) => {
		let { x, y, placement, elements, onClose, nodeId, tree } = _ref;
		return function onMouseMove(event) {
			function close() {
				clearTimeout(timeoutId);
				onClose();
			}
			clearTimeout(timeoutId);
			if (!elements.domReference || !elements.floating || placement == null || x == null || y == null) return;
			const { clientX, clientY } = event;
			const clientPoint = [clientX, clientY];
			const target = getTarget(event);
			const isLeave = event.type === "mouseleave";
			const isOverFloatingEl = contains(elements.floating, target);
			const isOverReferenceEl = contains(elements.domReference, target);
			const refRect = elements.domReference.getBoundingClientRect();
			const rect = elements.floating.getBoundingClientRect();
			const side = placement.split("-")[0];
			const cursorLeaveFromRight = x > rect.right - rect.width / 2;
			const cursorLeaveFromBottom = y > rect.bottom - rect.height / 2;
			const isOverReferenceRect = isInside(clientPoint, refRect);
			const isFloatingWider = rect.width > refRect.width;
			const isFloatingTaller = rect.height > refRect.height;
			const left = (isFloatingWider ? refRect : rect).left;
			const right = (isFloatingWider ? refRect : rect).right;
			const top = (isFloatingTaller ? refRect : rect).top;
			const bottom = (isFloatingTaller ? refRect : rect).bottom;
			if (isOverFloatingEl) {
				hasLanded = true;
				if (!isLeave) return;
			}
			if (isOverReferenceEl) hasLanded = false;
			if (isOverReferenceEl && !isLeave) {
				hasLanded = true;
				return;
			}
			if (isLeave && isElement$1(event.relatedTarget) && contains(elements.floating, event.relatedTarget)) return;
			if (tree && getChildren(tree.nodesRef.current, nodeId).some((_ref2) => {
				let { context } = _ref2;
				return context == null ? void 0 : context.open;
			})) return;
			if (side === "top" && y >= refRect.bottom - 1 || side === "bottom" && y <= refRect.top + 1 || side === "left" && x >= refRect.right - 1 || side === "right" && x <= refRect.left + 1) return close();
			let rectPoly = [];
			switch (side) {
				case "top":
					rectPoly = [
						[left, refRect.top + 1],
						[left, rect.bottom - 1],
						[right, rect.bottom - 1],
						[right, refRect.top + 1]
					];
					break;
				case "bottom":
					rectPoly = [
						[left, rect.top + 1],
						[left, refRect.bottom - 1],
						[right, refRect.bottom - 1],
						[right, rect.top + 1]
					];
					break;
				case "left":
					rectPoly = [
						[rect.right - 1, bottom],
						[rect.right - 1, top],
						[refRect.left + 1, top],
						[refRect.left + 1, bottom]
					];
					break;
				case "right":
					rectPoly = [
						[refRect.right - 1, bottom],
						[refRect.right - 1, top],
						[rect.left + 1, top],
						[rect.left + 1, bottom]
					];
					break;
			}
			function getPolygon(_ref3) {
				let [x, y] = _ref3;
				switch (side) {
					case "top": return [
						[isFloatingWider ? x + buffer / 2 : cursorLeaveFromRight ? x + buffer * 4 : x - buffer * 4, y + buffer + 1],
						[isFloatingWider ? x - buffer / 2 : cursorLeaveFromRight ? x + buffer * 4 : x - buffer * 4, y + buffer + 1],
						...[[rect.left, cursorLeaveFromRight ? rect.bottom - buffer : isFloatingWider ? rect.bottom - buffer : rect.top], [rect.right, cursorLeaveFromRight ? isFloatingWider ? rect.bottom - buffer : rect.top : rect.bottom - buffer]]
					];
					case "bottom": return [
						[isFloatingWider ? x + buffer / 2 : cursorLeaveFromRight ? x + buffer * 4 : x - buffer * 4, y - buffer],
						[isFloatingWider ? x - buffer / 2 : cursorLeaveFromRight ? x + buffer * 4 : x - buffer * 4, y - buffer],
						...[[rect.left, cursorLeaveFromRight ? rect.top + buffer : isFloatingWider ? rect.top + buffer : rect.bottom], [rect.right, cursorLeaveFromRight ? isFloatingWider ? rect.top + buffer : rect.bottom : rect.top + buffer]]
					];
					case "left": {
						const cursorPointOne = [x + buffer + 1, isFloatingTaller ? y + buffer / 2 : cursorLeaveFromBottom ? y + buffer * 4 : y - buffer * 4];
						const cursorPointTwo = [x + buffer + 1, isFloatingTaller ? y - buffer / 2 : cursorLeaveFromBottom ? y + buffer * 4 : y - buffer * 4];
						return [
							...[[cursorLeaveFromBottom ? rect.right - buffer : isFloatingTaller ? rect.right - buffer : rect.left, rect.top], [cursorLeaveFromBottom ? isFloatingTaller ? rect.right - buffer : rect.left : rect.right - buffer, rect.bottom]],
							cursorPointOne,
							cursorPointTwo
						];
					}
					case "right": return [
						[x - buffer, isFloatingTaller ? y + buffer / 2 : cursorLeaveFromBottom ? y + buffer * 4 : y - buffer * 4],
						[x - buffer, isFloatingTaller ? y - buffer / 2 : cursorLeaveFromBottom ? y + buffer * 4 : y - buffer * 4],
						...[[cursorLeaveFromBottom ? rect.left + buffer : isFloatingTaller ? rect.left + buffer : rect.right, rect.top], [cursorLeaveFromBottom ? isFloatingTaller ? rect.left + buffer : rect.right : rect.left + buffer, rect.bottom]]
					];
				}
			}
			if (isPointInPolygon([clientX, clientY], rectPoly)) return;
			if (hasLanded && !isOverReferenceRect) return close();
			if (!isLeave && requireIntent) {
				const cursorSpeed = getCursorSpeed(event.clientX, event.clientY);
				if (cursorSpeed !== null && cursorSpeed < .1) return close();
			}
			if (!isPointInPolygon([clientX, clientY], getPolygon([x, y]))) close();
			else if (!hasLanded && requireIntent) timeoutId = window.setTimeout(close, 40);
		};
	};
	fn.__options = { blockPointerEvents };
	return fn;
}
//#endregion
//#region node_modules/@salt-ds/core/dist-es/aria-announcer/AriaAnnouncerContext.js
var AriaAnnouncerContext = (0, import_react.createContext)(void 0);
//#endregion
//#region node_modules/@salt-ds/core/dist-es/utils/useId.js
var maybeReactUseId = import_react["useId".toString()];
var globalId = BigInt(0);
function useIdLegacy(idOverride) {
	const [defaultId, setDefaultId] = import_react.useState(idOverride);
	const id = idOverride || defaultId;
	import_react.useEffect(() => {
		if (defaultId == null) setDefaultId(`salt-${++globalId}`);
	}, [defaultId]);
	return id;
}
function useId(idOverride) {
	if (maybeReactUseId !== void 0) {
		const reactId = maybeReactUseId();
		return idOverride != null ? idOverride : reactId;
	}
	return useIdLegacy(idOverride);
}
function useIdMemo(idOverride) {
	return import_react.useMemo(() => {
		return idOverride != null ? idOverride : `salt-${++globalId}`;
	}, [idOverride]);
}
//#endregion
//#region node_modules/@salt-ds/core/dist-es/utils/useIsomorphicLayoutEffect.js
var useIsomorphicLayoutEffect = typeof window !== "undefined" && typeof window.document !== "undefined" && typeof window.document.createElement !== "undefined" ? import_react.useLayoutEffect : import_react.useEffect;
//#endregion
//#region node_modules/@salt-ds/core/dist-es/viewport/ViewportProvider.js
var ViewportContext = (0, import_react.createContext)(null);
var ViewportProvider = ({ children }) => {
	const existingViewport = (0, import_react.useContext)(ViewportContext);
	const [viewport, setViewport] = (0, import_react.useState)(existingViewport);
	const noExistingViewport = existingViewport === null;
	const viewportValue = existingViewport || viewport || 0;
	useIsomorphicLayoutEffect(() => {
		let observer = null;
		if (noExistingViewport) {
			observer = new ResizeObserver((observerEntries) => {
				setViewport(observerEntries[0].contentRect.width);
			});
			observer.observe(document.body);
			setViewport(document.body.getBoundingClientRect().width);
		}
		return () => {
			if (observer) observer.disconnect();
		};
	}, [noExistingViewport]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ViewportContext.Provider, {
		value: viewportValue,
		children
	});
};
var useViewport = () => {
	const value = (0, import_react.useContext)(ViewportContext);
	return value === null ? 0 : value;
};
//#endregion
//#region node_modules/@salt-ds/core/dist-es/aria-announcer/AriaAnnouncerProvider.js
var ARIA_ANNOUNCE_DELAY = 150;
function AriaAnnouncerProvider({ children, style }) {
	const [currentAnnouncement, setCurrentAnnouncement] = (0, import_react.useState)("");
	const announcementsRef = (0, import_react.useRef)([]);
	const isAnnouncingRef = (0, import_react.useRef)(false);
	const mountedRef = (0, import_react.useRef)(true);
	const announceAll = (0, import_react.useCallback)(() => {
		isAnnouncingRef.current = true;
		if (mountedRef.current) {
			setCurrentAnnouncement("");
			requestAnimationFrame(() => {
				if (mountedRef.current && announcementsRef.current.length) {
					const announcement = announcementsRef.current.shift();
					setCurrentAnnouncement(announcement);
					setTimeout(() => {
						announceAll();
					}, 150);
				} else isAnnouncingRef.current = false;
			});
		}
	}, []);
	const announce = (0, import_react.useCallback)((announcement) => {
		announcementsRef.current.push(announcement);
		if (!isAnnouncingRef.current) announceAll();
	}, [announceAll]);
	(0, import_react.useEffect)(() => {
		mountedRef.current = true;
		return () => {
			mountedRef.current = false;
		};
	}, []);
	const value = (0, import_react.useMemo)(() => ({ announce }), [announce]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(AriaAnnouncerContext.Provider, {
		value,
		children: [children, /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			"aria-atomic": "true",
			"aria-live": "assertive",
			style: {
				position: "absolute",
				height: 1,
				width: 1,
				padding: 0,
				margin: -1,
				overflow: "hidden",
				clip: "rect(0, 0, 0, 0)",
				whiteSpace: "nowrap",
				borderWidth: 0,
				...style
			},
			children: currentAnnouncement
		})]
	});
}
//#endregion
//#region node_modules/@salt-ds/core/dist-es/breakpoints/Breakpoints.js
var DEFAULT_BREAKPOINTS = {
	xs: 0,
	sm: 600,
	md: 960,
	lg: 1280,
	xl: 1920
};
//#endregion
//#region node_modules/@salt-ds/core/dist-es/salt-provider/SaltProvider.css.js
var css_248z$56 = ".salt-provider {\n  display: contents;\n}\n";
//#endregion
//#region node_modules/@salt-ds/window/dist-es/WindowProvider.js
var WindowContext = (0, import_react.createContext)(typeof window !== "undefined" ? window : null);
WindowContext.displayName = "WindowContext";
function useWindow() {
	return (0, import_react.useContext)(WindowContext);
}
//#endregion
//#region node_modules/@salt-ds/core/node_modules/@salt-ds/styles/dist-es/use-style-injection/InsertionPointProvider.js
var InsertionPointContext$1 = (0, import_react.createContext)(null);
InsertionPointContext$1.displayName = "InsertionPointContext";
function useInsertionPoint$1() {
	return (0, import_react.useContext)(InsertionPointContext$1);
}
//#endregion
//#region node_modules/@salt-ds/core/node_modules/@salt-ds/styles/dist-es/style-injection-provider/index.js
var StyleInjectionContext$1 = (0, import_react.createContext)(true);
function useStyleInjection$1(enableStyleInjection) {
	const enableStyleInjectionFromContext = (0, import_react.useContext)(StyleInjectionContext$1);
	return enableStyleInjection != null ? enableStyleInjection : enableStyleInjectionFromContext;
}
function StyleInjectionProvider(props) {
	const { value: enableStyleInjectionProp, children } = props;
	const value = useStyleInjection$1(enableStyleInjectionProp);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(StyleInjectionContext$1.Provider, {
		value,
		children
	});
}
//#endregion
//#region node_modules/@salt-ds/core/node_modules/@salt-ds/styles/dist-es/use-style-injection/useStyleInjection.js
var _a;
var maybeUseInsertionEffect$1 = (_a = import_react["useInsertionEffect".toString()]) != null ? _a : import_react.useLayoutEffect;
var windowSheetsMap$1 = /* @__PURE__ */ new WeakMap();
function useComponentCssInjection$1({ testId, css, window: targetWindow }) {
	const styleInjectionEnabled = useStyleInjection$1();
	const insertionPoint = useInsertionPoint$1();
	maybeUseInsertionEffect$1(() => {
		var _a2, _b;
		if (!targetWindow || !styleInjectionEnabled) return;
		const sheetsMap = (_a2 = windowSheetsMap$1.get(targetWindow)) != null ? _a2 : /* @__PURE__ */ new Map();
		const styleMap = (_b = sheetsMap.get(css)) != null ? _b : {
			styleElement: null,
			count: 0
		};
		if (styleMap.styleElement == null) {
			styleMap.styleElement = targetWindow.document.createElement("style");
			styleMap.styleElement.setAttribute("type", "text/css");
			styleMap.styleElement.setAttribute("data-salt-style", testId || "");
			styleMap.styleElement.textContent = css;
			styleMap.count = 1;
			targetWindow.document.head.insertBefore(styleMap.styleElement, insertionPoint || targetWindow.document.head.firstChild);
		} else {
			styleMap.styleElement.textContent = css;
			styleMap.count++;
		}
		sheetsMap.set(css, styleMap);
		windowSheetsMap$1.set(targetWindow, sheetsMap);
		return () => {
			const sheetsMap2 = windowSheetsMap$1.get(targetWindow);
			const styleMap2 = sheetsMap2 == null ? void 0 : sheetsMap2.get(css);
			if (styleMap2 == null ? void 0 : styleMap2.styleElement) {
				styleMap2.count--;
				if (styleMap2.count < 1) {
					targetWindow.document.head.removeChild(styleMap2.styleElement);
					styleMap2.styleElement = null;
					sheetsMap2?.delete(css);
				}
			}
		};
	}, [
		testId,
		css,
		targetWindow
	]);
}
//#endregion
//#region node_modules/@salt-ds/core/dist-es/salt-provider/SaltProvider.js
var DEFAULT_DENSITY = "medium";
var DEFAULT_THEME_NAME = "salt-theme";
var DEFAULT_MODE = "light";
var DensityContext = (0, import_react.createContext)(DEFAULT_DENSITY);
var ThemeContext = (0, import_react.createContext)({
	theme: "",
	mode: DEFAULT_MODE
});
var BreakpointContext = (0, import_react.createContext)(DEFAULT_BREAKPOINTS);
var createThemedChildren = (children, themeName, density, mode, applyClassesTo) => {
	var _a;
	const themeNames = themeName === DEFAULT_THEME_NAME ? [DEFAULT_THEME_NAME] : [DEFAULT_THEME_NAME, themeName];
	if (applyClassesTo === "root") return children;
	else if (applyClassesTo === "child") if (import_react.isValidElement(children)) return import_react.cloneElement(children, {
		className: clsx((_a = children.props) == null ? void 0 : _a.className, ...themeNames, `salt-density-${density}`),
		"data-mode": mode
	});
	else {
		console.warn(`
SaltProvider can only apply CSS classes for theming to a single nested child element of the SaltProvider.
        Either wrap elements with a single container or consider removing the applyClassesToChild prop, in which case a
        div element will wrap your child elements`);
		return children;
	}
	else return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		className: clsx(`salt-provider`, ...themeNames, `salt-density-${density}`),
		"data-mode": mode,
		children
	});
};
function InternalSaltProvider({ applyClassesTo: applyClassesToProp, children, density: densityProp, theme: themeProp, mode: modeProp, breakpoints: breakpointsProp }) {
	var _a;
	const inheritedDensity = (0, import_react.useContext)(DensityContext);
	const { theme: inheritedTheme, mode: inheritedMode, window: inheritedWindow } = (0, import_react.useContext)(ThemeContext);
	const isRootProvider = inheritedTheme === void 0 || inheritedTheme === "";
	const density = (_a = densityProp != null ? densityProp : inheritedDensity) != null ? _a : DEFAULT_DENSITY;
	const themeName = themeProp != null ? themeProp : inheritedTheme === "" ? DEFAULT_THEME_NAME : inheritedTheme;
	const mode = modeProp != null ? modeProp : inheritedMode;
	const breakpoints = breakpointsProp != null ? breakpointsProp : DEFAULT_BREAKPOINTS;
	const applyClassesTo = applyClassesToProp != null ? applyClassesToProp : isRootProvider ? "root" : "scope";
	const targetWindow = useWindow();
	useComponentCssInjection$1({
		testId: "salt-provider",
		css: css_248z$56,
		window: targetWindow
	});
	const themeContextValue = (0, import_react.useMemo)(() => ({
		theme: themeName,
		mode,
		window: targetWindow
	}), [
		themeName,
		mode,
		targetWindow
	]);
	const themedChildren = createThemedChildren(children, themeName, density, mode, applyClassesTo);
	useIsomorphicLayoutEffect(() => {
		const themeNames = themeName === DEFAULT_THEME_NAME ? [DEFAULT_THEME_NAME] : [DEFAULT_THEME_NAME, themeName];
		if (applyClassesTo === "root" && targetWindow) if (inheritedWindow != targetWindow) {
			targetWindow.document.documentElement.classList.add(...themeNames, `salt-density-${density}`);
			targetWindow.document.documentElement.dataset.mode = mode;
		} else console.warn("SaltProvider can only apply CSS classes to the root if it is the root level SaltProvider.");
		return () => {
			if (applyClassesTo === "root" && targetWindow) {
				targetWindow.document.documentElement.classList.remove(...themeNames, `salt-density-${density}`);
				targetWindow.document.documentElement.dataset.mode = void 0;
			}
		};
	}, [
		applyClassesTo,
		density,
		mode,
		themeName,
		targetWindow,
		inheritedWindow
	]);
	const saltProvider = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DensityContext.Provider, {
		value: density,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ThemeContext.Provider, {
			value: themeContextValue,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(BreakpointContext.Provider, {
				value: breakpoints,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ViewportProvider, { children: themedChildren })
			})
		})
	});
	if (isRootProvider) return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AriaAnnouncerProvider, { children: saltProvider });
	else return saltProvider;
}
function SaltProvider({ enableStyleInjection, ...restProps }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(StyleInjectionProvider, {
		value: enableStyleInjection,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(InternalSaltProvider, { ...restProps })
	});
}
var useTheme = () => {
	const { window, ...contextWithoutWindow } = (0, import_react.useContext)(ThemeContext);
	return contextWithoutWindow;
};
function useDensity(density) {
	var _a;
	const densityFromContext = (0, import_react.useContext)(DensityContext);
	return (_a = density != null ? density : densityFromContext) != null ? _a : DEFAULT_DENSITY;
}
var useBreakpoints = () => {
	return (0, import_react.useContext)(BreakpointContext);
};
//#endregion
//#region node_modules/@salt-ds/core/dist-es/utils/useFloatingUI/useFloatingUI.js
var FloatingComponentContext = (0, import_react.createContext)({ Component: (0, import_react.forwardRef)(function DefaultFloatingComponent2(props, ref) {
	const { open, top, left, position, width, height, focusManagerProps, ...rest } = props;
	const style = {
		top,
		left,
		position
	};
	if (focusManagerProps && open) return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FloatingPortal, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SaltProvider, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FloatingFocusManager, {
		...focusManagerProps,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			style,
			...rest,
			ref
		})
	}) }) });
	return open ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FloatingPortal, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SaltProvider, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		style,
		...rest,
		ref
	}) }) }) : null;
}) });
FloatingComponentContext.displayName = "FloatingComponentContext";
function FloatingComponentProvider(props) {
	const { Component, children } = props;
	const value = (0, import_react.useMemo)(() => ({ Component }), [Component]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FloatingComponentContext.Provider, {
		value,
		children
	});
}
function useFloatingComponent() {
	return (0, import_react.useContext)(FloatingComponentContext);
}
var defaultGetMiddleware = (defaultMiddleware) => defaultMiddleware;
var FloatingPlatformContext = (0, import_react.createContext)({
	platform,
	middleware: defaultGetMiddleware,
	animationFrame: false
});
function FloatingPlatformProvider(props) {
	const { platform: platformProp, middleware, animationFrame, children } = props;
	const floatingPlatformContextValue = (0, import_react.useMemo)(() => ({
		platform: platformProp != null ? platformProp : platform,
		middleware: middleware != null ? middleware : defaultGetMiddleware,
		animationFrame: animationFrame || false
	}), [
		platformProp,
		middleware,
		animationFrame
	]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FloatingPlatformContext.Provider, {
		value: floatingPlatformContextValue,
		children
	});
}
function useFloatingPlatform() {
	return (0, import_react.useContext)(FloatingPlatformContext);
}
var DEFAULT_FLOATING_UI_MIDDLEWARE = [flip(), shift({ limiter: limitShift() })];
function useFloatingUI(props) {
	const { placement, strategy, middleware = DEFAULT_FLOATING_UI_MIDDLEWARE, open = false, onOpenChange } = props;
	const handleOpenChange = (open2, boolean, reason) => {
		update();
		onOpenChange?.(open2, boolean, reason);
	};
	const { platform: contextPlatform, middleware: contextMiddleware, animationFrame } = useFloatingPlatform();
	const { refs, update, ...rest } = useFloating({
		placement,
		strategy,
		middleware: contextMiddleware(middleware),
		open,
		onOpenChange: handleOpenChange,
		whileElementsMounted: (...args) => {
			return autoUpdate(...args, { animationFrame });
		},
		platform: contextPlatform
	});
	return {
		reference: refs.setReference,
		floating: refs.setFloating,
		refs,
		update,
		...rest
	};
}
//#endregion
//#region node_modules/@salt-ds/core/dist-es/accordion/AccordionGroup.css.js
var css_248z$55 = ".saltAccordionGroup {\n  border-bottom: var(--salt-size-border) var(--salt-separable-borderStyle) var(--salt-separable-tertiary-borderColor);\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/accordion/AccordionGroup.js
var withBaseName$58 = makePrefixer("saltAccordionGroup");
var AccordionGroup = (0, import_react.forwardRef)(function AccordionGroup2(props, ref) {
	const { className, ...rest } = props;
	useComponentCssInjection$1({
		testId: "salt-accordion-group",
		css: css_248z$55,
		window: useWindow()
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		className: clsx(withBaseName$58(), className),
		ref,
		...rest
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/utils/createContext.js
function createContext(name, defaultValue) {
	const context = (0, import_react.createContext)(defaultValue);
	context.displayName = name;
	return context;
}
//#endregion
//#region node_modules/@salt-ds/core/dist-es/accordion/AccordionContext.js
var AccordionContext = createContext("AccordionContext", {
	value: "",
	expanded: false,
	toggle: () => void 0,
	disabled: false,
	id: ""
});
function useAccordion() {
	return (0, import_react.useContext)(AccordionContext);
}
//#endregion
//#region node_modules/@salt-ds/core/dist-es/accordion/AccordionPanel.css.js
var css_248z$54 = ".saltAccordionPanel {\n  display: grid;\n  transition: grid-template-rows var(--salt-duration-perceptible) ease-in-out, opacity var(--salt-duration-perceptible) ease-in-out, visibility var(--salt-duration-perceptible) ease-in-out;\n}\n\n.saltAccordionPanel[aria-hidden=\"true\"] {\n  grid-template-rows: 0fr;\n  opacity: 0;\n  visibility: hidden;\n}\n\n.saltAccordionPanel {\n  grid-template-rows: 1fr;\n  opacity: 1;\n  visibility: visible;\n}\n\n.saltAccordionPanel-inner {\n  overflow: hidden;\n}\n\n.saltAccordionPanel-content {\n  padding-left: calc(var(--salt-spacing-200) + var(--salt-size-icon));\n  padding-right: var(--salt-spacing-100);\n  padding-top: var(--salt-spacing-300);\n  padding-bottom: var(--salt-spacing-300);\n}\n\n@media (prefers-reduced-motion: reduce) {\n  .saltAccordionPanel {\n    transition: none;\n  }\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/accordion/AccordionPanel.js
var withBaseName$57 = makePrefixer("saltAccordionPanel");
var AccordionPanel = (0, import_react.forwardRef)(function AccordionPanel2(props, ref) {
	const { children, className, ...rest } = props;
	useComponentCssInjection$1({
		testId: "salt-accordion-panel",
		css: css_248z$54,
		window: useWindow()
	});
	const { id, expanded } = useAccordion();
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		ref,
		className: clsx(withBaseName$57(), className),
		role: "region",
		id: `${id}-panel`,
		"aria-labelledby": `${id}-header`,
		"aria-hidden": !expanded ? "true" : void 0,
		hidden: !expanded,
		...rest,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: withBaseName$57("inner"),
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: withBaseName$57("content"),
				children
			})
		})
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/utils/useControlled.js
function useControlled({ controlled, default: defaultProp, name, state = "value" }) {
	const { current: isControlled } = (0, import_react.useRef)(controlled !== void 0);
	const [valueState, setValue] = (0, import_react.useState)(defaultProp);
	const value = controlled !== void 0 ? controlled : valueState;
	const { current: defaultValue } = (0, import_react.useRef)(defaultProp);
	(0, import_react.useEffect)(() => {
		if (isControlled !== (controlled !== void 0)) console.error([
			`Salt: A component is changing the ${isControlled ? "" : "un"}controlled ${state} state of ${name} to be ${isControlled ? "un" : ""}controlled.`,
			"Elements should not switch from uncontrolled to controlled (or vice versa).",
			`Decide between using a controlled or uncontrolled ${name} element for the lifetime of the component.`,
			"The nature of the state is determined during the first render, it's considered controlled if the value is not `undefined`.",
			"More info: https://fb.me/react-controlled-components"
		].join("\n"));
	}, [
		state,
		name,
		controlled
	]);
	(0, import_react.useEffect)(() => {
		if (!isControlled && defaultValue !== defaultProp) console.error([`Salt: A component is changing the default ${state} state of an uncontrolled ${name} after being initialized. To suppress this warning opt to use a controlled ${name}.`].join("\n"));
	}, [JSON.stringify(defaultProp, ignoreReactElements)]);
	return [
		value,
		(0, import_react.useCallback)((newValue) => {
			if (!isControlled) setValue(newValue);
		}, [isControlled]),
		isControlled
	];
}
function ignoreReactElements(key, value) {
	return (0, import_react.isValidElement)(value) ? null : value;
}
//#endregion
//#region node_modules/@salt-ds/core/dist-es/accordion/Accordion.css.js
var css_248z$53 = ".saltAccordion {\n  border-top: var(--salt-size-border) var(--salt-separable-borderStyle) var(--salt-separable-tertiary-borderColor);\n}\n\n.saltAccordion-error {\n  border-top-color: var(--salt-status-error-borderColor);\n}\n\n.saltAccordion-warning {\n  border-top-color: var(--salt-status-warning-borderColor);\n}\n\n.saltAccordion-success {\n  border-top-color: var(--salt-status-success-borderColor);\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/accordion/Accordion.js
var withBaseName$56 = makePrefixer("saltAccordion");
var Accordion = (0, import_react.forwardRef)(function Accordion2(props, ref) {
	const { className, defaultExpanded, expanded: expandedProp, disabled, id: idProp, onToggle, status, value, ...rest } = props;
	const id = useId(idProp);
	useComponentCssInjection$1({
		testId: "salt-accordion",
		css: css_248z$53,
		window: useWindow()
	});
	const [expanded, setExpanded] = useControlled({
		controlled: expandedProp,
		default: Boolean(defaultExpanded),
		name: "Accordion",
		state: "expanded"
	});
	const toggle = (event) => {
		setExpanded((prev) => !prev);
		onToggle?.(event);
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AccordionContext.Provider, {
		value: {
			value,
			toggle,
			expanded,
			disabled: Boolean(disabled),
			id: id != null ? id : "",
			status
		},
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			ref,
			className: clsx(withBaseName$56(), { [withBaseName$56(status != null ? status : "")]: status }, className),
			...rest
		})
	});
});
//#endregion
//#region node_modules/@salt-ds/styles/dist-es/style-injection-provider/index.js
var StyleInjectionContext = (0, import_react.createContext)(true);
function useStyleInjection(enableStyleInjection) {
	const enableStyleInjectionFromContext = (0, import_react.useContext)(StyleInjectionContext);
	return enableStyleInjection ?? enableStyleInjectionFromContext;
}
//#endregion
//#region node_modules/@salt-ds/styles/dist-es/use-style-injection/InsertionPointProvider.js
var InsertionPointContext = (0, import_react.createContext)(null);
InsertionPointContext.displayName = "InsertionPointContext";
function useInsertionPoint() {
	return (0, import_react.useContext)(InsertionPointContext);
}
//#endregion
//#region node_modules/@salt-ds/styles/dist-es/use-style-injection/useStyleInjection.js
var maybeUseInsertionEffect = import_react["useInsertionEffect".toString()] ?? import_react.useLayoutEffect;
var windowSheetsMap = /* @__PURE__ */ new WeakMap();
function useComponentCssInjection({ testId, css, window: targetWindow }) {
	const styleInjectionEnabled = useStyleInjection();
	const insertionPoint = useInsertionPoint();
	maybeUseInsertionEffect(() => {
		if (!targetWindow || !styleInjectionEnabled) return;
		const sheetsMap = windowSheetsMap.get(targetWindow) ?? /* @__PURE__ */ new Map();
		const styleMap = sheetsMap.get(css) ?? {
			styleElement: null,
			count: 0
		};
		if (styleMap.styleElement == null) {
			styleMap.styleElement = targetWindow.document.createElement("style");
			styleMap.styleElement.setAttribute("type", "text/css");
			styleMap.styleElement.setAttribute("data-salt-style", testId || "");
			styleMap.styleElement.textContent = css;
			styleMap.count = 1;
			targetWindow.document.head.insertBefore(styleMap.styleElement, insertionPoint || targetWindow.document.head.firstChild);
		} else {
			styleMap.styleElement.textContent = css;
			styleMap.count++;
		}
		sheetsMap.set(css, styleMap);
		windowSheetsMap.set(targetWindow, sheetsMap);
		return () => {
			const sheetsMap2 = windowSheetsMap.get(targetWindow);
			const styleMap2 = sheetsMap2 == null ? void 0 : sheetsMap2.get(css);
			if (styleMap2 == null ? void 0 : styleMap2.styleElement) {
				styleMap2.count--;
				if (styleMap2.count < 1) {
					targetWindow.document.head.removeChild(styleMap2.styleElement);
					styleMap2.styleElement = null;
					sheetsMap2?.delete(css);
				}
			}
		};
	}, [
		testId,
		css,
		targetWindow
	]);
}
//#endregion
//#region node_modules/@salt-ds/icons/dist-es/icon/Icon.css.js
var css_248z$52 = "/* Style applied to the root element */\n.saltIcon {\n  --icon-color: currentColor;\n  --icon-size-multiplier: var(--saltIcon-size-multiplier, 1);\n  --icon-base-size: var(--salt-size-icon, 12px);\n  /**\n   * Icon size will be the multiplier (an integer from the size prop) * the base size (set by the theme per density)\n   * Icons should never be smaller than 12px for readability so we've added a max() to enforce this\n   * Consumers can use --saltIcon-size variable to override this rule as an escape hatch\n   */\n  --icon-size: var(--saltIcon-size, max(calc(var(--icon-base-size) * var(--icon-size-multiplier)), 12px));\n}\n\n.saltIcon {\n  fill: var(--saltIcon-color, var(--icon-color));\n  display: inline-block;\n  margin: var(--saltIcon-margin, 0);\n  position: relative;\n  width: var(--icon-size);\n  height: var(--icon-size);\n  min-width: var(--icon-size);\n  min-height: var(--icon-size);\n}\n\n.saltIcon-primary {\n  --icon-color: var(--salt-content-primary-foreground);\n}\n\n.saltIcon-secondary {\n  --icon-color: var(--salt-content-secondary-foreground);\n}\n";
//#endregion
//#region node_modules/@salt-ds/icons/dist-es/icon/Icon.js
var makePrefixer$1 = (prefix) => (...names) => [prefix, ...names].join("-");
var withBaseName$55 = makePrefixer$1("saltIcon");
var Icon = (0, import_react.forwardRef)(function Icon2({ "aria-hidden": ariaHidden, "aria-label": ariaLabel, children, className, color = "inherit", size = 1, style: styleProp, ...rest }, ref) {
	useComponentCssInjection({
		testId: "salt-icon",
		css: css_248z$52,
		window: useWindow()
	});
	const style = {
		...styleProp,
		"--saltIcon-size-multiplier": `${size}`
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("svg", {
		className: clsx(withBaseName$55(), { [withBaseName$55(color)]: color !== "inherit" }, className),
		style,
		role: ariaHidden ? void 0 : "img",
		"aria-label": ariaHidden ? void 0 : ariaLabel,
		"aria-hidden": ariaHidden,
		...rest,
		ref,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("g", {
			"aria-hidden": true,
			children
		})
	});
});
//#endregion
//#region node_modules/@salt-ds/icons/dist-es/components/ChevronDown.js
var ChevronDownIcon = (0, import_react.forwardRef)(function ChevronDownIcon2(props, ref) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Icon, {
		"data-testid": "ChevronDownIcon",
		"aria-label": "chevron down",
		viewBox: "0 0 12 12",
		ref,
		...props,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", { d: "M5.618 8.593 6 9l.382-.407L11 3.661 10.236 3 6 7.524 1.764 3 1 3.66z" })
	});
});
//#endregion
//#region node_modules/@salt-ds/icons/dist-es/components/ChevronLeft.js
var ChevronLeftIcon = (0, import_react.forwardRef)(function ChevronLeftIcon2(props, ref) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Icon, {
		"data-testid": "ChevronLeftIcon",
		"aria-label": "chevron left",
		viewBox: "0 0 12 12",
		ref,
		...props,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", { d: "M3.407 5.618 3 6l.407.382L8.339 11 9 10.236 4.476 6 9 1.764 8.34 1z" })
	});
});
//#endregion
//#region node_modules/@salt-ds/icons/dist-es/components/ChevronRight.js
var ChevronRightIcon = (0, import_react.forwardRef)(function ChevronRightIcon2(props, ref) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Icon, {
		"data-testid": "ChevronRightIcon",
		"aria-label": "chevron right",
		viewBox: "0 0 12 12",
		ref,
		...props,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", { d: "M8.593 5.618 9 6l-.407.382L3.661 11 3 10.236 7.524 6 3 1.764 3.66 1z" })
	});
});
//#endregion
//#region node_modules/@salt-ds/icons/dist-es/components/ErrorSolid.js
var ErrorSolidIcon = (0, import_react.forwardRef)(function ErrorSolidIcon2(props, ref) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Icon, {
		"data-testid": "ErrorSolidIcon",
		"aria-label": "error solid",
		viewBox: "0 0 12 12",
		ref,
		...props,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", {
			fillRule: "evenodd",
			d: "M9 0H3L0 3v6l3 3h6l3-3V3zM7 2H5v5h2zm-1 8a1 1 0 1 0 0-2 1 1 0 0 0 0 2",
			clipRule: "evenodd"
		})
	});
});
//#endregion
//#region node_modules/@salt-ds/icons/dist-es/components/InfoSolid.js
var InfoSolidIcon = (0, import_react.forwardRef)(function InfoSolidIcon2(props, ref) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Icon, {
		"data-testid": "InfoSolidIcon",
		"aria-label": "info solid",
		viewBox: "0 0 12 12",
		ref,
		...props,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", {
			fillRule: "evenodd",
			d: "M0 0h12v12H0zm6 2a1 1 0 1 1 0 2 1 1 0 0 1 0-2m1 3v5H5V5z",
			clipRule: "evenodd"
		})
	});
});
//#endregion
//#region node_modules/@salt-ds/icons/dist-es/components/Success.js
var SuccessIcon = (0, import_react.forwardRef)(function SuccessIcon2(props, ref) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Icon, {
		"data-testid": "SuccessIcon",
		"aria-label": "success",
		viewBox: "0 0 12 12",
		ref,
		...props,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", {
			fillRule: "evenodd",
			d: "m3.871 8.033 6.187-6.187 1.06 1.06-7.247 7.248-2.99-2.99 1.06-1.061z",
			clipRule: "evenodd"
		})
	});
});
//#endregion
//#region node_modules/@salt-ds/icons/dist-es/components/SuccessSmall.js
var SuccessSmallIcon = (0, import_react.forwardRef)(function SuccessSmallIcon2(props, ref) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Icon, {
		"data-testid": "SuccessSmallIcon",
		"aria-label": "success small",
		viewBox: "0 0 12 12",
		ref,
		...props,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", {
			fillRule: "evenodd",
			d: "m3.879 8.121 5.656-5.656.707.707L3.88 9.535 1.757 7.414l.707-.707z",
			clipRule: "evenodd"
		})
	});
});
//#endregion
//#region node_modules/@salt-ds/icons/dist-es/components/SuccessSmallSolid.js
var SuccessSmallSolidIcon = (0, import_react.forwardRef)(function SuccessSmallSolidIcon2(props, ref) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Icon, {
		"data-testid": "SuccessSmallSolidIcon",
		"aria-label": "success small solid",
		viewBox: "0 0 12 12",
		ref,
		...props,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", {
			fillRule: "evenodd",
			d: "M12 0H0v12h12zM9.535 2.465 3.88 8.12 2.465 6.707l-.707.707 2.12 2.121 6.365-6.363z",
			clipRule: "evenodd"
		})
	});
});
//#endregion
//#region node_modules/@salt-ds/icons/dist-es/components/SuccessSolid.js
var SuccessSolidIcon = (0, import_react.forwardRef)(function SuccessSolidIcon2(props, ref) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Icon, {
		"data-testid": "SuccessSolidIcon",
		"aria-label": "success solid",
		viewBox: "0 0 12 12",
		ref,
		...props,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", {
			fillRule: "evenodd",
			d: "M12 0H0v12h12zM9.858 1.846 3.672 8.033l-1.93-1.93-1.06 1.06 2.99 2.991 7.247-7.247z",
			clipRule: "evenodd"
		})
	});
});
//#endregion
//#region node_modules/@salt-ds/icons/dist-es/components/SuccessTick.js
var SuccessTickIcon = (0, import_react.forwardRef)(function SuccessTickIcon2(props, ref) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Icon, {
		"data-testid": "SuccessTickIcon",
		"aria-label": "success tick",
		viewBox: "0 0 12 12",
		ref,
		...props,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", {
			fillRule: "evenodd",
			d: "m3.871 8.033 6.187-6.187 1.061 1.06-7.248 7.248-2.99-2.99 1.06-1.061z",
			clipRule: "evenodd"
		})
	});
});
//#endregion
//#region node_modules/@salt-ds/icons/dist-es/components/TearOut.js
var TearOutIcon = (0, import_react.forwardRef)(function TearOutIcon2(props, ref) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Icon, {
		"data-testid": "TearOutIcon",
		"aria-label": "tear out",
		viewBox: "0 0 12 12",
		ref,
		...props,
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", { d: "M0 12h12V6h-1v5H1V1h5V0H0z" }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", { d: "M11.996 4V0h-4v1h2.293L5.996 5.293 6.703 6l4.293-4.293V4z" })]
	});
});
//#endregion
//#region node_modules/@salt-ds/icons/dist-es/components/Upload.js
var UploadIcon = (0, import_react.forwardRef)(function UploadIcon2(props, ref) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Icon, {
		"data-testid": "UploadIcon",
		"aria-label": "upload",
		viewBox: "0 0 12 12",
		ref,
		...props,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", { d: "M6.5 9.028h-1V1.914L2.818 4.596l-.707-.707L6 0l3.889 3.89-.707.706L6.5 1.914zM12 11v1H0v-1z" })
	});
});
//#endregion
//#region node_modules/@salt-ds/icons/dist-es/components/UserSolid.js
var UserSolidIcon = (0, import_react.forwardRef)(function UserSolidIcon2(props, ref) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Icon, {
		"data-testid": "UserSolidIcon",
		"aria-label": "user solid",
		viewBox: "0 0 12 12",
		ref,
		...props,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", { d: "M12 12v-.996q0-.68-.41-1.207a1.96 1.96 0 0 0-1.02-.715 4 4 0 0 0-.328-.117q-.176-.059-.422-.13a2 2 0 0 1-.14-.034 6 6 0 0 1-.223-.07l-.2-.047a1.4 1.4 0 0 0-.163-.059q-.785-.234-1.184-.54a1.26 1.26 0 0 1-.492-.784 4.5 4.5 0 0 0 .644-.867q.294-.504.493-1.079.21-.573.328-1.171Q9 3.574 9 3q0-.82-.234-1.395A2.4 2.4 0 0 0 7.172.164 4 4 0 0 0 6 0q-.621 0-1.172.164a2.4 2.4 0 0 0-1.594 1.441Q3 2.18 3 3q0 .574.117 1.184.117.597.328 1.171.2.575.493 1.079t.644.867q-.094.48-.492.785-.399.305-1.184.539-.082.024-.164.059l-.199.046a6 6 0 0 1-.223.07 2 2 0 0 1-.14.036q-.246.07-.422.129a4 4 0 0 0-.328.117 1.96 1.96 0 0 0-1.02.715Q0 10.324 0 11.004V12z" })
	});
});
//#endregion
//#region node_modules/@salt-ds/icons/dist-es/components/WarningSolid.js
var WarningSolidIcon = (0, import_react.forwardRef)(function WarningSolidIcon2(props, ref) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Icon, {
		"data-testid": "WarningSolidIcon",
		"aria-label": "warning solid",
		viewBox: "0 0 12 12",
		ref,
		...props,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", {
			fillRule: "evenodd",
			d: "m6 0 6 12H0zM5 5h2v3H5zm2 4.5a1 1 0 1 1-2 0 1 1 0 0 1 2 0",
			clipRule: "evenodd"
		})
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/status-indicator/StatusIndicator.css.js
var css_248z$51 = ".saltStatusIndicator {\n  --statusIndicator-warning-color: var(--salt-status-warning-foreground);\n  --statusIndicator-info-color: var(--salt-status-info-foreground);\n  --statusIndicator-success-color: var(--salt-status-success-foreground);\n  --statusIndicator-error-color: var(--salt-status-error-foreground);\n  color: var(--saltStatusIndicator-color, var(--statusIndicator-Color));\n}\n\n.saltStatusIndicator-error {\n  --statusIndicator-Color: var(--statusIndicator-error-color);\n}\n\n.saltStatusIndicator-warning {\n  --statusIndicator-Color: var(--statusIndicator-warning-color);\n}\n\n.saltStatusIndicator-success {\n  --statusIndicator-Color: var(--statusIndicator-success-color);\n}\n\n.saltStatusIndicator-info {\n  --statusIndicator-Color: var(--statusIndicator-info-color);\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/status-indicator/StatusIndicator.js
var icons$1 = {
	error: ErrorSolidIcon,
	success: SuccessTickIcon,
	warning: WarningSolidIcon,
	info: InfoSolidIcon
};
var statusToAriaLabelMap$1 = {
	error: "error",
	success: "success",
	warning: "warning",
	info: "info"
};
var withBaseName$54 = makePrefixer("saltStatusIndicator");
var StatusIndicator = (0, import_react.forwardRef)(function StatusIndicator2({ className, status, size = 1, ...restProps }, ref) {
	useComponentCssInjection$1({
		testId: "salt-status-indicator",
		css: css_248z$51,
		window: useWindow()
	});
	const IconComponent = icons$1[status];
	const ariaLabel = statusToAriaLabelMap$1[status];
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(IconComponent, {
		className: clsx(withBaseName$54(), withBaseName$54(status), className),
		size,
		"aria-label": ariaLabel,
		...restProps,
		ref
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/accordion/AccordionHeader.css.js
var css_248z$50 = ".saltAccordionHeader {\n  display: flex;\n  gap: var(--salt-spacing-100);\n  padding: var(--salt-spacing-50) var(--salt-spacing-100);\n  align-items: center;\n  border: 0;\n  width: 100%;\n  min-height: calc(var(--salt-size-base) + var(--salt-spacing-100));\n  background: var(--salt-actionable-secondary-background);\n  font-size: var(--salt-text-fontSize);\n  line-height: var(--salt-text-lineHeight);\n  font-family: var(--salt-text-fontFamily);\n  color: var(--salt-content-primary-foreground);\n  font-weight: var(--salt-text-fontWeight-strong);\n  text-align: left;\n  cursor: var(--salt-actionable-cursor-hover);\n}\n\n.saltAccordionHeader:focus-visible {\n  outline: var(--salt-focused-outline);\n  outline-offset: calc(-1 * var(--salt-focused-outlineWidth));\n}\n\n.saltAccordionHeader:disabled {\n  background: var(--salt-actionable-secondary-background);\n  color: var(--salt-content-primary-foreground-disabled);\n  cursor: var(--salt-actionable-cursor-disabled);\n}\n\n.saltAccordionHeader-icon {\n  transition: transform var(--salt-duration-perceptible) ease-in-out;\n}\n\n.saltAccordionHeader[aria-expanded=\"true\"] > .saltAccordionHeader-icon {\n  transform: rotate(90deg);\n}\n\n.saltAccordionHeader-error {\n  background: var(--salt-status-error-background);\n}\n\n.saltAccordionHeader-error:focus-visible {\n  outline-color: var(--salt-status-error-borderColor);\n}\n\n.saltAccordionHeader-warning {\n  background: var(--salt-status-warning-background);\n}\n\n.saltAccordionHeader-warning:focus-visible {\n  outline-color: var(--salt-status-warning-borderColor);\n}\n\n.saltAccordionHeader-success {\n  background: var(--salt-status-success-background);\n}\n\n.saltAccordionHeader-success:focus-visible {\n  outline-color: var(--salt-status-error-borderColor);\n}\n\n.saltAccordionHeader .saltAccordionHeader-statusIndicator {\n  margin-left: auto;\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/accordion/AccordionHeader.js
var withBaseName$53 = makePrefixer("saltAccordionHeader");
var AccordionHeader = (0, import_react.forwardRef)(function AccordionHeader2(props, ref) {
	const { children, className, onClick, ...rest } = props;
	const { value, expanded, toggle, disabled, id, status } = useAccordion();
	useComponentCssInjection$1({
		testId: "salt-accordion-header",
		css: css_248z$50,
		window: useWindow()
	});
	const handleClick = (event) => {
		toggle(event);
		onClick?.(event);
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("button", {
		ref,
		className: clsx(withBaseName$53(), { [withBaseName$53(status != null ? status : "")]: status }, className),
		disabled,
		onClick: handleClick,
		"aria-expanded": expanded,
		id: `${id}-header`,
		"aria-controls": `${id}-panel`,
		value,
		...rest,
		children: [
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChevronRightIcon, {
				"aria-hidden": "true",
				className: withBaseName$53("icon")
			}),
			children,
			status && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(StatusIndicator, {
				className: withBaseName$53("statusIndicator"),
				status
			})
		]
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/utils/debounce.js
function debounce(func, wait = 166, leading = false) {
	let timeout;
	const debounced = function debounced2(...args) {
		const later = () => {
			func.apply(this, args);
		};
		clearTimeout(timeout);
		if (leading) later();
		timeout = window.setTimeout(later, wait);
	};
	debounced.clear = () => {
		clearTimeout(timeout);
	};
	return debounced;
}
//#endregion
//#region node_modules/@salt-ds/core/dist-es/aria-announcer/useAriaAnnouncer.js
var warnedOnce = false;
var useAriaAnnouncer = ({ debounce: debounceInterval = 0 } = {}) => {
	const context = (0, import_react.useContext)(AriaAnnouncerContext);
	const mountedRef = (0, import_react.useRef)(true);
	const baseAnnounce = (0, import_react.useCallback)((announcement, delay) => {
		const isReactAnnouncerInstalled = context && context.announce;
		if (isReactAnnouncerInstalled && warnedOnce) {
			console.warn("useAriaAnnouncer is being used without an AriaAnnouncerProvider. Your application should be wrapped in an AriaAnnouncerProvider");
			warnedOnce = true;
		}
		function makeAnnouncement() {
			if (mountedRef.current) {
				if (isReactAnnouncerInstalled) context.announce(announcement);
			}
		}
		if (delay) setTimeout(makeAnnouncement, delay);
		else makeAnnouncement();
	}, [context]);
	const announce = (0, import_react.useMemo)(() => debounceInterval > 0 ? debounce(baseAnnounce, debounceInterval) : baseAnnounce, [baseAnnounce, debounceInterval]);
	const ariaAnnouncer = (0, import_react.useMemo)(() => ({
		...context,
		announce
	}), [context, announce]);
	(0, import_react.useEffect)(() => {
		mountedRef.current = true;
		return () => {
			mountedRef.current = false;
		};
	}, []);
	return ariaAnnouncer;
};
//#endregion
//#region node_modules/@salt-ds/core/dist-es/aria-announcer/AriaAnnounce.js
var AriaAnnounce = ({ announcement }) => {
	const { announce } = useAriaAnnouncer();
	(0, import_react.useEffect)(() => {
		if (announcement) announce(announcement);
	}, [announce, announcement]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, {});
};
//#endregion
//#region node_modules/@salt-ds/core/dist-es/avatar/useAvatarImage.js
function useAvatarImage({ src }) {
	const [status, setStatus] = (0, import_react.useState)("loading");
	(0, import_react.useEffect)(() => {
		setStatus(src ? "loading" : "pending");
	}, [src]);
	useIsomorphicLayoutEffect(() => {
		if (!src) return;
		let active = true;
		const image = new Image();
		image.src = src;
		const onLoad = () => active && setStatus("loaded");
		const onError = () => active && setStatus("error");
		image.addEventListener("load", onLoad, { once: true });
		image.addEventListener("error", onError, { once: true });
		return () => {
			image.removeEventListener("load", onLoad);
			image.removeEventListener("load", onError);
			active = false;
		};
	}, [src]);
	return status;
}
//#endregion
//#region node_modules/@salt-ds/core/dist-es/avatar/Avatar.css.js
var css_248z$49 = "/* Default variables applied to the root element */\n.saltAvatar {\n  --avatar-foreground: var(--saltAvatar-foreground, var(--salt-accent-foreground));\n  --avatar-size-multiplier: var(--saltAvatar-size-multiplier, 1);\n  --avatar-base-size: var(--salt-size-base, 12px);\n  --avatar-base-fontSize: var(--saltAvatar-fontSize, var(--salt-text-notation-fontSize));\n  --avatar-container-size: calc(var(--avatar-base-size) * var(--avatar-size-multiplier));\n  --avatar-fontSize: calc(var(--avatar-base-fontSize) * var(--avatar-size-multiplier));\n  /* Icon styling */\n  --saltIcon-size: calc(var(--avatar-container-size) / 2);\n}\n\n/* Style applied to the root element */\n.saltAvatar {\n  line-height: var(--saltAvatar-lineHeight, var(--salt-text-notation-lineHeight));\n  display: flex;\n  justify-content: center;\n  align-items: center;\n  background: var(--saltAvatar-background, var(--salt-accent-background));\n  color: var(--avatar-foreground);\n  font-size: var(--avatar-fontSize);\n  font-family: var(--salt-text-fontFamily);\n  font-weight: var(--salt-text-fontWeight);\n  width: var(--avatar-container-size);\n  min-width: var(--avatar-container-size);\n  height: var(--avatar-container-size);\n  min-height: var(--avatar-container-size);\n  overflow: hidden;\n  user-select: none;\n  border-radius: 50%;\n}\n.saltAvatar:has(img),\n.saltAvatar-withImage {\n  background: none;\n}\n/* Style applied to the image/svg element. We specifically allow SVGs as the Avatar image to enable SVGs such as Flags to take up the full Avatar space */\n.saltAvatar > img,\n.saltAvatar > svg:not(.saltIcon) {\n  width: 100%;\n  height: 100%;\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/avatar/Avatar.js
var withBaseName$52 = makePrefixer("saltAvatar");
var DEFAULT_AVATAR_SIZE = 2;
var defaultNameToInitials = (name) => name == null ? void 0 : name.split(" ").slice(0, 2).map((n) => n[0]).join("").toUpperCase();
var Avatar = (0, import_react.forwardRef)(function Avatar2({ className, children: childrenProp, name, nameToInitials = defaultNameToInitials, src, size = DEFAULT_AVATAR_SIZE, style: styleProp, fallbackIcon = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(UserSolidIcon, { "aria-label": "User Avatar" }), ...rest }, ref) {
	useComponentCssInjection$1({
		testId: "salt-avatar",
		css: css_248z$49,
		window: useWindow()
	});
	let children;
	const style = {
		...styleProp,
		"--saltAvatar-size-multiplier": `${size}`
	};
	const hasImgNotFailing = useAvatarImage({ src }) === "loaded";
	if (hasImgNotFailing) children = /* @__PURE__ */ (0, import_jsx_runtime.jsx)("img", {
		alt: name,
		src
	});
	else if (childrenProp != null) children = childrenProp;
	const avatarInitials = nameToInitials(name);
	const initialsProps = avatarInitials ? {
		role: "img",
		"aria-label": name
	} : {};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		ref,
		style,
		className: clsx(withBaseName$52(), { [withBaseName$52("withImage")]: hasImgNotFailing }, className),
		...initialsProps,
		...rest,
		children: children || avatarInitials || fallbackIcon
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/badge/Badge.css.js
var css_248z$48 = "/* Style applied to the root element */\n.saltBadge {\n  display: inline-flex;\n  position: relative;\n  flex-shrink: 0;\n  vertical-align: middle;\n}\n\n.saltBadge-badge {\n  /* Should this vary according to touch size */\n  padding-left: var(--salt-spacing-50);\n  padding-right: var(--salt-spacing-50);\n  height: var(--salt-text-notation-lineHeight);\n  min-width: var(--salt-text-notation-lineHeight);\n  border-radius: 9999px;\n  white-space: nowrap;\n  z-index: var(--salt-zIndex-default);\n\n  align-items: center;\n  justify-content: center;\n  display: inline-flex;\n  margin: auto;\n\n  font-size: var(--salt-text-notation-fontSize);\n  font-weight: var(--salt-text-notation-fontWeight);\n  font-family: var(--salt-text-fontFamily);\n  line-height: var(--salt-text-notation-lineHeight);\n  background: var(--salt-accent-background);\n  color: var(--salt-accent-foreground);\n\n  -webkit-font-smoothing: antialiased;\n  -moz-osx-font-smoothing: grayscale;\n}\n\n.saltBadge-topRight {\n  position: absolute;\n  right: var(--salt-spacing-100);\n  transform: translateX(100%);\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/badge/Badge.js
var withBaseName$51 = makePrefixer("saltBadge");
var Badge = (0, import_react.forwardRef)(function Badge2({ value, max = 999, className, children, ...rest }, ref) {
	useComponentCssInjection$1({
		testId: "salt-badge",
		css: css_248z$48,
		window: useWindow()
	});
	const valueText = typeof value === "number" && value > max ? `${max}+` : value;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", {
		className: clsx(withBaseName$51(), className),
		ref,
		...rest,
		children: [children, /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
			className: clsx(withBaseName$51("badge"), { [withBaseName$51("topRight")]: children }),
			children: valueText
		})]
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/banner/Banner.css.js
var css_248z$47 = "/* Styles applied to root component */\n.saltBanner {\n  --banner-background: var(--salt-container-primary-background);\n\n  background: var(--saltBanner-background, var(--banner-background));\n  border-color: var(--saltBanner-borderColor, var(--banner-borderColor));\n  border-width: var(--saltBanner-borderWidth, var(--salt-size-border));\n  border-style: var(--saltBanner-borderStyle, var(--salt-container-borderStyle));\n  box-sizing: border-box;\n  display: inline-flex;\n  gap: var(--salt-spacing-75);\n  padding: var(--saltBanner-padding, var(--salt-spacing-50) var(--salt-spacing-100));\n  width: 100%;\n  min-height: calc(var(--salt-size-base) + var(--salt-spacing-100));\n\n  font-family: var(--salt-text-fontFamily);\n  font-size: var(--salt-text-fontSize);\n  font-weight: var(--salt-text-fontWeight);\n  line-height: var(--salt-text-lineHeight);\n}\n\n/* Styles applied to icon */\n.saltBanner-icon.saltIcon {\n  min-height: var(--salt-size-base);\n}\n\n/* Styles applied when state = \"info\" */\n.saltBanner-info {\n  --banner-borderColor: var(--salt-status-info-borderColor);\n}\n\n/* Styles applied when state = \"error\" */\n.saltBanner-error {\n  --banner-borderColor: var(--salt-status-error-borderColor);\n}\n\n/* Styles applied when state = \"warning\" */\n.saltBanner-warning {\n  --banner-borderColor: var(--salt-status-warning-borderColor);\n}\n\n/* Styles applied when state = \"success\" */\n.saltBanner-success {\n  --banner-borderColor: var(--salt-status-success-borderColor);\n}\n\n.saltBanner-secondary.saltBanner-info {\n  --banner-background: var(--salt-status-info-background);\n}\n\n.saltBanner-secondary.saltBanner-error {\n  --banner-background: var(--salt-status-error-background);\n}\n\n.saltBanner-secondary.saltBanner-warning {\n  --banner-background: var(--salt-status-warning-background);\n}\n\n.saltBanner-secondary.saltBanner-success {\n  --banner-background: var(--salt-status-success-background);\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/banner/Banner.js
var withBaseName$50 = makePrefixer("saltBanner");
var Banner = (0, import_react.forwardRef)(function Banner2({ children, className, variant = "primary", status = "info", ...rest }, ref) {
	useComponentCssInjection$1({
		testId: "salt-banner",
		css: css_248z$47,
		window: useWindow()
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		className: clsx(withBaseName$50(), withBaseName$50(status), withBaseName$50(variant), className),
		ref,
		...rest,
		"aria-live": "polite",
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(StatusIndicator, {
			status,
			className: withBaseName$50("icon")
		}), children]
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/banner/BannerActions.css.js
var css_248z$46 = ".saltBannerActions {\n  margin-bottom: auto;\n  display: flex;\n  gap: var(--salt-spacing-100);\n  align-items: center;\n  min-height: var(--salt-size-base);\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/banner/BannerActions.js
var withBaseName$49 = makePrefixer("saltBannerActions");
var BannerActions = (0, import_react.forwardRef)(function BannerActions2(props, ref) {
	const { className, ...rest } = props;
	useComponentCssInjection$1({
		testId: "salt-banner-actions",
		css: css_248z$46,
		window: useWindow()
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		className: clsx(withBaseName$49(), className),
		...rest,
		ref
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/banner/BannerContent.css.js
var css_248z$45 = "/* Styles applied to BannerContent */\n.saltBannerContent {\n  flex: 1 0;\n  margin: var(--salt-spacing-75) 0;\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/banner/BannerContent.js
var withBaseName$48 = makePrefixer("saltBannerContent");
var BannerContent = (0, import_react.forwardRef)(function BannerContent2(props, ref) {
	const { className, ...rest } = props;
	useComponentCssInjection$1({
		testId: "salt-banner-content",
		css: css_248z$45,
		window: useWindow()
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		className: clsx(withBaseName$48(), className),
		...rest,
		ref
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/utils/useResponsiveProp.js
var getCurrentBreakpoint = (breakpoints, width) => {
	const breakpointList = Object.entries(breakpoints).sort(([, a], [, b]) => a - b);
	const [currentBreakpoint] = breakpointList.reduce((acc, val) => {
		const [, accWidth] = acc;
		const [breakpoint, breakpointWidth] = val;
		if (breakpointWidth < width && breakpointWidth > accWidth) return [breakpoint, breakpointWidth];
		return [...acc];
	}, breakpointList[0]);
	return currentBreakpoint;
};
var useCurrentBreakpoint = () => {
	const viewport = useViewport();
	return getCurrentBreakpoint(useBreakpoints(), viewport);
};
var useOrderedBreakpoints = () => {
	const breakpoints = useBreakpoints();
	return Object.entries(breakpoints).sort(([, a], [, b]) => a - b).map(([key]) => key);
};
var isObject = (value) => {
	const type = typeof value;
	return value !== null && (type === "object" || type === "function");
};
var hasBreakpointValues = (value, breakpoints) => {
	return isObject(value) && Object.keys(value).every((key) => key in breakpoints);
};
var getResponsiveValue = (breakpointValues, breakpoints, viewport, defaultValue) => {
	return Object.entries(breakpointValues).reduce((acc, val) => {
		const [accWidth] = acc;
		const [breakpoint, breakpointValue] = val;
		const breakpointWidth = breakpoints[breakpoint];
		if (breakpointWidth >= accWidth && breakpointWidth <= breakpoints[viewport]) return [breakpointWidth, breakpointValue];
		return acc;
	}, [0, defaultValue])[1];
};
var useResponsiveProp = (value, defaultValue) => {
	const breakpoints = useBreakpoints();
	const viewport = useViewport();
	if (value === defaultValue) return defaultValue;
	const currentViewport = getCurrentBreakpoint(breakpoints, viewport);
	if (hasBreakpointValues(value, breakpoints)) return getResponsiveValue(value, breakpoints, currentViewport, defaultValue);
	return value;
};
//#endregion
//#region node_modules/@salt-ds/core/dist-es/grid-item/GridItem.css.js
var css_248z$44 = "/* Style applied to the root element */\n.saltGridItem {\n  justify-self: var(--gridItem-justifySelf);\n  align-self: var(--gridItem-alignSelf);\n  grid-row-start: var(--gridItem-gridRowStart);\n  grid-column-start: var(--gridItem-gridColumnStart);\n  grid-row-end: var(--gridItem-gridRowEnd);\n  grid-column-end: var(--gridItem-gridColumnEnd);\n}\n\n.saltGridItem-area {\n  grid-area: var(--gridItem-gridArea);\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/grid-item/GridItem.js
var GRID_ALIGNMENT_BASE = [
	"start",
	"end",
	"center",
	"stretch"
];
var withBaseName$47 = makePrefixer("saltGridItem");
var colStart = "auto";
var colEnd = "auto";
var rowStart = "auto";
var rowEnd = "auto";
var GridItem = (0, import_react.forwardRef)(({ as, children, className, colSpan, rowSpan, horizontalAlignment = "stretch", verticalAlignment = "stretch", style, ...rest }, ref) => {
	useComponentCssInjection$1({
		testId: "salt-grid-item",
		css: css_248z$44,
		window: useWindow()
	});
	const Component = as || "div";
	const gridItemColSpan = useResponsiveProp(colSpan, "auto");
	const gridItemRowSpan = useResponsiveProp(rowSpan, "auto");
	const gridColumnStart = gridItemColSpan ? `span ${gridItemColSpan}` : colStart;
	const gridColumnEnd = gridItemColSpan ? `span ${gridItemColSpan}` : colEnd;
	const gridRowStart = gridItemRowSpan ? `span ${gridItemRowSpan}` : rowStart;
	const gridRowEnd = gridItemRowSpan ? `span ${gridItemRowSpan}` : rowEnd;
	const gridStyles = {
		...style,
		"--gridItem-justifySelf": horizontalAlignment,
		"--gridItem-alignSelf": verticalAlignment,
		"--gridItem-gridRowStart": gridRowStart,
		"--gridItem-gridColumnStart": gridColumnStart,
		"--gridItem-gridRowEnd": gridRowEnd,
		"--gridItem-gridColumnEnd": gridColumnEnd
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Component, {
		className: clsx(withBaseName$47(), className),
		style: gridStyles,
		ref,
		...rest,
		children
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/border-item/BorderItem.css.js
var css_248z$43 = "/* Style applied to BorderItem when `sticky` positioning is on */\n.saltBorderItem-sticky {\n  position: sticky;\n  overflow: auto;\n  top: 0;\n  left: 0;\n  right: 0;\n  bottom: 0;\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/border-item/BorderItem.js
var BORDER_POSITION = [
	"north",
	"west",
	"center",
	"east",
	"south"
];
var withBaseName$46 = makePrefixer("saltBorderItem");
var BorderItem = (0, import_react.forwardRef)(({ children, className, position, sticky = false, style, ...rest }, ref) => {
	useComponentCssInjection$1({
		testId: "salt-border-item",
		css: css_248z$43,
		window: useWindow()
	});
	const gridItemStyles = {
		...style,
		"--gridItem-gridArea": position
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(GridItem, {
		ref,
		className: clsx(withBaseName$46(), "saltGridItem-area", { [withBaseName$46("sticky")]: sticky }, className),
		style: gridItemStyles,
		...rest,
		children
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/grid-layout/GridLayout.css.js
var css_248z$42 = ".salt-density-touch {\n  --gridLayout-gap-density-multiplier: 2;\n}\n\n/* Default variables applied to the root element */\n.saltGridLayout {\n  --gridLayout-space: 1fr;\n  --gridLayout-rowGap: var(--gridLayout-gap-density-multiplier, 3);\n  --gridLayout-columnGap: var(--gridLayout-gap-density-multiplier, 3);\n}\n\n/* Style applied to the root element */\n.saltGridLayout {\n  display: grid;\n  column-gap: calc(var(--salt-size-unit) * var(--gridLayout-columnGap));\n  row-gap: calc(var(--salt-size-unit) * var(--gridLayout-rowGap));\n  grid-template-columns: repeat(var(--gridLayout-columns), var(--gridLayout-space));\n  grid-template-rows: repeat(var(--gridLayout-rows), var(--gridLayout-space));\n  grid-auto-columns: auto;\n  grid-auto-rows: auto;\n}\n\n.saltGridLayout-area {\n  grid-template: var(--gridLayout-gridTemplate);\n  grid-template-columns: min-content 1fr min-content;\n  grid-template-rows: min-content 1fr min-content;\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/grid-layout/GridLayout.js
var withBaseName$45 = makePrefixer("saltGridLayout");
var GridLayout = (0, import_react.forwardRef)(({ as, children, className, columns = 12, rows = 1, gap, columnGap, rowGap, style, ...rest }, ref) => {
	useComponentCssInjection$1({
		testId: "salt-grid-layout",
		css: css_248z$42,
		window: useWindow()
	});
	const Component = as || "div";
	const gridColumns = useResponsiveProp(columns, 12);
	const gridRows = useResponsiveProp(rows, 1);
	const gridGap = useResponsiveProp(gap, 3);
	const gridColumnGap = useResponsiveProp(columnGap, 3);
	const gridRowGap = useResponsiveProp(rowGap, 3);
	const gridLayoutStyles = {
		...style,
		"--gridLayout-columns": gridColumns,
		"--gridLayout-rows": gridRows,
		"--gridLayout-columnGap": gridColumnGap != null ? gridColumnGap : gridGap,
		"--gridLayout-rowGap": gridRowGap != null ? gridRowGap : gridGap
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Component, {
		className: clsx(withBaseName$45(), className),
		style: gridLayoutStyles,
		ref,
		...rest,
		children
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/border-layout/BorderLayout.js
var withBaseName$44 = makePrefixer("saltBorderLayout");
var numberOfColumns = 3;
var BorderLayout = (0, import_react.forwardRef)(({ children, className, gap, style, ...rest }, ref) => {
	const borderAreas = import_react.Children.map(children, (child) => child.props.position);
	const gridTemplateAreas = `"${borderAreas.includes("north") ? "north ".repeat(numberOfColumns) : "none ".repeat(numberOfColumns)}" "${`${borderAreas.includes("west") ? "west" : "center"} center ${borderAreas.includes("east") ? "east" : "center"}`}" "${borderAreas.includes("south") ? "south ".repeat(numberOfColumns) : "none ".repeat(numberOfColumns)}"`;
	const hasMainSection = borderAreas.includes("center");
	(0, import_react.useEffect)(() => {
		if (!hasMainSection) console.warn("No main section has been found. A main section should be provided.");
	}, [hasMainSection]);
	const borderLayoutStyles = {
		...style,
		"--gridLayout-gridTemplate": gridTemplateAreas
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(GridLayout, {
		className: clsx(withBaseName$44(), className, "saltGridLayout-area"),
		columns: numberOfColumns,
		gap: gap || 0,
		style: borderLayoutStyles,
		ref,
		...rest,
		children
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/button/Button.css.js
var css_248z$41 = "/* Styles applied to the root element if variant=\"cta\" */\n.saltButton-cta {\n  --button-background: var(--salt-actionable-cta-background);\n  --button-background-active: var(--salt-actionable-cta-background-active);\n  --button-background-disabled: var(--salt-actionable-cta-background-disabled);\n  --button-background-hover: var(--salt-actionable-cta-background-hover);\n  --button-text-color: var(--salt-actionable-cta-foreground);\n  --button-text-color-active: var(--salt-actionable-cta-foreground-active);\n  --button-text-color-disabled: var(--salt-actionable-cta-foreground-disabled);\n  --button-text-color-hover: var(--salt-actionable-cta-foreground-hover);\n}\n\n/* Styles applied to the root element if variant=\"primary\" (or no variant specified) */\n.saltButton-primary {\n  --button-background: var(--salt-actionable-primary-background);\n  --button-background-active: var(--salt-actionable-primary-background-active);\n  --button-background-disabled: var(--salt-actionable-primary-background-disabled);\n  --button-background-hover: var(--salt-actionable-primary-background-hover);\n  --button-text-color: var(--salt-actionable-primary-foreground);\n  --button-text-color-active: var(--salt-actionable-primary-foreground-active);\n  --button-text-color-disabled: var(--salt-actionable-primary-foreground-disabled);\n  --button-text-color-hover: var(--salt-actionable-primary-foreground-hover);\n}\n\n/* Styles applied to the root element if variant=\"secondary\" */\n.saltButton-secondary {\n  --button-background: var(--salt-actionable-secondary-background);\n  --button-background-active: var(--salt-actionable-secondary-background-active);\n  --button-background-disabled: var(--salt-actionable-secondary-background-disabled);\n  --button-background-hover: var(--salt-actionable-secondary-background-hover);\n  --button-text-color: var(--salt-actionable-secondary-foreground);\n  --button-text-color-active: var(--salt-actionable-secondary-foreground-active);\n  --button-text-color-disabled: var(--salt-actionable-secondary-foreground-disabled);\n  --button-text-color-hover: var(--salt-actionable-secondary-foreground-hover);\n}\n\n.saltButton {\n  align-items: var(--saltButton-alignItems, center);\n  appearance: none;\n  background: var(--saltButton-background, var(--button-background));\n  border-color: var(--saltButton-borderColor, transparent);\n  border-style: var(--saltButton-borderStyle, solid);\n  border-width: var(--saltButton-borderWidth, 0);\n  border-radius: var(--saltButton-borderRadius, 0);\n  color: var(--saltButton-text-color, var(--button-text-color));\n  cursor: var(--saltButton-cursor, pointer);\n  display: inline-flex;\n  gap: var(--salt-spacing-50);\n  justify-content: var(--saltButton-justifyContent, center);\n  font-size: var(--saltButton-fontSize, var(--salt-text-fontSize));\n  font-family: var(--saltButton-fontFamily, var(--salt-text-fontFamily));\n  line-height: var(--saltButton-lineHeight, var(--salt-text-lineHeight));\n  letter-spacing: var(--saltButton-letterSpacing, var(--salt-text-action-letterSpacing));\n  text-transform: var(--saltButton-textTransform, var(--salt-text-action-textTransform));\n  padding: 0 var(--saltButton-padding, var(--salt-size-unit));\n  margin: var(--saltButton-margin, 0);\n  height: var(--saltButton-height, var(--salt-size-base));\n  min-width: var(--saltButton-minWidth, unset);\n  position: relative;\n  text-align: var(--saltButton-textAlign, var(--salt-text-action-textAlign));\n  text-decoration: none;\n  transition: none;\n  width: var(--saltButton-width, auto);\n  -webkit-appearance: none;\n  -webkit-tap-highlight-color: transparent;\n  /* Styles applied to align children*/\n  font-weight: var(--saltButton-fontWeight, var(--salt-text-action-fontWeight));\n}\n\n/* Pseudo-class applied to the root element on focus */\n.saltButton:focus-visible {\n  outline-style: var(--salt-focused-outlineStyle);\n  outline-width: var(--salt-focused-outlineWidth);\n  outline-color: var(--salt-focused-outlineColor);\n  outline-offset: var(--salt-focused-outlineOffset);\n  background: var(--saltButton-background-hover, var(--button-background-hover));\n  color: var(--saltButton-text-color-hover, var(--button-text-color-hover));\n}\n\n/* Pseudo-class applied to the root element on focus when Button is active */\n.saltButton.saltButton-active:focus-visible,\n.saltButton:focus-visible(:active) {\n  background: var(--saltButton-background-active-hover, var(--button-background));\n  color: var(--saltButton-text-color-active-hover, var(--button-text-color));\n}\n\n/* Pseudo-class applied to the root element on hover when Button is not active or disabled */\n.saltButton:hover {\n  background: var(--saltButton-background-hover, var(--button-background-hover));\n  color: var(--saltButton-text-color-hover, var(--button-text-color-hover));\n}\n\n/* Pseudo-class applied to the root element when Button is active and not disabled */\n.saltButton:active,\n.saltButton.saltButton-active {\n  background: var(--saltButton-background-active, var(--button-background-active));\n  color: var(--saltButton-text-color-active, var(--button-text-color-active));\n}\n\n/* Pseudo-class applied to the root element if disabled={true} */\n.saltButton:disabled,\n.saltButton-disabled,\n  /* Overrides to apply the disabled style when the button is focusable while also disabled */\n.saltButton-disabled:active,\n.saltButton-disabled:focus-visible,\n.saltButton-disabled:hover {\n  background: var(--saltButton-background-disabled, var(--button-background-disabled));\n  color: var(--saltButton-text-color-disabled, var(--button-text-color-disabled));\n  cursor: var(--saltButton-cursor-disabled, var(--salt-actionable-cursor-disabled));\n}\n\n.saltButton[href] {\n  display: inline-flex;\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/button/useButton.js
var useButton = ({ disabled, focusableWhenDisabled, onKeyUp, onKeyDown, onClick, onBlur }) => {
	const [keyIsDown, setkeyIsDown] = (0, import_react.useState)("");
	const [active, setActive] = (0, import_react.useState)(false);
	const enter = "Enter";
	const space = " ";
	(0, import_react.useEffect)(() => {
		const t = setTimeout(() => {
			if (keyIsDown !== enter && keyIsDown !== space) setActive(false);
		}, 0);
		return () => {
			clearTimeout(t);
		};
	}, [active, keyIsDown]);
	const handleKeyUp = (event) => {
		setkeyIsDown("");
		setActive(false);
		onKeyUp?.(event);
	};
	const handleClick = (event) => {
		setActive(true);
		onClick?.(event);
	};
	const handleBlur = (event) => {
		setActive(false);
		onBlur?.(event);
	};
	const handleKeyDown = (event) => {
		if (event.key === enter || event.key === space) {
			setkeyIsDown(event.key);
			setActive(true);
		}
		onKeyDown?.(event);
	};
	return {
		active,
		buttonProps: {
			"aria-disabled": disabled && focusableWhenDisabled ? true : void 0,
			disabled: disabled && !focusableWhenDisabled,
			tabIndex: disabled && !focusableWhenDisabled ? -1 : 0,
			onBlur: handleBlur,
			onClick: !disabled ? handleClick : void 0,
			onKeyDown: handleKeyDown,
			onKeyUp: handleKeyUp
		}
	};
};
//#endregion
//#region node_modules/@salt-ds/core/dist-es/button/Button.js
var withBaseName$43 = makePrefixer("saltButton");
var ButtonVariantValues = [
	"primary",
	"secondary",
	"cta"
];
var Button = (0, import_react.forwardRef)(function Button2({ children, className, disabled, focusableWhenDisabled, onKeyUp, onKeyDown, onBlur, onClick, type = "button", variant = "primary", ...restProps }, ref) {
	const { active, buttonProps } = useButton({
		disabled,
		focusableWhenDisabled,
		onKeyUp,
		onKeyDown,
		onBlur,
		onClick
	});
	useComponentCssInjection$1({
		testId: "salt-button",
		css: css_248z$41,
		window: useWindow()
	});
	const { tabIndex, ...restButtonProps } = buttonProps;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
		...restButtonProps,
		className: clsx(withBaseName$43(), withBaseName$43(variant), {
			[withBaseName$43("disabled")]: disabled,
			[withBaseName$43("active")]: active
		}, className),
		...restProps,
		ref,
		type,
		children
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/utils/capitalize.js
function capitalize(value) {
	return value.length > 0 ? value[0].toUpperCase() + value.slice(1) : value;
}
//#endregion
//#region node_modules/@salt-ds/core/dist-es/card/Card.css.js
var css_248z$40 = "/* Styles applied to the root element */\n.saltCard {\n  border-width: var(--saltCard-borderWidth, var(--salt-size-border));\n  border-style: var(--salt-container-borderStyle);\n  padding: var(--saltCard-padding, var(--salt-spacing-200));\n  position: relative;\n}\n\n.saltCard-primary {\n  background: var(--saltCard-background, var(--salt-container-primary-background));\n  border-color: var(--salt-container-primary-borderColor);\n  --card-accent-color: var(--salt-container-primary-borderColor);\n}\n\n.saltCard-secondary {\n  background: var(--saltCard-background, var(--salt-container-secondary-background));\n  border-color: var(--salt-container-secondary-borderColor);\n  --card-accent-color: var(--salt-container-secondary-borderColor);\n}\n\n/* Styles applied if `hoverable` is set */\n.saltCard-hoverable:hover {\n  box-shadow: var(--saltCard-boxShadow-hover, var(--salt-overlayable-shadow-hover));\n  --card-accent-color: var(--salt-selectable-foreground-hover);\n}\n\n/* Styles applied to create accent */\n.saltCard-accent::after {\n  content: \"\";\n  position: absolute;\n  background-color: var(--saltCard-accent-color, var(--card-accent-color));\n}\n\n/* Styles applied to Card if `accent=\"bottom\"` (default) */\n.saltCard-accentBottom::after {\n  left: calc(-1 * var(--salt-size-border));\n  bottom: calc(-1 * var(--salt-size-border));\n  height: var(--salt-size-accent);\n  width: calc(100% + calc(2 * var(--salt-size-border)));\n}\n\n/* Styles applied to Card if `accent=\"left\"` */\n.saltCard-accentLeft::after {\n  left: calc(-1 * var(--salt-size-border));\n  top: calc(-1 * var(--salt-size-border));\n  height: calc(100% + calc(2 * var(--salt-size-border)));\n  width: var(--salt-size-accent);\n}\n\n/* Styles applied to Card if `accent=\"top\"` */\n.saltCard-accentTop::after {\n  left: calc(-1 * var(--salt-size-border));\n  top: calc(-1 * var(--salt-size-border));\n  height: var(--salt-size-accent);\n  width: calc(100% + calc(2 * var(--salt-size-border)));\n}\n\n/* Styles applied to Card if `accent=\"right\"` */\n.saltCard-accentRight::after {\n  right: calc(-1 * var(--salt-size-border));\n  top: calc(-1 * var(--salt-size-border));\n  height: calc(100% + calc(2 * var(--salt-size-border)));\n  width: var(--salt-size-accent);\n}\n\n/*\n * **Deprecated:** The following styles are deprecated\n *  Use Interactable Card component instead\n *  for interactable styling\n */\n\n/* **Deprecated:** Styles applied if `interactable={true}` */\n.saltCard-interactable {\n  display: block;\n  transition: none;\n}\n\n/* **Deprecated:** Styles applied on hover if `interactable={true}` */\na:focus .saltCard-interactable,\n.saltCard-interactable:hover {\n  box-shadow: var(--saltCard-boxShadow-hover, var(--salt-overlayable-shadow-hover));\n  cursor: var(--saltCard-cursor-hover, var(--salt-selectable-cursor-hover));\n  position: relative;\n}\n\n/* **Deprecated:** Styles applied on active state if `interactable={true}` */\n.saltCard-interactable:active {\n  box-shadow: var(--saltCard-boxShadow-active, var(--salt-overlayable-shadow-hover));\n  outline: var(--salt-focused-outline);\n  outline-offset: var(--salt-focused-outlineOffset);\n}\n\n/* **Deprecated:** Styles applied on hover if `interactable={true}` and `disabled={true}` */\na:focus .saltCard-interactable.saltCard-disabled,\n.saltCard-interactable.saltCard-disabled:hover,\n.saltCard-interactable.saltCard-disabled:active {\n  box-shadow: var(--saltCard-boxShadow-disabled, var(--salt-overlayable-shadow));\n}\n\n/* **Deprecated:** Styles applied if `disabled={true}` */\n.saltCard-disabled,\n.saltCard-disabled.saltCard-interactable,\na:focus .saltCard-interactable.saltCard-disabled {\n  border-color: var(--salt-container-primary-borderColor-disabled);\n  color: var(--saltCard-color-disabled, var(--salt-content-primary-foreground-disabled));\n  cursor: var(--saltCard-cursor-disabled, var(--salt-selectable-cursor-disabled));\n  outline: none;\n}\n\n/* **Deprecated:** Styles applied to nested divs if `disabled={true}` */\n.saltCard-disabled div {\n  pointer-events: none;\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/card/Card.js
var withBaseName$42 = makePrefixer("saltCard");
var Card = (0, import_react.forwardRef)(function Card2(props, ref) {
	const { accent, children, className, disabled, interactable, hoverable, variant = "primary", ...rest } = props;
	useComponentCssInjection$1({
		testId: "salt-card",
		css: css_248z$40,
		window: useWindow()
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		className: clsx(withBaseName$42(), withBaseName$42(variant), {
			[withBaseName$42("accent")]: accent,
			[withBaseName$42(`accent${capitalize(accent || "")}`)]: accent,
			[withBaseName$42("hoverable")]: hoverable,
			[withBaseName$42("disabled")]: disabled,
			[withBaseName$42("interactable")]: interactable
		}, className),
		ref,
		...rest,
		children
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/card/useInteractableCard.js
var useInteractableCard = ({ disabled, onKeyUp, onKeyDown, onClick, onBlur }) => {
	const [keyIsDown, setkeyIsDown] = (0, import_react.useState)("");
	const [active, setActive] = (0, import_react.useState)(false);
	const enter = "Enter";
	(0, import_react.useEffect)(() => {
		const t = setTimeout(() => {
			if (keyIsDown !== enter) setActive(false);
		}, 0);
		return () => {
			clearTimeout(t);
		};
	}, [active, keyIsDown]);
	const handleKeyUp = (event) => {
		setkeyIsDown("");
		setActive(false);
		onKeyUp?.(event);
	};
	const handleClick = (event) => {
		setActive(true);
		onClick?.(event);
	};
	const handleBlur = (event) => {
		setActive(false);
		onBlur?.(event);
	};
	const handleKeyDown = (event) => {
		if (event.key === enter) {
			setkeyIsDown(event.key);
			setActive(true);
		}
		onKeyDown?.(event);
	};
	return {
		active,
		cardProps: {
			"aria-disabled": disabled ? true : void 0,
			disabled,
			tabIndex: disabled ? -1 : 0,
			onBlur: handleBlur,
			onClick: !disabled ? handleClick : void 0,
			onKeyDown: handleKeyDown,
			onKeyUp: handleKeyUp
		}
	};
};
//#endregion
//#region node_modules/@salt-ds/core/dist-es/card/InteractableCard.css.js
var css_248z$39 = "/* Styles applied to InteractableCard */\n.saltInteractableCard {\n  box-shadow: var(--saltCard-boxShadow, var(--salt-overlayable-shadow));\n  border-width: var(--saltCard-borderWidth, var(--card-borderWidth));\n  border-style: var(--saltCard-borderStyle, var(--salt-container-borderStyle));\n  border-color: var(--saltCard-borderColor, var(--salt-accent-borderColor));\n  border-radius: var(--saltCard-borderRadius, 0);\n  display: block;\n  transition: box-shadow var(--salt-duration-instant) cubic-bezier(0.4, 0, 0.2, 1);\n  padding: var(--saltCard-padding, var(--salt-spacing-300));\n}\n\n/* Styles applied to InteractableCard when variant=\"primary\" */\n.saltInteractableCard-primary {\n  background: var(--saltCard-background, var(--salt-container-primary-background));\n}\n\n/* Styles applied to InteractableCard when variant=\"secondary\" */\n.saltInteractableCard-secondary {\n  background: var(--saltCard-background, var(--salt-container-secondary-background));\n}\n\n/* Styles applied to InteractableCard if `accentPlacement=\"bottom\"` (default) */\n.saltInteractableCard-accentBottom {\n  --card-borderWidth: 0 0 var(--salt-size-accent) 0;\n}\n\n/* Styles applied to InteractableCard if `accentPlacement=\"left\"` */\n.saltInteractableCard-accentLeft {\n  --card-borderWidth: 0 0 0 var(--salt-size-accent);\n}\n\n/* Styles applied to InteractableCard if `accentPlacement=\"top\"` */\n.saltInteractableCard-accentTop {\n  --card-borderWidth: var(--salt-size-accent) 0 0 0;\n}\n\n/* Styles applied to InteractableCard if `accentPlacement=\"right\"` */\n.saltInteractableCard-accentRight {\n  --card-borderWidth: 0 var(--salt-size-accent) 0 0;\n}\n\n/* Styles applied to InteractableCard on focus */\na:focus-visible .saltInteractableCard,\n.saltInteractableCard:focus-visible {\n  cursor: var(--saltCard-interactable-cursor-focus, var(--salt-selectable-cursor-hover));\n  box-shadow: var(--saltCard-interactable-shadow-focus, var(--salt-overlayable-shadow-hover));\n  color: var(--saltCard-color-focus, var(--salt-content-primary-foreground));\n  outline-color: var(--salt-focused-outlineColor);\n  outline-style: var(--salt-focused-outlineStyle);\n  outline-width: var(--salt-focused-outlineWidth);\n  outline-offset: var(--salt-focused-outlineOffset);\n}\n\na:link .saltInteractableCard * {\n  text-decoration: none;\n}\n\n/* Styles applied on active state to InteractableCard */\n.saltInteractableCard:active {\n  cursor: var(--saltCard-interactable-cursor-active, var(--salt-selectable-cursor-active));\n  box-shadow: var(--saltCard-interactable-shadow-active, var(--salt-overlayable-shadow-hover));\n}\n\n/* Styles applied on hover state to InteractableCard */\n.saltInteractableCard:hover {\n  cursor: var(--saltCard-interactable-cursor-hover, var(--salt-selectable-cursor-hover));\n  box-shadow: var(--saltCard-interactable-shadow-hover, var(--salt-overlayable-shadow-hover));\n}\n\n/* Styles applied to InteractableCard if `disabled={true}` */\n.saltInteractableCard-disabled,\n.saltInteractableCard-disabled:focus,\n.saltInteractableCard-disabled:hover,\n.saltInteractableCard-disabled:active {\n  box-shadow: var(--saltCard-interactable-shadow, var(--salt-overlayable-shadow));\n  color: var(--saltCard-color-disabled, var(--salt-content-primary-foreground-disabled));\n  cursor: var(--saltCard-interactable-cursor-disabled, var(--salt-selectable-cursor-disabled));\n  outline: none;\n}\n\n/* Styles applied to InteractableCard primary variant if `disabled={true}` */\n.saltInteractableCard-primary.saltInteractableCard-disabled,\n.saltInteractableCard-primary.saltInteractableCard-disabled:focus,\n.saltInteractableCard-primary.saltInteractableCard-disabled:hover,\n.saltInteractableCard-primary.saltInteractableCard-disabled:active {\n  border-color: var(--saltCard-borderColor-disabled, var(--salt-container-primary-borderColor-disabled));\n  background: var(--saltCard-background-disabled, var(--salt-container-primary-background-disabled));\n}\n\n/* Styles applied to InteractableCard secondary variant if `disabled={true}` */\n.saltInteractableCard-secondary.saltInteractableCard-disabled,\n.saltInteractableCard-secondary.saltInteractableCard-disabled:focus,\n.saltInteractableCard-secondary.saltInteractableCard-disabled:hover,\n.saltInteractableCard-secondary.saltInteractableCard-disabled:active {\n  border-color: var(--saltCard-borderColor-disabled, var(--salt-container-secondary-borderColor-disabled));\n  background: var(--saltCard-background-disabled, var(--salt-container-secondary-background));\n}\n\n/* Styles applied to nested divs in InteractableCard if `disabled={true}` */\n.saltInteractableCard-disabled div {\n  pointer-events: none;\n}\n\n/* Class that can be used for anchor tags wrapping InteractableCard */\n.saltInteractableCard-link {\n  color: var(--saltCard-link-color, var(--salt-content-primary-foreground));\n  inset: var(--salt-focused-outlineInset);\n  outline-color: var(--salt-focused-outlineColor);\n  outline-offset: var(--salt-focused-outlineOffset);\n  outline-style: var(--salt-focused-outlineStyle);\n  outline-width: var(--salt-focused-outlineWidth);\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/card/InteractableCard.js
var withBaseName$41 = makePrefixer("saltInteractableCard");
var InteractableCard = (0, import_react.forwardRef)(function InteractableCard2(props, ref) {
	const { accentPlacement = "bottom", children, className, variant = "primary", disabled, onBlur, onClick, onKeyUp, onKeyDown, ...rest } = props;
	useComponentCssInjection$1({
		testId: "salt-interactable-card",
		css: css_248z$39,
		window: useWindow()
	});
	const { active, cardProps } = useInteractableCard({
		disabled,
		onKeyUp,
		onKeyDown,
		onBlur,
		onClick
	});
	const { tabIndex, ...restCardProps } = cardProps;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		...restCardProps,
		className: clsx(withBaseName$41(), withBaseName$41(variant), withBaseName$41(`accent${capitalize(accentPlacement)}`), {
			[withBaseName$41("disabled")]: disabled,
			[withBaseName$41("active")]: active
		}, className),
		...rest,
		ref,
		children
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/checkbox/CheckboxIcon.css.js
var css_248z$38 = ".saltCheckboxIcon {\n  --checkbox-size: var(--salt-size-selectable);\n  width: var(--checkbox-size);\n  min-width: var(--checkbox-size);\n  height: var(--checkbox-size);\n  min-height: var(--checkbox-size);\n  border: var(--salt-size-border) var(--salt-selectable-borderStyle) var(--salt-selectable-borderColor);\n  color: var(--salt-selectable-foreground);\n  background: var(--salt-container-primary-background);\n  position: relative;\n  --saltIcon-size: 100%;\n  display: flex;\n}\n\n.saltCheckbox:hover .saltCheckboxIcon,\n.saltCheckbox:hover .saltCheckboxIcon-indeterminate {\n  border-color: var(--salt-selectable-borderColor-hover);\n  color: var(--salt-selectable-foreground-hover);\n}\n\n.saltCheckboxIcon-checked,\n.saltCheckbox:hover .saltCheckboxIcon-checked {\n  border-color: var(--salt-selectable-borderColor-selected);\n  color: var(--salt-selectable-foreground-selected);\n}\n\n.saltCheckboxIcon-disabled,\n.saltCheckbox:hover .saltCheckboxIcon-disabled,\n.saltCheckboxIcon-disabled.saltCheckboxIcon-indeterminate,\n.saltCheckbox:hover .saltCheckboxIcon-disabled.saltCheckboxIcon-indeterminate {\n  border-color: var(--salt-selectable-borderColor-disabled);\n  background: var(--salt-container-primary-background-disabled);\n  color: var(--salt-selectable-foreground-disabled);\n}\n\n.saltCheckboxIcon-checked.saltCheckboxIcon-disabled,\n.saltCheckbox:hover .saltCheckboxIcon-checked.saltCheckboxIcon-disabled {\n  border-color: var(--salt-selectable-borderColor-selectedDisabled);\n  color: var(--salt-selectable-foreground-selectedDisabled);\n}\n\n.saltCheckboxIcon-indeterminate::before {\n  content: \"\";\n  position: absolute;\n  top: 50%;\n  left: 50%;\n  width: calc(var(--checkbox-size) - 6px);\n  height: 2px;\n  background: currentColor;\n  transform: translate(-50%, -50%);\n}\n\n.saltCheckboxIcon-error,\n.saltCheckbox:hover .saltCheckboxIcon-error {\n  color: var(--salt-status-error-foreground);\n  border-color: var(--salt-status-error-borderColor);\n  outline-color: var(--salt-status-error-foreground);\n}\n\n.saltCheckboxIcon-warning,\n.saltCheckbox:hover .saltCheckboxIcon-warning {\n  color: var(--salt-status-warning-foreground);\n  border-color: var(--salt-status-warning-borderColor);\n  outline-color: var(--salt-status-warning-foreground);\n}\n\n.saltCheckboxIcon-readOnly,\n.saltCheckbox:hover .saltCheckboxIcon-readOnly {\n  border-color: var(--salt-selectable-borderColor-readonly);\n  color: var(--salt-content-primary-foreground);\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/checkbox/CheckboxIcon.js
var withBaseName$40 = makePrefixer("saltCheckboxIcon");
function CheckedIcon$1(props) {
	return useDensity() === "high" ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SuccessSmallSolidIcon, { ...props }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SuccessSolidIcon, { ...props });
}
function CheckedReadOnlyIcon(props) {
	return useDensity() === "high" ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SuccessSmallIcon, { ...props }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SuccessIcon, { ...props });
}
var CheckboxIcon = ({ checked = false, className, disabled, error, indeterminate, validationStatus, readOnly }) => {
	useComponentCssInjection$1({
		testId: "salt-checkbox-icon",
		css: css_248z$38,
		window: useWindow()
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"aria-hidden": "true",
		className: clsx(withBaseName$40(), {
			[withBaseName$40("checked")]: checked,
			[withBaseName$40("disabled")]: disabled,
			[withBaseName$40("error")]: error,
			[withBaseName$40(validationStatus || "")]: validationStatus,
			[withBaseName$40("indeterminate")]: indeterminate,
			[withBaseName$40("readOnly")]: readOnly
		}, className),
		children: [checked && !indeterminate && !readOnly && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CheckedIcon$1, { className: withBaseName$40("icon") }), checked && !indeterminate && readOnly && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CheckedReadOnlyIcon, { className: withBaseName$40("icon") })]
	});
};
//#endregion
//#region node_modules/@salt-ds/core/dist-es/form-field-context/FormFieldContext.js
var FormFieldContext = createContext("FormFieldContext", {});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/form-field-context/useFormFieldProps.js
function useFormFieldProps() {
	return (0, import_react.useContext)(FormFieldContext) || {};
}
//#endregion
//#region node_modules/@salt-ds/core/dist-es/checkbox/internal/CheckboxGroupContext.js
var CheckboxGroupContext = createContext("CheckboxGroupContext", void 0);
//#endregion
//#region node_modules/@salt-ds/core/dist-es/checkbox/internal/useCheckboxGroup.js
function useCheckboxGroup() {
	return (0, import_react.useContext)(CheckboxGroupContext);
}
//#endregion
//#region node_modules/@salt-ds/core/dist-es/checkbox/Checkbox.css.js
var css_248z$37 = "/* Styles applied to root component */\n.saltCheckbox {\n  display: flex;\n  gap: var(--salt-spacing-100);\n  position: relative;\n  cursor: var(--salt-selectable-cursor-hover);\n\n  font-size: var(--salt-text-fontSize);\n  line-height: var(--salt-text-lineHeight);\n  font-family: var(--salt-text-fontFamily);\n  font-weight: var(--salt-text-fontWeight);\n}\n\n/* Styles applied to root component if `disabled={true}` */\n.saltCheckbox-disabled {\n  color: var(--salt-content-primary-foreground-disabled);\n  cursor: var(--salt-selectable-cursor-disabled);\n}\n\n/* Styles applied to root component if `readOnly={true}` */\n.saltCheckbox-readOnly {\n  color: var(--salt-content-primary-foreground);\n  cursor: var(--salt-selectable-cursor-readonly);\n}\n\n.saltCheckbox-input,\n.saltCheckboxIcon {\n  margin-top: calc((var(--salt-text-lineHeight) - var(--salt-size-selectable)) / 2);\n}\n\n.saltCheckboxIcon > svg {\n  /* ensures svg is centered in all browsers */\n  transform: translate(0px, 0px);\n}\n/* Styles applied to the icon component if focused */\n.saltCheckbox-input:focus-visible + .saltCheckboxIcon {\n  outline-offset: var(--salt-focused-outlineOffset);\n  outline: var(--saltCheckbox-outline, var(--salt-focused-outline));\n  border-color: var(--salt-selectable-borderColor-hover);\n  color: var(--salt-selectable-foreground-hover);\n}\n\n.saltCheckbox-input:focus-visible + .saltCheckboxIcon-checked {\n  border-color: var(--salt-selectable-borderColor-selected);\n  color: var(--salt-selectable-foreground-selected);\n}\n\n.saltCheckbox-error .saltCheckbox-input:focus-visible + .saltCheckboxIcon {\n  outline-color: var(--salt-status-error-foreground);\n  border-color: var(--salt-status-error-foreground);\n  color: var(--salt-status-error-foreground);\n}\n\n.saltCheckbox-warning .saltCheckbox-input:focus-visible + .saltCheckboxIcon {\n  outline-color: var(--salt-status-warning-foreground);\n  border-color: var(--salt-status-warning-foreground);\n  color: var(--salt-status-warning-foreground);\n}\n\n/* Styles applied to input element */\n.saltCheckbox-input {\n  cursor: inherit;\n  margin: 0;\n  opacity: 0;\n  padding: 0;\n  position: absolute;\n  z-index: var(--salt-zIndex-default);\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/checkbox/Checkbox.js
var withBaseName$39 = makePrefixer("saltCheckbox");
var Checkbox = (0, import_react.forwardRef)(function Checkbox2({ checked: checkedProp, className, defaultChecked, disabled: disabledProp, error, indeterminate, inputProps = {}, label, name, onBlur, onChange, onFocus, value, validationStatus: validationStatusProp, readOnly: readOnlyProp, ...rest }, ref) {
	var _a, _b;
	useComponentCssInjection$1({
		testId: "salt-checkbox",
		css: css_248z$37,
		window: useWindow()
	});
	const checkboxGroup = useCheckboxGroup();
	const { "aria-describedby": inputDescribedBy, "aria-labelledby": inputLabelledBy, className: inputClassName, onChange: inputOnChange, ...restInputProps } = inputProps;
	const [checked, setChecked] = useControlled({
		controlled: (checkboxGroup == null ? void 0 : checkboxGroup.checkedValues) != null && value != null ? checkboxGroup.checkedValues.includes(value) : checkedProp,
		default: Boolean(defaultChecked),
		name: "Checkbox",
		state: "checked"
	});
	const { a11yProps: formFieldA11yProps, disabled: formFieldDisabled, readOnly: formFieldReadOnly, validationStatus: formFieldValidationStatus } = useFormFieldProps();
	const disabled = (checkboxGroup == null ? void 0 : checkboxGroup.disabled) || formFieldDisabled || disabledProp;
	const readOnly = (checkboxGroup == null ? void 0 : checkboxGroup.readOnly) || formFieldReadOnly || readOnlyProp;
	const validationStatus = !disabled ? (_b = (_a = checkboxGroup == null ? void 0 : checkboxGroup.validationStatus) != null ? _a : formFieldValidationStatus) != null ? _b : validationStatusProp : void 0;
	const inputRef = (0, import_react.useRef)(null);
	const handleChange = (event) => {
		var _a2;
		if (event.nativeEvent.defaultPrevented || readOnly) return;
		const value2 = event.target.checked;
		setChecked(value2);
		onChange?.(event);
		inputOnChange?.(event);
		(_a2 = checkboxGroup == null ? void 0 : checkboxGroup.onChange) == null || _a2.call(checkboxGroup, event);
	};
	useIsomorphicLayoutEffect(() => {
		if (inputRef.current != null) inputRef.current.indeterminate = indeterminate != null ? indeterminate : false;
	}, [indeterminate]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("label", {
		className: clsx(withBaseName$39(), {
			[withBaseName$39("disabled")]: disabled,
			[withBaseName$39("readOnly")]: readOnly,
			[withBaseName$39("error")]: error,
			[withBaseName$39(validationStatus != null ? validationStatus : "")]: validationStatus
		}, className),
		ref,
		...rest,
		children: [
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("input", {
				"aria-readonly": readOnly || void 0,
				"aria-describedby": clsx(checkboxGroup === void 0 ? formFieldA11yProps == null ? void 0 : formFieldA11yProps["aria-describedby"] : void 0, inputDescribedBy),
				"aria-labelledby": clsx(checkboxGroup === void 0 ? formFieldA11yProps == null ? void 0 : formFieldA11yProps["aria-labelledby"] : void 0, inputLabelledBy),
				name,
				value,
				checked,
				className: clsx(withBaseName$39("input"), inputClassName),
				"data-indeterminate": indeterminate,
				defaultChecked,
				disabled,
				readOnly,
				onBlur,
				onChange: handleChange,
				onFocus,
				type: "checkbox",
				ref: inputRef,
				...restInputProps
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(CheckboxIcon, {
				checked,
				disabled,
				readOnly,
				indeterminate,
				validationStatus,
				error
			}),
			label
		]
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/checkbox/CheckboxGroup.css.js
var css_248z$36 = "/* Styles applied to root component */\n.saltCheckboxGroup {\n  border: none;\n  margin: 0;\n  padding: 0;\n}\n\n/* Styles applied to root component if `direction={\"horizontal\"}` */\n.saltCheckboxGroup-horizontal {\n  display: flex;\n  gap: var(--salt-spacing-200);\n  flex-direction: row;\n  flex-wrap: wrap;\n}\n\n/* Styles applied to root component if `direction={\"vertical\"}` */\n.saltCheckboxGroup-vertical {\n  display: flex;\n  gap: var(--salt-spacing-100);\n  flex-direction: column;\n}\n\n.saltCheckboxGroup-noWrap {\n  flex-wrap: nowrap;\n}\n\n.saltCheckboxGroup-noWrap .saltCheckbox {\n  white-space: break-spaces;\n}\n\n/* Styles applied when checkbox button group is inside a form field with label left or right */\n.saltFormField-labelLeft .saltCheckboxGroup,\n.saltFormField-labelRight .saltCheckboxGroup {\n  padding-top: var(--salt-spacing-100);\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/checkbox/CheckboxGroup.js
var withBaseName$38 = makePrefixer("saltCheckboxGroup");
var CheckboxGroup = (0, import_react.forwardRef)(function CheckboxGroup2({ "aria-labelledby": ariaLabelledBy, "aria-describedby": ariaDescribedBy, checkedValues: checkedValuesProp, defaultCheckedValues = [], children, className, disabled: disabledProp, direction = "vertical", name, onChange, readOnly: readOnlyProp, wrap, validationStatus: validationStatusProp, ...other }, ref) {
	useComponentCssInjection$1({
		testId: "salt-checkbox-group",
		css: css_248z$36,
		window: useWindow()
	});
	const { a11yProps, disabled: formFieldDisabled, readOnly: formFieldReadOnly, validationStatus: formFieldValidationStatus } = useFormFieldProps();
	const disabled = formFieldDisabled || disabledProp;
	const readOnly = formFieldReadOnly || readOnlyProp;
	const validationStatus = formFieldValidationStatus != null ? formFieldValidationStatus : validationStatusProp;
	const [checkedValues, setCheckedValues] = useControlled({
		controlled: checkedValuesProp,
		default: defaultCheckedValues,
		name: "CheckboxGroup",
		state: "checkedValues"
	});
	const handleChange = (event) => {
		const name2 = event.target.value;
		setCheckedValues((oldValues = []) => {
			return oldValues.includes(name2) ? oldValues.filter((value) => value !== name2) : oldValues.concat(name2);
		});
		onChange?.(event);
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("fieldset", {
		"aria-labelledby": clsx(a11yProps == null ? void 0 : a11yProps["aria-labelledby"], ariaLabelledBy) || void 0,
		"aria-describedby": clsx(a11yProps == null ? void 0 : a11yProps["aria-describedby"], ariaDescribedBy) || void 0,
		className: clsx(withBaseName$38(), withBaseName$38(direction), { [withBaseName$38("noWrap")]: !wrap }, className),
		ref,
		...other,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CheckboxGroupContext.Provider, {
			value: {
				disabled,
				name,
				onChange: handleChange,
				checkedValues,
				readOnly,
				validationStatus
			},
			children
		})
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/file-drop-zone/internal/utils.js
var containsFiles = (e) => {
	if (!e.dataTransfer) {
		const target = e.target;
		return target == null ? void 0 : target.files;
	}
	return Array.prototype.some.call(e.dataTransfer.types, (type) => type === "Files");
};
var extractFiles = (e) => {
	var _a;
	if (containsFiles(e)) {
		if (e.dataTransfer) return Array.from(e.dataTransfer.files);
		if (e.target) return Array.from((_a = e.target.files) != null ? _a : []);
	}
	return [];
};
//#endregion
//#region node_modules/@salt-ds/core/dist-es/file-drop-zone/FileDropZone.css.js
var css_248z$35 = ".saltFileDropZone {\n  color: var(--saltFileDropZone-text-color, var(--salt-text-primary-foreground));\n  display: inline-flex;\n  background: var(--saltFileDropZone-background, var(--salt-container-primary-background));\n  text-align: center;\n  align-items: center;\n  justify-content: center;\n  border: var(--saltFileDropZone-border, var(--salt-size-border-strong) var(--salt-target-borderStyle) var(--salt-container-primary-borderColor));\n  flex-direction: column;\n  padding: var(--salt-spacing-200);\n  gap: var(--salt-spacing-200);\n  width: 100%;\n  font-size: var(--saltFileDropZone-fontSize, var(--salt-text-fontSize));\n  font-family: var(--saltFileDropZone-fontFamily, var(--salt-text-fontFamily));\n  line-height: var(--saltFileDropZone-lineHeight, var(--salt-text-lineHeight));\n}\n\n.saltFileDropZone-active {\n  background: var(--salt-target-background-hover);\n  border: var(--salt-size-border-strong) var(--salt-target-borderStyle-hover) var(--salt-target-borderColor-hover);\n}\n\n/* Styles applied if `status=\"error\" */\n.saltFileDropZone-error {\n  border: var(--salt-size-border-strong) var(--salt-target-borderStyle) var(--salt-status-error-borderColor);\n}\n\n/* Styles applied if `status=\"success\" */\n.saltFileDropZone-success {\n  border: var(--salt-size-border-strong) var(--salt-target-borderStyle) var(--salt-status-success-borderColor);\n}\n\n.saltFileDropZone .input-hidden {\n  display: none;\n}\n\n/* Styles applied if `disabled={true}` */\n.saltFileDropZone-disabled {\n  background: var(--salt-container-primary-background-disabled);\n  border: var(--salt-size-border-strong) var(--salt-target-borderStyle) var(--salt-container-primary-borderColor-disabled);\n  cursor: var(--salt-target-cursor-disabled);\n  color: var(--salt-content-primary-foreground-disabled);\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/utils/setRef.js
function setRef(ref, value) {
	if (typeof ref === "function") ref(value);
	else if (ref) ref.current = value;
}
//#endregion
//#region node_modules/@salt-ds/core/dist-es/utils/useForkRef.js
function useForkRef(refA, refB) {
	return (0, import_react.useMemo)(() => {
		if (refA == null && refB == null) return () => null;
		return (refValue) => {
			setRef(refA, refValue);
			setRef(refB, refValue);
		};
	}, [refA, refB]);
}
//#endregion
//#region node_modules/@salt-ds/core/dist-es/file-drop-zone/FileDropZone.js
var withBaseName$37 = makePrefixer("saltFileDropZone");
var FileDropZone = (0, import_react.forwardRef)(function FileDropZone2({ status, className, children, disabled, onDragOver, onDragLeave, onDrop, ...restProps }, ref) {
	useComponentCssInjection$1({
		testId: "salt-file-drop-zone",
		css: css_248z$35,
		window: useWindow()
	});
	const [isActive, setActive] = (0, import_react.useState)(false);
	const regionRef = (0, import_react.useRef)(null);
	const handleRef = useForkRef(ref, regionRef);
	const handleDragOver = (event) => {
		event.preventDefault();
		event.stopPropagation();
		if (disabled) {
			if (event.dataTransfer) event.dataTransfer.dropEffect = "none";
			return;
		}
		if (event.dataTransfer) event.dataTransfer.dropEffect = "copy";
		if (!isActive && containsFiles(event)) setActive(true);
		onDragOver?.(event);
	};
	const handleDragLeave = (event) => {
		if (disabled) return;
		const region = regionRef == null ? void 0 : regionRef.current;
		const eventTarget = event.relatedTarget;
		if (eventTarget !== region && !(region == null ? void 0 : region.contains(eventTarget))) setActive(false);
		onDragLeave?.(event);
	};
	const handleDrop = (event) => {
		if (disabled) return;
		event.preventDefault();
		const files = extractFiles(event);
		setActive(false);
		onDrop?.(event, files);
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		className: clsx(withBaseName$37(), {
			[withBaseName$37(status)]: status,
			[withBaseName$37("active")]: isActive,
			[withBaseName$37("disabled")]: disabled
		}, className),
		"aria-disabled": disabled,
		onDragLeave: handleDragLeave,
		onDragOver: handleDragOver,
		onDrop: handleDrop,
		ref: handleRef,
		...restProps,
		children
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/file-drop-zone/FileDropZoneIcon.js
var FileDropZoneIcon = (0, import_react.forwardRef)(function FileDropZoneIcon2({ status, size = 2, ...rest }, ref) {
	const iconProps = {
		ref,
		size,
		...rest
	};
	return status ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(StatusIndicator, {
		status,
		...iconProps
	}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(UploadIcon, { ...iconProps });
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/file-drop-zone/FileDropZoneTrigger.js
var FileDropZoneTrigger = (0, import_react.forwardRef)(function FileDropZoneTrigger2({ accept, children, disabled, multiple = false, onChange, ...rest }, ref) {
	const buttonRef = (0, import_react.useRef)(null);
	const fileInputRef = (0, import_react.useRef)(null);
	const triggerRef = useForkRef(ref, buttonRef);
	const handleFocus = (event) => {
		var _a;
		event.stopPropagation();
		(_a = buttonRef.current) == null || _a.focus();
	};
	const handleClick = (event) => {
		var _a;
		event.stopPropagation();
		(_a = fileInputRef.current) == null || _a.click();
	};
	const handleChange = (event) => {
		var _a;
		const files = Array.from((_a = event.target.files) != null ? _a : []);
		onChange?.(event, files);
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
		onClick: handleClick,
		disabled,
		ref: triggerRef,
		...rest,
		children: children != null ? children : "Browse files"
	}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("input", {
		accept,
		className: "input-hidden",
		disabled,
		multiple,
		onChange: handleChange,
		onFocus: handleFocus,
		ref: fileInputRef,
		type: "file"
	})] });
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/flex-item/FlexItem.css.js
var css_248z$34 = "/* Default variables applied to the root element */\n.saltFlexItem {\n  --saltFlexItem-alignment: auto;\n  --saltFlexItem-shrink: 1;\n  --saltFlexItem-grow: 0;\n  --saltFlexItem-basis: auto;\n}\n\n/* Style applied to the root element */\n.saltFlexItem {\n  position: relative;\n  align-self: var(--saltFlexItem-alignment);\n  flex-grow: var(--saltFlexItem-grow);\n  flex-shrink: var(--saltFlexItem-shrink);\n  flex-basis: var(--saltFlexItem-basis);\n}\n\n.saltFlexItem-stacked {\n  flex-grow: 2;\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/flex-item/FlexItem.js
var withBaseName$36 = makePrefixer("saltFlexItem");
var FLEX_ITEM_ALIGNMENTS = [
	"start",
	"end",
	"center",
	"stretch"
];
var FlexItem = (0, import_react.forwardRef)(({ as, align, children, className, shrink, grow, basis, style, ...rest }, ref) => {
	useComponentCssInjection$1({
		testId: "salt-flex-item",
		css: css_248z$34,
		window: useWindow()
	});
	const Component = as || "div";
	const itemStyle = {
		"--saltFlexItem-alignment": align,
		"--saltFlexItem-shrink": useResponsiveProp(shrink, 1),
		"--saltFlexItem-grow": useResponsiveProp(grow, 0),
		"--saltFlexItem-basis": useResponsiveProp(basis, "auto"),
		...style
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Component, {
		className: clsx(withBaseName$36(), className),
		ref,
		style: itemStyle,
		...rest,
		children
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/flex-layout/FlexLayout.css.js
var css_248z$33 = "/* Default variables applied to the root element */\n.saltFlexLayout {\n  --flexLayout-gap-multiplier: var(--flexLayout-gap-density-multiplier, 3);\n  --flexLayout-layout-display: flex;\n  --flexLayout-direction: row;\n  --flexLayout-wrap: nowrap;\n  --flexLayout-justify: flex-start;\n  --flexLayout-align: stretch;\n  --flexLayout-separator: var(--salt-size-border);\n  --flexLayout-gap: calc(var(--salt-size-unit) * var(--flexLayout-gap-multiplier));\n}\n\n/* Style applied to the root element */\n.saltFlexLayout {\n  gap: var(--flexLayout-gap);\n  display: var(--flexLayout-layout-display);\n  flex-direction: var(--flexLayout-direction);\n  flex-wrap: var(--flexLayout-wrap);\n  justify-content: var(--flexLayout-justify);\n  align-items: var(--flexLayout-align);\n}\n\n.saltFlexLayout-separator {\n  gap: calc(var(--flexLayout-gap) * 2);\n}\n\n.saltFlexLayout-separator > * {\n  position: relative;\n}\n.saltFlexLayout-separator > *:not(:last-child)::after {\n  position: absolute;\n  display: block;\n  content: \"\";\n  background-color: var(--salt-separable-secondary-borderColor);\n}\n\n/* Row separator */\n\n.saltFlexLayout-separator-row > *:not(:last-child)::after {\n  height: 100%;\n}\n\n.saltFlexLayout-separator-row > *:not(:last-child)::after {\n  width: var(--flexLayout-separator);\n  top: 0;\n}\n.saltFlexLayout-separator-row-start > *:not(:last-child)::after {\n  right: 0;\n}\n.saltFlexLayout-separator-row-center > *:not(:last-child)::after {\n  right: calc(var(--flexLayout-gap) * -1);\n}\n.saltFlexLayout-separator-row-end > *:not(:last-child)::after {\n  right: calc(var(--flexLayout-gap) * -2);\n}\n\n/* Column separator */\n.saltFlexLayout-separator-column > *:not(:last-child)::after {\n  width: 100%;\n}\n\n.saltFlexLayout-separator-column > *:not(:last-child)::after {\n  height: var(--flexLayout-separator);\n  left: 0;\n  width: 100%;\n}\n.saltFlexLayout-separator-column-start > *:not(:last-child)::after {\n  bottom: 0;\n}\n.saltFlexLayout-separator-column-center > *:not(:last-child)::after {\n  bottom: calc(var(--flexLayout-gap) * -1);\n}\n.saltFlexLayout-separator-column-end > *:not(:last-child)::after {\n  bottom: calc(var(--flexLayout-gap) * -2);\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/flex-layout/FlexLayout.js
var withBaseName$35 = makePrefixer("saltFlexLayout");
var FLEX_ALIGNMENT_BASE = [
	"start",
	"end",
	"center"
];
var FLEX_CONTENT_ALIGNMENT_BASE = [
	...FLEX_ALIGNMENT_BASE,
	"space-between",
	"space-around",
	"space-evenly"
];
var FlexLayout = (0, import_react.forwardRef)(({ as, align, children, className, direction, gap, justify, separators, style, wrap, ...rest }, ref) => {
	useComponentCssInjection$1({
		testId: "salt-flex-layout",
		css: css_248z$33,
		window: useWindow()
	});
	const Component = as || "div";
	const separatorAlignment = separators === true ? "center" : separators;
	const addPrefix = (style2) => {
		return style2 === "start" || style2 === "end" ? `flex-${style2}` : style2;
	};
	const flexGap = useResponsiveProp(gap, 3);
	const flexDirection = useResponsiveProp(direction, "row");
	const flexWrap = useResponsiveProp(wrap, false);
	const flexLayoutStyles = {
		...style,
		"--flexLayout-align": align && addPrefix(align),
		"--flexLayout-direction": flexDirection,
		"--flexLayout-gap-multiplier": flexGap,
		"--flexLayout-justify": justify && addPrefix(justify),
		"--flexLayout-wrap": flexWrap ? "wrap" : "nowrap"
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Component, {
		className: clsx(withBaseName$35(), {
			[withBaseName$35("separator")]: separatorAlignment && !wrap,
			[withBaseName$35(`separator-${flexDirection || "row"}-${separatorAlignment || "center"}`)]: separatorAlignment && !wrap,
			[withBaseName$35(`separator-${flexDirection || "row"}`)]: separatorAlignment && !wrap
		}, className),
		ref,
		style: flexLayoutStyles,
		...rest,
		children
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/flow-layout/FlowLayout.js
var FlowLayout = (0, import_react.forwardRef)(({ children, ...rest }, ref) => {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FlexLayout, {
		direction: "row",
		ref,
		wrap: true,
		...rest,
		children
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/form-field/FormField.css.js
var css_248z$32 = ".saltFormField {\n  display: inline-grid;\n  position: relative;\n  gap: var(--salt-spacing-100);\n  text-align: left;\n  width: var(--saltFormField-width, 100%);\n\n  font-family: var(--salt-text-fontFamily);\n  font-size: var(--salt-text-fontSize);\n  font-weight: var(--salt-text-fontWeight);\n  line-height: var(--salt-text-lineHeight);\n}\n\n.saltFormField-labelTop {\n  grid-template-areas:\n    \"label\"\n    \"controls\";\n}\n\n.saltFormField-labelTop .saltFormFieldHelperText {\n  grid-area: 3 / 1;\n}\n\n.saltFormField-labelLeft {\n  align-self: center;\n  grid-template-columns: var(--saltFormField-label-width, var(--formField-label-width, 40%)) 1fr;\n  grid-template-areas: \"label controls\";\n}\n\n.saltFormField-labelRight {\n  align-self: center;\n  grid-template-columns: var(--saltFormField-label-width, var(--formField-label-width, 40%)) 1fr;\n  grid-template-areas: \"label controls\";\n}\n\n.saltFormField-labelLeft .saltFormFieldHelperText,\n.saltFormField-labelRight .saltFormFieldHelperText {\n  grid-area: 2 / 2;\n}\n\n.saltFormField-labelRight .saltFormFieldLabel,\n.saltFormField-labelLeft .saltFormFieldLabel {\n  padding-top: var(--salt-spacing-100);\n}\n\n.saltFormField-labelRight .saltFormFieldLabel {\n  text-align: right;\n}\n\n.saltFormField-controls {\n  align-items: center;\n  display: grid;\n  grid-auto-columns: minmax(0, 1fr);\n  grid-auto-flow: column;\n  grid-column-gap: var(--salt-spacing-100);\n}\n\n.saltFormFieldLabel {\n  grid-area: label;\n}\n\n.saltFormFieldHelperText {\n  grid-area: helperText;\n}\n\n.saltFormField-disabled {\n  --saltInput-outline: none;\n  --saltCheckbox-outline: none;\n  --saltRadioButton-outline: none;\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/form-field/FormField.js
var withBaseName$34 = makePrefixer("saltFormField");
var FormField = (0, import_react.forwardRef)(({ children, className, disabled = false, id: idProp, labelPlacement = "top", necessity, onBlur, onFocus, readOnly = false, validationStatus, ...restProps }, ref) => {
	useComponentCssInjection$1({
		testId: "salt-form-field",
		css: css_248z$32,
		window: useWindow()
	});
	const formId = useId(idProp);
	const labelId = formId ? `label-${formId}` : void 0;
	const helperTextId = formId ? `helperText-${formId}` : void 0;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		ref,
		className: clsx(withBaseName$34(), {
			[withBaseName$34("disabled")]: disabled,
			[withBaseName$34(`label${capitalize(labelPlacement)}`)]: labelPlacement
		}, className),
		...restProps,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FormFieldContext.Provider, {
			value: {
				a11yProps: {
					"aria-labelledby": labelId,
					"aria-describedby": helperTextId
				},
				disabled,
				necessity,
				readOnly,
				validationStatus
			},
			children
		})
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/text/Text.css.js
var css_248z$31 = "/* Main css class. Style for body text */\n.saltText {\n  color: var(--saltText-color, var(--text-color));\n  font-family: var(--saltText-fontFamily, var(--salt-text-fontFamily));\n  font-size: var(--saltText-fontSize, var(--salt-text-fontSize));\n  line-height: var(--saltText-lineHeight, var(--salt-text-lineHeight));\n  font-weight: var(--saltText-fontWeight, var(--salt-text-fontWeight));\n}\n\n.saltText::selection {\n  background: var(--saltText-highlight, var(--salt-content-foreground-highlight));\n}\n\n/* Allows truncation */\n.saltText-lineClamp {\n  display: -webkit-box;\n  -webkit-box-orient: vertical;\n  -webkit-line-clamp: var(--text-max-rows, 0);\n  overflow: hidden;\n  word-break: break-word;\n}\n\n/* Primary variant */\n.saltText-primary {\n  --text-color: var(--salt-content-primary-foreground);\n}\n\n/* Disabled primary variant */\n.saltText-primary.saltText-disabled {\n  --text-color: var(--salt-content-primary-foreground-disabled);\n}\n\n/* Secondary variant */\n.saltText-secondary {\n  --text-color: var(--salt-content-secondary-foreground);\n}\n\n/* Disabled secondary variant */\n.saltText-secondary.saltText-disabled {\n  --text-color: var(--salt-content-secondary-foreground-disabled);\n}\n\n/* Body emphasis strong */\n.saltText strong {\n  font-weight: var(--salt-text-fontWeight-strong);\n}\n\n/* Body emphasis small */\n.saltText small {\n  font-size: inherit;\n  font-weight: var(--salt-text-fontWeight-small);\n}\n\n/* Notation */\n.saltText-notation.saltText {\n  font-family: var(--salt-text-notation-fontFamily);\n  font-size: var(--salt-text-notation-fontSize);\n  line-height: var(--salt-text-notation-lineHeight);\n  font-weight: var(--salt-text-fontWeight);\n}\n\n/* Notation emphasis strong */\n.saltText-notation.saltText strong {\n  font-weight: var(--salt-text-notation-fontWeight-strong);\n}\n\n/* Notation emphasis small */\n.saltText-notation.saltText small {\n  font-size: inherit;\n  font-weight: var(--salt-text-notation-fontWeight-small);\n}\n\n/* Action */\n.saltText-action.saltText {\n  letter-spacing: var(--salt-text-action-letterSpacing);\n  text-transform: var(--salt-text-action-textTransform);\n  text-align: var(--salt-text-action-textAlign);\n  font-weight: var(--salt-text-action-fontWeight);\n}\n\n/* Display 1 */\n.saltText-display1.saltText {\n  font-family: var(--salt-text-display1-fontFamily);\n  font-size: var(--salt-text-display1-fontSize);\n  font-weight: var(--salt-text-display1-fontWeight);\n  line-height: var(--salt-text-display1-lineHeight);\n}\n\n/* Display 2 */\n.saltText-display2.saltText {\n  font-family: var(--salt-text-display2-fontFamily);\n  font-size: var(--salt-text-display2-fontSize);\n  font-weight: var(--salt-text-display2-fontWeight);\n  line-height: var(--salt-text-display2-lineHeight);\n}\n\n/* Display 3 */\n.saltText-display3.saltText {\n  font-family: var(--salt-text-display3-fontFamily);\n  font-size: var(--salt-text-display3-fontSize);\n  font-weight: var(--salt-text-display3-fontWeight);\n  line-height: var(--salt-text-display3-lineHeight);\n}\n\n/* Heading 1 */\nh1.saltText,\n.saltText-h1.saltText {\n  font-family: var(--salt-text-h1-fontFamily);\n  font-size: var(--salt-text-h1-fontSize);\n  font-weight: var(--salt-text-h1-fontWeight);\n  line-height: var(--salt-text-h1-lineHeight);\n}\n\n/* H1 emphasis strong */\nh1.saltText strong,\n.saltText-h1.saltText strong {\n  font-weight: var(--salt-text-h1-fontWeight-strong);\n}\n\n/* H1 emphasis small */\nh1.saltText small,\n.saltText-h1.saltText small {\n  font-size: inherit;\n  font-weight: var(--salt-text-h1-fontWeight-small);\n}\n\n/* Heading 2 */\nh2.saltText,\n.saltText-h2.saltText {\n  font-family: var(--salt-text-h2-fontFamily);\n  font-size: var(--salt-text-h2-fontSize);\n  font-weight: var(--salt-text-h2-fontWeight);\n  line-height: var(--salt-text-h2-lineHeight);\n}\n\n/* H2 emphasis strong */\nh2.saltText strong,\n.saltText-h2.saltText strong {\n  font-weight: var(--salt-text-h2-fontWeight-strong);\n}\n\n/* H2 emphasis small */\nh2.saltText small,\n.saltText-h2.saltText small {\n  font-size: inherit;\n  font-weight: var(--salt-text-h2-fontWeight-small);\n}\n\n/* Heading 3 */\nh3.saltText,\n.saltText-h3.saltText {\n  font-family: var(--salt-text-h3-fontFamily);\n  font-size: var(--salt-text-h3-fontSize);\n  font-weight: var(--salt-text-h3-fontWeight);\n  line-height: var(--salt-text-h3-lineHeight);\n}\n\n/* H3 emphasis strong */\nh3.saltText strong,\n.saltText-h3.saltText strong {\n  font-weight: var(--salt-text-h3-fontWeight-strong);\n}\n\n/* H3 emphasis small */\nh3.saltText small,\n.saltText-h3.saltText small {\n  font-size: inherit;\n  font-weight: var(--salt-text-h3-fontWeight-small);\n}\n\n/* Heading 4 */\nh4.saltText,\n.saltText-h4.saltText {\n  font-family: var(--salt-text-h4-fontFamily);\n  font-size: var(--salt-text-h4-fontSize);\n  font-weight: var(--salt-text-h4-fontWeight);\n  line-height: var(--salt-text-h4-lineHeight);\n}\n\n/* H4 emphasis strong */\nh4.saltText strong,\n.saltText-h4.saltText strong {\n  font-weight: var(--salt-text-h4-fontWeight-strong);\n}\n\n/* H4 emphasis small */\nh4.saltText small,\n.saltText-h4.saltText small {\n  font-size: inherit;\n  font-weight: var(--salt-text-h4-fontWeight-small);\n}\n\n/* Label */\nlabel.saltText,\n.saltText-label.saltText {\n  font-family: var(--salt-text-label-fontFamily);\n  font-size: var(--salt-text-label-fontSize);\n  line-height: var(--salt-text-label-lineHeight);\n  font-weight: var(--salt-text-fontWeight);\n}\n\n/* Label emphasis strong */\nlabel.saltText strong,\n.saltText-label.saltText strong {\n  font-weight: var(--salt-text-label-fontWeight-strong);\n}\n\n/* Label emphasis small */\nlabel.saltText small,\n.saltText-label.saltText small {\n  font-size: inherit;\n  font-weight: var(--salt-text-fontWeight-small);\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/text/Text.js
var withBaseName$33 = makePrefixer("saltText");
var Text = (0, import_react.forwardRef)(({ as, children, className, disabled = false, maxRows, style, styleAs, variant = "primary", ...restProps }, ref) => {
	useComponentCssInjection$1({
		testId: "salt-text",
		css: css_248z$31,
		window: useWindow()
	});
	const Component = as || "div";
	const textStyles = {
		"--text-max-rows": maxRows,
		...style
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Component, {
		className: clsx(withBaseName$33(), {
			[withBaseName$33("disabled")]: disabled,
			[withBaseName$33("lineClamp")]: maxRows,
			[withBaseName$33(styleAs || "")]: styleAs,
			[withBaseName$33(variant)]: variant
		}, className),
		...restProps,
		ref,
		style: textStyles,
		children
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/text/Display.js
var Display1 = (0, import_react.forwardRef)(function Display12({ children, className, styleAs = "display1", ...rest }, ref) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		as: "span",
		styleAs,
		ref,
		...rest,
		children
	});
});
var Display2 = (0, import_react.forwardRef)(function Display22({ children, className, styleAs = "display2", ...rest }, ref) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		as: "span",
		styleAs,
		ref,
		...rest,
		children
	});
});
var Display3 = (0, import_react.forwardRef)(function Display32({ children, className, styleAs = "display3", ...rest }, ref) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		as: "span",
		styleAs,
		ref,
		...rest,
		children
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/text/Headings.js
var H1 = (0, import_react.forwardRef)(function H12({ children, ...rest }, ref) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		as: "h1",
		ref,
		...rest,
		children
	});
});
var H2 = (0, import_react.forwardRef)(function H22({ children, ...rest }, ref) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		as: "h2",
		ref,
		...rest,
		children
	});
});
var H3 = (0, import_react.forwardRef)(function H32({ children, ...rest }, ref) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		as: "h3",
		ref,
		...rest,
		children
	});
});
var H4 = (0, import_react.forwardRef)(function H42({ children, ...rest }, ref) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		as: "h4",
		ref,
		...rest,
		children
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/text/Label.js
var Label = (0, import_react.forwardRef)(function Label2({ children, ...rest }, ref) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		as: "label",
		ref,
		...rest,
		children
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/text/TextNotation.js
var TextNotation = (0, import_react.forwardRef)(function TextNotation2({ children, className, ...rest }, ref) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		as: "span",
		styleAs: "notation",
		ref,
		...rest,
		children
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/text/TextAction.js
var TextAction = (0, import_react.forwardRef)(function TextAction2({ children, className, ...rest }, ref) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		as: "span",
		styleAs: "action",
		ref,
		...rest,
		children
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/form-field/FormFieldHelperText.css.js
var css_248z$30 = ".saltFormFieldHelperText {\n  cursor: default;\n  display: flex;\n  flex-direction: row;\n  width: 100%;\n}\n\n.saltFormFieldHelperText .saltText {\n  font-style: var(--saltFormField-helperText-fontStyle, var(--salt-editable-help-fontStyle));\n}\n\n.saltFormFieldHelperText-withValidation {\n  column-gap: var(--salt-spacing-75);\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/form-field/FormFieldHelperText.js
var withBaseName$32 = makePrefixer("saltFormFieldHelperText");
var FormFieldHelperText = ({ className, children, ...restProps }) => {
	useComponentCssInjection$1({
		testId: "salt-form-field-helper-text",
		css: css_248z$30,
		window: useWindow()
	});
	const { a11yProps, disabled, readOnly, validationStatus } = useFormFieldProps();
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		className: clsx(withBaseName$32(), { [withBaseName$32("withValidation")]: validationStatus }, className),
		children: [!disabled && !readOnly && validationStatus && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(StatusIndicator, { status: validationStatus }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
			disabled,
			id: a11yProps == null ? void 0 : a11yProps["aria-describedby"],
			variant: "secondary",
			styleAs: "label",
			...restProps,
			children
		})]
	});
};
//#endregion
//#region node_modules/@salt-ds/core/dist-es/form-field/FormFieldLabel.css.js
var css_248z$29 = ".saltFormFieldLabel.saltText {\n  font-weight: var(--saltFormField-label-fontWeight, var(--salt-text-fontWeight-strong));\n  width: 100%;\n}\n\n.saltFormFieldLabel:hover + .saltFormField-controls * {\n  --input-borderColor: var(--input-borderColor-hover);\n}\n\n.saltFormField-labelLeft .saltFormFieldLabel.saltText {\n  align-self: baseline;\n  padding-top: var(--salt-spacing-100);\n}\n\n.saltFormFieldLabel-necessityLabel {\n  font-weight: var(--salt-text-fontWeight);\n  font-style: var(--salt-editable-help-fontStyle);\n}\n\n.saltFormFieldLabel-sentence.saltText {\n  color: var(--salt-content-primary-foreground);\n  font-size: var(--salt-text-fontSize);\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/form-field/FormFieldLabel.js
var withBaseName$31 = makePrefixer("saltFormFieldLabel");
var FormFieldLabel = ({ className, children, intent = "label", ...restProps }) => {
	const { a11yProps, disabled, necessity } = useFormFieldProps();
	useComponentCssInjection$1({
		testId: "salt-form-field-label",
		css: css_248z$29,
		window: useWindow()
	});
	const necessityLabel = necessity ? necessity === "asterisk" ? " *" : ` (${capitalize(necessity)})` : void 0;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Label, {
		as: "label",
		className: clsx(withBaseName$31(), withBaseName$31(intent), className),
		id: a11yProps == null ? void 0 : a11yProps["aria-labelledby"],
		disabled,
		variant: "secondary",
		...restProps,
		children: [children, necessityLabel && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
			className: withBaseName$31("necessityLabel"),
			children: necessityLabel
		})]
	});
};
//#endregion
//#region node_modules/@salt-ds/core/dist-es/status-adornment/ErrorAdornment.js
var ErrorAdornmentIcon = (0, import_react.forwardRef)(function ErrorAdornmentIcon2({ children, className, ...rest }, ref) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("svg", {
		className,
		...rest,
		role: "img",
		ref,
		viewBox: "0 0 8 8",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", {
			fillRule: "evenodd",
			clipRule: "evenodd",
			d: "M4 8C6.20914 8 8 6.20914 8 4C8 1.79086 6.20914 0 4 0C1.79086 0 0 1.79086 0 4C0 6.20914 1.79086 8 4 8Z"
		})
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/status-adornment/SuccessAdornment.js
var SuccessAdornmentIcon = (0, import_react.forwardRef)(function SuccessAdornmentIcon2({ children, className, ...rest }, ref) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("svg", {
		className,
		...rest,
		role: "img",
		viewBox: "0 0 10 8",
		ref,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", {
			fillRule: "evenodd",
			clipRule: "evenodd",
			d: "M2.92089 5.95735L8.96399 0L10 1.02133L2.92088 8L0 5.1205L1.03602 4.09918L2.92089 5.95735Z"
		})
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/status-adornment/WarningAdornment.js
var WarningAdornmentIcon = (0, import_react.forwardRef)(function WarningAdornmentIcon2({ children, className, ...rest }, ref) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("svg", {
		className,
		...rest,
		role: "img",
		viewBox: "0 0 9 8",
		ref,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", { d: "M0 8L4.5 0L9 8H0Z" })
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/status-adornment/StatusAdornment.css.js
var css_248z$28 = ".saltStatusAdornment {\n  align-self: center;\n  display: inline-block;\n  fill: var(--saltStatusAdornment-color, var(--statusAdornment-color));\n  height: var(--salt-size-adornment);\n  min-height: var(--salt-size-adornment);\n  padding-left: var(--salt-spacing-100);\n  position: relative;\n}\n\n.saltStatusAdornment-error {\n  --statusAdornment-color: var(--salt-status-error-foreground);\n}\n\n.saltStatusAdornment-warning {\n  --statusAdornment-color: var(--salt-status-warning-foreground);\n}\n\n.saltStatusAdornment-success {\n  --statusAdornment-color: var(--salt-status-success-foreground);\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/status-adornment/StatusAdornment.js
var icons = {
	error: ErrorAdornmentIcon,
	warning: WarningAdornmentIcon,
	success: SuccessAdornmentIcon
};
var statusToAriaLabelMap = {
	error: "error",
	warning: "warning",
	success: "success"
};
var withBaseName$30 = makePrefixer("saltStatusAdornment");
var StatusAdornment = (0, import_react.forwardRef)(function StatusAdornment2({ className, status, ...restProps }, ref) {
	const AdornmentComponent = icons[status];
	const ariaLabel = statusToAriaLabelMap[status];
	useComponentCssInjection$1({
		testId: "salt-status-adornment",
		css: css_248z$28,
		window: useWindow()
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AdornmentComponent, {
		className: clsx(withBaseName$30(), withBaseName$30(status), className),
		"aria-label": ariaLabel,
		...restProps,
		ref
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/input/Input.css.js
var css_248z$27 = "/* Style applied to the root element */\n.saltInput {\n  --input-borderColor: var(--salt-editable-borderColor);\n  --input-borderStyle: var(--salt-editable-borderStyle);\n  --input-outlineColor: var(--salt-focused-outlineColor);\n  --input-borderWidth: var(--salt-size-border);\n\n  align-items: center;\n  background: var(--saltInput-background, var(--input-background));\n  color: var(--saltInput-color, var(--salt-content-primary-foreground));\n  display: inline-flex;\n  font-family: var(--salt-text-fontFamily);\n  font-size: var(--saltInput-fontSize, var(--salt-text-fontSize));\n  height: var(--saltInput-height, var(--salt-size-base));\n  line-height: var(--saltInput-lineHeight, var(--salt-text-lineHeight));\n  min-height: var(--saltInput-minHeight, var(--salt-size-base));\n  min-width: var(--saltInput-minWidth, 4em);\n  padding-left: var(--saltInput-paddingLeft, var(--salt-spacing-100));\n  padding-right: var(--saltInput-paddingRight, var(--salt-spacing-100));\n  position: relative;\n  width: 100%;\n}\n\n.saltInput:hover {\n  --input-borderStyle: var(--salt-editable-borderStyle-hover);\n  --input-borderColor: var(--salt-editable-borderColor-hover);\n\n  background: var(--saltInput-background-hover, var(--input-background-hover));\n  cursor: var(--salt-editable-cursor-hover);\n}\n\n.saltInput:active {\n  --input-borderColor: var(--salt-editable-borderColor-active);\n  --input-borderStyle: var(--salt-editable-borderStyle-active);\n  --input-borderWidth: var(--salt-editable-borderWidth-active);\n\n  background: var(--saltInput-background-active, var(--input-background-active));\n  cursor: var(--salt-editable-cursor-active);\n}\n\n/* Class applied if `variant=\"primary\"` */\n.saltInput-primary {\n  --input-background: var(--salt-editable-primary-background);\n  --input-background-active: var(--salt-editable-primary-background-active);\n  --input-background-hover: var(--salt-editable-primary-background-hover);\n  --input-background-disabled: var(--salt-editable-primary-background-disabled);\n  --input-background-readonly: var(--salt-editable-primary-background-readonly);\n}\n\n/* Class applied if `variant=\"secondary\"` */\n.saltInput-secondary {\n  --input-background: var(--salt-editable-secondary-background);\n  --input-background-active: var(--salt-editable-secondary-background-active);\n  --input-background-hover: var(--salt-editable-secondary-background-active);\n  --input-background-disabled: var(--salt-editable-secondary-background-disabled);\n  --input-background-readonly: var(--salt-editable-secondary-background-readonly);\n}\n\n/* Style applied to input if `validationState=\"error\"` */\n.saltInput-error,\n.saltInput-error:hover {\n  --input-background: var(--salt-status-error-background);\n  --input-background-active: var(--salt-status-error-background);\n  --input-background-hover: var(--salt-status-error-background);\n  --input-borderColor: var(--salt-status-error-borderColor);\n  --input-outlineColor: var(--salt-status-error-borderColor);\n}\n\n/* Style applied to input if `validationState=\"warning\"` */\n.saltInput-warning,\n.saltInput-warning:hover {\n  --input-background: var(--salt-status-warning-background);\n  --input-background-active: var(--salt-status-warning-background);\n  --input-background-hover: var(--salt-status-warning-background);\n  --input-borderColor: var(--salt-status-warning-borderColor);\n  --input-outlineColor: var(--salt-status-warning-borderColor);\n}\n\n/* Style applied to input if `validationState=\"success\"` */\n.saltInput-success,\n.saltInput-success:hover {\n  --input-background: var(--salt-status-success-background);\n  --input-background-active: var(--salt-status-success-background);\n  --input-background-hover: var(--salt-status-success-background);\n  --input-borderColor: var(--salt-status-success-borderColor);\n  --input-outlineColor: var(--salt-status-success-borderColor);\n}\n\n/* Style applied to inner input component */\n.saltInput-input {\n  background: none;\n  border: none;\n  box-sizing: content-box;\n  color: inherit;\n  cursor: inherit;\n  display: block;\n  flex: 1;\n  font: inherit;\n  height: 100%;\n  letter-spacing: var(--saltInput-letterSpacing, 0);\n  margin: 0;\n  min-width: 0;\n  overflow: hidden;\n  padding: 0;\n  text-align: var(--input-textAlign);\n  width: 100%;\n}\n\n/* Reset in the  class */\n.saltInput-input:focus {\n  outline: none;\n}\n\n/* Style applied to selected input */\n.saltInput-input::selection {\n  background: var(--salt-content-foreground-highlight);\n}\n\n/* Style applied to placeholder text */\n.saltInput-input::placeholder {\n  color: var(--salt-content-secondary-foreground);\n  font-weight: var(--salt-text-fontWeight-small);\n}\n\n/* Styling when focused */\n.saltInput-focused {\n  --input-borderColor: var(--input-outlineColor);\n  --input-borderWidth: var(--salt-editable-borderWidth-active);\n\n  outline: var(--saltInput-outline, var(--salt-focused-outlineWidth) var(--salt-focused-outlineStyle) var(--input-outlineColor));\n}\n\n/* Style applied if `readOnly={true}` */\n.saltInput.saltInput-readOnly {\n  --input-borderColor: var(--salt-editable-borderColor-readonly);\n  --input-borderStyle: var(--salt-editable-borderStyle-readonly);\n  --input-borderWidth: var(--salt-size-border);\n\n  background: var(--input-background-readonly);\n  cursor: var(--salt-editable-cursor-readonly);\n}\n\n/* Styling when focused if `disabled={true}` */\n.saltInput-focused.saltInput-disabled {\n  --input-borderWidth: var(--salt-size-border);\n  outline: none;\n}\n\n/* Styling when focused if `readOnly={true}` */\n.saltInput-focused.saltInput-readOnly {\n  --input-borderWidth: var(--salt-size-border);\n}\n\n/* Style applied to selected input if `disabled={true}` */\n.saltInput-disabled .saltInput-input::selection {\n  background: none;\n}\n\n/* Style applied to input if `disabled={true}` */\n.saltInput.saltInput-disabled,\n.saltInput.saltInput-disabled:hover,\n.saltInput.saltInput-disabled:active {\n  --input-borderColor: var(--salt-editable-borderColor-disabled);\n  --input-borderStyle: var(--salt-editable-borderStyle-disabled);\n  --input-borderWidth: var(--salt-size-border);\n\n  background: var(--input-background-disabled);\n  cursor: var(--salt-editable-cursor-disabled);\n  color: var(--saltInput-color-disabled, var(--salt-content-primary-foreground-disabled));\n}\n\n.saltInput-activationIndicator {\n  left: 0;\n  bottom: 0;\n  width: 100%;\n  position: absolute;\n  border-bottom: var(--input-borderWidth) var(--input-borderStyle) var(--input-borderColor);\n}\n\n/* Style applied to start adornments */\n.saltInput-startAdornmentContainer {\n  align-items: center;\n  display: inline-flex;\n  padding-right: var(--salt-spacing-100);\n  column-gap: var(--salt-spacing-100);\n}\n\n/* Style applied to end adornments */\n.saltInput-endAdornmentContainer {\n  align-items: center;\n  display: inline-flex;\n  padding-left: var(--salt-spacing-100);\n  column-gap: var(--salt-spacing-100);\n}\n\n.saltInput-readOnly .saltInput-startAdornmentContainer {\n  margin-left: var(--salt-spacing-50);\n}\n\n.saltInput-startAdornmentContainer .saltButton ~ .saltButton {\n  margin-left: calc(-1 * var(--salt-spacing-50));\n}\n.saltInput-endAdornmentContainer .saltButton ~ .saltButton {\n  margin-left: calc(-1 * var(--salt-spacing-50));\n}\n\n.saltInput-startAdornmentContainer .saltButton:first-child {\n  margin-left: calc(var(--salt-spacing-50) * -1);\n}\n.saltInput-endAdornmentContainer .saltButton:last-child {\n  margin-right: calc(var(--salt-spacing-50) * -1);\n}\n\n.saltInput-startAdornmentContainer > .saltButton,\n.saltInput-endAdornmentContainer > .saltButton {\n  --saltButton-padding: var(--salt-spacing-50);\n  --saltButton-height: calc(var(--salt-size-base) - var(--salt-spacing-100));\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/input/Input.js
var withBaseName$29 = makePrefixer("saltInput");
var Input = (0, import_react.forwardRef)(function Input2({ "aria-activedescendant": ariaActiveDescendant, "aria-expanded": ariaExpanded, "aria-owns": ariaOwns, className: classNameProp, disabled, emptyReadOnlyMarker = "—", endAdornment, id, inputProps = {}, inputRef, placeholder, readOnly: readOnlyProp, role, startAdornment, style, textAlign = "left", value: valueProp, defaultValue: defaultValueProp = valueProp === void 0 ? "" : void 0, validationStatus: validationStatusProp, variant = "primary", ...other }, ref) {
	useComponentCssInjection$1({
		testId: "salt-input",
		css: css_248z$27,
		window: useWindow()
	});
	const { a11yProps: { "aria-describedby": formFieldDescribedBy, "aria-labelledby": formFieldLabelledBy } = {}, disabled: formFieldDisabled, readOnly: formFieldReadOnly, necessity: formFieldRequired, validationStatus: formFieldValidationStatus } = useFormFieldProps();
	const restA11yProps = {
		"aria-activedescendant": ariaActiveDescendant,
		"aria-expanded": ariaExpanded,
		"aria-owns": ariaOwns
	};
	const isDisabled = disabled || formFieldDisabled;
	const isReadOnly = readOnlyProp || formFieldReadOnly;
	const validationStatus = formFieldValidationStatus != null ? formFieldValidationStatus : validationStatusProp;
	const [focused, setFocused] = (0, import_react.useState)(false);
	const defaultValue = isReadOnly && !defaultValueProp && !valueProp ? emptyReadOnlyMarker : defaultValueProp;
	const { "aria-describedby": inputDescribedBy, "aria-labelledby": inputLabelledBy, onBlur, onChange, onFocus, required: inputPropsRequired, ...restInputProps } = inputProps;
	const isRequired = formFieldRequired ? ["required", "asterisk"].includes(formFieldRequired) : inputPropsRequired;
	const [value, setValue] = useControlled({
		controlled: valueProp,
		default: defaultValue,
		name: "Input",
		state: "value"
	});
	const handleChange = (event) => {
		const value2 = event.target.value;
		setValue(value2);
		onChange?.(event);
	};
	const handleBlur = (event) => {
		onBlur?.(event);
		setFocused(false);
	};
	const handleFocus = (event) => {
		onFocus?.(event);
		setFocused(true);
	};
	const inputStyle = {
		"--input-textAlign": textAlign,
		...style
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		className: clsx(withBaseName$29(), withBaseName$29(variant), {
			[withBaseName$29("focused")]: !isDisabled && focused,
			[withBaseName$29("disabled")]: isDisabled,
			[withBaseName$29("readOnly")]: isReadOnly,
			[withBaseName$29(validationStatus || "")]: validationStatus
		}, classNameProp),
		ref,
		style: inputStyle,
		...other,
		children: [
			startAdornment && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: withBaseName$29("startAdornmentContainer"),
				children: startAdornment
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("input", {
				"aria-describedby": clsx(formFieldDescribedBy, inputDescribedBy),
				"aria-labelledby": clsx(formFieldLabelledBy, inputLabelledBy),
				className: clsx(withBaseName$29("input"), inputProps == null ? void 0 : inputProps.className),
				disabled: isDisabled,
				id,
				readOnly: isReadOnly,
				ref: inputRef,
				role,
				tabIndex: isDisabled ? -1 : 0,
				onBlur: handleBlur,
				onChange: handleChange,
				onFocus: !isDisabled ? handleFocus : void 0,
				placeholder,
				value,
				...restA11yProps,
				...restInputProps,
				required: isRequired
			}),
			!isDisabled && !isReadOnly && validationStatus && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(StatusAdornment, { status: validationStatus }),
			endAdornment && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: withBaseName$29("endAdornmentContainer"),
				children: endAdornment
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { className: withBaseName$29("activationIndicator") })
		]
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/link/Link.css.js
var css_248z$26 = ".saltLink {\n  --link-color-hover: var(--saltLink-color-hover, var(--salt-content-foreground-hover));\n  --link-color-active: var(--saltLink-color-active, var(--salt-content-foreground-active));\n  --link-color-visited: var(--saltLink-color-visited, var(--salt-content-foreground-visited));\n  /* When focused, we also want hover style */\n  --link-color-focus: var(--saltLink-color-focus, var(--link-color-hover));\n\n  --link-textDecoration: var(--salt-navigable-textDecoration);\n  --link-textDecoration-hover: var(--salt-navigable-textDecoration-hover);\n\n  --link-fontFamily: var(--salt-text-fontFamily);\n  --link-focus-outline: var(--salt-focused-outline);\n}\n\n/* Main css class */\n.saltLink {\n  color: var(--link-color);\n  letter-spacing: 0px;\n  text-decoration: var(--link-textDecoration);\n  font-family: var(--link-fontFamily);\n}\n\n/* Primary variant */\n.saltText-primary {\n  --link-color: var(--salt-content-primary-foreground);\n}\n\n/* Secondary variant */\n.saltText-secondary {\n  --link-color: var(--salt-content-secondary-foreground);\n}\n\n/* External link's icon */\n.saltLink-icon {\n  margin-left: var(--salt-size-adornmentGap);\n  margin-bottom: -2px;\n}\n\n/* Set color for visited link */\n.saltLink:visited {\n  color: var(--link-color-visited);\n}\n\n/* Set color for hovered link */\n.saltLink:hover {\n  color: var(--link-color-hover);\n  text-decoration: var(--link-textDecoration-hover);\n}\n\n/* Set color for active link */\n.saltLink:active {\n  color: var(--link-color-active);\n}\n\n/* Set color for focused link */\n.saltLink:focus {\n  outline: var(--link-focus-outline);\n  color: var(--link-color-focus);\n  text-decoration: var(--link-textDecoration-hover);\n}\n\n/* Class for ADA content */\n.saltLink-externalLinkADA {\n  display: block;\n  position: absolute;\n  width: 1px;\n  height: 1px;\n  overflow: hidden;\n  top: 0px;\n  left: 0px;\n  opacity: 0;\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/link/Link.js
var withBaseName$28 = makePrefixer("saltLink");
var Link = (0, import_react.forwardRef)(function Link2({ IconComponent = TearOutIcon, href, className, children, variant = "primary", target = "_self", ...rest }, ref) {
	useComponentCssInjection$1({
		testId: "salt-link",
		css: css_248z$26,
		window: useWindow()
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Text, {
		as: "a",
		className: clsx(withBaseName$28(), className),
		href,
		ref,
		target,
		variant,
		...rest,
		children: [children, target === "_blank" && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [IconComponent && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(IconComponent, {
			className: withBaseName$28("icon"),
			"aria-hidden": true
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
			className: withBaseName$28("externalLinkADA"),
			children: "External"
		})] })]
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/multiline-input/MultilineInput.css.js
var css_248z$25 = "/* Style applied to the root element */\n.saltMultilineInput {\n  --multilineInput-borderColor: var(--salt-editable-borderColor);\n  --multilineInput-borderStyle: var(--salt-editable-borderStyle);\n  --multilineInput-outlineColor: var(--salt-focused-outlineColor);\n  --multilineInput-border: none;\n  --multilineInput-activationIndicator-borderWidth: var(--salt-size-border);\n\n  align-items: center;\n  background: var(--multilineInput-background);\n  border: var(--multilineInput-border);\n  color: var(--salt-content-primary-foreground);\n  display: inline-flex;\n  font-family: var(--salt-text-fontFamily);\n  font-size: var(--salt-text-fontSize);\n  height: auto;\n  line-height: var(--salt-text-lineHeight);\n  min-height: var(--salt-size-base);\n  min-width: 4em;\n  padding-left: var(--salt-spacing-100);\n  padding-right: var(--salt-spacing-100);\n  position: relative;\n  width: 100%;\n}\n\n/* Style applied on hover */\n.saltMultilineInput:hover {\n  --multilineInput-borderStyle: var(--salt-editable-borderStyle-hover);\n  --multilineInput-borderColor: var(--salt-editable-borderColor-hover);\n\n  background: var(--multilineInput-background-hover);\n  cursor: var(--salt-editable-cursor-hover);\n}\n\n/* Style applied when active */\n.saltMultilineInput:active {\n  --multilineInput-borderColor: var(--salt-editable-borderColor-active);\n  --multilineInput-borderStyle: var(--salt-editable-borderStyle-active);\n  --multilineInput-activationIndicator-borderWidth: var(--salt-editable-borderWidth-active);\n\n  background: var(--multilineInput-background-active);\n  cursor: var(--salt-editable-cursor-active);\n}\n\n/* Class applied if `variant=\"primary\"` */\n.saltMultilineInput-primary {\n  --multilineInput-background: var(--salt-editable-primary-background);\n  --multilineInput-background-active: var(--salt-editable-primary-background-active);\n  --multilineInput-background-hover: var(--salt-editable-primary-background-hover);\n  --multilineInput-background-disabled: var(--salt-editable-primary-background-disabled);\n  --multilineInput-background-readonly: var(--salt-editable-primary-background-readonly);\n}\n\n/* Class applied if `variant=\"secondary\"` */\n.saltMultilineInput-secondary {\n  --multilineInput-background: var(--salt-editable-secondary-background);\n  --multilineInput-background-active: var(--salt-editable-secondary-background-active);\n  --multilineInput-background-hover: var(--salt-editable-secondary-background-active);\n  --multilineInput-background-disabled: var(--salt-editable-secondary-background-disabled);\n  --multilineInput-background-readonly: var(--salt-editable-secondary-background-readonly);\n}\n\n/* Style applied to input if `validationState=\"error\"` */\n.saltMultilineInput-error,\n.saltMultilineInput-error:hover {\n  --multilineInput-background: var(--salt-status-error-background);\n  --multilineInput-background-active: var(--salt-status-error-background);\n  --multilineInput-background-hover: var(--salt-status-error-background);\n  --multilineInput-borderColor: var(--salt-status-error-borderColor);\n  --multilineInput-outlineColor: var(--salt-status-error-borderColor);\n}\n\n/* Style applied to input if `validationState=\"warning\"` */\n.saltMultilineInput-warning,\n.saltMultilineInput-warning:hover {\n  --multilineInput-background: var(--salt-status-warning-background);\n  --multilineInput-background-active: var(--salt-status-warning-background);\n  --multilineInput-background-hover: var(--salt-status-warning-background);\n  --multilineInput-borderColor: var(--salt-status-warning-borderColor);\n  --multilineInput-outlineColor: var(--salt-status-warning-borderColor);\n}\n\n/* Style applied to input if `validationState=\"success\"` */\n.saltMultilineInput-success,\n.saltMultilineInput-success:hover {\n  --multilineInput-background: var(--salt-status-success-background);\n  --multilineInput-background-active: var(--salt-status-success-background);\n  --multilineInput-background-hover: var(--salt-status-success-background);\n  --multilineInput-borderColor: var(--salt-status-success-borderColor);\n  --multilineInput-outlineColor: var(--salt-status-success-borderColor);\n}\n\n.saltMultilineInput.saltMultilineInput-withAdornmentRow {\n  display: flex;\n  flex-wrap: wrap;\n}\n\n/* Style applied to inner textarea element */\n.saltMultilineInput-textarea {\n  background: none;\n  border: none;\n  box-sizing: border-box;\n  color: inherit;\n  cursor: inherit;\n  flex-grow: 1;\n  font: inherit;\n  letter-spacing: 0;\n  line-height: var(--salt-text-lineHeight);\n  margin: var(--salt-spacing-75) 0;\n  min-width: 0;\n  min-height: 0;\n  resize: vertical;\n  padding: 0;\n}\n\n/* Style applied to placeholder */\n.saltMultilineInput-textarea::placeholder {\n  font-weight: var(--salt-text-fontWeight-small);\n}\n\n/* Reset in the  class */\n.saltMultilineInput-textarea:focus {\n  outline: none;\n}\n\n/* Style applied to selected input */\n.saltMultilineInput-textarea::selection {\n  background: var(--salt-content-foreground-highlight);\n}\n\n/* Styling when focused */\n.saltMultilineInput-focused {\n  --multilineInput-borderColor: var(--multilineInput-outlineColor);\n  --multilineInput-activationIndicator-borderWidth: var(--salt-editable-borderWidth-active);\n\n  outline: var(--salt-focused-outlineWidth) var(--salt-focused-outlineStyle) var(--multilineInput-outlineColor);\n}\n\n/* Style applied if `readOnly={true}` */\n.saltMultilineInput-readOnly,\n.saltMultilineInput-readOnly:active,\n.saltMultilineInput-readOnly:hover {\n  --multilineInput-borderColor: var(--salt-editable-borderColor-readonly);\n  --multilineInput-borderStyle: var(--salt-editable-borderStyle-readonly);\n  --multilineInput-activationIndicator-borderWidth: var(--salt-size-border);\n\n  background: var(--multilineInput-background-readonly);\n  cursor: var(--salt-editable-cursor-readonly);\n}\n\n/* Style applied to selected text if `disabled={true}` */\n.saltMultilineInput-disabled .saltMultilineInput-textarea::selection {\n  background: none;\n}\n\n/* Style applied when `disabled={true}` */\n.saltMultilineInput-disabled,\n.saltMultilineInput-disabled:hover,\n.saltMultilineInput-disabled:active {\n  --multilineInput-borderColor: var(--salt-editable-borderColor-disabled);\n  --multilineInput-borderStyle: var(--salt-editable-borderStyle-disabled);\n  --multilineInput-activationIndicator-borderWidth: var(--salt-size-border);\n\n  background: var(--multilineInput-background-disabled);\n  cursor: var(--salt-editable-cursor-disabled);\n  color: var(--salt-content-primary-foreground-disabled);\n}\n\n/* Style for activation indicator */\n.saltMultilineInput-activationIndicator {\n  left: 0;\n  bottom: 0;\n  width: 100%;\n  position: absolute;\n  border-bottom: var(--multilineInput-activationIndicator-borderWidth) var(--multilineInput-borderStyle) var(--multilineInput-borderColor);\n}\n\n/* Style applied if `bordered={true}` */\n.saltMultilineInput.saltMultilineInput-bordered {\n  --multilineInput-border: var(--salt-size-border) var(--salt-container-borderStyle) var(--multilineInput-borderColor);\n  --multilineInput-activationIndicator-borderWidth: 0;\n}\n\n/* Style applied if active or focused when `bordered={true}` */\n.saltMultilineInput-bordered:active,\n.saltMultilineInput-bordered.saltMultilineInput-focused {\n  --multilineInput-activationIndicator-borderWidth: var(--salt-editable-borderWidth-active);\n}\n\n/* Styling when focused if `disabled={true}` or `readOnly={true}` when `bordered={true}` */\n.saltMultilineInput-bordered.saltMultilineInput-readOnly:hover,\n.saltMultilineInput-bordered.saltMultilineInput-disabled:hover {\n  --multilineInput-activationIndicator-borderWidth: 0;\n}\n\n/* Style applied to adornment containers */\n.saltMultilineInput-endAdornmentContainer,\n.saltMultilineInput-startAdornmentContainer {\n  align-items: center;\n  display: inline-flex;\n  min-height: var(--salt-size-base);\n  column-gap: var(--salt-spacing-100);\n}\n\n/* Style applied to start adornment container */\n.saltMultilineInput-startAdornmentContainer {\n  align-self: self-start;\n  padding-right: var(--salt-spacing-100);\n}\n\n/* Style applied to suffix adornment container */\n.saltMultilineInput-suffixAdornments {\n  align-self: self-end;\n}\n\n/* Style applied to suffix adornment container when end adornments are provided */\n.saltMultilineInput-withAdornmentRow .saltMultilineInput-suffixAdornments {\n  display: inline-flex;\n  flex-basis: 100%;\n  justify-content: flex-end;\n}\n\n/* Style applied to end adornment container */\n.saltMultilineInput-endAdornmentContainer {\n  padding-left: var(--salt-spacing-100);\n}\n\n/* Style applied to status adornment container */\n.saltMultilineInput-statusAdornmentContainer {\n  align-self: self-end;\n  display: inline-flex;\n  min-height: var(--salt-size-base);\n}\n\n/* Style applied to button start adornment if first child */\n.saltMultilineInput-startAdornmentContainer .saltButton:first-child {\n  margin-left: calc(var(--salt-spacing-50) * -1);\n}\n\n/* Style applied to button end adornment if last child */\n.saltMultilineInput-endAdornmentContainer .saltButton:last-child {\n  margin-right: calc(var(--salt-spacing-50) * -1);\n}\n\n/* Style applied to button end adornment if last child */\n.saltMultilineInput-readOnly .saltMultilineInput-endAdornmentContainer .saltButton:last-child {\n  margin-right: 0;\n}\n\n/* Styles for button adornment */\n.saltMultilineInput-startAdornmentContainer > .saltButton,\n.saltMultilineInput-endAdornmentContainer > .saltButton {\n  --saltButton-padding: var(--salt-spacing-50);\n  --saltButton-height: calc(var(--salt-size-base) - var(--salt-spacing-100));\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/multiline-input/MultilineInput.js
var withBaseName$27 = makePrefixer("saltMultilineInput");
var MultilineInput = (0, import_react.forwardRef)(function MultilineInput2({ "aria-activedescendant": ariaActiveDescendant, "aria-expanded": ariaExpanded, "aria-owns": ariaOwns, bordered = false, className: classNameProp, disabled, endAdornment, id, placeholder, readOnly, role, rows = 3, startAdornment, style, textAreaProps = {}, textAreaRef, value: valueProp, defaultValue: defaultValueProp = valueProp === void 0 ? "" : void 0, validationStatus: validationStatusProp, variant = "primary", ...other }, ref) {
	const inputRef = (0, import_react.useRef)(null);
	const handleRef = useForkRef(inputRef, textAreaRef);
	useComponentCssInjection$1({
		testId: "salt-multiline-input",
		css: css_248z$25,
		window: useWindow()
	});
	const restA11yProps = {
		"aria-activedescendant": ariaActiveDescendant,
		"aria-expanded": ariaExpanded,
		"aria-owns": ariaOwns
	};
	const [focused, setFocused] = (0, import_react.useState)(false);
	const { "aria-describedby": textAreaDescribedBy, "aria-labelledby": textAreaLabelledBy, onBlur, onChange, onFocus, required: textAreaRequired, ...restTextAreaProps } = textAreaProps;
	const { a11yProps: { "aria-describedby": formFieldDescribedBy, "aria-labelledby": formFieldLabelledBy } = {}, disabled: formFieldDisabled, readOnly: formFieldReadOnly, necessity: formFieldRequired, validationStatus: formFieldValidationStatus } = useFormFieldProps();
	const isDisabled = disabled || formFieldDisabled;
	const isReadOnly = readOnly || formFieldReadOnly;
	const validationStatus = formFieldValidationStatus != null ? formFieldValidationStatus : validationStatusProp;
	const isRequired = formFieldRequired ? ["required", "asterisk"].includes(formFieldRequired) : textAreaRequired;
	const [value, setValue] = useControlled({
		controlled: valueProp,
		default: defaultValueProp,
		name: "MultilineInput",
		state: "value"
	});
	const previousHeight = (0, import_react.useRef)(void 0);
	const input = inputRef.current;
	const changeHeight = (0, import_react.useCallback)(() => {
		if (!input) return;
		if (!(previousHeight.current !== void 0 && input.style.height !== previousHeight.current)) {
			const previousOverflow = input.style.overflow;
			input.style.overflow = "hidden";
			input.style.height = "auto";
			const newHeight = `${input.scrollHeight + (input.offsetHeight - input.clientHeight)}px`;
			input.style.height = newHeight;
			previousHeight.current = newHeight;
			input.style.overflow = previousOverflow;
		}
	}, [input]);
	const handleChange = (event) => {
		const value2 = event.target.value;
		setValue(value2);
		onChange?.(event);
		changeHeight();
	};
	(0, import_react.useLayoutEffect)(() => {
		changeHeight();
	}, [value, changeHeight]);
	const handleBlur = (event) => {
		onBlur?.(event);
		setFocused(false);
	};
	const handleFocus = (event) => {
		onFocus?.(event);
		setFocused(true);
	};
	const multilineInputStyles = {
		"--saltMultilineInput-rows": rows,
		...style
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		className: clsx(withBaseName$27(), withBaseName$27(variant), {
			[withBaseName$27("withAdornmentRow")]: endAdornment,
			[withBaseName$27("bordered")]: bordered,
			[withBaseName$27("focused")]: !isDisabled && focused,
			[withBaseName$27("disabled")]: isDisabled,
			[withBaseName$27("readOnly")]: isReadOnly,
			[withBaseName$27(validationStatus != null ? validationStatus : "")]: validationStatus
		}, classNameProp),
		ref,
		style: multilineInputStyles,
		...other,
		children: [
			startAdornment && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: withBaseName$27("startAdornmentContainer"),
				children: startAdornment
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("textarea", {
				"aria-describedby": clsx(formFieldDescribedBy, textAreaDescribedBy),
				"aria-labelledby": clsx(formFieldLabelledBy, textAreaLabelledBy),
				className: clsx(withBaseName$27("textarea"), textAreaProps == null ? void 0 : textAreaProps.className),
				disabled: isDisabled,
				id,
				readOnly: isReadOnly,
				ref: handleRef,
				required: isRequired,
				role,
				rows,
				tabIndex: isDisabled ? -1 : 0,
				onBlur: handleBlur,
				onChange: handleChange,
				onFocus: !isDisabled ? handleFocus : void 0,
				placeholder,
				value,
				...restA11yProps,
				...restTextAreaProps
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: withBaseName$27("suffixAdornments"),
				children: [!isDisabled && !isReadOnly && validationStatus && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: withBaseName$27("statusAdornmentContainer"),
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(StatusAdornment, { status: validationStatus })
				}), endAdornment && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: withBaseName$27("endAdornmentContainer"),
					children: endAdornment
				})]
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { className: withBaseName$27("activationIndicator") })
		]
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/navigation-item/ExpansionIcon.js
var iconExpansionMap = {
	vertical: {
		expanded: ChevronDownIcon,
		collapsed: ChevronRightIcon
	},
	horizontal: {
		expanded: ChevronDownIcon,
		collapsed: ChevronDownIcon
	}
};
function ExpansionIcon({ expanded = false, orientation = "horizontal" }) {
	const Icon = iconExpansionMap[orientation][expanded ? "expanded" : "collapsed"];
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Icon, { "aria-hidden": "true" });
}
//#endregion
//#region node_modules/@salt-ds/core/dist-es/navigation-item/ConditionalWrapper.js
var ConditionalWrapper = ({ children, className, parent, expanded, onExpand, active, href }) => {
	const handleExpand = (event) => {
		event.stopPropagation();
		onExpand?.(event);
	};
	return parent || href === void 0 ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
		"aria-label": "expand",
		"aria-expanded": expanded,
		className,
		onClick: handleExpand,
		children
	}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)("a", {
		"aria-current": active ? "page" : void 0,
		href,
		className,
		children
	});
};
//#endregion
//#region node_modules/@salt-ds/core/dist-es/navigation-item/NavigationItem.css.js
var css_248z$24 = "/* Vars applied to root NavigationItem component */\n.saltNavigationItem-wrapper {\n  display: flex;\n  gap: var(--salt-spacing-100);\n  align-items: center;\n  position: relative;\n  background: none;\n  border: none;\n  font-size: var(--salt-text-fontSize);\n  text-decoration: none;\n  cursor: var(--salt-selectable-cursor-hover);\n  /* Hover off animation */\n  transition: all var(--salt-duration-instant) ease-in-out;\n}\n\n/* Vars applied to NavigationItem component when root */\n.saltNavigationItem-rootItem {\n  font-weight: var(--salt-text-fontWeight-strong);\n}\n\n/* Styles applied to NavigationItem icon */\n.saltNavigationItem-label .saltIcon {\n  top: var(--salt-spacing-25);\n}\n\n/* Styles applied when orientation = \"horizontal\" */\n.saltNavigationItem-horizontal {\n  min-height: calc(var(--salt-size-base) + var(--salt-spacing-100) * 2);\n  padding: 0 var(--salt-spacing-100);\n  width: fit-content;\n}\n\n/* Styles applied when orientation = \"vertical\" */\n.saltNavigationItem-vertical {\n  min-height: calc(var(--salt-size-base) + var(--salt-spacing-50) * 2);\n  padding-top: 0;\n  padding-bottom: 0;\n  padding-right: var(--salt-spacing-100);\n  padding-left: calc(var(--salt-spacing-300) * (min(var(--saltNavigationItem-level, 0) + 1, 2)));\n  width: 100%;\n}\n\n/* Styles applied to NavigationItem label */\n.saltNavigationItem-label {\n  color: var(--salt-content-primary-foreground);\n  line-height: var(--salt-text-lineHeight);\n  font-family: var(--salt-text-fontFamily);\n  padding-left: calc(var(--saltNavigationItem-level, 0) * var(--salt-spacing-100));\n  flex: 1;\n  text-align: left;\n  display: flex;\n  align-items: baseline;\n  gap: var(--salt-spacing-100);\n}\n\n/* Styles applied when orientation = \"horizontal\" */\n.saltNavigationItem-horizontal {\n  min-height: calc(var(--salt-size-base) + var(--salt-spacing-100) * 2);\n  padding: 0 var(--salt-spacing-100);\n  width: fit-content;\n}\n\n/* Styles applied when orientation = \"vertical\" */\n.saltNavigationItem-vertical {\n  --saltButton-margin: var(--salt-spacing-50) 0;\n\n  min-height: calc(var(--salt-size-base) + var(--salt-spacing-50) * 2);\n  padding-right: var(--salt-spacing-100);\n  padding-left: calc(var(--salt-spacing-300) * (min(var(--saltNavigationItem-level, 0) + 1, 2)));\n  width: 100%;\n}\n\n/* Styles applied to NavigationItem label */\n.saltNavigationItem-label {\n  color: var(--salt-content-primary-foreground);\n  line-height: var(--salt-text-lineHeight);\n  font-family: var(--salt-text-fontFamily);\n  padding-left: calc(var(--saltNavigationItem-level, 0) * var(--salt-spacing-100));\n  flex: 1;\n  text-align: left;\n  display: flex;\n  align-items: baseline;\n  gap: var(--salt-spacing-100);\n}\n\n/* Styles applied to NavigationItem Badge */\n.saltNavigationItem-label .saltBadge {\n  margin-left: auto;\n}\n\n/* Styles applied to NavigationItem when focus is visible */\n.saltNavigationItem-wrapper:focus-visible {\n  outline: var(--salt-focused-outline);\n}\n\n/* Styles applied to NavigationItem for non-keyboard focus */\n.saltNavigationItem-wrapper:focus:not(:focus-visible) {\n  outline: 0;\n}\n\n/* Styles applied to activation line */\n.saltNavigationItem-wrapper::after {\n  content: \"\";\n  position: absolute;\n  top: var(--salt-spacing-25);\n  left: 0;\n  display: block;\n}\n\n/* Styles applied to activation line when orientation = \"horizontal\" */\n.saltNavigationItem-horizontal::after {\n  width: 100%;\n  height: var(--salt-size-indicator);\n}\n\n/* Styles applied to activation line when orientation = \"vertical\" */\n.saltNavigationItem-vertical::after {\n  width: var(--salt-size-indicator);\n  left: var(--salt-spacing-25);\n  height: calc(100% - var(--salt-spacing-50));\n}\n\n/* Styles applied to activation line on hover  */\n.saltNavigationItem-wrapper:hover::after,\n.saltNavigationItem-wrapper:focus-visible::after {\n  background: var(--salt-navigable-indicator-hover);\n  /* Hover on animation */\n  transition: all var(--salt-duration-perceptible) ease-in-out;\n}\n\n/* Styles applied to activation line when item is active */\n.saltNavigationItem-active::after,\n.saltNavigationItem-active:hover::after,\n.saltNavigationItem-active:focus::after {\n  background: var(--salt-navigable-indicator-active);\n  /* Hover on animation */\n  transition: all var(--salt-duration-perceptible) ease-in-out;\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/navigation-item/NavigationItem.js
var withBaseName$26 = makePrefixer("saltNavigationItem");
var NavigationItem = (0, import_react.forwardRef)(function NavigationItem2(props, ref) {
	const { active, blurActive, children, className, expanded = false, orientation = "horizontal", parent, level = 0, onExpand, href, style: styleProp, ...rest } = props;
	useComponentCssInjection$1({
		testId: "salt-navigation-item",
		css: css_248z$24,
		window: useWindow()
	});
	const style = {
		...styleProp,
		"--saltNavigationItem-level": `${level}`
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		ref,
		className: clsx(withBaseName$26(), className),
		style,
		...rest,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(ConditionalWrapper, {
			className: clsx(withBaseName$26("wrapper"), {
				[withBaseName$26("active")]: active || blurActive,
				[withBaseName$26("blurActive")]: blurActive,
				[withBaseName$26("rootItem")]: level === 0
			}, withBaseName$26(orientation)),
			parent,
			expanded,
			onExpand,
			active,
			href,
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
				className: withBaseName$26("label"),
				children
			}), parent && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ExpansionIcon, {
				expanded,
				orientation
			})]
		})
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/pagination/PaginationContext.js
var paginationContext = (0, import_react.createContext)({
	page: 0,
	count: 0,
	onPageChange: () => void 0
});
paginationContext.displayName = "PaginationContext";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/pagination/Pagination.css.js
var css_248z$23 = ".saltPagination {\n  display: flex;\n  flex-direction: row;\n  align-items: center;\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/pagination/Pagination.js
var withBaseName$25 = makePrefixer("saltPagination");
var { Provider } = paginationContext;
var Pagination = (0, import_react.forwardRef)(function Pagination2({ className, count, children, defaultPage = 1, page: pageProp, onPageChange: onPageChangeProp, ...restProps }, ref) {
	useComponentCssInjection$1({
		testId: "salt-pagination",
		css: css_248z$23,
		window: useWindow()
	});
	const [pageState, setPageState] = useControlled({
		controlled: pageProp,
		default: defaultPage,
		name: "Pagination",
		state: "page"
	});
	const onPageChange = (0, import_react.useCallback)((event, page) => {
		setPageState(page);
		onPageChangeProp && onPageChangeProp(event, page);
	}, [onPageChangeProp, setPageState]);
	const contextValue = (0, import_react.useMemo)(() => ({
		page: pageState,
		count,
		onPageChange
	}), [
		pageState,
		count,
		onPageChange
	]);
	const { announce } = useAriaAnnouncer();
	const mounted = (0, import_react.useRef)(false);
	(0, import_react.useEffect)(() => {
		if (mounted.current) announce(`Page ${pageState}`);
		else mounted.current = true;
	}, [announce, pageState]);
	if (count < 2) return null;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Provider, {
		value: contextValue,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("nav", {
			className: clsx(withBaseName$25(), className),
			ref,
			...restProps,
			children
		})
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/pagination/usePaginationContext.js
var usePaginationContext = () => {
	const context = (0, import_react.useContext)(paginationContext);
	if (!context) console.error(`usePaginationContext should be used inside of Pagination`);
	return context;
};
//#endregion
//#region node_modules/@salt-ds/core/dist-es/pagination/PageButton.css.js
var css_248z$22 = ".saltPageButton {\n  --saltButton-minWidth: var(--salt-size-base);\n  --saltButton-fontWeight: var(--salt-text-fontWeight);\n  --saltButton-height: var(--salt-size-base);\n  --saltButton-text-color: var(--salt-content-primary-foreground);\n  --saltButton-background-active: var(--salt-selectable-background-selected);\n  --saltButton-text-color-active: var(--salt-content-primary-foreground);\n}\n\n.saltPageButton:hover,\n.saltPageButton:focus-visible {\n  --saltButton-background-hover: var(--salt-selectable-background-hover);\n}\n\n.saltPageButton:disabled {\n  --saltButton-text-color-disabled: var(--salt-content-secondary-foreground);\n  --saltButton-cursor-disabled: var(--salt-editable-cursor-readonly);\n}\n\n.saltPageButton-selected {\n  --saltButton-background: var(--salt-selectable-background-selected);\n}\n.saltPageButton-selected:focus-visible {\n  --saltButton-background-hover: var(--salt-selectable-background-selected);\n}\n\n.saltPageButton-fixed {\n  --saltButton-padding: 0;\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/pagination/PageButton.js
var withBaseName$24 = makePrefixer("saltPageButton");
var PageButton = (0, import_react.forwardRef)(function PageButton2({ page, selected, disabled }, ref) {
	useComponentCssInjection$1({
		testId: "salt-page-button",
		css: css_248z$22,
		window: useWindow()
	});
	const { count, onPageChange } = usePaginationContext();
	const onClick = (event) => {
		onPageChange(event, page);
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
		"aria-label": `Page ${page} of ${count}`,
		"aria-current": selected ? "page" : void 0,
		variant: "secondary",
		className: clsx(withBaseName$24(), {
			[withBaseName$24("selected")]: selected,
			[withBaseName$24("fixed")]: page < 100
		}),
		onClick,
		disabled,
		ref,
		children: page
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/pagination/usePagination.js
function usePagination(page, count, boundaryCount, siblingCount) {
	const isEnoughSpace = 2 * (boundaryCount + siblingCount) + 3 >= count;
	const isLeftEllipsis = !isEnoughSpace && page - boundaryCount - siblingCount > 2;
	const isRightEllipsis = !isEnoughSpace && page + boundaryCount + siblingCount + 1 < count;
	const leftPages = isLeftEllipsis ? [1, boundaryCount] : void 0;
	const rightPages = isRightEllipsis ? [count - boundaryCount + 1, count] : void 0;
	let middlePages;
	if (!isLeftEllipsis) middlePages = [1, isRightEllipsis ? boundaryCount + 2 + 2 * siblingCount : count];
	else if (!isRightEllipsis) middlePages = [count - boundaryCount - 1 - 2 * siblingCount, count];
	else middlePages = [page - siblingCount, page + siblingCount];
	return [
		leftPages,
		middlePages,
		rightPages
	];
}
//#endregion
//#region node_modules/@salt-ds/core/dist-es/pagination/PageRanges.css.js
var css_248z$21 = ".saltPageRanges-ellipsis {\n  display: inline-flex;\n  width: var(--salt-size-base);\n  height: var(--salt-size-base);\n  line-height: var(--salt-size-base);\n  justify-content: center;\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/pagination/PageRanges.js
var withBaseName$23 = makePrefixer("saltPageRanges");
var Ellipsis = () => {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		className: withBaseName$23("ellipsis"),
		children: `\u2026`
	});
};
var mapRange = (range, fn) => {
	const result = [];
	for (let i = range[0]; i <= range[1]; ++i) result.push(fn(i));
	return result;
};
function PageRanges({ siblingCount = 2, boundaryCount = 1 }) {
	useComponentCssInjection$1({
		testId: "salt-page-ranges",
		css: css_248z$21,
		window: useWindow()
	});
	const { count, page } = usePaginationContext();
	const renderPages = (0, import_react.useCallback)((range) => mapRange(range, (i) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PageButton, {
		page: i,
		selected: page === i
	}, i)), [page]);
	const [leftPages, middlePages, rightPages] = usePagination(page, count, Math.max(1, boundaryCount), siblingCount);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
		leftPages && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [renderPages(leftPages), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Ellipsis, {})] }),
		middlePages && renderPages(middlePages),
		rightPages && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Ellipsis, {}), renderPages(rightPages)] })
	] });
}
//#endregion
//#region node_modules/@salt-ds/core/dist-es/pagination/Paginator.css.js
var css_248z$20 = ".saltPaginator {\n  display: flex;\n  flex-wrap: nowrap;\n}\n\n.saltPaginator-arrowButton-previous {\n  --saltButton-margin: 0 var(--salt-spacing-100) 0 0;\n}\n\n.saltPaginator-arrowButton-next {\n  --saltButton-margin: 0 0 0 var(--salt-spacing-100);\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/pagination/Paginator.js
var withBaseName$22 = makePrefixer("saltPaginator");
var Paginator = (0, import_react.forwardRef)(function Paginator2({ className, boundaryCount, siblingCount, ...restProps }, ref) {
	useComponentCssInjection$1({
		testId: "salt-paginator",
		css: css_248z$20,
		window: useWindow()
	});
	const { count, page, onPageChange } = usePaginationContext();
	const onPreviousPage = (event) => {
		onPageChange(event, Math.max(1, page - 1));
	};
	const onNextPage = (event) => {
		onPageChange(event, Math.min(page + 1, count));
	};
	const isOnFirstPage = page === 1;
	const isOnLastPage = page === count;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		className: clsx(withBaseName$22(), className),
		...restProps,
		ref,
		children: [
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
				variant: "secondary",
				"aria-label": "Previous Page",
				onClick: onPreviousPage,
				disabled: isOnFirstPage,
				className: withBaseName$22("arrowButton-previous"),
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChevronLeftIcon, { "aria-hidden": true })
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(PageRanges, {
				siblingCount,
				boundaryCount
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
				variant: "secondary",
				"aria-label": "Next Page",
				onClick: onNextPage,
				disabled: isOnLastPage,
				className: withBaseName$22("arrowButton-next"),
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChevronRightIcon, { "aria-hidden": true })
			})
		]
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/pagination/CompactInput.css.js
var css_248z$19 = ".saltCompactInput {\n  --saltInput-paddingLeft: 0;\n  --saltInput-paddingRight: 0;\n}\n\n.saltCompactInput-defaultSize.saltInput {\n  --saltInput-minWidth: var(--salt-size-base);\n  width: var(--salt-size-base);\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/pagination/CompactInput.js
var withBaseName$21 = makePrefixer("saltCompactInput");
var CompactInput = (0, import_react.forwardRef)(function CompactInput2(props, ref) {
	useComponentCssInjection$1({
		testId: "salt-compact-input",
		css: css_248z$19,
		window: useWindow()
	});
	const { count, page, onPageChange } = usePaginationContext();
	const [inputValue, setInputValue] = (0, import_react.useState)(`${page}`);
	(0, import_react.useEffect)(() => {
		setInputValue(`${page}`);
	}, [page]);
	const handleChange = (event) => {
		setInputValue(event.target.value);
	};
	const handleKeyDown = (event) => {
		if (event.key === "Enter") {
			const pageValue = Number(inputValue);
			if (!isNaN(pageValue) && pageValue <= count && pageValue > 0) onPageChange(event, pageValue);
			else setInputValue(`${page}`);
		}
	};
	const handleBlur = () => {
		setInputValue(`${page}`);
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Input, {
		className: clsx(withBaseName$21(), { [withBaseName$21("defaultSize")]: count < 100 }),
		inputProps: {
			"aria-label": `Go to page, ${count} total`,
			style: { width: `${`${count}`.length}ch` }
		},
		onBlur: handleBlur,
		onChange: handleChange,
		onKeyDown: handleKeyDown,
		value: inputValue,
		textAlign: "center",
		ref,
		...props
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/pagination/CompactPaginator.css.js
var css_248z$18 = ".saltCompactPaginator {\n  flex-wrap: nowrap;\n  display: flex;\n  gap: var(--salt-spacing-100);\n  align-items: center;\n}\n\n.saltCompactPaginator-arrowButton {\n  --saltButton-width: var(--salt-size-base);\n  --saltButton-height: var(--salt-size-base);\n  --saltButton-padding: var(--salt-spacing-100);\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/pagination/CompactPaginator.js
var withBaseName$20 = makePrefixer("saltCompactPaginator");
var CompactPaginator = (0, import_react.forwardRef)(function CompactPaginator2({ children, className, ...restProps }, ref) {
	useComponentCssInjection$1({
		testId: "salt-compact-paginator",
		css: css_248z$18,
		window: useWindow()
	});
	const { count, page, onPageChange } = usePaginationContext();
	const onPreviousPage = (event) => {
		onPageChange(event, Math.max(1, page - 1));
	};
	const onNextPage = (event) => {
		onPageChange(event, Math.min(page + 1, count));
	};
	const isOnFirstPage = page === 1;
	const isOnLastPage = page === count;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		className: clsx(withBaseName$20(), className),
		...restProps,
		ref,
		children: [
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
				variant: "secondary",
				"aria-label": "Previous Page",
				onClick: onPreviousPage,
				disabled: isOnFirstPage,
				className: withBaseName$20("arrowButton"),
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChevronLeftIcon, { "aria-hidden": true })
			}),
			children ? children : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PageButton, {
				page,
				disabled: true
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
				as: "span",
				variant: "secondary",
				children: "of"
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(PageButton, {
				page: count,
				disabled: isOnLastPage
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
				variant: "secondary",
				"aria-label": "Next Page",
				onClick: onNextPage,
				disabled: isOnLastPage,
				className: withBaseName$20("arrowButton"),
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChevronRightIcon, { "aria-hidden": true })
			})
		]
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/pagination/GoToInput.css.js
var css_248z$17 = ".saltGoToInput.saltFormField {\n  display: flex;\n  flex-direction: row;\n  gap: var(--salt-spacing-100);\n  align-items: baseline;\n  text-wrap: nowrap;\n}\n\n.saltGoToInput-input {\n  height: var(--salt-size-base);\n  --saltInput-minWidth: unset;\n}\n\n.saltGoToInput-inputDefaultSize {\n  --saltInput-minWidth: var(--salt-size-base);\n  max-width: var(--salt-size-base);\n  width: var(--salt-size-base);\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/pagination/GoToInput.js
var withBaseName$19 = makePrefixer("saltGoToInput");
var GoToInput = (0, import_react.forwardRef)(function GoToInput2({ className, inputRef, inputVariant = "primary", label = "Go to", ...restProps }, ref) {
	useComponentCssInjection$1({
		testId: "salt-go-to-input",
		css: css_248z$17,
		window: useWindow()
	});
	const { count, onPageChange } = usePaginationContext();
	const [inputValue, setInputValue] = (0, import_react.useState)("");
	const onChange = (event) => {
		setInputValue(event.target.value);
	};
	const onKeyDown = (event) => {
		if (event.key === "Enter") {
			const pageValue = Number(inputValue);
			if (!inputValue.startsWith("0") && !isNaN(pageValue) && pageValue > 0 && pageValue <= count) onPageChange(event, pageValue);
			setInputValue("");
		}
	};
	const onBlur = () => {
		setInputValue("");
	};
	const widthCh = `${`${count}`.length}ch`;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(FormField, {
		labelPlacement: "left",
		className: clsx(withBaseName$19(), className),
		ref,
		...restProps,
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(FormFieldLabel, { children: label }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Input, {
			className: clsx(withBaseName$19("input"), { [withBaseName$19("inputDefaultSize")]: count < 100 }),
			ref: inputRef,
			inputProps: { style: { width: widthCh } },
			onBlur,
			onChange,
			onKeyDown,
			value: inputValue,
			textAlign: "center",
			variant: inputVariant
		})]
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/panel/Panel.css.js
var css_248z$16 = "/* Styles applied to the root element when variant=\"primary\" */\n.saltPanel-primary.saltPanel {\n  --panel-background: var(--salt-container-primary-background);\n}\n\n/* Styles applied to the root element when variant=\"secondary\" */\n.saltPanel-secondary.saltPanel {\n  --panel-background: var(--salt-container-secondary-background);\n}\n\n/* Styles applied to the root element */\n.saltPanel {\n  background: var(--saltPanel-background, var(--panel-background));\n  color: var(--saltPanel-color, initial);\n  height: var(--saltPanel-height, 100%);\n  overflow: auto;\n  padding: var(--saltPanel-padding, var(--salt-size-container-spacing));\n  width: var(--saltPanel-width, 100%);\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/panel/Panel.js
var withBaseName$18 = makePrefixer("saltPanel");
var Panel = (0, import_react.forwardRef)(function Panel2({ className, children, variant = "primary", ...restProps }, ref) {
	useComponentCssInjection$1({
		testId: "salt-panel",
		css: css_248z$16,
		window: useWindow()
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		className: clsx(withBaseName$18(), withBaseName$18(variant), className),
		ref,
		...restProps,
		children
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/pill/Pill.css.js
var css_248z$15 = "/* Styles applied to the root element */\n.saltPill {\n  appearance: none;\n  display: inline-flex;\n  align-items: center;\n  background: var(--saltPill-background, var(--salt-actionable-primary-background));\n  border-radius: 0;\n  border: 0;\n  height: calc(var(--salt-size-base) - var(--salt-spacing-100));\n  min-height: var(--salt-text-minHeight);\n  outline: 0;\n  position: relative;\n  gap: var(--salt-spacing-50);\n  padding-left: var(--salt-spacing-50);\n  padding-right: var(--salt-spacing-50);\n  color: var(--saltPill-color, var(--salt-actionable-primary-foreground));\n  font-family: var(--salt-text-fontFamily);\n  font-size: var(--salt-text-fontSize);\n  font-weight: var(--salt-text-fontWeight);\n  line-height: var(--salt-text-lineHeight);\n  overflow: hidden;\n  white-space: nowrap;\n}\n\n/* Style applied to Pill if pill is clickable  */\n.saltPill-clickable {\n  cursor: var(--salt-selectable-cursor-hover);\n}\n\n.saltPill-clickable:hover,\n.saltPill-clickable:focus-visible {\n  color: var(--salt-actionable-primary-foreground-hover);\n  background: var(--salt-actionable-primary-background-hover);\n}\n\n.saltPill-clickable.saltPill-active,\n.saltPill-clickable:active {\n  background: var(--salt-actionable-primary-background-active);\n  color: var(--salt-actionable-primary-foreground-active);\n}\n\n/* Style applied to Pill on focus */\n.saltPill:focus-visible {\n  outline: var(--salt-focused-outline);\n}\n\n/* Style applied to Pill when disabled */\n.saltPill:disabled,\n.saltPill:disabled:hover {\n  color: var(--salt-actionable-primary-foreground-disabled);\n  background: var(--salt-actionable-primary-background-disabled);\n  cursor: var(--salt-selectable-cursor-disabled);\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/pill/Pill.js
var withBaseName$17 = makePrefixer("saltPill");
var Pill = (0, import_react.forwardRef)(function Pill2({ children, className, disabled, onKeyUp, onKeyDown, onClick, onBlur, ...rest }, ref) {
	useComponentCssInjection$1({
		testId: "salt-pill",
		css: css_248z$15,
		window: useWindow()
	});
	const { buttonProps, active } = useButton({
		disabled,
		onKeyUp,
		onKeyDown,
		onClick,
		onBlur
	});
	const { tabIndex, ...restButtonProps } = buttonProps;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
		"data-testid": "pill",
		ref,
		className: clsx(withBaseName$17(), withBaseName$17("clickable"), { [withBaseName$17("active")]: active }, className),
		...restButtonProps,
		...rest,
		children
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/progress/CircularProgress/CircularProgress.css.js
var css_248z$14 = ".saltCircularProgress {\n  color: var(--salt-content-primary-foreground);\n  display: inline-flex;\n  position: relative;\n}\n\n.saltCircularProgress-progressLabel {\n  align-items: center;\n  color: var(--salt-content-primary-foreground);\n  display: flex;\n  height: 100%;\n  justify-content: center;\n  left: 0;\n  position: absolute;\n  width: 100%;\n}\n\n.saltCircularProgress-track {\n  inline-size: calc(var(--salt-size-base) * 3);\n  block-size: calc(var(--salt-size-base) * 3);\n  border-style: var(--salt-track-borderStyle);\n  border-width: var(--salt-size-bar-small);\n  border-radius: calc(var(--salt-size-base) * 3);\n  border-color: var(--salt-track-borderColor);\n}\n\n.saltCircularProgress-bar {\n  inline-size: calc(var(--salt-size-base) * 3);\n  block-size: calc(var(--salt-size-base) * 3);\n  border-style: var(--salt-track-borderStyle);\n  border-width: var(--salt-size-bar-strong);\n  border-radius: calc(var(--salt-size-base) * 3);\n  border-color: var(--salt-accent-background);\n}\n\n.saltCircularProgress-bars {\n  position: absolute;\n  inset-block-start: 0;\n  inset-inline-start: 0;\n  inline-size: 100%;\n  block-size: 100%;\n}\n\n.saltCircularProgress-barOverlayRight,\n.saltCircularProgress-barOverlayLeft {\n  inline-size: 50%;\n  block-size: 100%;\n  transform-origin: 100% center;\n  transform: rotate(180deg);\n  overflow: hidden;\n  position: absolute;\n}\n\n.saltCircularProgress-barSubOverlayRight,\n.saltCircularProgress-barSubOverlayLeft {\n  inline-size: 100%;\n  block-size: 100%;\n  transform-origin: 100% center;\n  overflow: hidden;\n  transform: rotate(-180deg);\n}\n\n.saltCircularProgress-barOverlayLeft {\n  transform: rotate(0deg);\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/progress/CircularProgress/CircularProgress.js
var withBaseName$16 = makePrefixer("saltCircularProgress");
var CircularProgress = (0, import_react.forwardRef)(function CircularProgress2({ className, hideLabel = false, max = 100, min = 0, value = 0, ...rest }, ref) {
	useComponentCssInjection$1({
		testId: "salt-circular-progress",
		css: css_248z$14,
		window: useWindow()
	});
	const subOverlayRightStyle = {};
	const subOverlayLeftStyle = {};
	const getRotationAngle = (progress2, shift = 0) => {
		return -180 + (progress2 - shift) / 50 * 180;
	};
	const progress = (value - min) / (max - min) * 100;
	if (progress <= 50) {
		subOverlayRightStyle.transform = `rotate(${getRotationAngle(progress)}deg)`;
		subOverlayLeftStyle.transform = "rotate(-180deg)";
	} else {
		const rotationAngle = getRotationAngle(progress, 50);
		subOverlayRightStyle.transform = "rotate(0deg)";
		subOverlayLeftStyle.transform = `rotate(${rotationAngle}deg)`;
	}
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		className: clsx(withBaseName$16(), className),
		ref,
		role: "progressbar",
		"aria-valuemax": max,
		"aria-valuemin": min,
		"aria-valuenow": Math.round(value),
		...rest,
		children: [
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { className: withBaseName$16("track") }),
			/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: withBaseName$16("bars"),
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: withBaseName$16("barOverlayRight"),
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
						className: withBaseName$16("barSubOverlayRight"),
						style: subOverlayRightStyle,
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { className: withBaseName$16("bar") })
					})
				}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: withBaseName$16("barOverlayLeft"),
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
						className: withBaseName$16("barSubOverlayLeft"),
						style: subOverlayLeftStyle,
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { className: withBaseName$16("bar") })
					})
				})]
			}),
			!hideLabel && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
				styleAs: "h2",
				className: withBaseName$16("progressLabel"),
				children: `${Math.round(progress)} %`
			})
		]
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/progress/LinearProgress/LinearProgress.css.js
var css_248z$13 = ".saltLinearProgress {\n  align-items: center;\n  color: var(--salt-content-primary-foreground);\n  display: flex;\n  min-width: 400px;\n  font-size: var(--linearProgress-fontSize);\n}\n\n.saltLinearProgress-barContainer {\n  background: none;\n  position: relative;\n  width: 100%;\n  overflow: hidden;\n  height: var(--salt-size-bar-strong);\n}\n\n.saltLinearProgress-bar {\n  width: 100%;\n  position: absolute;\n  left: 0;\n  bottom: 0;\n  top: 0;\n  transition: transform 0.2s linear;\n  transform-origin: left;\n  background: var(--salt-accent-background);\n  z-index: 2;\n}\n\n.saltLinearProgress-track {\n  background: var(--salt-track-borderColor);\n  width: 100%;\n  height: var(--salt-size-bar-small);\n  position: absolute;\n  top: calc((var(--salt-size-bar-strong) - var(--salt-size-bar-small)) / 2);\n  transition: transform 0.2s linear;\n  transform-origin: left;\n}\n\n.saltLinearProgress-progressLabel {\n  color: inherit;\n  margin: 0;\n  white-space: nowrap;\n  padding-left: var(--salt-spacing-100);\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/progress/LinearProgress/LinearProgress.js
var withBaseName$15 = makePrefixer("saltLinearProgress");
var LinearProgress = (0, import_react.forwardRef)(function LinearProgress2({ className, hideLabel = false, max = 100, min = 0, value = 0, ...rest }, ref) {
	useComponentCssInjection$1({
		testId: "salt-linear-progress",
		css: css_248z$13,
		window: useWindow()
	});
	const progress = (value - min) / (max - min) * 100;
	const barStyle = {};
	const trackStyle = {};
	barStyle.transform = `translateX(${progress - 100}%)`;
	trackStyle.transform = `translateX(${progress}%)`;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		className: clsx(withBaseName$15(), className),
		ref,
		role: "progressbar",
		"aria-valuemax": max,
		"aria-valuemin": min,
		"aria-valuenow": Math.round(value),
		...rest,
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: withBaseName$15("barContainer"),
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: withBaseName$15("bar"),
				style: barStyle
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: withBaseName$15("track"),
				style: trackStyle
			})]
		}), !hideLabel && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
			styleAs: "h2",
			className: withBaseName$15("progressLabel"),
			children: `${Math.round(progress)} %`
		})]
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/radio-button/internal/RadioGroupContext.js
var RadioGroupContext = createContext("RadioGroupContext", void 0);
//#endregion
//#region node_modules/@salt-ds/core/dist-es/radio-button/internal/useRadioGroup.js
function useRadioGroup() {
	return (0, import_react.useContext)(RadioGroupContext);
}
//#endregion
//#region node_modules/@salt-ds/core/dist-es/radio-button/RadioButtonIcon.css.js
var css_248z$12 = "/* Styles applied to radio button icon */\n.saltRadioButtonIcon {\n  --radioButton-size: var(--salt-size-selectable);\n  width: var(--radioButton-size);\n  height: var(--radioButton-size);\n  min-width: var(--radioButton-size);\n  min-height: var(--radioButton-size);\n  border-radius: 50%;\n\n  border: var(--salt-size-border) var(--salt-selectable-borderStyle) var(--salt-selectable-borderColor);\n  color: var(--salt-selectable-foreground);\n  background: var(--salt-container-primary-background);\n  --saltIcon-size: 100%;\n  display: flex;\n}\n\n.saltRadioButton:hover .saltRadioButtonIcon {\n  border-color: var(--salt-selectable-borderColor-hover);\n  color: var(--salt-selectable-foreground-hover);\n}\n\n.saltRadioButtonIcon-checked,\n.saltRadioButton:hover .saltRadioButtonIcon-checked {\n  border-color: var(--salt-selectable-borderColor-selected);\n  color: var(--salt-selectable-foreground-selected);\n}\n\n.saltRadioButtonIcon-disabled,\n.saltRadioButton:hover .saltRadioButtonIcon-disabled {\n  border-color: var(--salt-selectable-borderColor-disabled);\n  background: var(--salt-container-primary-background-disabled);\n  color: var(--salt-selectable-foreground-disabled);\n}\n\n.saltRadioButtonIcon-checked.saltRadioButtonIcon-disabled,\n.saltRadioButton:hover .saltRadioButtonIcon-checked.saltRadioButtonIcon-disabled {\n  border-color: var(--salt-selectable-borderColor-selectedDisabled);\n  color: var(--salt-selectable-foreground-selectedDisabled);\n}\n\n.saltRadioButtonIcon-error,\n.saltRadioButton:hover .saltRadioButtonIcon-error {\n  color: var(--salt-status-error-foreground);\n  border-color: var(--salt-status-error-borderColor);\n  outline-color: var(--salt-status-error-foreground);\n}\n\n.saltRadioButtonIcon-warning,\n.saltRadioButton:hover .saltRadioButtonIcon-warning {\n  color: var(--salt-status-warning-foreground);\n  border-color: var(--salt-status-warning-borderColor);\n  outline-color: var(--salt-status-warning-foreground);\n}\n\n.saltRadioButtonIcon-readOnly,\n.saltRadioButton:hover .saltRadioButtonIcon-readOnly {\n  border-color: var(--salt-selectable-borderColor-readonly);\n  color: var(--salt-content-primary-foreground);\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/radio-button/RadioButtonIcon.js
var withBaseName$14 = makePrefixer("saltRadioButtonIcon");
var RadioButtonIcon = ({ checked, className, error, disabled, validationStatus, readOnly, ...rest }) => {
	useComponentCssInjection$1({
		testId: "salt-radio-button-icon",
		css: css_248z$12,
		window: useWindow()
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"aria-hidden": "true",
		className: clsx(withBaseName$14(), {
			[withBaseName$14("checked")]: checked,
			[withBaseName$14("error")]: error,
			[withBaseName$14("disabled")]: disabled,
			[withBaseName$14(validationStatus || "")]: validationStatus,
			[withBaseName$14("readOnly")]: readOnly
		}, className),
		...rest,
		children: [checked && !readOnly && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("svg", {
			xmlns: "http://www.w3.org/2000/svg",
			viewBox: "0 0 12 12",
			fill: "currentColor",
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", {
				fillRule: "evenodd",
				d: "M0 6a6 6 0 1 1 12 0A6 6 0 0 1 0 6Zm6 3a3 3 0 1 1 0-6 3 3 0 0 1 0 6Z",
				clipRule: "evenodd"
			})
		}), checked && readOnly && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("svg", {
			xmlns: "http://www.w3.org/2000/svg",
			fill: "currentColor",
			viewBox: "0 0 12 12",
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", {
				fill: "#161616",
				d: "M9.5 6a3.5 3.5 0 1 1-7 0 3.5 3.5 0 0 1 7 0Z"
			})
		})]
	});
};
//#endregion
//#region node_modules/@salt-ds/core/dist-es/radio-button/RadioButton.css.js
var css_248z$11 = "/* Styles applied to RadioButton container */\n.saltRadioButton {\n  display: flex;\n  gap: var(--salt-spacing-100);\n  cursor: var(--salt-selectable-cursor-hover);\n  position: relative;\n\n  font-size: var(--salt-text-fontSize);\n  line-height: var(--salt-text-lineHeight);\n  font-family: var(--salt-text-fontFamily);\n  font-weight: var(--salt-text-fontWeight);\n\n  --radioButton-icon-marginTop: calc((var(--salt-text-lineHeight) - var(--salt-size-selectable)) / 2);\n}\n\n/* Styles applied when RadioButton is disabled */\n.saltRadioButton-disabled {\n  color: var(--salt-content-primary-foreground-disabled);\n  cursor: var(--salt-selectable-cursor-disabled);\n}\n\n/* Styles applied to root component if `readOnly={true}` */\n.saltRadioButton-readOnly {\n  color: var(--salt-content-primary-foreground);\n  cursor: var(--salt-selectable-cursor-readonly);\n}\n\n/* Styles applied to input component */\n.saltRadioButton-input {\n  cursor: inherit;\n  position: absolute;\n  height: var(--salt-size-selectable);\n  opacity: 0;\n  margin: var(--radioButton-icon-marginTop) 0 0 0;\n  padding: 0;\n  width: var(--salt-size-selectable);\n  z-index: var(--salt-zIndex-default);\n}\n\n.saltRadioButtonIcon {\n  margin-top: var(--radioButton-icon-marginTop);\n}\n\n.saltRadioButtonIcon > svg {\n  /* ensures svg is centered in all browsers */\n  transform: translate(0px, 0px);\n}\n\n/* Styles applied to icon when :focus-visible */\n.saltRadioButton-input:focus-visible + .saltRadioButtonIcon {\n  outline: var(--saltRadioButton-outline, var(--salt-focused-outline));\n  outline-offset: var(--salt-focused-outlineOffset);\n  border-color: var(--salt-selectable-borderColor-hover);\n  color: var(--salt-selectable-foreground-hover);\n}\n\n.saltRadioButton-input:focus-visible + .saltRadioButtonIcon-checked {\n  border-color: var(--salt-selectable-borderColor-selected);\n  color: var(--salt-selectable-foreground-selected);\n}\n\n.saltRadioButton-error .saltRadioButton-input:focus-visible + .saltRadioButtonIcon {\n  outline-color: var(--salt-status-error-foreground);\n  border-color: var(--salt-status-error-foreground);\n  color: var(--salt-status-error-foreground);\n}\n\n.saltRadioButton-warning .saltRadioButton-input:focus-visible + .saltRadioButtonIcon {\n  outline-color: var(--salt-status-warning-foreground);\n  border-color: var(--salt-status-warning-foreground);\n  color: var(--salt-status-warning-foreground);\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/radio-button/RadioButton.js
var withBaseName$13 = makePrefixer("saltRadioButton");
var RadioButton = (0, import_react.forwardRef)(function RadioButton2(props, ref) {
	var _a, _b;
	const { checked: checkedProp, className, disabled: disabledProp, error, inputProps = {}, label, name: nameProp, onFocus, onBlur, onChange, readOnly: readOnlyProp, value, validationStatus: validationStatusProp, ...rest } = props;
	useComponentCssInjection$1({
		testId: "salt-radio-button",
		css: css_248z$11,
		window: useWindow()
	});
	const { a11yProps: formFieldA11yProps, disabled: formFieldDisabled, readOnly: formFieldReadOnly, validationStatus: formFieldValidationStatus } = useFormFieldProps();
	const radioGroup = useRadioGroup();
	const { "aria-describedby": inputDescribedBy, "aria-labelledby": inputLabelledBy, className: inputClassName, onChange: inputOnChange, ...restInputProps } = inputProps;
	const disabled = (radioGroup == null ? void 0 : radioGroup.disabled) || formFieldDisabled || disabledProp;
	const readOnly = (radioGroup == null ? void 0 : radioGroup.readOnly) || formFieldReadOnly || readOnlyProp;
	const validationStatus = !disabled ? (_b = (_a = radioGroup == null ? void 0 : radioGroup.validationStatus) != null ? _a : formFieldValidationStatus) != null ? _b : validationStatusProp : void 0;
	const radioGroupChecked = (radioGroup == null ? void 0 : radioGroup.value) != null && value != null ? radioGroup.value === value : checkedProp;
	const name = nameProp != null ? nameProp : radioGroup == null ? void 0 : radioGroup.name;
	const [checked, setCheckedState] = useControlled({
		controlled: radioGroupChecked,
		default: Boolean(checkedProp),
		name: "RadioBase",
		state: "checked"
	});
	const handleChange = (event) => {
		var _a2;
		if (readOnly) return;
		const newChecked = event.target.checked;
		setCheckedState(newChecked);
		onChange?.(event);
		inputOnChange?.(event);
		(_a2 = radioGroup == null ? void 0 : radioGroup.onChange) == null || _a2.call(radioGroup, event);
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("label", {
		className: clsx(withBaseName$13(), {
			[withBaseName$13("disabled")]: disabled,
			[withBaseName$13("readOnly")]: readOnly,
			[withBaseName$13("error")]: error,
			[withBaseName$13(validationStatus || "")]: validationStatus
		}, className),
		ref,
		...rest,
		children: [
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("input", {
				"aria-describedby": clsx(radioGroup == void 0 ? formFieldA11yProps == null ? void 0 : formFieldA11yProps["aria-describedby"] : void 0, inputDescribedBy) || void 0,
				"aria-labelledby": clsx(radioGroup == void 0 ? formFieldA11yProps == null ? void 0 : formFieldA11yProps["aria-labelledby"] : void 0, inputLabelledBy) || void 0,
				className: clsx(withBaseName$13("input"), inputClassName),
				checked,
				disabled,
				readOnly,
				name,
				value,
				onBlur,
				onChange: handleChange,
				onFocus,
				type: "radio",
				...restInputProps
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(RadioButtonIcon, {
				checked,
				disabled,
				readOnly,
				validationStatus,
				error
			}),
			label
		]
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/radio-button/RadioButtonGroup.css.js
var css_248z$10 = "/* Styles applied to root element */\n.saltRadioButtonGroup {\n  border: none;\n  margin: 0;\n  padding: 0;\n}\n\n/* Styles applied when direction is vertical */\n.saltRadioButtonGroup-vertical {\n  display: flex;\n  gap: var(--salt-spacing-100);\n  flex-direction: column;\n}\n\n/* Styles applied when direction is horizontal */\n.saltRadioButtonGroup-horizontal {\n  display: flex;\n  gap: var(--salt-spacing-200);\n  flex-direction: row;\n  flex-wrap: wrap;\n}\n\n.saltRadioButtonGroup-noWrap {\n  flex-wrap: nowrap;\n}\n.saltRadioButtonGroup-noWrap .saltRadioButton {\n  white-space: break-spaces;\n}\n\n/* Styles applied when radio button group is inside a form field with label left or right */\n.saltFormField-labelLeft .saltRadioButtonGroup,\n.saltFormField-labelRight .saltRadioButtonGroup {\n  padding-top: var(--salt-spacing-100);\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/radio-button/RadioButtonGroup.js
var withBaseName$12 = makePrefixer("saltRadioButtonGroup");
var RadioButtonGroup = (0, import_react.forwardRef)(function RadioButtonGroup2(props, ref) {
	const { "aria-labelledby": ariaLabelledBy, "aria-describedby": ariaDescribedBy, children, className, defaultValue, direction = "vertical", disabled: disabledProp, wrap = true, name: nameProp, onChange, readOnly: readOnlyProp, value: valueProp, validationStatus: validationStatusProp, ...rest } = props;
	useComponentCssInjection$1({
		testId: "salt-radio-button-group",
		css: css_248z$10,
		window: useWindow()
	});
	const { a11yProps, disabled: formFieldDisabled, readOnly: formFieldReadOnly, validationStatus: formFieldValidationStatus } = useFormFieldProps();
	const disabled = formFieldDisabled || disabledProp;
	const readOnly = formFieldReadOnly || readOnlyProp;
	const validationStatus = formFieldValidationStatus != null ? formFieldValidationStatus : validationStatusProp;
	const [value, setStateValue] = useControlled({
		controlled: valueProp,
		default: defaultValue,
		state: "value",
		name: "RadioButtonGroup"
	});
	const handleChange = (event) => {
		setStateValue(event.target.value);
		onChange?.(event);
	};
	const name = useId(nameProp);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("fieldset", {
		role: "radiogroup",
		"aria-readonly": readOnly,
		"aria-labelledby": clsx(a11yProps == null ? void 0 : a11yProps["aria-labelledby"], ariaLabelledBy) || void 0,
		"aria-describedby": clsx(a11yProps == null ? void 0 : a11yProps["aria-describedby"], ariaDescribedBy) || void 0,
		className: clsx(withBaseName$12(), withBaseName$12(direction), { [withBaseName$12("noWrap")]: !wrap }, className),
		"data-testid": "radio-button-group",
		ref,
		...rest,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RadioGroupContext.Provider, {
			value: {
				disabled,
				name,
				onChange: handleChange,
				readOnly,
				validationStatus,
				value
			},
			children
		})
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/scrim/Scrim.css.js
var css_248z$9 = "/* Style applied to the root element */\n.saltScrim {\n  top: 0;\n  left: 0;\n  right: 0;\n  width: 100%;\n  bottom: 0;\n  height: 100%;\n  display: flex;\n  z-index: calc(var(--salt-zIndex-drawer) - 1);\n  position: absolute;\n  align-items: center;\n  justify-content: center;\n  background: var(--saltScrim-background, var(--salt-overlayable-background));\n  -webkit-tap-highlight-color: transparent;\n}\n\n.saltScrim:focus {\n  outline: none;\n}\n\n/* Style applied to the root element when fixed={true} */\n.saltScrim-fixed {\n  position: fixed;\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/scrim/Scrim.js
var withBaseName$11 = makePrefixer("saltScrim");
var Scrim = (0, import_react.forwardRef)(function Scrim2({ className, children, fixed = false, open = true, ...rest }, ref) {
	useComponentCssInjection$1({
		testId: "salt-scrim",
		css: css_248z$9,
		window: useWindow()
	});
	if (!open) return null;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		className: clsx(withBaseName$11(), { [withBaseName$11("fixed")]: fixed }, className),
		"data-testid": "scrim",
		ref,
		...rest,
		children
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/spinner/svgSpinners/SpinnerSVG.js
var withBaseName$10 = makePrefixer("saltSpinner");
var sizeAndStrokeWidthMapping = {
	small: {
		high: {
			width: 12,
			strokeWidth: 2
		},
		medium: {
			width: 12,
			strokeWidth: 2
		},
		low: {
			width: 14,
			strokeWidth: 2
		},
		touch: {
			width: 16,
			strokeWidth: 2
		}
	},
	medium: {
		high: {
			width: 20,
			strokeWidth: 2
		},
		medium: {
			width: 28,
			strokeWidth: 4
		},
		low: {
			width: 36,
			strokeWidth: 6
		},
		touch: {
			width: 44,
			strokeWidth: 8
		}
	},
	large: {
		high: {
			width: 40,
			strokeWidth: 2
		},
		medium: {
			width: 56,
			strokeWidth: 4
		},
		low: {
			width: 72,
			strokeWidth: 6
		},
		touch: {
			width: 88,
			strokeWidth: 8
		}
	}
};
var SpinnerSVG = ({ id = "svg-spinner", rest, size, density }) => {
	const { width, strokeWidth } = sizeAndStrokeWidthMapping[size][density];
	const radius = (width - strokeWidth) / 2;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("svg", {
		className: withBaseName$10("spinner"),
		viewBox: `0 0 ${width} ${width}`,
		id,
		...rest,
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("defs", { children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("linearGradient", {
			id: `${id}-1`,
			x1: "0",
			y1: "0",
			x2: "100%",
			y2: "0",
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("stop", {
				className: withBaseName$10("gradientStop"),
				offset: "15%",
				stopOpacity: "1"
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("stop", {
				className: withBaseName$10("gradientStop"),
				offset: "100%",
				stopOpacity: "0"
			})]
		}) }), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("g", {
			fill: "none",
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", {
				d: `M${width - strokeWidth / 2},${width / 2} a${radius},${radius} 0 1,0 -${width - strokeWidth},0`,
				stroke: "var(--saltSpinner-gradient-color, var(--salt-accent-background)",
				strokeWidth: "var(--spinner-strokeWidth)",
				fill: "none"
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", {
				d: `M${width / 2},${strokeWidth / 2} a${radius},${radius} 0 1,0 0,${width - strokeWidth}`,
				stroke: `url(#${id}-1)`,
				strokeWidth: "var(--spinner-strokeWidth)",
				fill: "none"
			})]
		})]
	});
};
//#endregion
//#region node_modules/@salt-ds/core/dist-es/spinner/Spinner.css.js
var css_248z$8 = "/* Styles applied to the root element */\n.saltSpinner {\n  position: relative;\n}\n\n/* Styles applied to the SVG stop elements */\n.saltSpinner-gradientStop {\n  stop-color: var(--saltSpinner-gradient-color, var(--salt-accent-background));\n}\n\n/* Styles applied when `size=\"medium\"` */\n.saltSpinner-medium {\n  --spinner-strokeWidth: var(--salt-size-bar);\n  height: var(--salt-size-base);\n  width: var(--salt-size-base);\n}\n\n/* Styles applied when `size=\"large\"` */\n.saltSpinner-large {\n  --spinner-strokeWidth: var(--salt-size-bar);\n  height: calc(var(--salt-size-base) * 2);\n  width: calc(var(--salt-size-base) * 2);\n}\n\n/* Styles applied when `size=\"small\"` */\n.saltSpinner-small {\n  --spinner-strokeWidth: var(--salt-size-bar-small);\n  height: var(--salt-size-icon);\n  width: var(--salt-size-icon);\n}\n\n/* Styles applied to the SVG */\n.saltSpinner-spinner {\n  animation: spinner 0.9s linear infinite;\n  width: 100%;\n  height: 100%;\n  position: absolute;\n  left: 0;\n  top: 0;\n}\n\n@keyframes spinner {\n  0% {\n    transform: rotate(0);\n  }\n  100% {\n    transform: rotate(360deg);\n  }\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/spinner/Spinner.js
var SpinnerSizeValues = [
	"default",
	"large",
	"small",
	"medium"
];
var handleSize = (size) => size === "default" ? "medium" : size;
var withBaseName$9 = makePrefixer("saltSpinner");
var Spinner = (0, import_react.forwardRef)(function Spinner2({ "aria-label": ariaLabel = "loading", announcerInterval = 5e3, announcerTimeout = 2e4, completionAnnouncement = `finished ${ariaLabel}`, disableAnnouncer, role = "img", className, size = "medium", id: idProp, ...rest }, ref) {
	const id = useId(idProp);
	useComponentCssInjection$1({
		testId: "salt-spinner",
		css: css_248z$8,
		window: useWindow()
	});
	const { announce } = useAriaAnnouncer();
	const density = useDensity();
	size = handleSize(size);
	(0, import_react.useEffect)(() => {
		if (disableAnnouncer) return;
		announce(ariaLabel);
		const startTime = (/* @__PURE__ */ new Date()).getTime();
		const interval = announcerInterval > 0 && setInterval(() => {
			if ((/* @__PURE__ */ new Date()).getTime() - startTime > announcerTimeout) {
				announce(`${ariaLabel} is still in progress, but will no longer announce.`);
				interval && clearInterval(interval);
				return;
			}
			announce(ariaLabel);
		}, announcerInterval);
		return () => {
			if (disableAnnouncer) return;
			interval && clearInterval(interval);
			if (completionAnnouncement) announce(completionAnnouncement);
		};
	}, [
		announce,
		announcerInterval,
		announcerTimeout,
		ariaLabel,
		completionAnnouncement,
		disableAnnouncer
	]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"aria-label": ariaLabel,
		className: clsx(withBaseName$9(), withBaseName$9(size), className),
		ref,
		role,
		...rest,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SpinnerSVG, {
			size,
			density,
			id
		})
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/stack-layout/StackLayout.css.js
var css_248z$7 = "/* Default variables applied to the root element */\n.saltStackLayout {\n  --stackLayout-gap-multiplier: var(--stackLayout-gap-density-multiplier, 3);\n  --stackLayout-separator-weight: var(--salt-size-border, 1);\n  --stackLayout-gap: calc(var(--salt-size-unit) * var(--stackLayout-gap-multiplier));\n}\n\n.saltStackLayout-separator > * {\n  position: relative;\n}\n.saltStackLayout-separator > *:not(:last-child)::after {\n  position: absolute;\n  display: block;\n  content: \"\";\n  background-color: var(--salt-separable-secondary-borderColor);\n}\n\n/*\n * Seperator offset based on seperator alignment\n * var(--stackLayout-separator-weight) is used to take into account the thickness of the seperator,so even at higher weights it will be at the start/center/end\n*/\n.saltStackLayout-separator-start {\n  --stackLayout-seperator-offset: calc(var(--stackLayout-separator-weight) * -1);\n}\n.saltStackLayout-separator-center {\n  --stackLayout-seperator-offset: calc((var(--stackLayout-gap) * -0.5) + (var(--stackLayout-separator-weight) * -0.5));\n}\n.saltStackLayout-separator-end {\n  --stackLayout-seperator-offset: calc(var(--stackLayout-gap) * -1);\n}\n\n/* When alignment is 'row' and we have seperators (last child doesn't need a seperator) */\n.saltStackLayout-row.saltStackLayout-separator > *:not(:last-child)::after {\n  height: 100%;\n  width: var(--stackLayout-separator-weight);\n  right: var(--stackLayout-seperator-offset);\n  top: 0;\n}\n\n/* When alignment is 'column' and we have seperators (last child doesn't need a seperator) */\n.saltStackLayout-column.saltStackLayout-separator > *:not(:last-child)::after {\n  width: 100%;\n  height: var(--stackLayout-separator-weight);\n  bottom: var(--stackLayout-seperator-offset);\n  left: 0;\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/stack-layout/StackLayout.js
var withBaseName$8 = makePrefixer("saltStackLayout");
var StackLayout = (0, import_react.forwardRef)(({ children, className, direction = "column", gap, separators, style, ...rest }, ref) => {
	useComponentCssInjection$1({
		testId: "salt-stack-layout",
		css: css_248z$7,
		window: useWindow()
	});
	const flexGap = useResponsiveProp(gap, 3);
	const separatorAlignment = separators === true ? "center" : separators;
	const flexDirection = useResponsiveProp(direction, "column");
	const stackLayoutStyles = {
		...style,
		"--stackLayout-gap-multiplier": flexGap
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FlexLayout, {
		className: clsx(withBaseName$8(), withBaseName$8(flexDirection), {
			[withBaseName$8("separator")]: !!separatorAlignment,
			[separatorAlignment ? withBaseName$8(`separator-${separatorAlignment}`) : ""]: separatorAlignment
		}, className),
		ref,
		direction,
		style: stackLayoutStyles,
		wrap: false,
		gap: flexGap,
		...rest,
		children
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/status-indicator/ValidationStatus.js
var VALIDATION_NAMED_STATUS = [
	"error",
	"warning",
	"success",
	"info"
];
//#endregion
//#region node_modules/@salt-ds/core/dist-es/theme/Density.js
var DensityValues = [
	"high",
	"medium",
	"low",
	"touch"
];
//#endregion
//#region node_modules/@salt-ds/core/dist-es/theme/Theme.js
var getCharacteristicValue = (themeName, characteristicName, variant, scopeElement) => {
	const cssVariableName = `--salt-${characteristicName}-${variant}`;
	const scopeTarget = scopeElement || document.body.querySelector(`.${themeName}`);
	if (scopeTarget) {
		const variableValue = getComputedStyle(scopeTarget).getPropertyValue(cssVariableName);
		if (variableValue) return variableValue;
	}
	return null;
};
//#endregion
//#region node_modules/@salt-ds/core/dist-es/theme/Mode.js
var ModeValues = ["light", "dark"];
//#endregion
//#region node_modules/@salt-ds/core/dist-es/toast/Toast.css.js
var css_248z$6 = "/* Styles applied to Toast root element */\n.saltToast {\n  --toast-background: var(--salt-container-primary-background);\n\n  background: var(--saltToast-background, var(--toast-background));\n  border-color: var(--saltToast-borderColor, var(--toast-borderColor));\n  border-width: var(--saltToast-borderWidth, var(--salt-size-border));\n  border-style: var(--saltToast-borderStyle, var(--salt-container-borderStyle));\n\n  box-sizing: border-box;\n  box-shadow: var(--salt-overlayable-shadow-popout);\n  display: flex;\n  gap: var(--salt-spacing-75);\n  padding: var(--saltToast-padding, var(--salt-spacing-100));\n  min-height: calc(var(--salt-size-base) + var(--salt-spacing-100));\n  margin: 0 var(--salt-spacing-100) var(--salt-spacing-100) var(--salt-spacing-100);\n\n  font-family: var(--salt-text-fontFamily);\n  font-size: var(--salt-text-fontSize);\n  font-weight: var(--salt-text-fontWeight);\n  line-height: var(--salt-text-lineHeight);\n}\n\n.saltToast:last-child {\n  margin-bottom: var(--salt-spacing-200);\n}\n\n/* Styles applied to icon */\n.saltToast-icon.saltIcon {\n  margin: calc(var(--salt-spacing-75) + 2px) 0 var(--salt-spacing-75) 0;\n}\n.salt-density-high.saltToast-icon.saltIcon {\n  margin: calc(var(--salt-spacing-75) + 1px) 0 var(--salt-spacing-75) 0;\n}\n\n/* Styles applied when state = \"info\" */\n.saltToast-info {\n  --toast-borderColor: var(--salt-status-info-borderColor);\n}\n\n/* Styles applied when state = \"error\" */\n.saltToast-error {\n  --toast-borderColor: var(--salt-status-error-borderColor);\n}\n\n/* Styles applied when state = \"warning\" */\n.saltToast-warning {\n  --toast-borderColor: var(--salt-status-warning-borderColor);\n}\n\n/* Styles applied when state = \"success\" */\n.saltToast-success {\n  --toast-borderColor: var(--salt-status-success-borderColor);\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/toast/Toast.js
var withBaseName$7 = makePrefixer("saltToast");
var Toast = (0, import_react.forwardRef)(function Toast2(props, ref) {
	const { children, className, status = "info", ...rest } = props;
	useComponentCssInjection$1({
		testId: "salt-toast",
		css: css_248z$6,
		window: useWindow()
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		className: clsx(withBaseName$7(), withBaseName$7(status), className),
		role: "alert",
		...rest,
		ref,
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(StatusIndicator, {
			status,
			className: withBaseName$7("icon")
		}), children]
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/toast/ToastContent.css.js
var css_248z$5 = "/* Styles applied to ToastContent */\n.saltToastContent {\n  flex: 1 0;\n  margin: var(--salt-spacing-75) 0;\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/toast/ToastContent.js
var withBaseName$6 = makePrefixer("saltToastContent");
var ToastContent = (0, import_react.forwardRef)(function ToastContent2({ children, className, ...restProps }, ref) {
	useComponentCssInjection$1({
		testId: "salt-toast-content",
		css: css_248z$5,
		window: useWindow()
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		className: clsx(withBaseName$6(), className),
		...restProps,
		ref,
		children
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/split-layout/SplitLayout.css.js
var css_248z$4 = ".saltSplitLayout {\n  flex-grow: 1;\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/split-layout/SplitLayout.js
var withBaseName$5 = makePrefixer("saltSplitLayout");
var SplitLayout = (0, import_react.forwardRef)(({ endItem, startItem, className, ...rest }, ref) => {
	useComponentCssInjection$1({
		testId: "salt-split-layout",
		css: css_248z$4,
		window: useWindow()
	});
	const justify = endItem && !startItem ? "end" : "space-between";
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(FlexLayout, {
		className: clsx(withBaseName$5(), className),
		ref,
		justify,
		...rest,
		children: [startItem, endItem]
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/switch/Switch.css.js
var css_248z$3 = ".saltSwitch {\n  display: flex;\n  gap: var(--salt-spacing-100);\n  position: relative;\n  cursor: var(--salt-selectable-cursor-hover);\n  color: var(--salt-content-primary-foreground);\n\n  font-family: var(--salt-text-fontFamily);\n  font-size: var(--salt-text-fontSize);\n  font-weight: var(--salt-text-fontWeight);\n  line-height: var(--salt-text-lineHeight);\n}\n\n.saltSwitch-disabled {\n  color: var(--salt-content-primary-foreground-disabled);\n  cursor: var(--salt-selectable-cursor-disabled);\n}\n\n.saltSwitch-track {\n  border: var(--salt-size-border) var(--salt-selectable-borderStyle) var(--salt-selectable-borderColor);\n  --saltIcon-size: 100%;\n  display: flex;\n  flex-shrink: 0;\n  height: calc(var(--salt-size-base) - var(--salt-spacing-100));\n  width: calc((var(--salt-size-selectable) + var(--salt-spacing-25) + var(--salt-size-border)) * 2);\n  align-items: center;\n  color: var(--salt-selectable-foreground);\n  background: var(--salt-container-primary-background);\n}\n\n.saltSwitch-label {\n  padding-top: var(--salt-spacing-25);\n}\n\n/* Styles applied to the icon component if focused */\n.saltSwitch-input:focus-visible + .saltSwitch-track {\n  outline-offset: var(--salt-focused-outlineOffset);\n  outline: var(--salt-focused-outline);\n  border-color: var(--salt-selectable-borderColor-hover);\n  color: var(--salt-selectable-foreground-hover);\n}\n\n.saltSwitch:hover .saltSwitch-track {\n  border-color: var(--salt-selectable-borderColor-hover);\n  color: var(--salt-selectable-foreground-hover);\n}\n\n.saltSwitch-checked .saltSwitch-track,\n.saltSwitch-checked:hover .saltSwitch-track {\n  border-color: var(--salt-selectable-borderColor-selected);\n  color: var(--salt-selectable-foreground-selected);\n}\n\n.saltSwitch-disabled .saltSwitch-track,\n.saltSwitch-disabled:hover .saltSwitch-track {\n  border-color: var(--salt-selectable-borderColor-disabled);\n  color: var(--salt-selectable-foreground-disabled);\n  background: var(--salt-container-primary-background-disabled);\n}\n\n.saltSwitch-checked.saltSwitch-disabled .saltSwitch-track,\n.saltSwitch-checked.saltSwitch-disabled:hover .saltSwitch-track {\n  border-color: var(--salt-selectable-borderColor-selectedDisabled);\n  color: var(--salt-selectable-foreground-selectedDisabled);\n}\n\n.saltSwitch-thumb {\n  display: flex;\n  aspect-ratio: 1;\n  height: var(--salt-size-selectable);\n  background: currentColor;\n  margin: var(--salt-spacing-25);\n  border: var(--salt-size-border) var(--salt-selectable-borderStyle) var(--salt-selectable-borderColor);\n}\n\n.saltSwitch-input:focus-visible + .saltSwitch-track .saltSwitch-thumb,\n.saltSwitch:hover .saltSwitch-thumb {\n  border-color: var(--salt-selectable-borderColor-hover);\n  transition: transform var(--salt-duration-perceptible) ease-in-out;\n}\n\n.saltSwitch-checked .saltSwitch-thumb,\n.saltSwitch-checked:hover .saltSwitch-thumb {\n  background: transparent;\n  border-color: var(--salt-selectable-borderColor-selected);\n  transform: translateX(100%);\n}\n\n.saltSwitch-disabled .saltSwitch-thumb,\n.saltSwitch-disabled:hover .saltSwitch-thumb {\n  border: 0;\n}\n\n/* Styles applied to input element */\n.saltSwitch-input {\n  cursor: inherit;\n  left: 0;\n  margin: 0;\n  opacity: 0;\n  padding: 0;\n  position: absolute;\n  top: 0;\n  z-index: var(--salt-zIndex-default);\n  height: calc(var(--salt-size-base) - var(--salt-spacing-100));\n  width: calc((var(--salt-size-selectable) + var(--salt-spacing-25) + var(--salt-size-border)) * 2);\n}\n\n@media (prefers-reduced-motion) {\n  .saltSwitch-input:focus-visible + .saltSwitch-track .saltSwitch-thumb,\n  .saltSwitch:hover .saltSwitch-thumb {\n    transition: none;\n  }\n}\n\n.salt-density-high .saltSwitch-thumb {\n  border: 0;\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/switch/Switch.js
var withBaseName$4 = makePrefixer("saltSwitch");
function CheckedIcon(props) {
	return useDensity() === "high" ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SuccessSmallSolidIcon, { ...props }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SuccessSolidIcon, { ...props });
}
var Switch = (0, import_react.forwardRef)(function Switch2(props, ref) {
	const { checked: checkedProp, className, defaultChecked, disabled: disabledProp, inputProps = {}, label, name, onBlur, onChange, onFocus, value, ...rest } = props;
	useComponentCssInjection$1({
		testId: "salt-switch",
		css: css_248z$3,
		window: useWindow()
	});
	const { "aria-describedby": inputDescribedBy, "aria-labelledby": inputLabelledBy, className: inputClassName, onChange: inputOnChange, ...restInputProps } = inputProps;
	const [checked, setChecked] = useControlled({
		controlled: checkedProp,
		default: Boolean(defaultChecked),
		name: "Switch",
		state: "checked"
	});
	const { a11yProps: formFieldA11yProps, disabled: formFieldDisabled } = useFormFieldProps();
	const disabled = formFieldDisabled || disabledProp;
	const handleChange = (event) => {
		if (event.nativeEvent.defaultPrevented) return;
		const value2 = event.target.checked;
		setChecked(value2);
		onChange?.(event);
		inputOnChange?.(event);
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("label", {
		className: clsx(withBaseName$4(), {
			[withBaseName$4("disabled")]: disabled,
			[withBaseName$4("checked")]: checked
		}, className),
		ref,
		...rest,
		children: [
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("input", {
				"aria-describedby": clsx(formFieldA11yProps == null ? void 0 : formFieldA11yProps["aria-describedby"], inputDescribedBy),
				"aria-labelledby": clsx(formFieldA11yProps == null ? void 0 : formFieldA11yProps["aria-labelledby"], inputLabelledBy),
				name,
				value,
				checked,
				className: clsx(withBaseName$4("input"), inputClassName),
				defaultChecked,
				disabled,
				onBlur,
				onChange: handleChange,
				onFocus,
				type: "checkbox",
				...restInputProps
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
				className: withBaseName$4("track"),
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
					className: withBaseName$4("thumb"),
					children: checked && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CheckedIcon, {
						"aria-hidden": true,
						className: withBaseName$4("icon")
					})
				})
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
				className: withBaseName$4("label"),
				children: label
			})
		]
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/toggle-button-group/ToggleButtonGroupContext.js
var ToggleButtonGroupContext = createContext("ToggleButtonGroupContext", void 0);
function useToggleButtonGroup() {
	return (0, import_react.useContext)(ToggleButtonGroupContext);
}
//#endregion
//#region node_modules/@salt-ds/core/dist-es/toggle-button-group/ToggleButtonGroup.css.js
var css_248z$2 = ".saltToggleButtonGroup {\n  display: flex;\n  background: var(--salt-container-primary-background);\n  border: var(--salt-size-border) var(--salt-container-borderStyle) var(--salt-container-primary-borderColor);\n  width: fit-content;\n  gap: var(--salt-spacing-50);\n  padding: calc(var(--salt-spacing-50) - var(--salt-size-border));\n  flex-direction: row;\n}\n\n.saltToggleButtonGroup-horizontal .saltToggleButton {\n  height: calc(var(--salt-size-base) - var(--salt-spacing-100));\n}\n\n.saltToggleButtonGroup-vertical {\n  flex-direction: column;\n}\n\n.saltToggleButtonGroup-vertical .saltToggleButton {\n  justify-content: start;\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/toggle-button-group/ToggleButtonGroup.js
var withBaseName$3 = makePrefixer("saltToggleButtonGroup");
var ToggleButtonGroup = (0, import_react.forwardRef)(function ToggleButtonGroup2(props, ref) {
	const { children, className, value: valueProp, defaultValue, disabled, onChange, onKeyDown, orientation = "horizontal", ...rest } = props;
	useComponentCssInjection$1({
		testId: "salt-toggle-button-group",
		css: css_248z$2,
		window: useWindow()
	});
	const groupRef = (0, import_react.useRef)(null);
	const handleRef = useForkRef(ref, groupRef);
	const [value, setValue] = useControlled({
		default: defaultValue,
		controlled: valueProp,
		name: "ToggleButtonGroup",
		state: "value"
	});
	const [focused, setFocused] = (0, import_react.useState)(value);
	const select = (0, import_react.useCallback)((event) => {
		const newValue = event.currentTarget.value;
		setValue(newValue);
		if (value !== newValue) onChange?.(event);
	}, [
		onChange,
		value,
		setValue
	]);
	const isSelected = (0, import_react.useCallback)((id) => {
		return value === id;
	}, [value]);
	const focus = (id) => {
		setFocused(id);
	};
	const isFocused = (0, import_react.useCallback)((id) => {
		return focused === id || !focused;
	}, [focused]);
	const contextValue = (0, import_react.useMemo)(() => ({
		select,
		isSelected,
		focus,
		isFocused,
		disabled,
		orientation
	}), [
		select,
		isSelected,
		isFocused,
		disabled,
		orientation
	]);
	const handleKeyDown = (event) => {
		var _a, _b, _c, _d;
		const elements = Array.from((_b = (_a = groupRef.current) == null ? void 0 : _a.querySelectorAll("button:not([disabled])")) != null ? _b : []);
		const currentIndex = elements.findIndex((element) => element === document.activeElement);
		switch (event.key) {
			case "ArrowDown":
			case "ArrowRight":
				(_c = elements[(currentIndex + 1) % elements.length]) == null || _c.focus();
				break;
			case "ArrowUp":
			case "ArrowLeft":
				(_d = elements[(currentIndex - 1 + elements.length) % elements.length]) == null || _d.focus();
				break;
		}
		onKeyDown?.(event);
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ToggleButtonGroupContext.Provider, {
		value: contextValue,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: clsx(withBaseName$3(), withBaseName$3(orientation), className),
			role: "radiogroup",
			ref: handleRef,
			onKeyDown: handleKeyDown,
			...rest,
			children
		})
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/toggle-button/ToggleButton.css.js
var css_248z$1 = ".saltToggleButton {\n  align-items: center;\n  justify-content: center;\n  appearance: none;\n  -webkit-appearance: none;\n  display: inline-flex;\n  background: var(--salt-actionable-secondary-background);\n  border: 0 solid transparent;\n  border-radius: 0;\n  height: var(--salt-size-base);\n  color: var(--salt-actionable-secondary-foreground);\n  text-transform: var(--salt-text-action-textTransform);\n  font-weight: var(--salt-text-action-fontWeight);\n  font-family: var(--salt-text-fontFamily);\n  text-align: var(--salt-text-action-textAlign);\n  letter-spacing: var(--salt-text-action-letterSpacing);\n  line-height: var(--salt-text-lineHeight);\n  font-size: var(--salt-text-fontSize);\n  padding-left: var(--salt-spacing-100);\n  padding-right: var(--salt-spacing-100);\n  gap: var(--salt-spacing-50);\n}\n\n.saltToggleButton:hover {\n  background: var(--salt-actionable-secondary-background-hover);\n  color: var(--salt-actionable-secondary-foreground-hover);\n  cursor: var(--salt-actionable-cursor-hover);\n}\n\n.saltToggleButton:focus-visible {\n  outline: var(--salt-focused-outline);\n  background: var(--salt-actionable-secondary-background-hover);\n  color: var(--salt-actionable-secondary-foreground-hover);\n  cursor: var(--salt-actionable-cursor-hover);\n}\n\n.saltToggleButton[aria-checked=\"true\"]:focus-visible,\n.saltToggleButton[aria-pressed=\"true\"]:focus-visible {\n  background: var(--salt-actionable-secondary-background-active);\n  color: var(--salt-actionable-secondary-foreground-active);\n  cursor: var(--salt-actionable-cursor-active);\n}\n\n.saltToggleButton[aria-checked=\"true\"],\n.saltToggleButton[aria-pressed=\"true\"] {\n  background: var(--salt-actionable-secondary-background-active);\n  color: var(--salt-actionable-secondary-foreground-active);\n  cursor: var(--salt-actionable-cursor-active);\n}\n\n.saltToggleButton:disabled {\n  background: var(--salt-actionable-secondary-background-disabled);\n  cursor: var(--salt-actionable-cursor-disabled);\n  color: var(--salt-actionable-secondary-foreground-disabled);\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/toggle-button/ToggleButton.js
var withBaseName$2 = makePrefixer("saltToggleButton");
var ToggleButton = (0, import_react.forwardRef)(function ToggleButton2(props, ref) {
	const { children, className, disabled: disabledProp, value, onClick, onFocus, onChange, selected: selectedProp, ...rest } = props;
	useComponentCssInjection$1({
		testId: "salt-toggle-button",
		css: css_248z$1,
		window: useWindow()
	});
	const handleRef = useForkRef(ref, (0, import_react.useRef)(null));
	const toggleButtonGroup = useToggleButtonGroup();
	const toggleButtonGroupSelected = toggleButtonGroup ? toggleButtonGroup.isSelected(value) : selectedProp;
	const focusable = toggleButtonGroup ? toggleButtonGroup == null ? void 0 : toggleButtonGroup.isFocused(value) : true;
	const disabled = (toggleButtonGroup == null ? void 0 : toggleButtonGroup.disabled) || disabledProp;
	const [selected, setSelected] = useControlled({
		controlled: toggleButtonGroupSelected,
		default: Boolean(selectedProp),
		name: "ToggleButton",
		state: "selected"
	});
	const handleClick = (event) => {
		toggleButtonGroup?.select(event);
		setSelected(!selected);
		onChange?.(event);
		onClick?.(event);
	};
	const handleFocus = (event) => {
		toggleButtonGroup?.focus(value);
		onFocus?.(event);
	};
	const ariaChecked = selected && !disabled;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
		"aria-pressed": !toggleButtonGroup ? ariaChecked : void 0,
		"aria-checked": toggleButtonGroup ? ariaChecked : void 0,
		role: toggleButtonGroup ? "radio" : void 0,
		className: clsx(withBaseName$2(), className),
		disabled,
		ref: handleRef,
		onClick: handleClick,
		onFocus: handleFocus,
		tabIndex: focusable && !disabled ? 0 : -1,
		value,
		...rest,
		children
	});
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/utils/createChainedFunction.js
function createChainedFunction(...funcs) {
	return funcs.reduce((acc, func) => {
		if (func == null) return acc;
		return function chainedFunction(...args) {
			acc.apply(this, args);
			func.apply(this, args);
		};
	}, () => {});
}
//#endregion
//#region node_modules/@salt-ds/core/dist-es/utils/mergeProps.js
function mergeProps(propsA, propsB) {
	const props = { ...propsA };
	Object.keys(propsB).forEach((key) => {
		const a = props[key];
		const b = propsB[key];
		if (typeof a === "function" && typeof b === "function" && key.indexOf("on") === 0) props[key] = createChainedFunction(a, b);
		else if (typeof a === "string" && typeof b === "string" && key === "className") props[key] = clsx(a, b);
		else props[key] = b !== void 0 ? b : a;
	});
	return props;
}
//#endregion
//#region node_modules/@salt-ds/core/dist-es/tooltip/useAriaAnnounce.js
function getDocument(floating) {
	var _a;
	return (_a = floating == null ? void 0 : floating.ownerDocument) != null ? _a : document;
}
function getWindow(value) {
	var _a;
	return (_a = getDocument(value).defaultView) != null ? _a : window;
}
function isElement(value) {
	return value ? value instanceof getWindow(value).Element : false;
}
function getDelay(value, prop, pointerType) {
	if (pointerType && pointerType !== "mouse") return 0;
	if (typeof value === "number") return value;
	return value == null ? void 0 : value[prop];
}
var useAriaAnnounce = (context, { delay = 0 }) => {
	const { open, dataRef, refs } = context;
	const pointerTypeRef = (0, import_react.useRef)();
	const timeoutRef = (0, import_react.useRef)();
	const blockMouseMoveRef = (0, import_react.useRef)(true);
	const { announce } = useAriaAnnouncer();
	useIsomorphicLayoutEffect(() => {
		if (!open) pointerTypeRef.current = void 0;
	});
	(0, import_react.useEffect)(() => {
		const reference = refs.reference.current;
		function announceFloating() {
			var _a;
			const tooltipContent = (_a = refs.floating.current) == null ? void 0 : _a.innerText;
			if (tooltipContent) announce(tooltipContent);
		}
		function onMouseEnter(event) {
			clearTimeout(timeoutRef.current);
			if (open) return;
			blockMouseMoveRef.current = false;
			dataRef.current.openEvent = event;
			if (delay) timeoutRef.current = window.setTimeout(() => {
				announceFloating();
			}, getDelay(delay, "open", pointerTypeRef.current));
			else announceFloating();
		}
		if (isElement(reference)) {
			reference.addEventListener("mouseenter", onMouseEnter);
			return () => {
				reference.removeEventListener("mouseenter", onMouseEnter);
			};
		}
	}, [
		dataRef,
		delay,
		open,
		refs.reference,
		refs.floating,
		announce
	]);
	function setPointerRef(event) {
		pointerTypeRef.current = event.pointerType;
	}
	return {
		reference: {
			onPointerDown: setPointerRef,
			onPointerEnter: setPointerRef
		},
		floating: { onMouseEnter() {
			clearTimeout(timeoutRef.current);
		} }
	};
};
//#endregion
//#region node_modules/@salt-ds/core/dist-es/tooltip/useTooltip.js
function useTooltip(props) {
	const { enterDelay, leaveDelay, open: openProp, onOpenChange, placement: placementProp, disableHoverListener, disableFocusListener } = props || {};
	const arrowRef = (0, import_react.useRef)(null);
	const [open, setOpen] = useControlled({
		controlled: openProp,
		default: false,
		name: "Tooltip",
		state: "open"
	});
	const handleOpenChange = (open2) => {
		setOpen(open2);
		onOpenChange?.(open2);
	};
	const { floating, reference, x, y, strategy, placement, context, elements } = useFloatingUI({
		open,
		onOpenChange: handleOpenChange,
		placement: placementProp,
		middleware: [
			offset(8),
			flip(),
			shift({ limiter: limitShift() }),
			arrow({ element: arrowRef })
		]
	});
	const { getReferenceProps, getFloatingProps } = useInteractions([
		useHover(context, {
			delay: {
				open: enterDelay,
				close: leaveDelay
			},
			enabled: !disableHoverListener,
			handleClose: safePolygon()
		}),
		useFocus(context, { enabled: !disableFocusListener }),
		useRole(context, { role: "tooltip" }),
		useDismiss(context),
		useAriaAnnounce(context, { delay: {
			open: enterDelay,
			close: leaveDelay
		} })
	]);
	const arrowProps = {
		ref: arrowRef,
		context
	};
	const getTooltipProps = () => {
		const { tabIndex, ...tooltipProps } = getFloatingProps({
			"data-placement": placement,
			ref: floating
		});
		return tooltipProps;
	};
	const getTriggerProps = () => getReferenceProps({ ref: reference });
	const getTooltipPosition = () => {
		var _a, _b;
		return {
			top: y != null ? y : 0,
			left: x != null ? x : 0,
			position: strategy,
			width: (_a = elements.floating) == null ? void 0 : _a.offsetWidth,
			height: (_b = elements.floating) == null ? void 0 : _b.offsetHeight
		};
	};
	return {
		arrowProps,
		open,
		floating,
		reference,
		getTooltipProps,
		getTriggerProps,
		getTooltipPosition
	};
}
//#endregion
//#region node_modules/@salt-ds/core/dist-es/tooltip/Tooltip.css.js
var css_248z = "/* Styles applied to root component */\n.saltTooltip {\n  --tooltip-background: var(--saltTooltip-background, var(--salt-container-primary-background));\n  --tooltip-zIndex: var(--saltTooltip-zIndex, var(--salt-zIndex-flyover));\n  /* apply icon margin based on the text line height so it's aligned on all densities */\n  --tooltip-icon-marginTop: calc((var(--salt-text-lineHeight) - max(var(--salt-icon-size-base), 12px)) / 2);\n}\n\n.saltTooltip {\n  background: var(--tooltip-background);\n  border-color: var(--saltTooltip-borderColor, var(--tooltip-status-borderColor));\n  border-style: var(--saltTooltip-borderStyle, var(--salt-container-borderStyle));\n  border-width: var(--saltTooltip-borderWidth, var(--salt-size-border));\n  box-shadow: var(--saltTooltip-shadow, var(--salt-overlayable-shadow-popout));\n  color: var(--saltTooltip-text-color, var(--salt-content-primary-foreground));\n  font-family: var(--salt-text-fontFamily);\n  font-size: var(--saltTooltip-fontSize, var(--salt-text-fontSize));\n  font-weight: var(--saltTooltip-fontWeight, var(--salt-text-fontWeight));\n  line-height: var(--saltTooltip-lineHeight, var(--salt-text-lineHeight));\n  max-width: var(--saltTooltip-maxWidth, 230px);\n  padding: var(--saltTooltip-padding, var(--salt-size-unit));\n  position: relative;\n  text-align: var(--saltTooltip-textAlign, left);\n  z-index: var(--tooltip-zIndex);\n}\n\n/* Styles applied to container */\n.saltTooltip-container {\n  display: flex;\n  align-items: baseline;\n  position: relative;\n}\n\n/* Styles applied to content */\n.saltTooltip-content {\n  overflow: hidden;\n}\n\n/* Styles applied to status indicator */\n.saltTooltip-icon {\n  --saltIcon-margin: var(--tooltip-icon-marginTop) var(--saltTooltip-icon-marginRight, 6px) 0 0;\n  vertical-align: top;\n  align-self: flex-start;\n}\n\n/* Styles applied when status = \"info\" */\n.saltTooltip-info {\n  --tooltip-status-borderColor: var(--salt-status-info-borderColor);\n}\n\n/* Styles applied when status = \"error\" */\n.saltTooltip-error {\n  --tooltip-status-borderColor: var(--salt-status-error-borderColor);\n}\n\n/* Styles applied when status = \"warning\" */\n.saltTooltip-warning {\n  --tooltip-status-borderColor: var(--salt-status-warning-borderColor);\n}\n\n/* Styles applied when status = \"success\" */\n.saltTooltip-success {\n  --tooltip-status-borderColor: var(--salt-status-success-borderColor);\n}\n";
//#endregion
//#region node_modules/@salt-ds/core/dist-es/tooltip/TooltipBase.js
var withBaseName$1 = makePrefixer("saltTooltip");
var TooltipBase = (props) => {
	useComponentCssInjection$1({
		testId: "salt-tooltip",
		css: css_248z,
		window: useWindow()
	});
	const { a11yProps } = useFormFieldProps();
	const { arrowProps, content, hideArrow, hideIcon, status } = props;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		className: withBaseName$1("container"),
		children: [!hideIcon && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(StatusIndicator, {
			status,
			size: 1,
			className: withBaseName$1("icon")
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
			id: a11yProps == null ? void 0 : a11yProps["aria-describedby"],
			className: withBaseName$1("content"),
			children: content
		})]
	}), !hideArrow && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FloatingArrow, {
		...arrowProps,
		className: withBaseName$1("arrow"),
		strokeWidth: 1,
		fill: "var(--salt-container-primary-background)",
		stroke: "var(--tooltip-status-borderColor)",
		height: 5,
		width: 10
	})] });
};
//#endregion
//#region node_modules/@salt-ds/core/dist-es/tooltip/Tooltip.js
var withBaseName = makePrefixer("saltTooltip");
var Tooltip = (0, import_react.forwardRef)(function Tooltip2(props, ref) {
	const { children, className, disabled: disabledProp = false, hideArrow = false, hideIcon = false, open: openProp, content, status: statusProp = "info", placement = "right", enterDelay = 300, leaveDelay = 0, ...rest } = props;
	const { disabled: formFieldDisabled, validationStatus: formFieldValidationStatus } = useFormFieldProps();
	const disabled = disabledProp || formFieldDisabled;
	const status = formFieldValidationStatus !== void 0 && VALIDATION_NAMED_STATUS.includes(formFieldValidationStatus) ? formFieldValidationStatus : statusProp;
	const { Component: FloatingComponent } = useFloatingComponent();
	const { arrowProps, open, floating, reference, getTriggerProps, getTooltipProps, getTooltipPosition } = useTooltip({
		open: openProp,
		placement,
		enterDelay,
		leaveDelay,
		...rest
	});
	const triggerRef = useForkRef((0, import_react.isValidElement)(children) ? children.ref : null, reference);
	const floatingRef = useForkRef(floating, ref);
	const hasContent = !!content;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [(0, import_react.isValidElement)(children) && (0, import_react.cloneElement)(children, {
		...mergeProps(getTriggerProps(), children.props),
		ref: triggerRef
	}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FloatingComponent, {
		className: clsx(withBaseName(), withBaseName(status), className),
		open: open && !disabled && hasContent,
		...getTooltipProps(),
		ref: floatingRef,
		...getTooltipPosition(),
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(TooltipBase, {
			hideIcon,
			status,
			content,
			hideArrow,
			arrowProps
		})
	})] });
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/utils/marginMiddleware.js
var margin = (value = 0) => ({
	name: "margin",
	options: value,
	fn(middlewareState) {
		const { x, y, elements } = middlewareState;
		if (elements.floating.children.length > 0) elements.floating.children[0].style.setProperty("margin", value.toString());
		return {
			x,
			y
		};
	}
});
//#endregion
//#region node_modules/@salt-ds/core/dist-es/utils/ownerDocument.js
function ownerDocument(node) {
	return (node == null ? void 0 : node.ownerDocument) || document;
}
//#endregion
//#region node_modules/@salt-ds/core/dist-es/utils/ownerWindow.js
function ownerWindow(node) {
	return ownerDocument(node).defaultView || window;
}
//#endregion
//#region node_modules/@salt-ds/core/dist-es/utils/useIsFocusVisible.js
var hadKeyboardEvent = true;
var hadFocusVisibleRecently = false;
var hadFocusVisibleRecentlyTimeout;
var inputTypesWhitelist = {
	text: true,
	search: true,
	url: true,
	tel: true,
	email: true,
	password: true,
	number: true,
	date: true,
	month: true,
	week: true,
	time: true,
	datetime: true,
	"datetime-local": true
};
function focusTriggersKeyboardModality(node) {
	if (node.tagName === "INPUT" && inputTypesWhitelist[node.type] && !node.readOnly) return true;
	if (node.tagName === "TEXTAREA" && !node.readOnly) return true;
	return node.isContentEditable;
}
function handleKeyDown(event) {
	if (event.metaKey || event.altKey || event.ctrlKey) return;
	hadKeyboardEvent = true;
}
function handlePointerDown() {
	hadKeyboardEvent = false;
}
function handleVisibilityChange() {
	if (this.visibilityState === "hidden") {
		if (hadFocusVisibleRecently) hadKeyboardEvent = true;
	}
}
function prepare(doc) {
	doc.addEventListener("keydown", handleKeyDown, true);
	doc.addEventListener("mousedown", handlePointerDown, true);
	doc.addEventListener("pointerdown", handlePointerDown, true);
	doc.addEventListener("touchstart", handlePointerDown, true);
	doc.addEventListener("visibilitychange", handleVisibilityChange, true);
}
function teardown(doc) {
	doc.removeEventListener("keydown", handleKeyDown, true);
	doc.removeEventListener("mousedown", handlePointerDown, true);
	doc.removeEventListener("pointerdown", handlePointerDown, true);
	doc.removeEventListener("touchstart", handlePointerDown, true);
	doc.removeEventListener("visibilitychange", handleVisibilityChange, true);
}
function isFocusVisible(event) {
	const { target } = event;
	try {
		return target.matches(":focus-visible");
	} catch (error) {}
	return hadKeyboardEvent || focusTriggersKeyboardModality(target);
}
function useIsFocusVisible() {
	const ref = (0, import_react.useCallback)((node) => {
		if (node != null) prepare(node.ownerDocument);
	}, []);
	const isFocusVisibleRef = (0, import_react.useRef)(false);
	function handleBlurVisible() {
		if (isFocusVisibleRef.current) {
			hadFocusVisibleRecently = true;
			window.clearTimeout(hadFocusVisibleRecentlyTimeout);
			hadFocusVisibleRecentlyTimeout = window.setTimeout(() => {
				hadFocusVisibleRecently = false;
			}, 100);
			isFocusVisibleRef.current = false;
			return true;
		}
		return false;
	}
	function handleFocusVisible(event) {
		if (isFocusVisible(event)) {
			isFocusVisibleRef.current = true;
			return true;
		}
		return false;
	}
	return {
		isFocusVisibleRef,
		onFocus: handleFocusVisible,
		onBlur: handleBlurVisible,
		ref
	};
}
//#endregion
//#region node_modules/@salt-ds/core/dist-es/utils/usePrevious.js
function usePrevious(value, deps = [], initialValue) {
	const ref = (0, import_react.useRef)(initialValue);
	(0, import_react.useEffect)(() => {
		ref.current = value;
	}, deps);
	return ref.current;
}
//#endregion
export { ARIA_ANNOUNCE_DELAY, Accordion, AccordionGroup, AccordionHeader, AccordionPanel, AriaAnnounce, AriaAnnouncerContext, AriaAnnouncerProvider, Avatar, BORDER_POSITION, Badge, Banner, BannerActions, BannerContent, BorderItem, BorderLayout, BreakpointContext, Button, ButtonVariantValues, Card, Checkbox, CheckboxGroup, CheckboxIcon, CircularProgress, CompactInput, CompactPaginator, DEFAULT_BREAKPOINTS, DEFAULT_DENSITY, DEFAULT_FLOATING_UI_MIDDLEWARE, DensityContext, DensityValues, Display1, Display2, Display3, FLEX_ALIGNMENT_BASE, FLEX_CONTENT_ALIGNMENT_BASE, FLEX_ITEM_ALIGNMENTS, FileDropZone, FileDropZoneIcon, FileDropZoneTrigger, FlexItem, FlexLayout, FloatingComponentProvider, FloatingPlatformProvider, FlowLayout, FormField, FormFieldContext, FormFieldHelperText, FormFieldLabel, GRID_ALIGNMENT_BASE, GoToInput, GridItem, GridLayout, H1, H2, H3, H4, Input, InteractableCard, Label, LinearProgress, Link, ModeValues, MultilineInput, NavigationItem, Pagination, Paginator, Panel, Pill, RadioButton, RadioButtonGroup, RadioButtonIcon, SaltProvider, Scrim, Spinner, SpinnerSizeValues, SplitLayout, StackLayout, StatusAdornment, StatusIndicator, Switch, Text, TextAction, TextNotation, ThemeContext, Toast, ToastContent, ToggleButton, ToggleButtonGroup, Tooltip, VALIDATION_NAMED_STATUS, ViewportContext, ViewportProvider, capitalize, createChainedFunction, createContext, debounce, getCharacteristicValue, getCurrentBreakpoint, makePrefixer, margin, mergeProps, ownerDocument, ownerWindow, setRef, teardown, useAriaAnnouncer, useAvatarImage, useBreakpoints, useButton, useControlled, useCurrentBreakpoint, useDensity, useFloatingComponent, useFloatingPlatform, useFloatingUI, useForkRef, useFormFieldProps, useId, useIdMemo, useIsFocusVisible, useIsomorphicLayoutEffect, useOrderedBreakpoints, usePrevious, useResponsiveProp, useTheme, useToggleButtonGroup, useTooltip, useViewport };
